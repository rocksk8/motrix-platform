# MOTRIX ERP — 本週更新拆解與異常排查索引（2026-09-07 ~ 2026-09-10）

> 產出日期：2026-09-10｜盤點依據：實際 git 紀錄、部署 log、正式機 API 實測，**不是只讀文件**
> 與 `MOTRIX-ERP-QUICK.md`（行為規格＋逐日 changelog）、`MOTRIX-ERP-ARCHITECTURE-MAP.md`（架構地圖）互補。
> 本文件的用途是**下次出事時的座標系**：每一條異常都標到「哪個模組、哪個檔案、第幾行、什麼時候開始、現在修好沒」。

---

## 目錄

- [§A 範圍與正式機現況](#a-範圍與正式機現況)
- [§B 模組 × 功能 × 部署狀態總表](#b-模組--功能--部署狀態總表)
- [§C 異常時間軸（本週逐筆）](#c-異常時間軸本週逐筆)
- [§D 排查排程 checklist（12 項，附指令與本次實測結果）](#d-排查排程-checklist12-項附指令與本次實測結果)
- [§E 已驗證缺陷清冊](#e-已驗證缺陷清冊)
- [§F 未部署差異](#f-未部署差異)
- [§G 文件漂移清單](#g-文件漂移清單)
- [§H 本輪處置與待辦](#h-本輪處置與待辦)

---

## §A 範圍與正式機現況

**commit 範圍**：`359e23a`（2026-09-07 07:31）→ `aeefcc6`（2026-09-10 11:41），共 **82 個 commit**。

```powershell
git -C "C:\Users\hichan\Desktop\MOTRIX-ERP" log --since="2026-09-07 00:00" --date=format:"%Y-%m-%d %H:%M" --pretty=format:"%h|%ad|%s"
```

**正式機實測（2026-09-10 本輪查證）**

| 項目 | 實測值 | 備註 |
|------|--------|------|
| 目前部署 commit | `9b0ad79` | `applied_at` **2026-09-10 11:32:13**、`built_at` 11:31:31 |
| 系統顯示版本 | `2026-09-09c` | **落後一天**——`version_manifest.json` 沒補 09-10 條目，登入頁版本號抓的就是這支 API |
| 協定 | **HTTPS only** | 明文 HTTP 連線直接被拒（`RemoteDisconnected`）；§0/§11 早期「尚未啟用 HTTPS」的記載已完全過時 |
| 未部署的 commit | `aeefcc6` | 見 [§F](#f-未部署差異)，影響 Passkey 可用性 |

查法（開發機執行，走 Python 的 `ssl` 不經過 Schannel，比 `curl.exe` 穩）。
⚠️ 下面關掉憑證驗證是**診斷用途**、對象是區網內自簽憑證的自家主機，沿用
`backend/tools/_healthcheck_ping.py` 既有作法；正式的用戶端連線不要照抄，
要嘛把自簽 CA 加進信任存放區，要嘛換成正式簽發的憑證：

```python
import ssl, urllib.request
ctx = ssl.create_default_context(); ctx.check_hostname = False; ctx.verify_mode = ssl.CERT_NONE
for ep in ("/api/system/version", "/api/system/deployed-version"):
    with urllib.request.urlopen(f"https://172.16.10.177:666{ep}", timeout=6, context=ctx) as r:
        print(ep, r.status, r.read().decode())
```

**部署嘗試統計**（`backend/tools/deploy_dashboard_history.json`）：20 筆，09-08 03:57 → 09-09 22:16，其中 **6 筆 `success:false`**。
⚠️ **09-10 那次真正上線的部署不在這個檔案裡**——11:28／11:31 的兩份部署包沒有對應的 `deploy_logs/*_build_*.log`，代表是直接執行腳本、沒走儀表板。詳見 [§E-2](#e-2打包測試關卡長期紅燈p0已修)。

---

## §B 模組 × 功能 × 部署狀態總表

「在正式機」＝該 commit 是 `9b0ad79` 的祖先。

### B-1 認證與登入

| 功能 | commit | 主要檔案:行 | 正式機 | 測試 |
|------|--------|------------|--------|------|
| TOTP 兩步驟驗證（自助啟用，DB v72） | `0571458` 09-07 09:17 | `backend/db.py::_m072_totp()`、`backend/routers/auth.py` | ✅ | `test_totp_2026_09_07.py` |
| TOTP 登入手機掃 QR 核准 | `e3717b3` 09-08 02:58 | `routers/auth.py`（`qr-info`／`qr-approve`／`qr-status`）、`frontend/pages/login-qr-approve.html` | ✅ | `test_totp_qr_push_2026_09_08.py`（8 題）＋ e2e |
| QR 核准手機端免密碼（已登入 session 自動核准） | `7596382` 09-08 17:37 | `routers/auth.py::login_qr_approve()` | ✅ | 同上 +5 題 |
| WebAuthn／Passkey 後端 V1（新表） | `efdb06f` 09-09 17:11 | `backend/db.py`、`routers/auth.py:722-960`、`backend/requirements.txt`（`webauthn>=2.1.0`） | ✅ | `test_webauthn_basic.py` |
| Passkey 前端（登入按鈕）＋修用戶枚舉漏洞 | `0527524` 09-09 17:14 | `frontend/pages/login.html`、`routers/auth.py` | ✅ | — |
| Passkey 裝置管理卡片 | `4f14c68` 09-09 17:16 | `frontend/pages/change-password.html` | ✅ | — |
| Passkey challenge 編碼檢查 | `cce89fe` 09-09 23:19 | `login.html`／`change-password.html` | ✅ | — |
| Passkey base64 解碼修正（P1） | `4d97142` 09-10 09:30 | `login.html`、`change-password.html` | ✅ | — |
| **WebAuthn RP ID／Origin 改為系統可設定** | `f8198e9` 09-10 11:27 | `routers/auth.py:730-749`（`_webauthn_rp_id()`／`_webauthn_origin()`）、`routers/system.py:708-741`、`main.py:62` | ✅ | 09-10 未補，**本輪補齊** |
| WebAuthn 設定 UI | `aeefcc6` 09-10 11:41 | `frontend/pages/company-profile-settings.html` | ❌ **未部署** | — |

### B-2 部署工具鏈

| 功能 | commit | 主要檔案 | 正式機 | 測試 |
|------|--------|---------|--------|------|
| 本機部署儀表板（打包/推送/套用/回滾一鍵化） | `8144d58` 09-08 03:41 | `backend/tools/deploy_dashboard.py`／`.html` | 開發機工具，不部署 | 手動 + Playwright |
| `rollback_update.ps1`（手動回滾） | `8144d58` | `backend/tools/rollback_update.ps1` | ✅（隨套件） | 無 |
| `_dashboard_remote.ps1`（WinRM 遠端執行） | `8144d58` | `backend/tools/_dashboard_remote.ps1` | 開發機 | 無 |
| `_healthcheck_ping.py`（取代 curl.exe） | `47d0cca` 09-08 05:38 | `backend/tools/_healthcheck_ping.py` | ✅ | 無 |
| `apply_update.ps1 -CheckOnly` 乾跑 | `8f1b623` 09-08 01:41 | `backend/tools/apply_update.ps1` | ✅ | 語法檢查關卡 |
| `apply_update.ps1 -SkipAutoRollback` | `88abc2f` 09-08 10:03 | 同上 | ✅ | 同上 |
| 儀表板開關 GUI（取代兩支 .bat） | `507fff3` 09-09 12:28 | `backend/tools/deploy_dashboard_ctl.pyw` | 開發機 | 無 |
| 打包測試 `pytest-xdist` 平行化 | `98f3d7b` 09-09 11:31 | `build_deploy_package.ps1` L135 附近、`requirements-dev.txt` | 開發機 | — |
| 正式機狀態 WebSocket `/ws/prod-status` | `6ee3a6d` 09-08 08:56 | `deploy_dashboard.py` | 開發機 | 手動 |
| `GET /api/system/deployed-version` | `8144d58` | `routers/auth.py` | ✅ | `test_deployed_version_endpoint_2026_09_08.py` |

> `backend/tools/apply_update.ps1` 本週被改 **14 次**、`_dashboard_remote.ps1` 8 次、`build_deploy_package.ps1` 8 次——是本週改動最密集也最不穩定的區域，且**完全沒有 pytest 覆蓋**（只有 `build_deploy_package.ps1` L93-101 的語法 tokenize 檢查）。

### B-3 測試基礎建設

| 功能 | commit | 主要檔案 | 說明 |
|------|--------|---------|------|
| conftest 雲端備份隔離死碼修復 | `a55fe26` 09-07 15:22 | `backend/tests/conftest.py` | 曾讓測試假資料寫進真實 `G:` |
| 第一條瀏覽器 e2e（Playwright） | `3868896` 09-07 09:57 | `test_e2e_playwright_2026_09_07.py` | 本週被改 7 次 |
| 階段端點測試補齊（11 題） | `75caff4` 09-07 08:49 | `test_case_stages_endpoints_2026_09_07.py` | — |
| vendor 快取標頭迴歸（3 題） | `03723e8` 09-08 00:29 | `test_vendor_cache_headers_2026_09_08.py` | — |
| 打包跳過 flaky 測試 | `ee4664a` 09-09 17:26 | `build_deploy_package.ps1` | ⚠️ 把關卡挖洞，**本輪改為修測試本身** |

### B-4 營運報表／首頁 Dashboard

| 功能 | commit | 主要檔案:行 | 正式機 |
|------|--------|------------|--------|
| 反派/國稅局視角複查修 9 項 | `bb911d8` 09-02 | `routers/reports.py` | ✅ |
| 當月/當年度應收獨立檢視 | `56e52b3` 09-09 16:54 | `routers/reports.py`、`frontend/js/reports.js`、`reports.html` | ✅ |
| 應收應付卡片可點選＋當月收支獨立顯示 | `3f9755f` 09-09 17:42 | `reports.html`、`case-management.html` | ✅ |
| Dashboard 初始化自動載入當月收支 | `c063a72` 09-09 17:43 | `frontend/js/reports.js` | ✅ |
| 精算未完結額外支出納入月支出 | `c15ef84` 09-09 12:29 | `helpers/quotations.py::settlement_extra_expenses()`、`dashboard.py:511`、`reports.py:3299` | ✅ |
| 精算額外支出無日期改用建立時間 | `0b67dfe` 09-09 17:52 | `helpers/quotations.py`、`settlement.html` | ✅ |
| 當月收支日期格式 bug（跨月污染） | `c5a5d11` 09-09 22:10 | `frontend/js/reports.js` | ✅ |
| 當月收支邏輯統一＋未收 | `da88433` 09-09 22:49 | `routers/reports.py`、`reports.js` | ✅ |
| 當月收支完整修正＋部門篩選 | `6bfcafb` 09-09 22:51 | `routers/dashboard.py`、`frontend/index.html`、`reports.js`、`reports.html` | ✅ |
| 月支出頁籤徽章依 scope 顯示 | `f8198e9` 09-10 11:27 | **`frontend/pages/reports.html:434`** | ✅ |

> **「當月收支」在 09-09 一晚被改了 6 次**（17:42 → 22:51），是本週單一功能反覆次數最高的。原因見 [§C-4](#c-4-當月收支一晚六輪)。

### B-5 案件管理／案件財務／精算

| 功能 | commit | 主要檔案:行 | 正式機 |
|------|--------|------------|--------|
| 案件財務「應收應付總覽」 | `4c267ce` 09-09 12:11 | `GET /api/quotations/{no}/finance-summary`（`routers/quotations.py`）、`helpers/quotations.py::summarize_payment_items()` | ✅ |
| 精算額外支出單號 `docNo` | `4c267ce` | `settlement.html`、`pdf_gen.py`（欄位 5→6） | ✅ |
| 精算額外支出明細展開 | `f935119` 09-09 15:57 | `case-management.html` | ✅ |
| 卡片點選自動捲動 | `3e29ba7` 09-09 19:33 | `case-management.html` | ✅ |
| 強制填寫收款日期 | `d03f453` 09-09 23:23 | `frontend/js/case-management.js` | ✅ |
| **案件「專案期間」＋超期通知** | `f8198e9` 09-10 11:27 | `case-management.js:680-682`、`case-management.html:824-837`、`routers/daily_tasks.py:1092-1135`、`helpers/email_notify.py:1370-1404`、`helpers/__init__.py:66` | ✅ |
| 導出 `notify_case_project_overdue` | `9b0ad79` 09-10 11:31 | `helpers/__init__.py` | ✅ |
| 業務開發暫擱置狀態（180 天自動轉未成案） | `d994a7c` 09-02 | `routers/dev_crm.py` | ✅ |
| 業務開發暗黑模式白底修正 | `aaeffb2` 09-09 23:46 | `frontend/pages/dev-crm.html` | ✅ |

### B-6 庫存／叫料

| 功能 | commit | 主要檔案:行 | 正式機 | 測試 |
|------|--------|------------|--------|------|
| 自動採購建議 `GET /api/inventory/purchase-suggestions` | `ea81526` 09-07 14:50 | `routers/inventory.py`、`frontend/pages/inventory.html` | ✅ | `test_purchase_suggestions_2026_09_07.py` |
| **叫料（材料訂購）API** | `7c2a276` 09-10 09:35 | `backend/routers/material_orders.py`、`main.py` | ✅ | ❌ 上線時零測試 |
| 叫料寫入權限檢查 | `83c0a1a` 09-10 09:45 | `material_orders.py:80-81`（修復後位置） | ✅ | ❌ |

> ⚠️ **叫料功能是半成品**：只有後端，`grep materialOrders` 全 repo 僅命中 `main.py`／`material_orders.py`／`db_migration_plan.md` 三處，**沒有任何前端 UI**。且四個缺陷同時存在，見 [§E-1](#e-1叫料-api-完全無效p0已修)。

### B-7 備份與雲端

| 功能 | commit | 主要檔案 | 正式機 | 測試 |
|------|--------|---------|--------|------|
| 雲端備份可插拔＋S3 相容後端 | `36add8e` 09-07 09:35 | `backend/cloud_storage.py`、`archive.py` | ✅ | `test_cloud_storage_2026_09_07.py` |
| PDF 存檔納入雲端每日鏡像 | `0f36aa0` 09-07 14:31 | `archive.py::_mirror_pdf_archives()` | ✅ | `test_pdf_archive_mirror_2026_09_07.py` |
| `logs/server.log` 大小輪替 | `f6fab0a` 09-07 12:24 | `helpers/startup.py` | ✅ | `test_log_rotation_2026_09_07.py` |

### B-8 PDF／網路架構規劃書

| 功能 | commit | 主要檔案 | 正式機 |
|------|--------|---------|--------|
| PDF 產生並發限制（`EDGE_PDF_SEMAPHORE`，上限 3） | `48906b5` 09-07 10:25 | `pdf_gen.py`、`network_plan_export.py`、`routers/reports.py` | ✅ |
| 完整版規劃書拓樸圖三輪修復 | `500e354`/`3a0c0ce`/`98cb9c8` 09-06 | `network_plan_topology.py` | ✅ |
| PDF 生成失敗返回碼邏輯 | `3a9332d` 09-10 10:31 | `backend/network_plan_export.py` | ✅ |

### B-9 前端基礎建設

| 功能 | commit | 主要檔案 | 正式機 |
|------|--------|---------|--------|
| 外部函式庫全面自架、移除 CDN | `7e501de` 09-07 10:07 | `frontend/static/vendor/*`（alpine/chart/xlsx/sortable/frappe-gantt）＋ 全站 40+ 頁 | ✅ |
| `no_cache_static` 不再誤傷 vendor | `03723e8` 09-08 00:29 | `backend/main.py::no_cache_static()` | ✅ |
| 品牌文字「營運管理系統」補漏 | `da0aaa9` 09-06 | 全站頁面標題／Email 頁尾 | ✅ |

### B-10 依賴與環境

| 項目 | commit | 檔案 | 狀態 |
|------|--------|------|------|
| 補齊 requirements 缺漏＋弱點掃描工具 | `90c6f31` 09-07 12:39 | `requirements.txt`、`tools/check_dependencies.py` | ⚠️ **仍漏 `python-multipart`**，見 [§E-3](#e-3requirementstxt-缺-python-multipartp0已修) |
| `pytest-xdist` | `98f3d7b` 09-09 | `requirements-dev.txt` | ✅ |

---

## §C 異常時間軸（本週逐筆）

格式：**症狀** → 起點 → 根因（檔案:行）→ 現況

### C-1 部署工具鏈：一整晚「怎麼修都測不到自己剛寫的程式碼」

這是本週最大的一團，因果鏈如下（**由外而內，越後面越根本**）：

| # | 症狀 | 起點 | 根因位置 | 現況 |
|---|------|------|---------|------|
| 1 | 缺套件導致健康檢查失敗自動回滾 | 09-07 19:26 正式機事故 | `apply_update.ps1` 沒有 pip install 步驟 | ✅ `332dd0a` |
| 2 | pip install 本身在 PS 5.1 崩潰 | 09-07 20:40 | 原生執行檔 stderr 在 `$ErrorActionPreference="Stop"` 下被包成 `NativeCommandError` | ✅ `084eb78` |
| 3 | HTTPS 健康檢查 Runspace 崩潰 | 09-08 01:18 | `ServerCertificateValidationCallback` 用 PowerShell 指令碼區塊當委派，.NET 從**背景執行緒**呼叫、該執行緒沒有 Runspace | ✅ `3af9b71`（改 `curl.exe -k`） |
| 4 | 部署包是空殼（只有 manifest） | 09-08 04:20 | `build_deploy_package.ps1` 解析到 Git 的 Unix `tar`，把 `C:\...` 當遠端主機語法；**且完全沒檢查 exit code** | ✅ `549d319` → 09-10 `942e3c4` 又改回 Git tar（見 C-2） |
| 5 | db 備份／migration 乾跑只印 Traceback 第一行 | 09-08 04:20 | `apply_update.ps1:191`／`:223` 兩處 `& python ... 2>&1` 沒有 `$ErrorActionPreference="Continue"` 防護 | ✅ `549d319` |
| 6 | `_ps_cmd()` 參數名被當成值加引號 | 09-08 04:00 | `deploy_dashboard.py` 把參數名與值混在同一 list 統一跳脫 | ✅ `8d83021` |
| 7 | 密碼讀取在管線 stdin 下卡死 | 09-08 04:48 | `_dashboard_remote.ps1` | ✅ `ebd182f` |
| 8 | 健康檢查連續三次偽陽性 | 09-08 05:14~05:38 | curl.exe 走 Schannel，自簽憑證兩次 TLS renegotiation 不穩（**合理猜測，非證實**） | ⚠️ `47d0cca` 改用 `_healthcheck_ping.py`，**未證實根因** |
| 9 | `rollback_update.ps1` 姊妹腳本沒同步 | 09-08 05:49 | 同一份 `Test-Ping` 有兩份副本，只改了一份 | ✅ `46fb6b6` |
| 10 | 打包 commit hash 記錄時機競態 | 09-08 05:49 | pytest 跑 6 分鐘期間 HEAD 可能變動 | ✅ `46fb6b6`（改成 pytest 前先釘住） |
| 11 | **儀表板「假成功」** | 09-08 08:56 | `_dashboard_remote.ps1` 呼叫遠端腳本從不檢查 `$LASTEXITCODE`，`Invoke-Command` 吞掉結束碼 → `success = returncode == 0` 永遠成立 | ✅ `6ee3a6d` |
| 12 | 健康檢查失敗原因全程看不到 | 09-08 08:56 | `Test-Ping` 用 `2>$null` 把例外整個丟掉 | ✅ `6ee3a6d` |
| 13 | 良性 asyncio 雜訊被算成錯誤 | 09-08 09:20 | `"Traceback\|ERROR"` 不分大小寫掃描，命中 Windows 已知良性 `ConnectionResetError` | ✅ `2ea87e8`（逐行狀態機跳過） |
| 14 | **【根因】整晚跑的都是套用前就凍結的舊腳本** | 09-08 10:19 | `_dashboard_remote.ps1` 呼叫的是正式機**既有安裝路徑**的 `apply_update.ps1`；PowerShell 進程用的是啟動當下讀進記憶體的內容，Step 3 複製新檔案不影響本次執行，且失敗回滾又會把新檔案蓋回去 | ✅ `ad04397`（先同步 `backend/tools/` 再呼叫） |
| 15 | `deployed-version` 回空物件 | 09-08 10:36 | 讀 `.deployed_commit.json` 沒處理 BOM（PowerShell 寫出來的 JSON 帶 BOM，Python 純 `utf-8` 會丟 `JSONDecodeError`） | ✅ `1c8f2e8` |

**教訓（已重複三次以上的模式）**
1. **PATH 解析歧義**：`tar`（#4）→ `python`（[§E-2](#e-2打包測試關卡長期紅燈p0已修)）。凡是在腳本裡呼叫裸執行檔名，都要問「在別的呼叫環境下會解析到哪一支」。
2. **同一段邏輯有兩份副本**：`apply_update.ps1` ⇄ `rollback_update.ps1`（#9）。改部署腳本前先 grep 姊妹檔。
3. **PowerShell 5.1 對原生執行檔 stderr 的地雷**：pip（#2）、tar（#4）、db備份/migration（#5）——第三次踩到同一個。
4. **「檔案已經複製過去」≠「這次執行的是新版」**（#14）：進程用的是啟動當下的記憶體內容。

### C-2 tar 實作在 48 小時內來回翻了兩次

- 09-08 04:20 `549d319`：Unix tar 誤判 `C:` 為遠端主機 → 改用 `$env:SystemRoot\System32\tar.exe`（Windows 內建 BSD tar）
- 09-10 00:04 `1935c3c`：git archive 命令語法錯誤
- 09-10 00:29 `942e3c4`：**又改回 Git 內建的 Unix tar**（`build_deploy_package.ps1:194-203`），理由是「確保與 git archive 生成的 tar 格式完全兼容」

**✅ 2026-09-10 已徹底解決（`d2ff23f`）**——而且是被本輪新加的關卡當場抓到的：實際跑 `build_deploy_package.ps1` 打包 `2ea463f` 時直接重現

```
/usr/bin/tar: Cannot connect to C: resolve failed
[FAIL] tar 解壓失敗（exit code 128），部署包不完整，已中止。
```

`942e3c4` 的理由是「確保與 git archive 生成的 tar 格式完全兼容」，但 git archive 產生的是標準 POSIX tar，Windows 內建 bsdtar（實測 3.8.8）讀得好好的——這個理由不成立，改回去之後 bug 立刻重現。

修法是把**兩個互相獨立的歧義來源一起處理**（只修一個都還會壞）：

1. **用哪一支 tar**：改回優先 `%SystemRoot%\System32\tar.exe` 並寫完整路徑，不受 PATH 順序影響。這台機器 `where tar` 其實就是它，但腳本刻意優先挑 Git tar 所以還是踩到。沒有內建 bsdtar 的舊系統才退回 Git tar，且那條路徑補 `--force-local` 明確告訴 GNU tar「冒號不是主機名」。
2. **傳什麼形式的路徑**：反正已經 `Push-Location $pkgDir` 了，改傳相對檔名而非 `C:\...` 絕對路徑，讓 `-f host:path` 遠端磁帶機語法從根本上咬不到，兩支 tar 都吃得下。

同時印出實際使用的解壓工具路徑，跟同一支腳本剛加的 Python 直譯器守門同一個做法。

> **這件事本身就是本週最大教訓的實證**：先前 11:28/11:31 那兩份包能成功解壓，代表這個失敗是**環境相依**的——所以真正保護你的不是「這次跑得過」，而是那三層防護（`git archive` exit code / `tar` exit code / 解壓後驗證 `backend`、`frontend` 目錄存在，L190/L206/L215）。這次就是被第二層擋下，沒有再產出一份空殼部署包。

### C-3 測試基礎建設：flaky 排查與意外挖出的真問題

| 症狀 | 起點 | 根因 | 現況 |
|------|------|------|------|
| 測試假資料寫進真實 `G:` 雲端硬碟 | 09-07 15:22 發現 | `conftest.py` patch 的是早已重構成函式的 5 個大寫常數，是死碼；`clients.json`／`suppliers.json` 全量快照一度被測試資料整個覆寫 | ✅ `a55fe26`（改 patch `archive._archive_base` 函式本身） |
| `test_login_create_submit_approve_smoke` 整套跑偶發逾時 | 09-07 21:07 | 排除執行緒洩漏；找到放大因子：`no_cache_static` 對自架 vendor 也發 `no-store` | ⚠️ `03723e8` 修了放大因子，**根因仍未證實** |
| `test_daily_backup_writes_to_s3_and_marker_prevents_rerun` flaky | 09-09 17:26 | 斷言比對「整個 fake S3 物件總數」，會被其他測試殘留的背景備份執行緒干擾（實測 501 != 500，單獨跑穩定） | ✅ **本輪修復**：改為只比對每日備份前綴（`test_cloud_storage_2026_09_07.py:194-206`），並移除打包腳本裡的跳過需求 |

### C-4 「當月收支」一晚六輪

09-09 17:42 → 22:51 之間 `frontend/js/reports.js` 被改 4 次、`routers/reports.py` 2 次、`routers/dashboard.py` 1 次：

1. `3f9755f` 17:42 卡片可點選＋當月收支獨立顯示
2. `c063a72` 17:43 初始化沒自動載入（前一分鐘的改動立刻補洞）
3. `3e29ba7` 19:33 點選後自動捲動
4. `c5a5d11` 22:10 **日期格式 bug 導致跨月污染**
5. `da88433` 22:49 當月收支邏輯統一＋未收
6. `6bfcafb` 22:51 完整修正＋部門篩選

底層原因是 09-09 12:29 `c15ef84` 已經記載過的那件事——**首頁與營運報表對「同一個數字」本來就有兩套歸月邏輯**（reports.py 09-02 起用 `expenseDate`，dashboard.py 還在用精算完結時間），抽出 `helpers/quotations.py::settlement_extra_expenses()` 共用之後，前端才開始跟著一路對齊。這種「同一個數字兩處各算一次」是本專案反覆出現的缺陷型態（另見 `summarize_payment_items()` 抽出的緣由）。

### C-5 09-10 上午：測試關卡失效期間的四批改動

| 時間 | commit | 內容 | 測試關卡狀態 |
|------|--------|------|------------|
| 09:35 | `7c2a276` | 叫料 API + `diagnose.ps1` | 09:50/09:53 打包 → **全 E** |
| 09:45 | `83c0a1a` | 叫料寫入權限檢查 | |
| 09:48→09:49 | `cf5a4d2`→`00f1224` | `diagnose.ps1` 語法錯誤 → 直接刪除 | |
| 10:31 | `3a9332d` | PDF 返回碼邏輯 | 10:44 打包 → **全 E + PermissionError** |
| 11:27 | `f8198e9` | 月支出頁籤／WebAuthn 可設定化／案件超期通知 | **沒有 build log** |
| 11:31 | `9b0ad79` | 補導出 → **部署上正式機 11:32:13** | |
| 11:41 | `aeefcc6` | WebAuthn 設定 UI | 未部署 |

`CHANGELOG.md:61` 自述「正式機部署前所有改動已通過**基本語法檢查**」——這句話是準確的，也正是問題所在：**這次上正式機的版本沒跑過 pytest**。

---

## §D 排查排程 checklist（12 項，附指令與本次實測結果）

> 用法：出事時或每週固定跑一次，從上到下。前 4 項是「先確定現況」，5-8 是「確認關卡有效」，9-12 是「確認紀錄沒漂移」。

| # | 查什麼 | 指令 | 2026-09-10 實測 |
|---|--------|------|----------------|
| 1 | 正式機在跑哪個 commit | 見 [§A](#a-範圍與正式機現況) 的 Python 片段 | `9b0ad79`，11:32:13 套用 |
| 2 | 正式機顯示版本 vs manifest | 同上 `/api/system/version` ＋ `backend/version_manifest.json` 第一筆 | 兩邊都是 `2026-09-09c`；**但程式碼已到 09-10** ⇒ manifest 漏補 |
| 3 | 有幾個 commit 還沒上正式機 | `git log --oneline <正式機commit>..HEAD` | 1 個（`aeefcc6`） |
| 4 | 部署歷史與失敗率 | `Get-Content backend\tools\deploy_dashboard_history.json` | 20 筆／6 失敗；**09-10 那次不在檔案裡** |
| 5 | 打包關卡是否真的會擋 | `Get-ChildItem backend\tools\deploy_logs\*_build_*.log \| Sort LastWriteTime -Desc \| Select -First 3` | 最後一次 10:45 **FAIL**，之後的部署包沒有 log |
| 6 | 測試跑在哪個 Python | `python -c "import sys;print(sys.executable)"` ＋ `where python` | PATH 上有 **4 個** Python；打包實際跑 `pythoncore-3.14-64` |
| 7 | 關鍵依賴是否齊全 | `python -c "import multipart, fastapi, webauthn, pyotp, qrcode, boto3, openpyxl, PIL"` | 3.14 缺 `multipart` ⇒ 全套測試 E |
| 8 | 全套測試 | `python -m pytest tests/ -q -m "not e2e" -n auto --basetemp=<乾淨路徑>` | **502 passed, exit 0**（本輪修復後） |
| 9 | repo 是否乾淨 | `git status --short` | 乾淨 |
| 10 | 部署包是不是空殼 | 數 `deploy_packages\<pkg>\backend`／`frontend` 檔案數 | 174／74，正常 |
| 11 | `deploy_manifest.json` 欄位完整性 | `Get-Content deploy_packages\<pkg>\deploy_manifest.json` | `version_manifest_latest` 為 **null**（見 §E-5） |
| 12 | 文件與程式碼的落差 | grep QUICK.md §12／CHANGELOG 最新日期，對照 `git log -1` | §12 停在 09-09 15:57、CHANGELOG 缺 09-08/09-09 兩天 |

**第 8 項的陷阱（本輪實測踩到）**：`%TEMP%\pytest-of-hichan\pytest-current` 是一個目標讀不到的損壞 reparse point，`os.stat()` 回 `WinError 5` 而不是「找不到」，pytest 的 `cleanup_dead_symlinks` 因此在 session 收尾整個炸掉——**即使測試全過，pytest 也會回非 0**。用 `--basetemp` 指到別的路徑可完全繞開；要根治需以系統管理員身分執行：

```
rd "C:\Users\hichan\AppData\Local\Temp\pytest-of-hichan\pytest-current"
```

（一般權限下 `Remove-Item`／`rd`／`del`／`icacls` 全部 Access denied，已實測。）

---

## §E 已驗證缺陷清冊

### E-1 叫料 API 完全無效（P0，已修）

**檔案**：`backend/routers/material_orders.py`（`7c2a276` 09-10 09:35 新增，同日 11:32 隨 `9b0ad79` 上正式機）

四個缺陷同時存在，而且**最外層那個會遮蔽其餘三個**：

| # | 位置（修復前） | 問題 | 影響 |
|---|--------------|------|------|
| 1 | L106-109 缺 `conn.commit()` | `db.py:119` 用 `sqlite3.connect(path, timeout=30)`，非 autocommit；`finally` 直接 `conn.close()` → 交易丟棄 | **端點回 200 `{"status":"ok"}`，資料庫一個字都沒寫** |
| 2 | L106-109 參數錯位 | `save_quotation_json(conn, no, data, user["id"], "更新叫料清單（N 項）")`，真實簽名是 `(conn, quote_no, data, status=None, updated_at=None)`（`helpers/quotations.py:354-360`） | 一旦補上 commit，會執行 `UPDATE quotations SET status=<user_id>, updated_at='更新叫料清單…'`（`helpers/quotations.py:367-373`）——**污染報價單狀態欄位** |
| 3 | L112-113 `_audit(conn, ...)` | 真實簽名 `_audit(token, action, target_type, target_id, ...)`（`helpers/audit.py:70-77`）；`conn` 當 token 進 SQL 參數會拋例外，被 L78 的 `try` 吞掉 | 稽核記錄從來沒寫成功 |
| 4 | L72 `data.get("deal_tag")` | data_json 裡的鍵是 `dealTag`（`routers/quotations.py:1067, 1308`），權威來源是資料表欄位（正確寫法見 `routers/quotations.py:248-251`） | 「已結案不能改叫料」是死碼 |
| 5 | 兩支端點皆無擁有者檢查 | `quote_no` 格式可列舉（MQ-YYYYMM-NNN） | **IDOR**——任何登入者可讀/改別的業務的案件叫料。這正是 2026-08-24 安全審查修過的同一類問題（`_check_quotation_owner()` 的存在理由） |
| 6 | L145-146 直接索引 `o["totalPrice"]` | 自由格式 JSON 不保證有該 key | 舊資料會讓 GET 整支 500 |

**本輪修復**
- `save_quotation_json(conn, quote_no, data)` + `conn.commit()`（比照 `routers/quotations.py:1313-1314` 既有寫法）
- `_audit(_tok(authorization), 'material_orders.update', 'quotation', quote_no, ...)`
- 已結案改用 `SQL_DEAL_TAG`（欄位優先、pre-v6 舊列回退 data_json）
- **`_check_quotation_owner()` 從 `routers/quotations.py` 抽到 `helpers/quotations.py`**——這是第三個呼叫點，比照 `summarize_payment_items()` 的既有慣例，讓之後任何「用 quote_no 直接取單筆」的新端點都能直接引用
- 合計改用 `.get()` + `float()`
- `mo.dict()` → `mo.model_dump()`（pydantic v2）
- 新增 `backend/tests/test_material_orders_2026_09_10.py`（7 題，每題都**從資料庫讀回來核對**而不是只看 HTTP 200——回應本身正是當初最會騙人的東西）

**仍未做**：前端 UI（`db_migration_plan.md:17-19` 規劃的「叫料清單卡片」），功能目前仍是後端-only 半成品。

### E-2 打包測試關卡長期紅燈（P0，已修）

**兩個獨立原因疊在一起**，任一個都足以讓關卡失效：

1. **PATH 解析到沒裝 `python-multipart` 的 Python**。`build_deploy_package.ps1` 用裸 `python` 呼叫 pytest；`where python` 顯示 4 個候選，儀表板子行程解析到 `C:\Users\hichan\AppData\Local\Python\pythoncore-3.14-64`。實測 `py -3.14 -c "import multipart"` → `ModuleNotFoundError`，於是所有走 `Form`/`File` 的端點測試在 fixture 階段就 `RuntimeError: Form data requires "python-multipart" to be installed.`，整套幾乎全 E（見 `deploy_logs/20260910_095031`、`095336`、`104435`）。
2. **損壞的 `pytest-current` reparse point** 讓 pytest 在 session 收尾拋 `PermissionError [WinError 5]`，**測試全過也會回非 0**（2026-09-10 10:06 起）。

**後果**：09-10 10:44 之後的兩份部署包沒有走儀表板（`deploy_logs` 沒有對應 log），其中 `9b0ad79` 直接上了正式機。

**本輪處置（已全部完成）**

1. 補 `requirements.txt`（見 E-3）＋修 flaky 測試 ⇒ 全套測試回到 **502 passed / exit 0**。
2. **`build_deploy_package.ps1` 新增 Step 2.5「釘住 Python 直譯器並驗證依賴齊全」**：先用 `Get-Command python` 解析出實際路徑、印出來、印版本，再跑一次 `import` 檢查涵蓋 `requirements.txt` 全部套件；缺任何一個就直接 `Fail`，訊息裡指名是哪一支直譯器、缺什麼、兩種修法（對這支裝依賴 / 調整 PATH 順序）。之後所有 pytest 呼叫一律用 `& $pyExe -m pytest`，不再用裸 `python`。
3. **移除 `--deselect`**（`ee4664a` 對 flaky 測試挖的洞）——測試本身已修好，不該再從關卡跳過它。
4. **pytest 加 `--basetemp`**（每次執行一個時間戳目錄），繞開 `%TEMP%` 下那個損壞的 `pytest-current` reparse point。

**已實測兩條路徑**（用同樣的 `$ErrorActionPreference = "Stop"` 環境）：

| 情境 | 結果 |
|------|------|
| `$pyExe` = `pythoncore-3.14-64`（缺 multipart） | 印出直譯器路徑＋`ModuleNotFoundError: No module named 'multipart'`＋兩種修法，**exit 1 擋下** |
| `$pyExe` = PATH 第一順位（3.11.15，依賴齊全） | `[OK] 依賴齊全。` **exit 0 放行** |

> 附帶確認：這段沒有用 `2>&1`，所以不會踩到本專案已知的 PS 5.1 `NativeCommandError` 地雷（那個坑只在腳本內部重導 stderr 時才會觸發）——上面兩次實測都是在 `$ErrorActionPreference = "Stop"` 下跑的，行為正確。

理由：缺套件原本的症狀是「470 題全部 E」，要往下捲三千行才看得到真正的 `RuntimeError`；現在直接指名「用的是哪一支 Python、缺哪個套件」。這與 `549d319` 對 `tar` 做的「用完整路徑避開 PATH 歧義」是同一個結論，只是當時沒有推廣到 Python 呼叫。

### E-3 `requirements.txt` 缺 `python-multipart`（P0，已修）

`backend/requirements.txt` 原本只有 10 行、沒有這支。FastAPI 的 `Form`/`File` 端點硬依賴它，正式機能跑純粹是因為環境早就裝過。任何依 requirements 重建的環境（DR 還原、換機、`apply_update.ps1` 的 `[4/6] pip install`）都會缺，且症狀是**服務啟動成功、只有上傳類端點 500**，非常難第一時間聯想。
09-07 `90c6f31`「補齊 requirements.txt 缺漏套件」漏了它。✅ 本輪已補 `python-multipart>=0.0.9`。

### E-4 WebAuthn 改動的測試沒同步（P1，已修）

`f8198e9`（09-10 11:27）把 RP ID／Origin 改成未設定就回 503，但沒改 `backend/tests/test_webauthn_basic.py`，導致 3 題失敗（`assert 503 == 200` / `assert 503 == 404`）並隨部署上線。因為當時測試關卡已經因 E-2 全紅，沒有任何機制擋下。
✅ 本輪：加 `webauthn_config` fixture 設好網域、另補一題正面驗證「未設定就是 503」這個新行為；並修正 `routers/auth.py:730-749` 兩處 docstring（原本寫「fall back to localhost」，實際回 `""`）。

### E-5 `deploy_manifest.json` 的 `version_manifest_latest` 為 null（P2，已修）

兩份 09-10 部署包都是 `null`。`build_deploy_package.ps1` 讀 `version_manifest.json` 取 `$entries[0]`，失敗時只印 `[WARN]` 就繼續；但**檔案找不到時連 `[WARN]` 都沒有**（`Test-Path` 為假就整段跳過，靜默留 `null`）。本輪實測該檔案可以正常解析（無 BOM、288 筆、`entries[0]` = `2026-09-09c`），所以 null 是在別的執行環境下產生的，而當時完全沒有任何線索能分辨是「檔案沒找到」還是「解析失敗」。
影響：部署包失去「這包對應哪個版本標籤」的資訊，跨機版本比對只剩 commit hash。
**本輪修復**：補上 `else` 分支（找不到檔案時明確警告並印出路徑）、解析成功時印出實際取到的版本、解析失敗時把例外訊息一併印出——三種結果都看得見，不再有靜默的 null。

### E-6 其他 P2

| 項目 | 位置 | 說明 |
|------|------|------|
| ✅ 徽章金額為 0 時整個消失 | `frontend/pages/reports.html:434` | `x-show="...? monthExpenseTotal : expensesTotals.total"` 用金額當真值，當月支出剛好 0 時徽章不顯示，看起來像功能壞掉而不是「這個月沒有支出」。**已修**：改成 `x-show="!!expensesData"`（依資料載入與否判斷），$0 會正常顯示成 $0 |
| ✅ 誤入 repo 的暫存檔 | `backend/.commit_msg_webauthn.txt` | `0527524` 連同 commit message 暫存檔一起提交了。**已修**：`git rm` 移除，`.gitignore` 加 `**/.commit_msg*.txt` 防再犯 |
| ✅ 腳本說明已過時 | `build_deploy_package.ps1` 檔頭 | 仍寫「這台開發機的 git repo 根目錄是整個使用者家目錄」，但 MOTRIX-ERP 已於 `e6bf102` 拆成獨立 repo，`$relPath` 現在恆為空字串。**已修**：改寫成兩種情況都說明，並註記「Project path: 印出空白不是壞掉」 |
| ✅ 版本端點沒防 BOM | `routers/auth.py:281`（已修，現為 `:290`） | `system_version()` 用 `encoding="utf-8"` 讀 `version_manifest.json`，例外被 `except` 吞成空字串。目前該檔無 BOM 所以沒事，但只要有人用 PowerShell `Set-Content -Encoding UTF8` 重寫它就會 BOM 化，登入頁版本號會直接變空白且無任何錯誤——與 `1c8f2e8` 修過的 `deployed-version` 是同一類問題。**已修**：改 `utf-8-sig`；同一輪發現 `helpers/startup.py::_sync_module_versions()` 也是同樣寫法（BOM 化會讓模組版本同步靜默停擺），一併改掉 |
| 健康檢查根因未證實 | `apply_update.ps1` / `_healthcheck_ping.py` | §12 已誠實標註「合理猜測」。下一個該懷疑的方向是「port 666 重新綁定那個瞬間的競態」，前三個假設已排除 |

---

## §F 未部署差異

正式機 `9b0ad79` → 開發機 HEAD `aeefcc6`，**差 1 個 commit**：

```
aeefcc6  feat: 新增 WebAuthn 設定 UI 至公司資料設定頁面
         -> frontend/pages/company-profile-settings.html
```

⚠️ **這一個 commit 的缺席有實際後果**：`f8198e9` 已經讓四個 WebAuthn 端點在 RP ID 未設定時回 **503**，而唯一的設定入口就在 `aeefcc6` 這支未部署的頁面裡。也就是說——

> **正式機目前的 Passkey 功能是不可用狀態**：後端已上線、前端登入頁的按鈕會依 `GET /api/system/webauthn-config-status` 隱藏，但沒有任何地方能填 RP ID。要啟用需先部署 `aeefcc6`，再以 superadmin 進入 `company-profile-settings.html` 填入內部 DNS 網域（需先把 DNS 指向 172.16.10.177）。

---

## §G 文件漂移清單

| 文件 | 漂移 | 本輪處置 |
|------|------|---------|
| `MOTRIX-ERP-QUICK.md` §12 | 最新條目停在 09-09 15:57（`f935119`），**其後 28 個 commit 無紀錄** | 已補 09-09 深夜～09-10 |
| `CHANGELOG.md` | **09-08、09-09 兩天完全 0 筆**（grep 命中 0 次），從 09-10 直接跳到 09-07 | 已補兩則 |
| `MOTRIX-ERP-QUICK.md` §11 | WebAuthn（L1176）、案件超期通知（L1178）、營運報表月/年分離（L1179）三項仍標「尚未實作」，實際都已上線 | 已改 ✅ 並指向本文件 |
| `backend/version_manifest.json` | 最新 `2026-09-09c`，09-10 四項改動未補；正式機登入頁版本號因此停在昨天 | 已補 |
| `MOTRIX-ERP-QUICK.md` §14.3c | 寫「還沒有真正跑過一次完整的部署/回滾」，實際已跑 20 次 | 已更正 |

---

## §H 本輪處置與待辦

### 已完成（程式碼）

| 檔案 | 改動 |
|------|------|
| `backend/routers/material_orders.py` | E-1 全部六項 |
| `backend/helpers/quotations.py` | 新增 `_check_quotation_owner()`（從 routers 抽出） |
| `backend/helpers/__init__.py` | 匯出 `_check_quotation_owner` |
| `backend/routers/quotations.py` | 改為從 helpers 引用，刪除本地定義 |
| `backend/requirements.txt` | 補 `python-multipart>=0.0.9` |
| `backend/routers/auth.py` | 修正 WebAuthn 兩處過時 docstring |
| `backend/tests/test_material_orders_2026_09_10.py` | 新增，7 題 |
| `backend/tests/test_webauthn_basic.py` | 修 3 題 + 新增 1 題（503 行為） |
| `backend/tests/test_cloud_storage_2026_09_07.py` | 修 flaky 斷言 |

**第二輪（後續修復）**

| 檔案 | 改動 |
|------|------|
| `backend/tools/build_deploy_package.ps1` | 新增 Step 2.5 釘住 Python 直譯器＋依賴前置檢查；移除對 flaky 測試的 `--deselect`；pytest 加 `--basetemp` 繞開損壞的 `pytest-current`；`version_manifest_latest` 三種結果都印出來（不再靜默 null）；更新過時的 repo 根目錄說明 |
| `backend/routers/auth.py` | `system_version()` 改 `utf-8-sig` 讀 manifest |
| `backend/helpers/startup.py` | `_sync_module_versions()` 同上 |
| `frontend/pages/reports.html` | 月支出徽章改依資料載入與否顯示，$0 不再消失 |
| `.gitignore` ＋ `backend/.commit_msg_webauthn.txt` | 移除誤入的 commit message 草稿，加規則防再犯 |
| `backend/tools/build_deploy_package.ps1`（`d2ff23f`） | 徹底解決 tar 路徑歧義（見 [§C-2](#c-2-tar-實作在-48-小時內來回翻了兩次)）——實際打包時被新關卡當場抓到並修好 |

**驗證（實際跑過完整打包流程，不是只跑測試）**

| 關卡 | 結果 |
|------|------|
| `.ps1` 語法檢查 | OK（5 支） |
| **Python 直譯器守門（新）** | 印出 `hermes-agent\venv\python.exe` / 3.11.15，依賴齊全 |
| 非 e2e pytest | **502 passed** |
| e2e pytest（真實瀏覽器） | **4 passed** |
| 版本標籤 | `2026-09-10d`（不再是 null） |
| tar 解壓 | 印出 `C:\WINDOWS\System32\tar.exe`，成功 |
| 產出 | `deploy_packages\20260910_134616_d2ff23f`，backend 174 檔／frontend 74 檔，無殘留 `snapshot.tar` |
| **build exit code** | **0** |

另：`build_deploy_package.ps1` 每次改動後都確認 BOM（`EF BB BF`）與 CRLF 保留、PSParser 語法檢查 0 errors。

### 待辦

| 優先 | 項目 | 位置 |
|------|------|------|
| 🔴 | **套用部署包到正式機**——包已備妥且通過完整關卡，只差輸入正式機密碼那一步（見下方「部署交接」） | — |
| 🟡 | 以系統管理員身分刪掉損壞的 `pytest-current` reparse point（`--basetemp` 只是繞開，沒有根治） | [§D](#d-排查排程-checklist12-項附指令與本次實測結果) 第 8 項 |
| 🟡 | 叫料功能補前端 UI，或明確標記為未完成不要讓使用者看到 | `db_migration_plan.md:17-19` |
| 🟢 | 健康檢查偽陽性根因仍未證實，下次失敗時查「port 666 重新綁定瞬間的競態」 | [§C-1](#c-1-部署工具鏈一整晚怎麼修都測不到自己剛寫的程式碼) #8 |
| 🟢 | 部署一律走儀表板，不要直接跑腳本——否則同時失去打包測試關卡與歷史紀錄兩層保障 | [§A](#a-範圍與正式機現況) |

### 部署交接（2026-09-10）

部署包已備妥並通過完整關卡，**只差一個需要人的步驟：輸入正式機密碼**（`motrix_cred.xml` 未儲存，`_dashboard_remote.ps1` 從 STDIN 讀密碼，設計上就不落地）。

| 項目 | 值 |
|------|-----|
| 部署包 | `deploy_packages\20260910_134616_d2ff23f` |
| commit | `d2ff23f`（master，已 push 到 origin） |
| 涵蓋範圍 | 正式機 `9b0ad79` → `d2ff23f`，含**先前一直沒部署的 `aeefcc6`**（WebAuthn 設定 UI）＋本次兩輪修復 |
| 部署儀表板 | 已在跑（`127.0.0.1:8765`，實測 `/api/packages` 已看得到這個包） |
| 正式機 WinRM | `172.16.10.177:5985` 實測可達 |

步驟：瀏覽器開 `http://127.0.0.1:8765` → 套件下拉選 `20260910_134616_d2ff23f` → 輸入 `Motrix` 帳號密碼 → 兩段式確認（第一次跳摘要卡片，再按【確認套用】才真的執行）。

套用後建議確認：

1. `GET https://172.16.10.177:666/api/system/deployed-version` 應回 `d2ff23f`
2. `GET https://172.16.10.177:666/api/system/version` 應回 `2026-09-10e`（先前停在 `2026-09-09c`）
3. 以 superadmin 進 `company-profile-settings.html` 填入 WebAuthn RP ID／Origin，Passkey 才會從 503 變成可用（**需先把內部 DNS 指向 172.16.10.177**）
4. 隨手開一張案件的財務分頁，確認月支出徽章在金額為 0 時仍會顯示 `$0`
