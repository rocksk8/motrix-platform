# 販售化與手機版 — 兩條端到端細線

> 建立 2026-09-21。維護者：視窗 A。搭配 [`MULTIWIN-PROTOCOL.md`](MULTIWIN-PROTOCOL.md) 使用。
> **這份定義「做到哪裡算完成」。每一輪的產出都要站在某一步上，站不上去的不做。**
>
> ⚠️ **2026-09-21 下午優先順序已重排，先讀下一節。**後面的細線 1／2 內容仍然正確，但**優先序已經不是那樣了**。

---

# 🔄 2026-09-21（下午）· 優先順序重排（使用者裁示）

> 使用者原話：「今天的順序是**既有模組的不足、未跟大型系統擁有的功能先做開發**，
> **販售部分先放未來**，旅宿管理的部分，**先研擬即可不動工**。」

## §R.1 · 四條線的新狀態

| 線 | 新狀態 | 說明 |
|---|--------|------|
| **細線 1 · 授權底座** | 🧊 **凍結在第 3 步（已完成）** | 見 §R.2 |
| **細線 2 · 手機專用頁面** | ⏸ 維持暫緩 | 不變 |
| **細線 3 · 旅宿體驗顧問** | 📐 **只研擬，不動工** | 規格已寫完；`outcomes` 可信度設計題在 `docs/windows/STATE.md` §8 交給 C。**設計結論產出後就停，不出開發單** |
| **細線 4 · 成本閉環** | 🆕 **今天的主線** | 見 §R.3 |
| **細線 5 · 售後閉環** | 🆕 排在細線 4 之後 | 見 §R.4 |
| **細線 6 · 標案雷達** | 🆕 **使用者 2026-09-21 交辦**，排在暖身輪之後 | 見本檔末尾〈細線 6〉 |

## §R.2 · 細線 1 凍結在哪、為什麼是安全的

**不是半途而廢，是剛好在一個乾淨的邊界上停下來。**

| 步 | 狀態 |
|---|------|
| 1 產金鑰 / 2 status 端點 | ✅ 完成（`2dadfb6`，27 題綠，⚠️ 待 C 的⑤⑥） |
| 3 無效／過期擋住 | ✅ 完成（`fb329be` 總開關＋kind＋快取、`a63cc49` middleware、`3f969c2` 註解） |
| 4 模組閘 / 5 抬頭 / 6 到期提醒 / 7 換機 | 🧊 **凍結，未開始** |

**為什麼停在這裡零風險**：`LICENSE_GATE_ENABLED = False` 是預設值，
middleware **完全不介入**（連 `verify_license()` 都不呼叫）。
整套授權程式碼在 repo 裡是**惰性的**——它存在、被測試涵蓋、但不做任何事。

⚠️ **凍結期間唯一要守住的性質**：「開關 False ＝ 完全不介入」。
這一條由 C 在第 1 輪的⑤⑥一併釘住，**不可以因為細線 1 凍結就跳過**——
程式碼已經 commit 進 repo 且 `main.py` 已經 import 它，沒驗過就是 repo 裡有未驗證的碼。

⚠️ **解凍時要記得的三件事**（不然會重新踩一次）：
① 第 5 步（抬頭）是**任何外部客戶的絕對阻擋**——不做的話對方單據上是「允碩整合集創」
①b 🔴 **解凍第一件事不是打開開關，是補寫 `backend/tests/test_licensing_gate_2026_09_21.py`**。
第 2 輪（middleware／`kind`／快取）的 13 條驗收條件，**除了「開關關著時完全不介入」那一條以外，一條都沒被測試過**
（A 當時用第 1 輪的證據結了第 2 輪的案，見 STATE §5 第十次裁決）。
⚠️ 另：`a63cc49` 的 middleware **關著時無害已驗**（1,111 綠），但**開著時的行為零覆蓋**——`backend/tests/` 對 `LICENSE_GATE_ENABLED` 零命中，而 middleware 第一行就是 `if not ...: return`，所以 **`main.py:282` 以下從未被任何測試執行過一次**。（C 先拓到回歸沒涵蓋，B 再拓到「載入了≠跑到了」；A 兩次都把證據讀得比事實寬。）

② 八個套餐的展開表已經定案在 `docs/windows/STATE.md` §4
③ `_PUBKEY_PROD` 還是空的，正式私鑰保管方式未決

## §R.3 · 細線 4 · 成本閉環（今天的主線）

**定義：一個案件從「事前定預算」到「事後看得出差多少」的完整一圈。**

這條線同時命中使用者講的兩類：
- **既有模組的不足**：`SELLABLE-AND-MOBILE-SPEC.md` §5.1 第 3 項（預算 vs 實際）
  與第 6 項（內部工時→成本）實測都是**零**
- **大型系統有而我們沒有**：專案型 ERP 的核心指標就是這個

| # | 步驟 | 新開發？ |
|---|------|---------|
| 1 | 報價／案件可以填**成本預算**（不只是售價） | 🆕 小 |
| 2 | 案件可以**分派人員到執行進度關卡** | 🆕 中 |
| 3 | 被分派的人可以**填工時** | 🆕 中 |
| 4 | 工時**自動換算成本**進精算 | 🆕 小 |
| 5 | 看得到**預算 vs 實際**的差異（含超支警示） | 🆕 小 |

**⭐ 第 2、3 步就是 `assignments` 模組，也就是細線 3（旅宿）的第 5 步。**
做一次，內部與旅宿垂直**同時**受益——這是「先開發我內部的功能」與
「旅宿先研擬」兩件事的交會點，也是為什麼它排在最前面。

⚠️ **風險：工時靠人填，會被湊數。** 對策：工時必須掛在**具體的執行進度關卡**上，
不能只填一個案件總數。填不到關卡的工時就是填不出來——**用資料結構擋，不要靠自律**。

## §R.4 · 細線 5 · 售後閉環（細線 4 之後）

**定義：完工不是終點，是下一張報價的起點。**

現在的生命週期是一條線，完工單之後就斷了：
```
報價 → 簽核 → 案件 → 執行 → 出貨 → 完工單 → ■
```

| # | 步驟 |
|---|------|
| 1 | 完工單結案 → 建立**保養排程**（沿用既有設備登載／保固追蹤的資料） |
| 2 | 保養到期前**自動提醒**（掛既有 `daily_tasks.py`，比照憑證到期檢查） |
| 3 | 產生**保養工單** |
| 4 | 工單**派人、執行、回報**（沿用細線 4 的 `assignments`） |
| 5 | 保養完成 → **一鍵產生下一張報價**（回到 CRM，圈就合起來了） |

## §R.5 · 暖身輪（在細線 4 之前，一輪就做完）

**兩個已知的 v1 缺口，加起來大概一天。先做便宜的，證明迴圈還會動。**

1. **供應商前置時間**（`SELLABLE-AND-MOBILE-SPEC.md` §5.1 第 4 項，實測 `lead_time` 0 處。⚠️ **2026-09-21 更正（視窗 B 查到）**：`suppliers.html:636` 本來就有一個 `form.leadTime`（標籤「標準交期」、自由文字如「4~6 週」、存 `data_json`），**而後端沒有任何一行讀它**。欄位一直都在，只是沒人用——所以這一輪補的不是「從無到有」，是「從一個沒人讀的文字欄位，變成一個算得動的數值欄位」）
   —— 補了它，採購建議才算得出 ETA
2. **採購建議的下單／已到貨狀態追蹤**（架構地圖 §6.6 自述「v1 刻意不含」）

這兩項都是**既有模組自己承認的缺口**，不是新功能，正好對應使用者說的「既有模組的不足」。

## §R.6 · 明確不做（維持先前裁決，不要再提）

- BOM／套件組合（系統整合不是製造業，用報價範本解便宜十倍）
- 對外 API／webhook（沒有客戶要求過）
- 自訂報表產生器（真的缺就加第 14 個頁籤）
- 複式記帳／總帳／多幣別


---

## §0 · 開工前的事實盤點（2026-09-21 實測，非引用文件）

| 項目 | 實測值 |
|------|--------|
| 後端 router | 39 支，29,077 行 |
| 跨 router 相依 | **只有 10 處**，集中在 6 支（`accounting_export`／`cashier`／`dashboard`／`reports`／`search`／`vendor_contractors`） |
| 資料表 | **78 張全在同一個 `backend/db.py`**（3,639 行、84 個 migration、單一 SQLite 檔） |
| 前端頁面 | 56 頁、49,456 行；`frontend/js/` 只有 2 支有效外置 JS，其餘 Alpine 全 inline |
| 前端 RWD | 殼層**已完成**（`style.css` 1,668 行，平板 icon-rail ＋ 手機抽屜側欄）；**但 56 頁只有 18 頁有自己的 `@media`** |
| 測試 | 136 檔、**1,084 題**（`pytest --collect-only`；⚠️ 本表原記 1,034，那是 `grep -c "def test_"` 算的，漏掉 7 個檔的 parametrize 展開共 50 題，2026-09-21 由視窗 C 抓到並更正） |
| 授權／多租戶機制 | **不存在。全 codebase 零 license／序號／到期／tenant 概念** |
| 公司抬頭寫死 | **56 行 / 14 檔**（2026-09-21 複查更正，見下） |
| 模組權限 | 權限目錄 **40 個** key，**35 個後端真的會擋**，只有 **5 個**是純側欄開關 |
| 既有設定 | `system_settings.company_profile` 已存在，但**只有銀行欄位被 PDF 讀取**（`pdf_gen.py:2006`），名稱／統編／電話仍是寫死 |
| 相依 | `cryptography>=42.0.0` **已在 `requirements.txt`** → 離線簽章零新增套件 |

**判定：**
- 模組「拆開獨立販售」在**業務邏輯層可行**（相依圖乾淨），在**資料層不可行**（單一 db.py）。
- 正確的產品型態是 **底座（Core）＋ 可插拔模組包**，不是 N 個獨立系統。
- 依使用者 2026-09-21 裁示：**先做授權底座，不先拆模組。**

---

# 細線 1 · 授權底座

**定義：能把這套系統賣給第二家公司，並且裝得起來、過期會擋、抬頭是他們自己的。**

## 七步

| # | 步驟 | 完成的樣子（驗收條件） |
|---|------|----------------------|
| 1 | **產一把金鑰** | 跑 `python backend/tools/issue_license.py --customer "某某公司" --modules ... --expires 2027-09-21 --machine <指紋>` 產出一個 `.key` 檔 |
| 2 | **裝進一台機器** | 把 `.key` 放到 `backend/license.key`，重啟服務，`GET /api/license/status` 回 `{valid:true, customer:"某某公司", expires:"..."}` |
| 3 | **無效就要擋住** | 金鑰不存在／簽章錯／機器指紋不符／已過期 → 服務**照樣起得來**，但所有業務 API 回 **402** 並附人看得懂的原因；只有登入頁與授權頁還能開 |
| 4 | **金鑰決定開哪些模組** | 金鑰裡沒有 `monitor_guide` → 該 router 的端點回 402、側欄不出現該項目。**與使用者模組旗標是兩回事**：金鑰管「這台機器買了什麼」，旗標管「這個人能看什麼」 |
| 5 | **抬頭不再寫死** | 56 行全部改讀 `company_profile`；報價單／出貨單／請款單／完工單 PDF 上印的是設定值。收斂條件見下方〈第 5 步的真實範圍〉 |
| 6 | **到期前會提醒** | 剩餘 30／14／7／1 天時，登入後畫面顯示橫幅 ＋ 寄信給 superadmin（掛在既有 `daily_tasks.py` 排程，比照 `_check_cert_expiry()`） |
| 7 | **換機裝得上去** | 在 B 機器用 A 機器的金鑰 → 第 3 步的擋住行為；重發一把綁 B 機器的 → 第 2 步的通過行為 |

## 第 5 步的真實範圍（2026-09-21 A 實測，**更正先前的 124 處**）

先前寫的「124 處」是錯的——那次 grep 沒有排除 `backend/rollback_snapshots/`
（gitignore 過的還原快照，裡面是整份 backend 的舊副本）與 `backend/tests/`。
**以 `git ls-files` 為準、排除測試檔，四個識別字串（統編／允碩／miactw／3610-6566）
去重後的實際命中是 56 行、14 個檔：**

| 檔案 | 行數 |
|------|------|
| `backend/pdf_gen.py` | **24** ← 接近一半集中在這一支 |
| `backend/db.py` | 4 |
| `backend/helpers/startup.py` | 4 |
| `backend/network_plan_export.py` | 4 |
| `frontend/pages/company-profile-settings.html` | 4 |
| `frontend/pages/users.html` | 3 |
| `backend/routers/reports.py` | 2 |
| `backend/tools/sync_pending_data_20260817.py` | 2 |
| `frontend/index.html` | 2 |
| `frontend/pages/login.html` | 2 |
| `frontend/pages/quotation-form.html` | 2 |
| `backend/helpers/auth.py` | 1 |
| `backend/main.py` | 1 |
| `backend/routers/system.py` | 1 |

**收斂條件用指令的結束碼，不要用「每個檔都改過了」**（用檔案清單劃範圍，
範圍的邊緣就是盲點）：

```bash
git ls-files backend frontend | grep -E '\.(py|html|js|css)$' | grep -v '^backend/tests/' \
  | xargs grep -Il -e 60575481 -e 允碩 -e miactw -e 3610-6566
# 收斂條件：這條指令沒有輸出
```

⚠️ **`company-profile-settings.html` 那 4 行是 placeholder**（給使用者看「該填什麼樣子」），
**那是對的、不要改掉**——改掉的話設定頁反而沒有範例。B 到第 5 步時要分辨
「印在單據上的值」與「輸入框的提示文字」，後者留著。

⚠️ `backend/tools/sync_pending_data_20260817.py` 是一次性資料搬移腳本，
`main.py` 不 import 它。**確認之後整支刪掉比改它划算**，但那是 A 的決定，到時再說。

## 設計決定（A 已裁決，不需再議）

- **離線驗證，不連網。** 客戶系統跑在自己的區網（`https://motrix.internal:666`），
  任何需要連我們伺服器的方案都會在客戶斷網時變成故障。
- **簽章用 Ed25519**（`cryptography` 已在相依裡）。私鑰只存在我們這邊，**絕對不進 repo**；
  公鑰嵌在 `backend/helpers/licensing.py` 裡。
- **金鑰內容**：客戶名稱、統編、機器指紋、模組清單、發行日、到期日、簽章。
- **機器指紋**：主機板 UUID ＋ 第一張實體網卡 MAC 的雜湊。單機方案夠用，不做硬體綁定加密狗。
- **擋住的方式掛在 `main.py` 既有 middleware 鏈上**，不要在每支 router 裡各寫一份。
- **402 不是 403。** 403 是「你沒權限」，402 Payment Required 才是「這台機器沒買」，
  兩者在客服現場要分得開。

## 不做（刻意）

- 多租戶（一個 db 裝多家公司）—— 那是另一個產品形態，不在這條線上。
- 線上授權伺服器、自動續約、金流。
- 把模組拆成獨立 repo 或獨立安裝包 —— 第 4 步做的是**開關**，不是拆檔案。

---

# 細線 2 · 手機專用頁面（現場四模組）

**定義：現場人員用手機完成一筆真實工作，回到桌面版看得到同一筆。**

> **使用者 2026-09-21 裁示：另建手機專用頁面（`frontend/m/`），不是現有頁面做 RWD。**
> A 已提出顧慮並記錄於此：**這是第二份前端實作，每次改模組要改兩個地方**，
> 本專案有過 `MIAC官網(最終)` vs `MIAC-New` 分岐 567 行的前例。裁示照辦，但加兩道防線：
>
> 1. **手機頁零業務邏輯。** 只做「呼叫既有 API ＋ 顯示 ＋ 送出」。任何計算、驗證、
>    狀態機判斷一律留在後端。手機頁不可以自己算金額、自己判斷能不能簽核。
> 2. **端點對照守門。** C 要寫一題測試，掃出 `frontend/m/**` 打的所有 API 路徑，
>    比對桌面版對應頁面打的路徑清單；桌面版改了端點而手機版沒跟上 → 該題變紅。
>    **沒有這題，分岐會在無聲無息中發生，而且是上線後才發現。**

## 七步

| # | 步驟 | 完成的樣子（驗收條件） |
|---|------|----------------------|
| 1 | **登入** | 手機開 `/m/`，用 QR 登入（2026-09-08 已上線）或密碼，拿到 session |
| 2 | **看到我的案件** | `/m/cases` 列出自己負責的案件，一頁能看完、不用橫向捲動 |
| 3 | **看執行進度** | 點進一個案件看得到 `case_stages` 各關卡狀態 |
| 4 | **勾一個進度** | 在現場勾完成 → 寫回同一張 `case_stages` 表 → **桌面版 `case-management.html` 重新整理看得到** |
| 5 | **送一張叫料** | 走既有 `routers/material_orders.py`，不新增端點 |
| 6 | **報一筆額外支出** | 走既有 `routers/case_extra_expenses.py`，含手機拍照上傳 |
| 7 | **主管手機簽核** | `/m/approvals` 列出待簽 → 按核准 → 走既有 `helpers/tiered_approval.py` → 桌面版簽核歷史看得到同一筆 |

## 設計決定（A 已裁決）

- **路徑 `frontend/m/`**，與 `frontend/pages/` 平行。共用 `frontend/css/style.css` 的
  變數與色票，**不另開一套設計語言**（另開＝第二份設計系統，分岐加倍）。
- **不新增任何後端端點。** 手機版打的每一支 API 桌面版都已經在用。
  需要新端點就代表這件事不只是「換個畫面」，退回 A 重新出單。
- **不做離線編輯／同步佇列。** 現場沒網路就是沒網路，假裝能寫入會產生對不起來的資料。
- **PWA 殼（manifest ＋ service worker）留到七步都走通之後再加**，先讓流程是對的。

## 不做（刻意）

- 原生 App 上架。理由：①系統跑在客戶區網自簽憑證，App Store 要求可公開存取的服務
  ＋隱私權政策；②原生 App 等於**第三份**前端實作；③QR 登入已解掉手機登入痛點。
  客戶真要上架時，PWA 用 wrapper 包成本最低，那時再說。
- 選型資料庫的手機版（純桌面矩陣設計，RWD 適配度未評估過）。
- 其餘 52 頁的手機版。

---

## §3 · 順序

**細線 1 全部七步走完，才開始細線 2。** 只有一個寫手，平行只會製造交接成本。

細線 1 進行中，C 在等待時可以先寫細線 2 的驗收測試骨架 —— **不寫產品碼**。

---

## §4 · 本期不碰的既有債（避免範圍蔓延）

以下在 `MOTRIX-ERP-QUICK.md` §11 是開著的，但**不在這兩條線上，本期一律不動**：

- 🟠 每案資料的 IDOR 面還沒收完
- 🟢 ~~16 個模組後端完全不讀~~ —— **2026-09-21 複查：這筆已經過時，現在只剩 5 個。**
  `MODULE-AUDIT-2026-09-13.md` 寫的 16 個是 2026-09-13 的狀態，**2026-09-14 那一輪
  （DB v84「取消 admin 直通、模組權限對管理員也生效」）把大部分補上了**，文件沒同步更新。
  實測：權限目錄 40 個 key，**35 個後端真的會擋**，只剩 5 個是純側欄開關——
  `dashboard`／`netplan`／`project_approve_biz`／`project_approve_eng`／`selection_overview`。
  死 key 0 個（後端在擋、目錄裡沒有的 key 一個也沒有）。
  **這對第 4 步是好消息**：金鑰關掉一個模組，35/40 的情況下 API 真的會擋得住。
  剩下那 5 個的處置見 `docs/windows/STATE.md` §4 第 2 項。
- 🟠 pytest 暫存不自動清（用 `--basetemp` 繞過即可，不要順手去修）
- ⚪ Let's Encrypt 憑證／RP ID 決策、Passkey 恢復
- 🟢 DR 演練

**⚠️ 一件必須在細線 2 開工前處理的：WCAG 對比度算錯了底色**

`css/style.css:48/63` 的註解把比值標成「對白底」，但站內實際底色是奶白
`--white: #F5F4F0`（`style.css:39`）。2026-09-21 逐色實算：

| 色票 | 對純白 | **對實際底色 `#F5F4F0`** | 判定 |
|------|--------|------------------------|------|
| `--text-dim: #767676` | 4.54 | **4.13** | ★ **不過 AA（4.5）** |
| `--success: #15803D` | 5.02 | 4.56 | 過，但餘裕只有 0.06 |
| `--danger: #B91C1C` | 6.47 | 5.88 | 過 |
| `--accent: #C8102E` | 5.88 | 5.35 | 過（註解本來就寫對底色，這個沒問題） |

**建議值**：`--text-dim` 改 **`#6B6B6B`**（對奶白 4.84，餘裕 0.34）。
不要用 `#6F6F6F` —— 它是 4.57，餘裕 0.07，等於把 `--success` 的同一個問題再複製一次。

手機在戶外強光下看，對比度不足會直接變成「看不到」，**這是細線 2 的前置條件不是裝飾**。
順手把兩處註解的「對白底」改成實際底色，否則下一個人還會照著同一個錯誤基準算。
**B 動 `style.css` 時一併修，C 要寫一題把三個比值釘住**（算對底色的那個版本）。
---

# §5 · 對照大型模組的功能差距（A 於 2026-09-21 裁決）

> 使用者交辦：「既有已開發的內容對照大型模組少了哪些功能或是建議擴增的，
> 就由你這邊決定是否開發，讓既有模組最大化。」
>
> **裁決：本期一個都不開發。** 理由在 §5.3。
> 這一節是**排序與理由的落地**，不是待辦清單——避免下次又從印象重新盤點一次。

## §5.1 · 實測的缺口（`grep` 查證過，不是憑印象）

⚠️ 先排除三個字面巧合：程式碼裡的「工單」是**完工單**、「合約」只是 `caseRecord`
底下的欄位與衍生的「合約總額」、「BOM」是 **byte order mark**。三者都不是模組。

| # | 缺口 | 實測 | 對誰重要 |
|---|------|------|---------|
| 1 | **售後服務：保養排程／維修工單／客戶報修** | 「保養」0 檔、「維修」0 檔、「報修」1 檔（不相關） | 🔴 **最大的結構缺口** |
| 2 | **合約管理** | 只有欄位，沒有模組、沒有合約編號、沒有履約/保固金追蹤 | 🟠 |
| 3 | **預算 vs 實際** | 零。`settlement` 是**事後**算實際成本，沒有事前預算可對比 | 🟠 |
| 4 | **供應商前置時間** | 零（`lead_time` 0 處）。所以採購建議算不出 ETA，這是 v1 的已知缺口 | 🟡 便宜 |
| 5 | **採購建議的下單／已處理狀態** | v1 刻意不含（架構地圖 §6.6 自述） | 🟡 便宜 |
| 6 | **內部員工工時 → 專案成本** | 工作日誌存在，但不計入成本 | 🟡 |
| 7 | **自訂報表產生器** | 營運報表是 13 個固定頁籤 | ⚪ |
| 8 | **對外 API／webhook** | 零 | ⚪ |
| 9 | **BOM／套件組合** | 零 | ⚪ |
| 10 | 複式記帳／總帳、多幣別 | 零 | ⚫ 刻意不做 |

## §5.2 · 為什麼第 1 項是最大的

**現在這套系統的生命週期停在完工單。**

```
報價 → 簽核 → 案件 → 執行進度 → 出貨 → 完工單 → ■ 斷掉
```

專案結束之後發生什麼事，系統裡沒有任何概念。但對一家做監控／門禁／網路整合的公司，
**保養合約才是毛利最高、最穩定的那塊收入**，而且它會長出下一張報價單——
生命週期本來應該是個圈，現在是條線。

而且它**最大化既有模組、幾乎不新增底層**：
設備登載／保固追蹤已經有設備資料、客戶主檔有、簽核引擎有、PDF 引擎有、
通知排程有（`daily_tasks.py` 已經在跑憑證到期、案件逾期、專案到期三種提醒）。
保養排程本質上是**在既有資料上加一層排程與提醒**，不是一個新系統。

**商業意義**：它會是第 9 個套餐 `service`，是**新的可賣單位**——
這才是「讓既有模組最大化」的真正意思：不是把功能堆高，是讓已經有的東西能多賣一次。

## §5.3 · 裁決：本期一個都不開發

**理由不是「不值得做」，是「現在做會讓已經承諾的東西做不完」。**

現況：細線 1 走到第 3 步（七步之三），細線 2 一步都還沒開始。
協定 §7 寫的是「**每一輪的產出都必須站在某一步上，站不上去的不做**」——
上面 10 項沒有任何一項站得上那 14 步裡的任何一步。

⚠️ 這正是 `feedback-loop-objective-function` 講的那個失敗形狀：
**每一輪都成功，而整體愈來愈難完成。** 迴圈的目標函數是「可出貨」不是「功能完整」。
一套賣不出去的完整 ERP，價值是零。

**排程（等細線 1、2 都走完之後，按這個順序）**：

| 順序 | 做什麼 | 為什麼是這個順序 |
|------|--------|-----------------|
| 1 | **#4 前置時間 ＋ #5 採購建議狀態** | 兩項加起來大概一天，而且是**已知的 v1 缺口**，補完採購建議才算完整。先做便宜的，證明迴圈還會動 |
| 2 | **#3 預算 vs 實際** | 精算已經有實際成本，缺的只是一個預算欄位與對比視圖。專案型生意的核心指標 |
| 3 | **#1 售後服務（新套餐 `service`）** | 最大的一塊，值得等到前面兩個暖身完、細線都穩了再動 |
| 4 | #2 合約管理 | 等 #1 做完再看——保養合約做出來之後，合約管理的形狀才會清楚 |

**明確不做（不要再提，提了就是浪費一輪）**：
- **#9 BOM／套件組合** —— 系統整合不是製造業。「套件組合」用報價範本解，便宜十倍。
- **#8 對外 API／webhook** —— 沒有任何客戶要求過，純投機。有人要求再做。
- **#7 自訂報表產生器** —— 大工程。13 個固定頁籤已經涵蓋實際在用的需求，
  真的缺就加第 14 個頁籤，不要做產生器。
- **#10 複式記帳／總帳／多幣別** —— 架構地圖 §6.1 已裁決「不要在 MOTRIX 內重造會計
  系統」，維持。多幣別：國內生意。

**⚠️ 這一節本身就是規劃，而規劃會膨脹。** 下次要動它之前先問一句：
細線 1 的七步走完了嗎？沒走完就不要改這一節。

---

# 細線 3 · 旅宿體驗顧問（2026-09-21 新增，A 裁決）

> 使用者交辦：客戶是**旅宿管理業**，負責旅宿管理營運，協助旅館做**體驗設計提案**與
> **營運建議**。要思考體驗設計成效、體驗設計提案、專案執行分配等模組。
> 「我是最終負責使用，風險皆有你們評估，我做最後驗證，主要由你決定。」

## §A · 先看清楚這個客戶在賣什麼

他們是**旅宿顧問公司**，客戶是旅館。他們的工作流程是：

```
接洽旅館 → 現況診斷 → 體驗設計提案 → 旅館核准 → 專案執行 → 成效驗證 → 續約/再提案
```

**他們向旅館收費的依據，是最後那一步：「你花這筆錢，換到了什麼」。**
沒有成效驗證，顧問費就只是一張嘴。

對照 MOTRIX 現況：前五步幾乎都有現成的（CRM／報價單／簽核／案件／執行進度），
**第六步「成效驗證」是零** —— 而那正好是這個客戶的核心價值主張。

## §B · 七步（細線 3）

**定義：一家旅館從「現況診斷」走到「看得到成效對比」的完整一圈。**

| # | 步驟 | 新開發？ |
|---|------|---------|
| 1 | 建立一家旅館的**物業檔案**（房型／房數／區位／客群／通路） | 🆕 小 |
| 2 | 對它做一次**現況診斷**，留下 baseline 指標 | 🆕 **核心** |
| 3 | 從**體驗知識庫**挑方案，產出一份體驗設計提案 | 🔁 複製第 8 個導覽 |
| 4 | 提案走**既有簽核流程** → 旅館核准 | ✅ **零開發** |
| 5 | 轉成案件、**分派人員與工時** | 🆕 **核心** |
| 6 | 執行完成 → 回填成效指標 | 🔁 用第 2 步的機制 |
| 7 | **看到 baseline vs 成效的對比** ← 顧問拿這個向旅館證明價值 | 🆕 |

**第 4 步零開發是刻意講出來的**：簽核引擎（五種單據、多層、代理人、組織架構動態解析）
是這個系統最成熟的部分，旅宿提案直接掛上去，一行都不用改。
**能不開發的地方要明確標示，否則會被當成待辦重做一次。**

## §C · 三個新模組，以及為什麼它們不是旅宿專用

⚠️ **最重要的設計決定：做通用機制，按垂直配置，不要做兩套。**
做成旅宿專用＝這套系統從此有兩個分支；做成通用＝允碩自己也能用，而且是新的可賣套餐。

### C.1 `outcomes` — 成效量測（🆕 核心，也是最有價值的）

```
metric_definitions   指標定義：名稱、單位、方向（越高越好／越低越好）、適用垂直
metric_snapshots     量測點：對象、指標、時間、值、階段（baseline / interim / final）
```

- 旅宿用它量：住房率、ADR、RevPAR、評價分數、回客率、NPS
- **允碩自己用它量**：監控系統導入前後的誤報率、門禁導入前後的異常進出次數
- ⇒ 這是**平台能力**不是垂直外掛，會是新套餐 **`outcomes`**

### C.2 `assignments` — 專案執行分配（🆕 核心，**也是允碩自己的缺口**）

人員分派 ＋ 工時 ＋ 工時換算成本 → 回到案件精算。

**這一項同時是使用者交辦的「先開發我內部的功能」**：
`SELLABLE-AND-MOBILE-SPEC.md` §5.1 第 6 項（內部員工工時→專案成本）實測為零，
第 3 項（預算 vs 實際）也是零。**工時就是成本**，兩件事是同一件事的兩面。
⇒ 做完這個，允碩內部與旅宿客戶**同時**受益。**所以它排第一。**

### C.3 `experience_guide` — 體驗知識庫（🔁 複製，不是新開發）

資料形狀與既有選型導覽**完全相同**（情境 × 分類 × 適配矩陣 × 方案連結）：

- 旅宿版：**客群**（商務／親子／情侶／背包客／長住）× **體驗場景**（抵達／入住／客房／餐飲／離店）
- 已查證：`switch/monitor/access/gateway/automation_guide.py` 是**同一份 310 行模板複製五次**，
  兩支之間 diff 只有 168 行且幾乎全是名稱替換 ⇒ **這是複製調整，不是從零開發**。

⚠️ **已知技術債，本期刻意不還**：那五支是 1,550 行的重複碼，正確解是抽成一支通用引擎。
但那是**重構五個正在運作的模組**，風險遠高於再複製一份。**要出貨就先複製，債記在這裡。**

### C.4 `properties` — 物業檔案（🆕 小）

旅館的基本盤。**不要塞進 `customers`** —— 一個顧問客戶可能管多家旅館，
而指標是掛在「旅館」上不是掛在「客戶」上。一對多關係要在資料層就分開。

## §D · 順序與取捨（A 的裁決）

**細線 1 不停，但收斂成「賣得出去的最小集合」：**

| 步 | 處置 | 為什麼 |
|---|------|-------|
| 3（擋住） | **做完**（B 進行中，`fb329be` 已完成總開關） | 半成品的守門比沒有守門更糟 |
| 4（模組閘） | **做** | 不做就沒辦法把旅宿當獨立套餐賣 |
| 5（抬頭） | **做，且是硬需求** | **不做的話，旅宿客戶印出來的單據上會是「允碩整合集創 統編 60575481」**。這是任何外部客戶的絕對阻擋 |
| 6（到期提醒） | ⏸ **暫緩** | 續約期才用得到，第一筆生意用不上 |
| 7（換機） | ⏸ **暫緩** | 同上 |

**細線 2（手機專用頁面）⏸ 暫緩。** 它原本鎖定的是**現場施工**情境
（案件進度／叫料／額外支出）。旅宿顧問是坐在桌前做分析與提案的工作，
手機的邊際價值低很多。⚠️ **這是推翻使用者先前裁示的決定，理由寫在這裡，可隨時推翻我。**

**最終順序**：
```
細線1 第3步(B進行中) → 第4步 → 第5步 → 細線3 → 細線2(回頭再看)
```

## §E · 風險（使用者說「風險皆有你們評估」，這是評估結果）

| 風險 | 嚴重度 | 處置 |
|------|--------|------|
| **成效對比會說謊** | 🔴 **最高** | 見下 |
| 工時靠人填，會被湊數 | 🟠 | 工時要能對到具體的執行進度關卡，不能只填一個總數 |
| 物業檔案與 `customers` 的關係搞錯 | 🟠 | 一對多，資料層就分開（C.4） |
| 為旅宿做成專用分支 | 🔴 | C.1/C.2 一律做通用機制（已裁決） |
| 再複製一份 310 行模板 | 🟡 | 已知債，刻意不還，記在 C.3 |

### 🔴 成效對比會說謊 —— 這是這條細線最大的風險

第 7 步是顧問向旅館收費的依據。**一張誤導的對比圖，比沒有圖更糟**：
它會讓顧問拿著錯的結論去說服客戶，而錯被發現的時候賠上的是信任不是一張圖。

它會怎麼說謊（先想清楚才動工）：
- **季節性**：9 月比 2 月住房率高，跟你做了什麼沒有關係
- **同期變因**：同時間旅館換了通路／降價／隔壁開了捷運站
- **量測口徑不一致**：baseline 用的是旅館自己報的數字，成效用的是系統算的
- **取樣期間長短不同**：baseline 量一週、成效量三個月
- **只挑好看的指標呈現**

**⚠️ 這一題我要跟視窗 C 一起定案**（使用者交辦「由你跟其中一個模組共同討論」）。
選 C 的理由：它的職責就是「這東西怎麼可能是假的」，第 1 輪它已經抓到
middleware 假綠燈、測試基準數字錯、`verify_license` 簽名缺一段讀檔——
**這正好是同一種能力**。討論結論回填到本節。

---

# 細線 6 · 標案雷達（2026-09-21 使用者交辦，A 定案）

> 使用者原話：「加入台灣政府標案自動化搜尋然後通知最高管理者的功能，**內容由你決定**。」

## §T.1 · 它不是新模組，是既有管線的最前面一段

現在 `業務開發 CRM` 的案源**全部靠人手動建**。標案接在它前面：

```
（新）標案雷達 → 業務開發 CRM → 報價單 → 簽核 → 案件 → 執行 → 完工
```

**所以第 7 步「一鍵轉成 dev_case」才是這條線的價值所在。**
少了它，這功能只是一個新聞牆——看過就算了，不會變成生意。

## §T.2 · 🔴 先講最重要的事實：**官方沒有即時 API**（2026-09-21 A 實際查證）

| 來源 | 即時性 | 授權 | 判定 |
|------|--------|------|------|
| **官方開放資料集**（`web.pcc.gov.tw/tps/tp/OpenData/showList`） | ❌ **延遲兩個月**（「每月 5 號產出 2 個月前的資料」），XML、雙週一包 | ✅ 政府資料開放授權條款第 1 版 | ✅ 可用，**但對「還能投標的案子」完全沒用** |
| **官網招標查詢頁**（`/prkms/tender/common/basic/indexTenderBasic`） | ✅ 即時 | ⚠️ 無 `robots.txt`（`/robots.txt` 302 導回首頁）；著作權聲明為「得重製、公開播送或公開傳輸，**利用時請註明出處**」 | ✅ 可用，**但要節制** |
| **社群 API**（`pcc-api.openfun.app`，原 `pcc.g0v.ronny.tw`） | ✅ 即時 | ❌ **明文：「商業用途需使用政府採購網另提供的開放資料集」** | 🚫 **不可用** |

⚠️ **社群 API 不可用這一條是硬的**：MOTRIX ERP 是**要賣的商業產品**。
它即時、免費、CORS 全開、用起來最舒服——**而那正是會讓人忽略授權條款的組合**。
**寫進「明確不做」，不要因為好用就回頭考慮。**

> `data.gov.tw` 上有一筆「請開放政府電子採購網的各式招標公告…查詢 API」的民眾建議案，
> **那筆建議案本身就是「官方即時 API 不存在」的證明**。

## §T.3 · 設計：資料來源可插拔，兩層各司其職

比照 `archive.py` 的雲端備份可插拔（`backend="local_drive"` / `"s3"`）作法：

| 層 | 來源 | 做什麼 |
|----|------|--------|
| **即時層**（預設啟用） | 官網招標查詢 | 找**還能投標**的案子 → 通知 |
| **歷史層**（可選） | 官方開放資料集 XML | **機關雷達**：哪些機關常發我們做得來的案子、預算區間、誰在得標 |

**歷史層不是附屬品**——它回答的是「該往哪裡投」，而即時層只回答「現在有什麼」。
但**本輪只做即時層**，歷史層列為細線 6 的第二期。

**技術約束（已查證）**：
- **用 stdlib `urllib.request`，零新增相依**。既有慣例就是這樣
  （`heartbeat_job.py`、`helpers/google_calendar.py` 都用它）。
  `requests`／`beautifulsoup4` 只在 `requirements-dev.txt` 裡，**不要把它們搬進正式相依**。
- 解析：HTML 要用 stdlib 的 `html.parser`，不要引入 bs4。
  **若解析複雜到非 bs4 不可，回來找 A 重新決定，不要自己加相依。**

## §T.4 · 七步

| # | 步驟 |
|---|------|
| 1 | 設定搜尋條件：**關鍵字 ＋ 排除詞 ＋ 機關 ＋ 金額區間**（可多組） |
| 2 | 每日自動抓取（掛既有 `daily_tasks.py` 排程） |
| 3 | 比對條件，命中的存下來 |
| 4 | **去重**：同一標案不重複通知（標案案號為鍵） |
| 5 | **每日一封彙總**通知最高管理者（不是每筆一封） |
| 6 | 畫面上看得到命中清單，可標記「有興趣／不相關」 |
| 7 | **一鍵轉成業務開發 CRM 的案件** ← 圈合起來，這步才是價值 |

### 第 1 步的「排除詞」是必要的不是加值

關鍵字打「監視器」會撈到一堆學校採購單價 3 萬的攝影機。
**沒有排除詞，這個功能會在第三天就被使用者關掉**——通知太吵等於沒有通知。

## §T.5 · 🔴 速率與失控：設計時就要想「怎麼關掉」

這是本專案踩過的教訓（〈告警必須有速率上限〉），**不是事後補的**：

1. **每日一封彙總，不是每筆一封。** 命中 40 筆就是信裡 40 列，不是 40 封信。
2. **總開關 `TENDER_RADAR_ENABLED`，預設 `False`。** 比照 `LICENSE_GATE_ENABLED`／
   `PASSKEY_ENABLED` 的既有慣例。**它會對外連線，預設就不該是開的。**
3. **首次啟用強制「純記錄模式」7 天**：只抓、只存、**不發信**。
   讓使用者先看關鍵字準不準，再決定要不要開通知。
   ⚠️ 這條不是貼心，是防止「第一天就寄 200 封信給老闆」。
4. **抓取頻率硬上限：每日一次。** 不提供「每小時檢查」的設定——
   標案公告期以天為單位，每小時檢查沒有任何業務價值，只有被擋的風險。
5. **對方站台失敗時要安靜退場**：連不上／改版／解析失敗 → 記 log、**不發信**、
   下次再試。⚠️ **不要把「抓失敗」也做成通知**，否則站台掛一週就是七封錯誤信。
6. **註明出處**：畫面與通知信都要標示資料來自政府電子採購網（授權條款的要求）。

## §T.6 · 🔴 解析失敗要拒絕那一筆，不要送空值

政府網站改版是**一定會發生**的事，不是意外。

> **讀不到的欄位就拒絕那一筆，不要送一筆欄位是空的進來。**

一筆「標案名稱有、截止日期是空白」的資料進到通知信裡，
使用者會以為那個案子沒有截止日 ⇒ **降級而它還是會動**，而降級不會有人報修。
解析不出必要欄位（案號／名稱／機關／截止日）就整筆丟掉並計數，
**當「丟掉的筆數」超過當次的一半，就是對方改版了**——那個才值得發一次信。

## §T.7 · 通知對象：「最高管理者」要用既有機制，不要新造

系統已經有組織架構（`divisions`／`departments`／`manager_user_id`）與
通知偏好（`helpers/notification_prefs.py` 的 `EVENT_GROUPS`）。

- 新增通知事件 `tender_found`，**掛進既有 `EVENT_GROUPS`**，
  讓它跟其他通知一樣可以逐人開關（`users.html` 的通知偏好）
- 預設收件人：**superadmin**。⚠️ 不要寫死某個人的信箱
- 走既有 `helpers/email_notify.py`，不要另建寄信路徑

## §T.8 · 授權套餐

這會是**第 9 個套餐 `tender`**（見 `docs/windows/STATE.md` §4 的八套餐表）。
細線 1 解凍做第 4 步時要把它加進展開表，**並記得同步那題「聯集 == 權限目錄」的守門**。

## §T.9 · 明確不做

- 🚫 **社群 API**（授權不允許商業用途）——**不要因為它好用就回頭考慮**
- 🚫 自動投標、自動下載標單附件（那是另一個風險等級）
- 🚫 每小時／即時推播（§T.5 第 4 點）
- 🚫 為了解析而引入 `beautifulsoup4` 到正式相依（§T.3）
- 🚫 歷史層（開放資料集 XML）——**列為第二期**，本輪不做

---

# 細線 7 · 商圈雷達（2026-09-21）

> ⚠️ **先讀 §W.14**（A 的自我更正）：管線那一半可以做，只有**排序推薦**那一半維持暫緩。
> 前面 §W.12 寫的「整條暫緩」是錯的。

> 使用者原話：「增加一個待開發事項，結合 google map 對附近公司產業結構進行彙整分析
> 找出可開發公司」。**交辦的是「加入待辦」，不是「現在做」。**

## §W.1 · 它與細線 6 是同一件事的兩半

```
細線 6 標案雷達  →  公部門案源（政府標案）
細線 7 商圈雷達  →  私部門案源（周邊企業）
                        ↓ 兩者都接在這裡
                   業務開發 CRM → 報價單 → 案件
```

**所以它的第七步跟細線 6 一樣是「一鍵轉成 `dev_case`」** —— 少了那一步，
它只是一張地圖，看過就算了。

## §W.2 · 🔴 我認為最難的不是技術，是「Google Maps 回答不了那個問題」

**使用者要的是「找出可開發公司」。Google Maps 能回答的是「附近有哪些店家」。**
這兩件事之間有一段距離，而那段距離才是這條線的價值所在：

| 決定「值不值得開發」的資訊 | Google Places 有嗎 |
|---|---|
| 這家公司在做什麼行業 | 🟡 有 `types`，但那是**消費者導向分類**（`restaurant`／`lodging`／`school`），不是產業分類 |
| 有幾個出入口、幾層樓、多少員工 | ❌ **沒有** ← 而這才是決定「需不需要監控門禁」的東西 |
| 是不是已經有系統、什麼時候裝的 | ❌ 沒有 |
| 決策者是誰、怎麼接觸 | ❌ 沒有 |
| 附近有沒有我們做過的案子 | ❌ Google 沒有，**但我們自己有** ← ⭐ |

⭐ **最後一列是這條線真正的差異化**：

> **把 Google Maps 的「附近有誰」，疊上 MOTRIX 自己的「附近我們做過誰」。**
>
> 單純列出附近店家，任何人都做得到、也沒有價值。
> 但「這棟大樓隔壁三間我們都做過，而這一間還沒」——
> **參考案例是系統整合業最強的開發工具**，而那份資料只有我們有。

⇒ **設計方向：Google Maps 提供母體，我們自己的資料提供篩選與排序。**

## §W.3 · 查證結果（2026-09-21）

### ✅ 成本：可控（已查證）

| 項目 | 數字 |
|---|---|
| Nearby Search | **$32 / 1,000 次**（Pro SKU，前 10 萬次） |
| 加 `rating` 欄位 | $35 / 1,000（跳 Enterprise） |
| 加 `reviews` | $40 / 1,000 |
| 免費額度 | **每月 5,000 次 Pro**（2025-03 起改成 per-SKU 上限，取代舊的 $200 共用額度） |

⇒ **以「業務出門前查一個區域」的用法，每月 5,000 次遠遠夠用。錢不是問題。**

### 🔴 儲存限制：**可能是決定性的，而我沒有讀到條款原文**

⚠️ **以下標記為「待查證」——我用 WebFetch 取 Google Maps Platform ToS，
內容被截斷，它回的是通則不是引文。實作前必須先確認原文。**

一般理解（**未經我查證原文**）：

| 資料 | 限制 |
|---|---|
| `place_id` | 可**永久**儲存 |
| 其他欄位（名稱／地址／電話／評分） | **僅可快取 30 天** |
| 建立競爭性 Places 資料庫 | ❌ 禁止 |
| 大量下載／系統性抓取 | ❌ 禁止 |
| 匯出給第三方 | ❌ 禁止 |
| 內部商業分析 | ✅ 一般允許 |

**若上述成立，設計必須是**：
- **永久存**：`place_id` ＋ **我們自己的標註**（產業判定、開發狀態、拜訪紀錄、備註）
  —— 那些是我們的資料，不是 Google 的
- **不永久存**：名稱／地址／電話等 Google 欄位，**顯示時即時取或 30 天內更新**
- ⇒ 「彙整分析」跑在**我們的標註**上，不是跑在一份 Google 資料的拷貝上

⚠️ **這跟細線 6 是同一個形狀**：那次是「社群 API 即時好用但禁商業用途」，
這次是「Google 資料好取但不能永久留」。
**兩次都是條款而不是技術決定了資料模型。**

## §W.4 · 七步（草案，未定案）

| # | 步驟 |
|---|------|
| 1 | 指定一個中心點與半徑（通常是既有客戶或剛完工的案場） |
| 2 | 取回附近商家（`place_id` ＋ 類型） |
| 3 | 依**我們自己的規則**判定目標客群（旅館／診所／工廠／學校＝有需求；小吃店＝沒有） |
| 4 | ⭐ **疊上自己的資料**：哪些已是客戶、附近我們做過哪些案子 |
| 5 | 排序：**離我們做過的案子越近、類型越對、越優先** |
| 6 | 可標註「有興趣／不相關／已接觸」 |
| 7 | **一鍵轉成 `dev_case`** ← 圈合起來 |

## §W.5 · 開工前必須先答的三題

1. 🔴 **條款原文到底怎麼寫**（§W.3 那張表是未查證的通則）
   —— 這題沒答之前不要設計資料表
2. 🟠 **「產業判定」的規則從哪來**：Google 的 `types` 顆粒度不夠，
   要不要人工維護一張「類型 → 是不是我們的菜」對照表？**那張表誰維護？**
3. 🟠 **誰有 Google Cloud 帳號、誰付那張帳單**
   （比照 §8.0 雲端備份那次：整合代碼寫好了，卡在沒有真實帳號）

## §W.6 · 明確不做

- 🚫 爬 Google Maps 網頁（條款明文禁止，而且有技術對抗）
- 🚫 建立並保存一份「附近所有公司」的資料庫（若 §W.3 成立即為違約）
- 🚫 自動寄開發信／自動撥號

## §W.7 · 排程

**未排程。** 目前順序：第 4 輪細線 6（暫停中）→ 細線 4 成本閉環 → 細線 5 售後閉環。
⚠️ **它與細線 6 共用「案源 → `dev_case`」那一段**，
所以**細線 6 的第 7 步做完之後再做它，可以直接沿用**，反過來就要做兩次。

---

## §W.8 · 🔴 條款已查到原文，**比我假設的更嚴**（2026-09-21，A 查證）

我先前在 §W.3 把「其他欄位僅可快取 30 天」列為待查證。**查到了，而且我假設錯了。**

**來源**：`developers.google.com/maps/documentation/places/web-service/policies`

> **"You must not pre-fetch, cache, or store Places API content
> beyond the allowed exceptions"**
>
> **"the place ID, used to uniquely identify a place, is exempt from
> the caching restrictions"** —— 可 **"store place ID values indefinitely"**

⇒ **不是「存 30 天」，是「不得快取或儲存」，`place_id` 是唯一明文豁免。**

⚠️ 我原本寫的「30 天」是一個**聽起來合理的數字**，而它讓整個設計看起來可行。
**真正的條文比它嚴得多**，而嚴的那一側正好打掉我原本的設計。

### 這直接打掉兩件事（視窗 C 先指出，A 查證後確認）

| 原本的設計 | 為什麼不行 |
|---|---|
| 存一張「附近公司 ＋ 產業分類 ＋ 評分」的表做彙整分析 | **那就是一個競爭性資料庫**（C 的原話），而且 Places content 本來就不得儲存 |
| §W.2 第 2 題的解法（留存名單、事後比對「排後面卻成交」） | 同上——要比對就要留存 |

⇒ **C 指出的先後順序是對的：ToS 原文 → 決定能存什麼 → 才談排序驗證。**
反過來做會設計出一套依賴長期留存的驗證機制，然後發現不能留存，整套作廢。

---

## §W.9 · 視窗 C 對「建議型輸出怎麼驗」的三個答案（A 全部採納）

**前提：放棄「驗排序正確」這個目標。它在數學上不可驗（沒有 ground truth）。** 改驗三類：

### (a) 不變量 —— 最有力，而且抓得到真正會發生的 bug

- ❌ 不要寫「A 應該排第 3」
- ✅ 要寫「**若 A 在每一個輸入訊號上都不劣於 B，則 A 不得排在 B 後面**」（偏序，合成資料就測得出）

**它抓得到的實務錯誤**：權重正負號寫反、正規化漏做、tie-break 用了不穩定的 key。

**配套：敏感度上限** —— 「其他條件相同時，單一特徵從最小值變到最大值，
不得讓名次移動超過 N 名」。
⚠️ 這條專抓「**某個未正規化的原始欄位（例如員工數）把其他訊號全部淹掉**」，
而**那樣產出的名單看起來完全正常**。

### (b) 黃金樣本 —— 唯一真正在驗「規則對不對」而非「規則跑了」

請業務挑 **20 家**（10 家一看就該開發、10 家一看就不該）。
驗收用**門檻**不是精確名次：該開發的必須進前 20%、不該開發的不得進前 50%。

> **本質是把業務腦袋裡的默會知識變成一條會紅的斷言。**
> 沒有它，所有測試都只在驗「程式照著我寫的規則跑」，
> **而規則本身從來沒有被任何東西挑戰過。**

⚠️ **真實成本**：占用業務時間，而且市場會變、要重做。**這要讓使用者知道再決定。**

### (c) 可反駁性 —— 把驗證外包給現場

每一筆**必須帶出「為什麼排這裡」**（各特徵的貢獻），否則不准進名單。

> **無法從輸出本身驗證的東西，就要讓它在使用現場變得可反駁。**
> 沒有理由的名單，業務只能全信或全不信；
> **有理由的名單，錯誤會在第一週就被人罵出來。**

---

## §W.10 · 怎麼知道「這東西到底有沒有用」：對照組，不是回溯追蹤

**C 的方法**：每次發出的名單，**混入 10% 從排名之外隨機抽的，且不標記**。

- 排名組成交率**顯著高於**隨機組 → 排序有訊號
- 兩組**一樣** → **排序完全沒作用**，而且**幾週內就知道，不是幾個月**

🔑 **關鍵差別**：不是驗「每一家排得對不對」（需要大量樣本），
是驗**一個總體命題**「排序優於亂數」。
**驗總體命題所需的樣本數，比驗每一項少一個數量級。**

⚠️ **兩個真實成本**：①業務會花時間跑一些隨機抽出來的公司——
**這要使用者同意，不是我們替他決定**　②**同樣撞到 ToS**（要比較就要留存名單）。

---

## §W.11 · 🔑 這個功能不是一個東西，要拆開治理（C 的結論，A 採納）

| 部分 | 有沒有對錯 | 治理方式 |
|---|---|---|
| 取資料／`place_id` 去重／快取 TTL／產業分類對應／篩選／**成本上限（免費額度不得超出）** | **有明確對錯** | ✅ **走七步迴圈，而且這是大部分的程式碼** |
| **排序判斷本身** | **沒有對錯** | ❌ 不走七步。**當成「實驗」治理**：產出不是綠燈，是**對照組的數字 ＋ 業務的反駁紀錄**，並有固定檢討週期 |

⚠️ **最危險的是把兩者混在同一份驗收清單裡** ——
**那會讓「管線全綠」被讀成「這條線是對的」**，而管線全綠只代表
資料拿得到、沒重複、沒超支。

> 這正是今天反覆出現的那件事：**綠燈會把「沒有人檢查過」偽裝成「已經檢查過了」。**

---

## §W.12 · 裁決：暫緩 —— **但理由要寫對**

**C 的提醒我完全採納**：不要把理由寫成「因為它驗不了」。

> 那個理由是錯的（可驗證性**有解**，見 §W.9），
> **而錯的理由會在三個月後被人用「我們現在想到辦法驗了」推翻，
> 然後在沒有確認條款的情況下開工。**

**正確的理由，三條**：

1. 🔴 **ToS 限制已查證成立，且它打掉了原設計的資料模型**
   （不得儲存 Places content，只有 `place_id` 豁免）
   → **要做的話必須重新設計成「只存 `place_id` ＋ 我們自己的標註」，
   顯示時即時取**。那是可行的，但**不是現在這份草案**。
2. 🟠 **不在本期優先序**：使用者 2026-09-21 上午裁示
   「既有模組的不足、未跟大型系統擁有的功能先做開發」。
   **商圈雷達是新的獲客功能，不是既有模組的不足。**
3. 🟠 **開工前必須先有兩項成本共識**（要使用者點頭，不是我們決定）：
   ①業務提供 20 家黃金樣本的時間 ②名單混入 10% 隨機對照的機會成本

---

## §W.13 · 🔴 我寫的差異化前提**不成立**——不是資料髒，是資料幾乎不存在（視窗 B 查證）

§W.2 我寫：真正的差異化是「把 Google Maps 的**附近有誰**，疊上 MOTRIX 自己的
**附近我們做過誰**」。**B 查證後，那個前提不成立。**

### 查證輸出（B 提供，來源是正式機今日雲端備份，非本機開發 DB）

```
$ grep -in "lat|latitude|lng|longitude|geo|coord" backend/db.py
(無輸出)                                    ← 整個 schema 零座標欄位
```

| 地址在哪裡 | 形態 | 正式機非空 |
|---|---|---|
| `quotations.data_json.deliveryAddress` | 自由文字（JSON 內） | **30 / 35** |
| `quotations.data_json.caseRecord.contract.deliveryAddress` | 自由文字 | 3 |
| `customers.data_json.{invoice,delivery}Address` | 自由文字 | 13 / 18 |
| `completion_notes.site_address` | **真欄位** | **1 / 1** |
| `shipping_notes.delivery_address` | **真欄位** | 2 / 2 |

### 🔴 但真正打掉設計的是這個

```
正式機報價單 35 筆，有 caseRecord（已成案）14 筆
建立日期範圍：2026-07-09 ~ 2026-09-13   ← 只回溯 2.5 個月
```

**這 35 筆不是公司的歷史案件，是系統上線後輸入的那些。
公司以前做過的案場根本不在系統裡。**

⇒ 差異化前提要的是「隔壁三間我們都做過」的**密度**，
而現在全系統可定位的案子是**個位數**。
**任意一個商圈裡命中既有案例的機率接近零。**

> **B 的話：「第 4 步不是『疊上自己的資料』，是『先有資料』。」**

⚠️ **地理編碼的一次性成本不是問題**（30 筆地址、幾十次 API 呼叫）。
**問題是編碼完之後地圖上只有 30 個點。**

### 📌 B 另外指出的一個兩難（要寫進設計，不要忘記）

| 欄位 | 語意 | 資料量 |
|---|---|---|
| `completion_notes.site_address` | ✅ **案場地址**，真欄位、名字就對 | **1 筆** |
| `quotations.data_json.deliveryAddress` | ⚠️ **出貨地址**，多數相同但「收貨到辦公室、案場在別處」會錯 | **30 筆** |

> **語意最準的欄位資料最少，資料最多的欄位語意最歪。**

### 三條路（B 列的，A 加上成本判斷）

| 路 | 成本 | A 的評估 |
|---|---|---|
| ① 等它自己長 | 每月 5～10 筆成案 | **要幾年才有密度**，等於這條線幾年內沒有價值 |
| ② 一次性匯入歷史案場 | **人的成本，不是工程成本**——要人回想／翻紙本 | **這是使用者的決定，我們做不了** |
| ③ 改變差異化的定義 | — | ⚠️ 但**想不出替代品**：MOTRIX 其他的獨有資產（選型資料庫）回答的是「該裝什麼」，不是「該找誰」 |

### ⇒ 裁決更新：暫緩的第一條理由改寫

原本 §W.12 第 1 條寫的是「ToS 打掉原設計的資料模型」。**那仍然成立，但它不是最強的一條。**

**最強的一條是**：

> 🔴 **沒有歷史案場資料，商圈雷達就沒有任何東西勝過「打開 Google Maps 自己看」。**
> 而補齊歷史案場**不是工程問題，是有人要去把以前做過的案子一筆一筆找出來**。

**⇒ 給使用者的一句話**：這個功能想做的話，**第一步不是寫程式，
是決定要不要花人力把歷史案場補進系統**。那件事做了，這條線才有意義；
不做的話，它做出來會是一張「附近有哪些店家」的地圖，**而那個 Google Maps 本來就有**。

---

## §W.14 · 🔴 A 自我更正：我用錯了評判標準，把「加分題」寫成了「前提」

**使用者問：「不能自動帶出附近商家嗎？」**
**答案是可以，而且那正是這個功能的主體。上一輪我的判斷過度了。**

### 我錯在哪

我問的是「**這比競爭對手強在哪**」，使用者要的是「**這能不能省業務的時間**」。
**那是兩個問題，而我拿前者去否定後者。**

「疊上歷史案場」是我自己加的**加分題**，我卻把它寫成**前提**，
然後用 B 查到的「歷史案場不存在」去否定整條線。
⚠️ **B 的查證是對的（案場資料確實幾乎沒有），錯的是我從那個事實推出的結論。**

### 就算完全沒有歷史案場，這些價值一個都不少

| 現在人工做 | 自動化之後 |
|---|---|
| 開地圖一家一家看 | 一次帶出整個區域 |
| 心裡篩「這家是不是我們的菜」 | **照我們的規則自動篩**（旅館／診所／工廠／學校 vs 小吃店） |
| 抄名稱、地址、電話 | **一鍵轉 `dev_case`，不用重打** |
| 下次掃同一區再看一遍 | **標記過的不再出現**（有興趣／不相關／已接觸） |
| 不知道哪些區掃過了 | 掃過的區域留紀錄 |

⇒ **歷史案場那層改列為「日後可加的強化」，不是開工前提。**

### ToS 限制的是「儲存」不是「取用」——而且有一個不碰灰色地帶的設計

**查證補充**（2026-09-21，A）：Places policies 頁**沒有列出 "allowed exceptions" 的內容**，
只明文豁免 `place_id`。⇒ 「把名稱／地址／電話長期存進自己的系統」
**既沒有被明確允許、也沒有被明確討論**——**那是一塊不確定，不是一塊禁區。**

**不必踩進那塊不確定的設計**：

| 資料 | 怎麼處理 | 依據 |
|---|---|---|
| 名稱／地址／電話／類型 | **顯示時即時取，不落地** | 避開不確定 |
| `place_id` | **永久存** | ✅ 明文豁免 |
| 我們的標註（有興趣／不相關／已接觸／掃過的區域） | **永久存** | 那是**我們的**資料 |
| 轉成 `dev_case` 時的客戶資料 | **由業務確認後建檔** | ⇒ 那一刻資料來自**使用者輸入**，不是快取的 Google 回應 |

⚠️ 最後一列不是規避，是**本來就該這樣**：
Places 對 B2B 的資料常常不準（公司改名、掛牌的是樓裡某一間店），
**業務本來就要確認過才建檔**。

### ⇒ 裁決更新：不是「暫緩」，是「拆開之後，管線那半可以做」

照 §W.11 的拆法（視窗 C 提的），使用者問的正好是**管線那一半**：

| 部分 | 狀態 |
|---|---|
| **管線**：取附近商家 → 依規則篩 → 標記 → 一鍵轉 `dev_case` → 掃過的不重複 | ✅ **有明確對錯、驗得了、價值明確** |
| **排序／推薦「哪家最值得開發」** | ⏸ 維持暫緩（§W.9～W.12 的不可驗證性仍然成立） |

🔑 **兩者可以分開做，而且管線先做完全合理**——
它本身就有價值，而排序是加在它上面的一層。

**仍然要使用者決定的只剩一件**：**優先序**。
2026-09-21 上午裁示「既有模組的不足、未跟大型系統擁有的功能先做開發」，
**商圈雷達管線是新功能**。要不要插隊是使用者的決定，不是我的。

