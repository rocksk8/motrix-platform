# 多視窗開發協定（MOTRIX-ERP）

> 建立 2026-09-21。**每個視窗開工第一件事：讀這份，再讀自己的視窗檔。**
> 維護者：視窗 A（彙整）。B／C 不要改這份，有意見寫在自己的視窗檔「給彙整」段。

---

## §1 · 為什麼要有這份

兩個視窗在**同一台機器、同一個工作目錄**上動同一個 repo。本專案已經有過血的前例：
2026-09-10 兩台機器同時有 session 在改 WebAuthn，開發機打包好的更新包差點覆蓋掉
正式機 session 正在改的檔案（見 `MOTRIX-ERP-QUICK.md` §0 落差表）。
**同一個目錄裡兩個視窗，撞車機率比跨機更高，而且撞了不會有任何錯誤訊息。**

---

## §2 · 三個視窗的角色

| 視窗 | 角色 | 寫什麼 | 不寫什麼 |
|------|------|--------|----------|
| **A** | 彙整／派工／裁決 | 所有 `.md` 文件、開發單、`docs/windows/STATE.md` | **不寫任何程式碼** |
| **B** | 寫手 | 產品程式碼（`backend/` 與 `frontend/`） | **不寫任何測試** |
| **C** | 驗證常駐 | 測試（`backend/tests/`）、跑測試、瀏覽器實測 | **不寫任何產品程式碼** |

**B 不寫測試、C 不寫產品碼，是刻意的。** 理由見 `MEMORY` 的假綠燈教訓：
寫碼的人寫的測試，觀測點會自然落在「自己設的值」上，驗到的是自己的假設不是行為。

---

## §2b · 視窗 D · A 的副手（2026-09-21 使用者指示設立）

> 使用者原話：「安排 D 去當你的副手，**看你容易犯錯的部分**，他當跟你校驗或是分攤」。

### 🔑 設計輸入是 A 實際犯的錯，不是憑感覺

`STATE.md` §5 到本節寫入時共四十七次裁決，其中**約 50 處是 A 自己的錯**。
歸成四族，**D 的四項查核一對一對應**：

| A 的錯誤族 | 今天的實例 | ⇒ D 查什麼 |
|---|---|---|
| **一個成立的部分，替一個沒查的部分背書** | 「掛既有 `daily_tasks.py` 排程」（**那個檔不存在**）／「圖示鍵不在表裡⇒會破圖」（**那張表沒人讀**）／「新增 `scheduler.py`」（**既有慣例是分散式**） | **①點名查核**：單／裁決裡點到的**每個檔、符號、慣例**，實際存不存在、是不是 A 說的那樣 |
| **只看我要編輯的那一段，不看整體** | `cells[:7]` 砍掉第 8~10 欄／`MEMORY.md` 第 1 行同一行裡有兩份相同指示 | **②整體查核**：A 改文件時，那個字串在**整份裡出現幾次**（`count`，不是 `grep`） |
| **只追一條路徑就以為追完了** | `_async_send` 三支呼叫端只檢查了一支，**而漏掉的那兩支後果更嚴重** | **③呼叫端全列**：A 說某個共用元件有缺陷時，列出**它所有的呼叫端** |
| **兩份文件對同一件事給不同答案** | §T.5 #5「改版不發信」vs §T.6「改版值得發一次信」／SPEC 順序表停在舊裁示 | **④互相矛盾**：新裁決寫進去之後，掃 `§3`／SPEC／協定有沒有打架 |

### 🔴 ⑤ 跨視窗比數字之前，**先確認兩邊用的是同一把尺**

D 設立當天就示範了這一項，而且**它是在指控 A 之前自己抓到的**：

```
A 回報：MEMORY.md 第 1 行 2050 → 2000 字元
D 量到：3723
D 查下去： wc -m 與 wc -c 算出同一個數  ⇒ 退化了
          LANG=[] LC_ALL=[]              ⇒ Git Bash 的 locale 是空的
中文 UTF-8 是 3 bytes/字 ⇒ 2,000 字的中英混合行量成 3,723 完全合理
⇒ A 的數字是對的，錯的是 D 的尺
```

🔑 **通則**：A 記過「`grep` 告訴你『有』，不告訴你『有幾個』」；
**這一條是「量出來的數字不告訴你它的單位」** —— 兩個都是**工具靜默給了一個看起來合理的答案**。

☠️ **而這一條在多視窗裡特別貴**：
> **失敗的樣子是「對方的數字錯了」。**
> D 若沒多查那一步，就會發一則質疑 A 的訊息，A 再去重數一遍 ——
> **兩個視窗為了一個 locale 各花一輪。**

📌 判準：`wc -m` 與 `wc -c` **算出同一個數就是退化了**；要真的數字元用 `LC_ALL=C.UTF-8 wc -m`。
⚠️ 順帶（D 實測）：這台機器的 **Git Bash 裡 `python` 不可用**，別當後備。

### 🔴 時機：**出單前，不是事後**

**A 的錯大多落在單裡，而單裡的錯要兩個視窗的時間去撞。**
⇒ D 的查核在 **A 送出開發單或裁決之前**跑；事後稽核是次要的。

### D 不做的事

- ❌ **不裁決、不派工** —— 派工管道只有 A 一條（§5i）
- ❌ **不寫 repo 檔案**：`backend/**`／`frontend/**`／`backend/tests/**`／`docs/**.md`／根目錄 `.md`
- ❌ **不跑全量回歸**（§5 第 5 條：一次只能一個人跑，而 D 不在排程表上）
- ❌ **不越過 A 去找 B／C**

### ⚠️ D 的回報格式（照 A 今天付過學費的教訓）

1. **把「查過的」與「推論的」分開寫** —— 不要讓具體的那一半替沒查的那一半背書
2. **附查證的指令與輸出**，不是結論（§5f）
3. **講 B／C 的狀態時寫明「截至我讀的時候」**，或當場重看一次
4. **識別碼（SHA／行號）要自己驗過存不存在**

---
## §2c · **視窗 A-2 · 共同校驗人**（2026-09-22 使用者設立）

> 使用者原話：「我創建了 A-2，作為共同校驗人」
> 「當 A 判別錯誤提出質疑，順序是 A->A-2->B、C、D，B、C、D 會同時將資料
> 傳送給 A 跟 A、A-2，你們兩個共同討論做出決策」

### 設立的理由是實際的

```
2026-09-22 一天之內 A 被推翻的判定：
  「填了金鑰會一次性重查 200 個地址」          被 A 自己推翻
  「manifest 358 筆、差三列」                      被 D 推翻
  「填了金鑰也不會升級」（涵蓋太廣）              被 D 推翻
  「每日與週兩層」                                被 D 推翻
  「空備份是開發機排程覆蓋」                      被 D 推翻
```
🔑 **五個被推翻的判定裡，四個是 B／C／D 推翻的，而他們都不是裁決者。**
☠️ 意思是：**抱著錯判定派出去的工作，要等執行的人撞上才會回頭。**
⇒ A-2 的工作是把那個回頭點**提前到出單之前**。

### 職責

```
A     排程、派工、寫規格。日常派工不需要 A-2 會簽。
A-2   校驗 A 的判定。可以質疑任何一項，包括已經發出去的。
兩人   共同裁決「被質疑的、或 A 自知不確定的」那些。
```

### 🔴 需要共同決策的是哪些（不是全部）

☠️ **全部都要共同決策 ＝ 每一件事都要兩個人跑一遍 ＝ 回到兩個 A。** 分界是：

```
需要   推翻一個已經發出的判定
需要   順序、優先級、「這件不做」這種否決
需要   資料安全、備份、權限、金錢（額度／計費）相關的任何判定
需要   打包放行（出貨前最後一道）
不需要 日常排序、把規格裡已有的條目派出去、轉達實量結果
```

### 🔴 「打包放行」由什麼組成（2026-09-22 補，`STATE.md §59`）

☠️ **在這之前，協定只說「打包放行要共同決策」，而沒有任何一份文件說那一道由什麼組成。**
實查：`DEPLOY.md` 與本檔對 `驗包`／`verify_package` 各 **0 處**
⇒ 驗包這一步**只存在於 A 某一則訊息裡** ⇒ 〈要求寫在訊息裡等於沒下達〉，
而這一次它落在**出貨關卡**上：
```
這一輪 D 會驗，是因為 A 在訊息裡派了它
下一輪沒有人派的話 —— **不會有任何東西提醒任何人**
```

**放行清單（每一項都要有實際輸出，不可以憑印象勾）**

```
① 全量測試全綠（含 e2e 的**斷言**失敗，不是只看逾時）
② python backend/tools/verify_package.py <包> --expect-db-version <N>  ⇒ **EXIT 0**
   ⚠️ 不帶 --expect-db-version 會 FAIL（VP1 已修）—— 那是刻意的，不要拿掉參數讓它過
③ C 判定「綠的是產品不是題目」（對一個 **commit** 做，不對工作樹做）
④ A 與 A-2 **都**背書
⑤ 交付說明寫完，且**不含超出證據的措辭**
   📌 例：只有讀碼＋靜態＋合成三層證據的項目，**不可以寫成「已驗證」**
🔴 ⑥ **權限側的實看** —— 只有 `superadmin` 看得到的那一面**不算驗過**
   ```
   (a) 用一個**權限受限**的帳號登入，確認他看得到的側欄與頁面是對的
       ⚠️ 需要使用者手上有那種帳號（建立帳號不是視窗能做的事）
   (b) (a) 不存在時的次佳：用 node 直接跑權限組合
       （B 做過：「財務有下拉」只對有出納權限的人成立）
   ```
   🔑 **(a) 與 (b) 不可互相替代**：
   **(b) 涵蓋權限組合、涵蓋不到 CSS 與實際渲染；(a) 反過來。**
   ☠️ 而使用者是 `superadmin` ⇒ **受限那一側的畫面，他結構上看不到**
   ⇒ 這個產品要賣，**客戶員工的權限組合會比他多得多**
```

🔴 **這一道的每一項，沒做就是沒做 —— 不可以用「這次沒有人派」當理由。**
🔑 把它寫在這裡的目的，是讓「沒有人想到」變成「**有人跳過**」——
**而那兩者的可查性差很多。**


### 回報路徑

```
B、C、D 的回報  → 同一則、同時送給 A 與 A-2
☠️ 兩人不可以收到不同版本 —— 否則共同決策會建立在兩個不同的事實上，
而那種歧見看起來跟「判斷不同」一模一樣。
```

### 🔴 A-2 不可以做的一件事：**附和**

📌 〈主持人的記憶是負債〉：**附和看起來跟查證一樣。**
⇒ A-2 表示同意時，**必須同句講出它自己查了什麼**（指令、行號、數字）。
☠️ **「我同意」不是校驗，是把 A 的錯變成兩個人的錯。**
⚠️ 且兩人一致時要問一句：**我們是不是用了同一個錯的前提？**
（今天實例：A 跟 D 都拿 `G:` 上的 mtime 當寫入時間 —— **兩個人都錯，而且錯得一模一樣**。）

### 🔴 僵局要有出口，不然工作會停在這裡

```
兩人歧見   → 把兩個立場寫在**同一則**裡給使用者，各附證據，不要各發各的
A-2 沒回 → ⭐ **死線 15 分鐘**。過了就 A 自己走，**且要在派工裡明記
              「未經 A-2 校驗」**，後補。
```
📌 〈長時間沒有輸出的動作要有死線〉＋〈握著鎖的一方沉默成本不在自己身上〉：
☠️ **「等 A-2 回覆」不是合法的停工理由** —— 停下來的成本落在 B／C／D 身上。

### ⚠️ 這份協定自己的風險

**新增一個人就新增一條「訊息可能只送給其中一個人」的路徑。**
📌 〈要求寫在訊息裡等於沒下達〉：**裁定一律落在 `STATE.md`**，
訊息只是副本。A-2 與 A 的共識以 `STATE.md` 為準，**不以訊息為準**。

---

## §3 · 檔案佔用（撞車防線）

**動之前先確認這個路徑屬於誰。不屬於你的，在自己的視窗檔「給彙整」提出，由 A 協調。**

| 路徑 | 屬於 |
|------|------|
| `backend/**`（不含 `backend/tests/`） | **B** |
| `frontend/**` | **B** |
| `backend/tests/**` | **C** |
| 根目錄 `*.md`、`docs/**.md` | **A** |
| `docs/windows/B.md` | 只有 **B** 寫 |
| `docs/windows/C.md` | 只有 **C** 寫 |
| `docs/windows/STATE.md` | 只有 **A** 寫 |

**特別鎖定（動它要先在自己的視窗檔宣告，並等 A 回覆）：**

- `backend/db.py` — 84 個 migration、78 張表全在這一支。兩個人同時加 migration
  會產生兩個 `_m085_`，而且 merge 不會衝突（各加各的函式），**只會在執行時撞版本號**。
- `backend/main.py` — router 掛載處，加模組一定會動到。
- `frontend/static/sidebar.js` — 模組→頁面對應表，加模組一定會動到。
- `backend/helpers/__init__.py` — 共用匯出。

---

## §4 · 一輪的七步（迴圈）

```
① A 出開發單          → docs/windows/STATE.md〈本輪開發單〉
② C 先寫驗收測試       → 應該是紅的。紅不起來＝這題沒在驗東西，退回 ①
③ C 回報「紅在哪一行」  → docs/windows/C.md
④ B 寫碼直到綠         → docs/windows/B.md 記「改了哪些檔、為什麼」
⑤ C 複驗 ＋ 反向驗證    → 把 bug 放回去，確認那題精準變紅、其餘仍綠
⑥ C 跑全量回歸         → 題數不可以少於上一輪（基準的算法見下）
⑦ A 彙整、裁決、出下一輪 → 更新 STATE.md
```

**②先於④不可以省。**

🔴 **而且這一條是 A 的責任：A 在對 B 說「開工」之前，必須先確認 C 的②已完成且是紅的。**
不是「我以為 C 應該差不多了」，是**去看 `backend/tests/` 裡那個檔案存不存在**。

⚠️ A 在 2026-09-21 一天內犯了**三次同類排程錯誤**：
① 准 B 開第 2 輪而第 1 輪的⑥還沒做完；② 漏回 B 的 `db.py` 宣告把它卡住；③ 叫 B 開工而 C 的②還沒發生。
**共同根因：A 把 B 與 C 當成兩條獨立佇列在調度，而七步是把它們綁在一起的。**
⇒ **要對任何一邊說「可以了」之前，先看另一邊在哪裡。** 先有測試才寫碼，測試就不可能是照著實作長出來的。

🔴 **⑥ 沒跑完之前不准開下一輪。**
A 要出下一張開發單之前，先確認 C 的⑥已完成且**量的是一個靜止的樹**。
「⑥的數字」與「被量的程式碼」必須是同一個時間點的東西，否則那個數字不能用。
⚠️ 2026-09-21 A 犯過：准了 B 開第 2 輪，而第 1 輪的⑥還沒做完。
C 跑回歸的 20 分鐘內，`helpers/licensing.py` 被 B 改過（+3,419 bytes），
**C 沒辦法證明它載入的是哪一個版本**。

### 🔴 停手握手（2026-09-21 補，C 提案、A 採用）

上面那條規則**只有規則、沒有握手**：沒有人知道「B 停了沒」，
於是它靠 A 記得——**而 A 在第 2 輪就忘過一次。**

| 步 | 誰 | 做什麼 |
|---|---|---|
| 1 | **B** | 本輪程式寫完，最後一個 commit 推完 |
| 2 | **B** | 在 `B.md` 寫一行 **「第 N 輪停手，SHA = `<最後那個 commit>`」** |
| 3 | **C** | 🔴 **自己 `git log` 讀那一行、自己驗、自己開跑**。不要等 A 轉——**宣告在 repo 裡，C 有第一手來源，轉述多一個環節不是少一個**（§5j） |
| 4 | **C** | 跑完回報，**附它當時 `HEAD` 的 SHA** |
| 5 | **A** | 🔑 **事後稽核**（不是事前放行）：`git log <宣告SHA>..<C的HEAD> -- backend frontend` 要是**空**的。不是空就作廢重跑 |

⚠️ **不可以寫成「兩個 SHA 對得起來」** —— A 自己 commit 文件之後 `HEAD` 就會往前跑，那個寫法會誤報。
🔑 **判準是「往前跑的都不是產品碼」，不是「HEAD 沒動」。**

### 🔴 ⑤ 除錯突變時查出來的結構，要當場回頭問一句

«**我的測試分得出它們嗎？**»

2026-09-21 視窗 C：突變寫錯三次，黑箱探測才發現「形狀驗證」其實是**三道**獨立檢查。
它把這個發現寫進 `C.md`、commit message、回報，**然後沒有回頭把測試拆開**。

> **「我把發現寫成了知識，沒有把它變成守門。」**（C）

⇒ 後果：下一個人覺得三行重複而合併掉兩道時，**⑤的突變仍然會紅（還剩一道），看起來完全正常**。
🔑 **那個資訊只在除錯的那幾分鐘裡存在**，過了就變成 commit message 裡的一段文字——**而文字不會在別人合併掉兩道的時候變紅。**

### 🔴 `git status` 說髂、`git diff` 說沒差異時，**去比位元組**

轉回 LF 之後 `git status` 仍顯示 `M` 而 `git diff` 是空的（index 的 stat 快取，`git add` 才清掉）。
🔑 **相信前者會去追一個不存在的差異；相信後者會在握手回報裡寫下「工作樹乾淨」而它是錯的。**

🔴 **而且要拆成兩個檢查（它們問的不是同一件事）：**

| 檢查 | 指令 | 含不含測試 |
|---|---|---|
| **(甲) B 真的停了** | `git log <B宣告SHA>..<C起跑HEAD> -- backend frontend ':!backend/tests'` 為空 | 🔴 **要排除 `backend/tests`** —— C 在這個空檔補自己的測試是正常且必要的 |
| **(乙) 跑的期間沒人動** | `git diff <C起跑HEAD> <C結束HEAD> --name-only -- backend frontend` 為空＋工作樹乾淨 | ✅ **不排除任何東西** —— 測試本身也是受測對象 |

⚠️ 2026-09-21 A 寫錯過：一個檢查想涵蓋兩件事，**而那兩件事的「該不該含測試」答案是相反的**，會誤報一次重跑。

⚠️ **第 5 步是整個握手唯一有效的部分。**
前四步都只是「說」，**只有兩個 SHA 對得起來，
才證明「量的那棵樹」與「停的那棵樹」是同一棵。**

🔑 **「不要讓人閒著」是排程的目標，不是驗證的目標。**
用前者去壓後者，省下的 20 分鐘會變成一個不能用的數字。

**⑥ 的基準數字只能用 `pytest --collect-only` 算，不可以用 `grep -c "def test_"`。**
2026-09-21 A 犯過這個錯：grep 得到 1,034、實際收集到 **1,084**，差的 50 題是
7 個檔的 `@pytest.mark.parametrize` 展開出來的。基準寫低了的後果不是「數字不好看」，
是**少掉 50 題也不會有人發現** —— 而「少於基準」正是唯一能抓到「有東西沒被跑到」的訊號。
基準數字本身就是一道守門，**守門寫錯了跟沒有守門是一樣的**。

**基準會隨每一輪新增的測試往上走，要比的是「上一輪的數字」不是一個寫死的常數。**
時間軸：本期起點 **1,084** → 第 1 輪加 C 的 27 題後為 **1,111**
（B 實跑 1,055 過／55 skip／1 紅，合計 1,111，數字對得上）。
**每一輪把新的總數寫進自己的視窗檔**，下一輪才比對得了。

```bash
cd backend && git ls-files tests/ | grep -c "\.py$"          # 檔數
python -m pytest tests/ --collect-only -q --basetemp=<固定路徑> | tail -1   # 題數
```

---

## §5 · 硬規則

1. **不准用會動的名字。** `HEAD~1`、`$(git rev-parse HEAD)`、`ls -t | head -1`、
   `sorted(glob(...))[-1]` 一律禁止 —— 在共用工作目錄裡，它們會指到另一個視窗的東西，
   而失敗的樣子是「成功了，只是動到別人的」。要講哪一個 commit 就寫完整 SHA。
2. **commit 前先 `git status`，只 `git add` 到檔案層級，而且 `git commit` 一律帶 pathspec：`git commit -m ... -- <路徑>`。**
   `git add <目錄>` 等同那個目錄下的 `-A`，會把另一個視窗正在改的東西一起帶走。
   🔴 **而且「只 add 自己的檔」防不了真正的坐池：`git index` 是共用狀態，`git commit` 送出的是**整個 index**，不是「我剛 `add` 的那些」。**
   ⇒ 別人暫存好的檔會進你的 commit，而你完全不會發現。
   **A 已實測確認**（沙箱 repo，2026-09-21）：
   ```
   兩人各自 add 一個檔 → git commit -m "..."        ⇒ commit 含兩個檔 ❌
   兩人各自 add 一個檔 → git commit -m "..." -- b.txt ⇒ 只含 b.txt，
                                                    a.txt 仍留在 index ✅
   ```
   （這個坑是 MSP 安控平台視窗3 告知的，它們真的踩到了：某個 session 明確單檔 `add`，照樣把別人已暫存的 12 個檔送進自己的 commit。）
3. **跑 pytest 一律帶 `--basetemp`，而且要「固定但分用途」**：
   ```
   全量回歸    --basetemp=...\motrix-pytest-<視窗>-full
   臨時單檔跑  --basetemp=...\motrix-pytest-<視窗>-adhoc
   ```
   固定 ⇒ 不會指到別人的；分用途 ⇒ **同一個視窗的兩次跑也不會互刪**。
   ⚠️ 2026-09-21 視窗 C 實測：**pytest 會把 `--basetemp` 整個刪掉重建**，觸發時機是**第一次有測試用到 `tmp_path`／`tmp_path_factory`**（不是 session 開始，視窗 C 實測更正）。全量回歸跑到 86% 時另跑一支單檔測試，
   把它正在用的 `motrix_app0/motrix_erp.db` 刪掉了。
   ⚠️ **這一句的精確度有影響**：照舊的「session 開始」說法，下一個人寫一支不碰 `tmp_path` 的測試去驗證，會看到目錄**沒有**被刪，並據此認定這條規則是假的。
   （C 自己就差點這樣推翻自己的假設；換成 `def test_x(tmp_path)` 就刪了。）
   **規則本身不變**——本專案 `conftest.py::_app` 幾乎一啟動就`tmp_path_factory.mktemp()`，實際效果等同「一跑就刪」。
   🔑 **描述機制寫錯，會讓下一個人把對的規則当成錯的而拿掉。**
   ☠️ **這條的失敗模式比 CPU 那條更糟**：CPU 競爭讓斷言不成立（吵、看得見）；
   basetemp 被刪是**檔案在腳下消失**，錯誤訊息指向資料庫，**看起來像真的有 bug**。
   （原本只要求「固定」是 A 為了防「會動的名字」，卻製造了這個洞——**兩個都要防**。），否則測試全過也會回非 0，且 `%TEMP%` 會被吃掉
   1～2 GB／次不自動清（見 `MOTRIX-ERP-QUICK.md` §11）。
   🔑 **「我沒撞到」跟「這個做法沒問題」是兩件事**（視窗 B 回頭自查後講的）。
   B 先前全量與單檔也用同一個 basetemp，只因為那兩次回歸期間剛好沒再跑 pytest 而避開——**靠運氣，而運氣不是防線**。
4. **不要動正式機。** 這是開發機（`C:\Users\hichan\Desktop\MOTRIX-ERP`）。
   打包與部署是使用者自己的動作，任何視窗都不要自己跑 `apply_update.ps1`。
5. **全量回歸一次只能有一個人跑**，而且跑之前要在自己的視窗檔寫一行「我開始跑了」。
   共用工作目錄防的是**檔案**撞車，但 **CPU 也是共用資源**。
   2026-09-21 踩過：A 為了回答使用者問題開了背景全量回歸，與 B 的同時跑，
   把 `test_semaphore_caps_concurrent_holders`（斷言是「應該有一批真的頂到上限」）搞紅了。
   ☠️ **失敗的樣子跟「真的有 bug」一模一樣**——若不是 B 順手註明
   「當下有三個 pytest 在跑」，這一輪會有人去追一個根本不存在的併發 bug。
6. **收工一定要更新自己的視窗檔。** 沒寫下來的結論等於沒發生 —— 下一棒讀不到。
7. **`.ps1` 含中文一律存 UTF-8 with BOM**，`.bat` 一律 CRLF（PS 5.1 會用 CP950 讀壞，
   而且是非終止型錯誤，守門判斷式會被直接跳過，見 §1.1）。

---

## §5a2 · 🔴 訊息是規則的副本，而副本不會跟著更新

```
from helpers import tender_source   ← 走模組：之後 patch 打得到
from ... import fetch_raw           ← 複製走副本：之後 patch 永遠打不到
```

**把規則的內容抄進訊息 = 給對方一份 `from ... import` 的副本。**

⚠️ **同一個形狀的第三種載體：SHA。** 2026-09-21 視窗 C 回報過**三個不存在的 commit SHA**，成因是 `git show --stat --oneline HEAD | tail -3` —— **SHA 在第一行，多檔 commit 就被砍掉**，而它**補了一個看起來很像的上去**。
🔑 **缺少觀測的那一刻要停下來，不要把空缺填滿。**
權威檔後來改了，**那份副本不會跟著變，而它還在對方手上被當成現行規則用。**

⚠️ 這跟〈要求寫在訊息裡等於沒下達〉**不一樣，而且更難發現**：
那條說的是「只寫在訊息裡」；這次**權威檔真的更新了**，
問題是**舊訊息不會自動作廢**。

🔴 **兩條硬規則：**
1. **改規則時兩邊都通知**，訊息要寫「**去讀協定，不要用我之前那則**」。
   ⚠️ 特別是**停手中的那一邊** —— 停手的人不會主動來對規則。
2. **引用規則時給位置不給內容**（⚠️ **對視窗檔也成立** —— 抄進 `B.md`／`C.md` 的副本會被下一棒讀到，而下一棒讀不到你發過的訊息。**一次複製產生的不是一份副本，是一條會自我繁殖的鏈**）：寫「協定 §4 的握手表」，
   **不要把指令抄進訊息**。抄進去的那一刻就產生了一份會過期的副本。

⚠️ **第四種載體：同一行裡的兩份。** 2026-09-21 A 在 `MEMORY.md` 第 1 行（24 段、2,050 字元）加了一段，**而同樣的指示早在段 1 就有** —— A 自己三小時前寫的。**最難發現，因為沒有人會把一行切成 24 段來看。**
⇒ **改文件前先數那個字串出現幾次（`assert count == 1`），不要只看你要編輯的那一段。**

⚠️ 2026-09-21 A 犯過：第三十一次改了握手判準，訊息只發給 C（當下是 C 要跑⑥），
B 停手中沒收到 —— 兩小時後 B 自己重新發現了同一個洞，**那個成本是 A 造成的**。

---

## §5b · 通則：狀態旗標不可以蓋過真實狀態

**真實狀態**（庫存夠不夠、授權過期沒有）是**算出來的**、會自己更新；
**旗標**（`received`、快取的值）是**人設的**、會過期。
讓過期的旗標去抑制算出來的事實，失敗的樣子是**「它安靜地不再提醒你」**。

2026-09-21 一天內踩到兩次，兩次都是 **A 寫的規格**、都是寫手拓到的：
- `received` 永久抑制採購建議 ⇒ 料號「只能被採購一次」（視窗 B）
- 快取鍵只有 mtime ⇒ `days_left` 永遠停在第一次的值，**年費授權靜默變成永久授權**（視窗 B）

跟〈降級之後它還是會動〉同一家族：**壞掉會被報修，安靜地少做一件事不會。**

---

## §5c · 通則：證據存在、而且是真的，只是它證明的不是你以為的那件事

**這是 2026-09-21 一天內四次失敗的共同形狀**（視窗 B 講出來的）：

| 手上的證據 | 是真的嗎 | 它證明的是 | 我以為它證明的是 |
|---|---|---|---|
| 第 1 輪⑤⑥全綠 | ✅ 真的 | 第 1 輪的 27 題沒問題 | ❌ 第 1、2 輪都結案了 |
| 全量回歸 1,111 全綠 | ✅ 真的 | **掛 middleware 之前**的樹沒問題 | ❌ 含 middleware 的樹沒問題 |
| 「單獨跑那題是綠的」 | ✅ 真的 | 它在無競爭時會過 | ❌ 它是 flaky |
| 「我 13 個 commit 都沒夾帶」 | ✅ 真的 | 那 13 次時機湊巧 | ❌ 不帶 pathspec 沒問題 |

⚠️ **這比「證據是假的」難發現得多。** 假證據會被質疑；
**真證據會讓人停止質疑**——而停止質疑的那一刻，錯誤就定型了。

**怎麼防**：拿到一個綠燈時問一句——

> **「它是什麼時候、對哪一個版本、量出來的？」**

三個答案有一個說不出來，那個綠燈就還不能用來下結論。
（C 用 sha256 前後對比算出「那次回歸跑的是 10:11:25 之前的 `main.py`」，
就是把這三個問題真的答出來的樣子。）

---

## §5d · A 是排程器，不是觀察者——**idle 不等於做完，idle 等於在等指令**

**這三個視窗都是互動式 session：收到訊息才動，動完就停。**
它們**不會自己接著做下一件事**，即使上一個回合明確說了「接下來我要做 X」。

2026-09-21 下午因此停了兩次，每次約 40 分鐘：
- 第一次：A 聯絡不到 C（地址找錯），C 做完⑤⑥寫好報告就停住
- 第二次：C 回覆 A 說「接下來我開始寫測試」，**然後回合結束**。
  A 以為它在寫，實際上它 idle 了 20 分鐘，檔案一個字都沒有

⚠️ **失敗的樣子是「很安靜」——安靜跟一切正常長得一模一樣。**
A 靠檔案 mtime 猜「它在不在動」猜了整個下午，**而那個訊號根本分不出
「正在寫」與「停住了」**。

🔑 **`ListAgents` 的 `busy` / `idle` 才是正確訊號**：
- `busy` = 真的在跑，**不要打擾**
- `idle` = **回合結束了，在等下一個指令**，不管它上次說要做什麼

**A 的義務**：每一輪先看 `ListAgents`。
**看到該做事的視窗是 `idle`，就是 A 漏發指令了，立刻補。**
不要用 mtime、不要用「它應該還在忙」、不要等——**那些都是猜**。

---

## §5e · 不要用 `git checkout --` 還原檔案

這個 repo `core.autocrlf=true` 且**沒有 `.gitattributes`**，
而磁碟上的 `.py`／`.html` 是 LF。
⇒ `git checkout -- <某支 .py>` 會把它拉回 git 認為的樣子（**CRLF**），
`git diff` 看不出來，**但位元組變了、雜湊變了**。

> **「還原」這個詞在這裡會騙人：它還原成 git 認為的原樣，不是你動手之前的原樣。**

**要還原就先複製一份副本，用副本蓋回去。**
做突變測試時**優先用 monkeypatch／`-p` 注入，根本不要碰磁碟上的檔**。

⚠️ 真正的修法是加 `.gitattributes`，但那會重寫整棵樹的位元組，
**已排成獨立一輪**（條件見 `docs/windows/STATE.md` 第十四次裁決）。

---

## §5f · 🔴 這個架構會造出一個「無人可查核的時段」，而 A 正好站在裡面

**視窗 C 於 2026-09-21 指出的，是本協定自己製造的失效模式：**

> 為了讓測試誠實，**C 被限制不能讀實作**；
> 為了讓量測乾淨，**B 被要求停工**。
> **兩道各自正確的限制疊起來，就造出了一個
> 「只有 A 看得到實作、而且沒有人能查核它的敘述」的時段。**

A 在那個時段講的任何關於實作的話，**都會被下游當成事實**——
C 會照著寫測試，B 會照著改碼。

**實際發生過（2026-09-21）**：A 宣稱「若呼叫端用 UTC 的今天，
台灣每天有 8 小時會算錯一天」，**寫得像既存缺陷**。
查證後根本不存在（程式用 `date.today()`，取的本來就是本地日期）。
**當下 C 不能讀實作、B 停工中，兩邊都沒有辦法反駁。**

### 規則：在那種時段講「實作現在是怎樣」，要附查證的指令與輸出，不是結論

❌ 「它用的是 UTC」
✅ 「我跑 `grep -n 'today' backend/routers/inventory.py` 得到 `192: today = date.today()`」

> **「我查了 X 指令得到這幾行」跟「它是 Y」是兩種不同強度的東西。**
> 附上指令與輸出，下游即使不能讀實作，**也判斷得出這句話的強度**。

⚠️ 同理適用於**提風險**：**提風險時要同時說「我還沒查證」，或者自己先查。**
在沒有人能反駁的位置上，**一個沒查證的風險會變成沒有根據的事實**。

---

## §5g · 證據品質 ≠ 證據範圍

鐵則 (d)（拿到綠燈先問「什麼時候、哪個版本、什麼條件下量的」）查的是
**證據品質**——「我看到的這個綠燈可信嗎」。

它**查不到**另一件事：**證據範圍**——「我該看的都看了嗎」。

> **範圍的缺口不會讓任何一項證據變得可疑——
> 它讓你「不會想到要去找那一項證據」。**（視窗 C 的講法）

2026-09-21 A 因此結錯兩次案：
- 用第 1 輪的⑤⑥結了第 2 輪（第 2 輪的測試從沒被寫過）
- 結第 3 輪時三個品質問題都答得出來，**但沒查「這一輪派下去的事都做完了嗎」**

⇒ **鐵則 (i)：結案前要另外問一次「這一輪派下去的事都做完了嗎」。**
那不是 (d) 的一部分，是獨立的一題。

---

## §5h · 驗「可替換的接縫」要三次，不是兩次

**視窗 B 於 2026-09-21 示範的方法**（驗 `procurement.today()` 這個接縫）：

```
未換接縫        eta = 2026-09-26   （今天 + 5）
換掉 today()    eta = 2030-01-06   ← 會動，所以接縫是真的
還原之後        eta = 2026-09-26   ← 精準回到原值
```

> **B 的話：「第三行才是關鍵。只驗『換了會動』證明不了它沒有副作用。」**

**第三次（還原後要精準回到原值）證明的是「純替換、沒有殘留」。**
少了它，一個「換得掉但會污染後續呼叫」的接縫也會通過前兩行的檢驗——
而那種污染會在**下一題測試**裡爆出來，**而且看起來像那一題自己的問題**。

⇒ 這也適用於**突變測試**：植入 → 驗紅 → **移除 → 驗回到原本的綠**。
⚠️ **第三步要驗「回到原本那個綠」，不是「又綠了」**（視窗 C 補充）：
**題數與通過數都要對得上**，否則突變順手弄壞一題再修好，**看起來一樣是綠的**。
第三步不是形式，它排除的是「突變留下了殘留」這一類假訊號。

---

## §5i · 誰有權查證「實作現在是怎樣」——把它寫成授權，不只是習慣

§5f 說 A 要附查證的指令與輸出。**視窗 B 指出那還不夠**：

> 「當時我停工、C 不能讀實作，**不是我們不想查，是我們被規則擋住了**。
> 分工本身製造了一個沒有人有權查證的區域，而風險宣告正好落在那裡。
> ⇒ 它需要的不是『我更小心』，是**把『誰有權查證這一類主張』寫成協定的一部分**。」

**規則**：

1. **A 對「實作現狀」的任何主張，若會變成驗收條件或工作項，B 有權（也有責任）複驗。**
2. 🔑 **B 停工期間仍然可以被要求做唯讀查證，那不算開工。**
   停工是為了讓量測的樹不動；**讀檔不會讓樹動**。
3. **B 複驗不同意時，以 B 的查證為準**——它是唯一能讀實作又不受 C 那道限制的人。
4. ⚠️ **C 不要因為「A 說了」就把某個實作行為寫進測試的預期值**；
   §3 沒寫的，回頭問 A 要查證輸出。

### 📌 附帶：待命不等於零責任（B 自己講的）

> 「我把『等派工』執行得很乾淨，**乾淨到你漏派的那一件就這樣躺了一整輪**。」

⇒ **進入待命前，回頭掃一次那一輪的裁決裡有沒有「寫到我、卻沒變成工作項」的句子。**
A 會漏派；**而最不可能發現 A 漏派的時刻，正是被漏派的人已經停工的時候。**


### 📌 底傑掃描要**對照 STATE 而不是對照對話**（視窗 B 提）

進待命前回頭掃那一輪的裁決時，**用 `grep` 掃 `STATE.md` 裡寫到自己的句子**，不要靠回想對話：

```bash
grep -n "請 B\|B 要\|派給 B\|由 B\|B 補\|B 加\|B 動" docs/windows/STATE.md
```

> **訊息會漏細節，而漏掉的那一格正好不會出現在我腦中的清單裡
> ——我不會去找一個我不知道存在的句子。**（B）

⚠️ 2026-09-21 實際掃出兩件，而且成因不同：
- 一件是**執行到一半**（做了接縫、沒做確認）——**這種最難發現，因為那一項看起來已經完成了**
- 一件是**派工管道不一致**（訊息裡沒寫、STATE 裡有寫，對方只讀了訊息）

---

## §5j · 轉述的授權比直接的指令**多一個環節，不是少一個**

**視窗 C 於 2026-09-21 拒收 A 轉述的使用者答覆，理由是對的：**

> 「**如果我拒絕了『A 說可以開工』，那我更不能接受『A 說使用者說可以開工』。**
> 轉述比你自己的指令多一個環節，不是少一個。
> 如果我現在因為多繞了一層就收下，那才真的是演的，而且演得更像。」

**規則**：
- 一個視窗**主動向它的使用者提出**的問題，**只能由它的使用者在它自己的對話裡回答**
- A **不要轉述**，也**不要替對方解讀**（即使逐字轉述並標出歧義也一樣）
- A 也**不需要替它去問**——**它的使用者就在它的對話裡，它直接問比 A 轉一手準確，
  而且少打擾使用者一次**
- A 能做的只有一件：**跟自己的使用者講清楚「那邊停在你身上，要去那個視窗直接說」**

---

## §5k · 🔴 答案沒有錯，是題目問錯了

**2026-09-21 A 犯過，而且是被使用者追問才救回來的：**

使用者要「用 Google Maps 自動帶出附近商家」，A 查證了條款、查證了資料現況，
推論通順、證據都是真的，結論是「沒有歷史案場就沒有差異化，**整條暫緩**」。

**錯的不是推理，是 A 問的問題**：
A 問「**這比競爭對手強在哪**」，使用者要的是「**這能不能省業務的時間**」。
A 自己加的加分題（疊歷史案場）被寫成了**前提**。

> ⚠️ **這比「查證不足」難防**：查證不足至少你知道自己沒查；
> **評判標準用錯，你會覺得自己想得很完整**——
> 每一步都有根據、每個數字都查證過，而整串指向一個**沒有人問的問題**。

**檢查（C 提的，A 採納）**：

> **做暫緩／否決這種不可逆的建議之前，先把使用者的原話抄下來，
> 問「我的判準是不是他要的判準」。**

⚠️ 那次是使用者剛好追問了一句「不能自動帶出附近商家嗎」才救回來的
——**那是運氣，不是機制**。
📌 **對方問「不能 X 嗎」時，通常不是在問可行性，是在說「你答的不是我問的」。**

---

## §5l · 回報紅燈時要附**當時 §3 的 commit SHA**

**視窗 C 提的，A 採納。**

> **沒有版本號的「我照單寫了」，等於沒有說自己照的是哪一張。**

2026-09-21 實際發生：C 於 13:49 寫完 28 題，A 於 **13:50:53** 把 §3 改成第二版
——**晚一分鐘**。若 C 當時沒有記下 SHA，事後無從分辨它照的是哪一版。

⚠️ **而 C 指出的更深一層**：

> 「如果你當時為了『不打擾 C』而沒通知，我會照著一張**已知有錯**的單把測試寫完，
> **而且全紅，看起來完全正常**。」

🔑 **紅燈只證明「實作還沒跟上規格」，不證明「規格是對的」。
一張錯的單也會產生漂亮的全紅。**

⚠️ 那次救回來的是**規則**（A 自己立的「改單要主動通知」），**不是判斷**——
A 當時看 `ListAgents` 顯示 idle 就推論「C 還沒開工」，
**而 §5d 寫的是「idle ＝ 回合結束在等指令」，那不是同一件事。**

---

## §5m · 驗去重要兩個方向：該併的有併、**該分的有分**

只驗「重複的有被擋下」，**一個把唯一鍵取得太寬的實作會完整通過**。

2026-09-21 C 新增 `test_05c`（同案號**不同機關**要留兩筆）：
只驗「同機關同案號被擋下」的話，一個「唯一鍵只有 `case_no`」的實作會全綠
——**而那正是要被防掉的那個實作**。

🔑 **擋下重複的斷言，擋不出「鍵的範圍是不是太寬」。**

---

## §5n · 樣本也是受測對象的一部分，也需要對照組

**視窗 C 於 2026-09-21 第 4 輪實際踩到並自己抓到：**

它寫了一道「**手術真的改到東西了嗎**」的守門（`test_00b`），第一次跑就紅——
**而紅的是它自己的樣本，不是受測程式**：
從真實 fixture 切測試樣本時，`_rebuild()` 把不該砍的四列一起砍了，
本來要做「5 筆裡有 1 筆缺欄位」，做出來變成「只有 1 筆而且它缺欄位」。

> ⚠️ **沒有那道守門**，後面那題會斷言 `len(items) == 4` 而樣本只有 1 列 ⇒
> **永遠紅，而查的人會去查一個根本沒錯的實作——因為紅燈指的是它。**

🔑 **「受測物沒產出東西」與「輸入裡根本沒有東西」，在紅燈上長得一模一樣。**

**做法**：凡是**由程式加工出來的測試輸入**（切片、改欄位、造壞資料），
都要有一道守門斷言「**加工結果真的是我要的那樣**」：
- 動過手術的樣本，**列數不可以變**，只有內容能變
- 要造「缺某欄」的樣本，就斷言**那一欄真的空了、而其餘欄位沒被動到**

### 🔑 統一框架（視窗 C 提）：它們都在**受測對象之外**

2026-09-21 同一天出現三次，位置不同但是同一個家族：

| 位置 | 發生了什麼 |
|---|---|
| **輸入端**（A） | `cells[:7]` 截斷 —— **工具給我的東西不是現實**（少了） |
| **輸入端**（A） | strip+unescape 後的表頭 —— **工具給了一個更好看的版本**（沒少，更難發現） |
| **測試資料端**（C） | `_rebuild()` 把四列一起砍了 —— **我準備的東西不是我以為的那個** |

> ⚠️ **兩邊都不在「受測對象」裡，所以都不會被任何斷言保護。**

⇒ **測試只守住了中間那一段。兩頭（我怎麼知道現實、我怎麼造出輸入）要自己守。**

⚠️ 這跟 §5h（驗接縫要三次）、突變測試的「移除後要回到**原本那個**綠」是同一條線：
**先證明你的操作真的只做了你以為的那件事，再拿它去驗別人。**

---

## §5o · 長期記憶檔是**共用池**，寫入前先查同一件事在不在

`~/.claude/projects/C--Users-hichan/memory/` **七個視窗共用一個池**
（那是 CLI 啟動目錄決定的副作用，不是設計）。

🔴 **2026-09-23 實例**：同一個晚上 A 與 D **各自**把同兩件事寫進同兩個檔 ——
```
feedback_scanner_positive_control.md   「正對照通過而數字錯了」  **兩份**
feedback_worktree_vs_repo.md           「未追蹤區讓破壞看不見」  **兩份**
```
☠️ 而重複**不會報錯、不會衝突、`MEMORY.md` 的掛勾也只有一行** ⇒
  下一次 recall 會拿到同一件事的兩種說法，而**它們的詳略不同** ——
  短的那份少了「怎麼抓到的」，讀到短的那份的人會以為那就是全部。

**規則**
```
① 寫記憶檔前：grep 該檔今天的日期 ＋ 關鍵詞，確認同一件事沒有人寫過
   ⚠ **要掃兩輪**：一輪用「**缺陷的名字**」（基準／污染／中間值），
      一輪用「**正確做法的名字**」（不變量／invariant／上游）
      🔑 2026-09-23 實例：第一輪無重複，**第二輪才找到一條該互指的** ——
        因為那一條講的是「怎麼做對」，用的是正面詞彙
   ⚠ grep 要對**磁碟上**的檔，不可以用 context 裡那份
      （自動載入的 `MEMORY.md` 是 session 起跑時的快照，之後不會更新）
② 已經有了 => **合併進那一段**，不要另起新段
③ 兩份都存在時 => 保留**有「怎麼抓到的」與 How to apply 的那一份**，刪短的
④ 刪除要用唯一錨點 ＋ assert，且刪完複驗「長的那份還在」
```
📌 誰都可以寫，**不需要經過 A** —— 限制寫入權會浪費實際查到東西的那個視窗。
⚠️ 而 `MEMORY.md` 的掛勾**一個檔只能有一行**，掛勾重複要當場併掉。

---

## §5p · **規格落檔不等於交付** —— 交付是送給會照著做的那個人

```
落檔      作者說「寫好了」
**交付**  作者**自己**把它送給會照著動手的那個人，並寫一份摘要
```
🔑 **價值來自重讀**：摘要要寫給「會照著動手的人」，
而那個視角與「寫規格」的視角不同。
⚠ ⇒ **那一步不可以由 A 轉述** —— 轉述的話重讀就沒發生。

🔴 **2026-09-23 實例**：A-2 為了寫摘要而重讀自己剛落檔的 `SPEC-BN1-PLAN.md`，抓到兩個錯
```
① 反向控制走不通：他寫「把 person_source 改成解析不出人的值」，
   而 POST /items 對不在 PERSON_SOURCES 的值直接 400
   => 照它寫只能**直接 INSERT 繞過 router** = 測試繞過生產路徑
② 漏 is_active 過濾：create_award 有 WHERE is_active = 1 而 plan 契約沒寫
   => 畫面列出已停用的項目 => 使用者填完比例按下去才收到 400
```
☠ **兩個錯都不在他自己列的「我沒做的」六項裡** ——
  〈「查過的」那一欄裡也會有沒查過的句子〉。
🔑 而 ② 的形狀值得單記：**「先問再做」失效的樣子是「問過了，而答案是錯的」**
  —— 整個模組的設計前提就是先問再做，而那份規格差一點讓那句話變成假的。

---

## §6 · 停機條件

**一輪產出的待決事項多於解決的，就是負產出；連續兩輪負產出就停下來找 A。**

迴圈的目標函數是「**可出貨**」，不是「正確性」。每一輪都成功、而整體離出貨愈來愈遠，
問題不在任何一輪裡，在派工的方向上。

---

## §7 · 兩條端到端細線

這一期只做兩條線，**每一輪的產出都必須站在某一步上，站不上去的不做**。
細線的定義與逐步驗收條件見 [`SELLABLE-AND-MOBILE-SPEC.md`](SELLABLE-AND-MOBILE-SPEC.md)。

- **細線 1 — 授權底座**：能把這套系統賣給第二家公司並裝起來。
- **細線 2 — 手機專用頁面**：現場人員用手機完成一筆真實工作並回到桌面版看得到。

**線 1 先做完再做線 2**（只有一個寫手，平行只會製造交接成本）。
線 1 進行中，C 在等待時做線 2 的驗收測試骨架 —— 不寫產品碼。

---

# §2f 視窗訊息格式（使用者 2026-09-23 02:30 明訂）

> 「交接精準、文字精簡、講重點」—— **對使用者與對視窗都適用**。

## 四段式（順序固定，沒有的那段直接省略）
```
裁定／派工   要做什麼，一到三行
依據       行號、SHA、實測數字 —— **只貼數字不貼推論**
界線       不要做什麼／哪一格未決
下一步     做完交給誰
```

## 不要寫
```
✖ 把對方的話引回去再評論一次
✖ 「這是〈某條原則〉的第 N 次」之類的归類 —— **那寫進 STATE，不寫進訊息**
✖ 稱讚、道歉、「你說得對」
✖ 重述對方剛告訴我的事
```

## 問誰要知道
```
裁定與界線 → 訊息（對方要馬上行動）
成因與归類 → `STATE.md`（別人日後會讀）
☠️ 兩者混在一起 ⇒ 裁定被埋在第四段，而對方只看前三行
```

## 回報時必附
```
量測時間 ＋ 當下 HEAD（清單會過期，一印出來就過期）
「我沒查什麼」（否定句要同句講出查的範圍）
```

---

# §2g 量測回報要附 dirty 檔的**內容雜湊**

> D 實測指出 `§2f`「量測時間＋HEAD」不夠。

**缺口**
```
HEAD                     只釘住**已提交**的部分
git status --porcelain   只釘住**檔名**
=> 兩次執行可以 HEAD 相同、dirty 檔名相同，而那些檔的**內容不同**
```
**實例**（`DM1` 02:29 批次紅、02:37 綠）：
兩次之間 B 把未提交的 `db.py` 提交了 ⇒ **原始內容已不存在，成因不可回溯**。

**做法**
```
git status --porcelain | while read st p; do
  printf '%s %s ' "$st" "$p"; sha256sum "$p" 2>/dev/null | cut -c1-16
done
```
⇒ 回報量測時一律附：**量測時間 ＋ HEAD ＋ 每個 dirty 檔的雜湊前 16 碼**。

**而共用工作樹上「凍結重跑」只有一種做法**
```
❌ git stash              會帶走別人的（禁用）
✅ git archive <sha> 解到自己的 scratchpad 再跑
   ⇒ 不寫 repo，而要自己處理相依與 conftest 路徑
```
