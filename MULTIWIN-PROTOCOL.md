# 多視窗開發協定（MOTRIX-ERP）

> 建立 2026-09-21。**每個視窗開工第一件事：讀這份，再讀自己的視窗檔。**
> 維護者：視窗 A（彙整）。B／C 不要改這份，有意見寫在自己的視窗檔「給彙整」段。

---

## §1 · 為什麼要有這份

兩個視窗在**同一台機器、同一個工作目錄**上動同一個 repo。本專案已經有過血的前例：
2026-09-10 兩台機器同時有 session 在改 WebAuthn，開發機打包好的更新包差點覆蓋掉
正式機 session 正在改的檔案（見 `MOTRIX-ERP-QUICK.md` §0 落差表）。
**同一個目錄裡兩個視窗，撞車機率比跨機更高，而且撞了不會有任何錯誤訊息。**

---

## §2 · 三個視窗的角色

| 視窗 | 角色 | 寫什麼 | 不寫什麼 |
|------|------|--------|----------|
| **A** | 彙整／派工／裁決 | 所有 `.md` 文件、開發單、`docs/windows/STATE.md` | **不寫任何程式碼** |
| **B** | 寫手 | 產品程式碼（`backend/` 與 `frontend/`） | **不寫任何測試** |
| **C** | 驗證常駐 | 測試（`backend/tests/`）、跑測試、瀏覽器實測 | **不寫任何產品程式碼** |

**B 不寫測試、C 不寫產品碼，是刻意的。** 理由見 `MEMORY` 的假綠燈教訓：
寫碼的人寫的測試，觀測點會自然落在「自己設的值」上，驗到的是自己的假設不是行為。

---

## §3 · 檔案佔用（撞車防線）

**動之前先確認這個路徑屬於誰。不屬於你的，在自己的視窗檔「給彙整」提出，由 A 協調。**

| 路徑 | 屬於 |
|------|------|
| `backend/**`（不含 `backend/tests/`） | **B** |
| `frontend/**` | **B** |
| `backend/tests/**` | **C** |
| 根目錄 `*.md`、`docs/**.md` | **A** |
| `docs/windows/B.md` | 只有 **B** 寫 |
| `docs/windows/C.md` | 只有 **C** 寫 |
| `docs/windows/STATE.md` | 只有 **A** 寫 |

**特別鎖定（動它要先在自己的視窗檔宣告，並等 A 回覆）：**

- `backend/db.py` — 84 個 migration、78 張表全在這一支。兩個人同時加 migration
  會產生兩個 `_m085_`，而且 merge 不會衝突（各加各的函式），**只會在執行時撞版本號**。
- `backend/main.py` — router 掛載處，加模組一定會動到。
- `frontend/static/sidebar.js` — 模組→頁面對應表，加模組一定會動到。
- `backend/helpers/__init__.py` — 共用匯出。

---

## §4 · 一輪的七步（迴圈）

```
① A 出開發單          → docs/windows/STATE.md〈本輪開發單〉
② C 先寫驗收測試       → 應該是紅的。紅不起來＝這題沒在驗東西，退回 ①
③ C 回報「紅在哪一行」  → docs/windows/C.md
④ B 寫碼直到綠         → docs/windows/B.md 記「改了哪些檔、為什麼」
⑤ C 複驗 ＋ 反向驗證    → 把 bug 放回去，確認那題精準變紅、其餘仍綠
⑥ C 跑全量回歸         → 題數不可以少於上一輪（基準的算法見下）
⑦ A 彙整、裁決、出下一輪 → 更新 STATE.md
```

**②先於④不可以省。**

🔴 **而且這一條是 A 的責任：A 在對 B 說「開工」之前，必須先確認 C 的②已完成且是紅的。**
不是「我以為 C 應該差不多了」，是**去看 `backend/tests/` 裡那個檔案存不存在**。

⚠️ A 在 2026-09-21 一天內犯了**三次同類排程錯誤**：
① 准 B 開第 2 輪而第 1 輪的⑥還沒做完；② 漏回 B 的 `db.py` 宣告把它卡住；③ 叫 B 開工而 C 的②還沒發生。
**共同根因：A 把 B 與 C 當成兩條獨立佇列在調度，而七步是把它們綁在一起的。**
⇒ **要對任何一邊說「可以了」之前，先看另一邊在哪裡。** 先有測試才寫碼，測試就不可能是照著實作長出來的。

🔴 **⑥ 沒跑完之前不准開下一輪。**
A 要出下一張開發單之前，先確認 C 的⑥已完成且**量的是一個靜止的樹**。
「⑥的數字」與「被量的程式碼」必須是同一個時間點的東西，否則那個數字不能用。
⚠️ 2026-09-21 A 犯過：准了 B 開第 2 輪，而第 1 輪的⑥還沒做完。
C 跑回歸的 20 分鐘內，`helpers/licensing.py` 被 B 改過（+3,419 bytes），
**C 沒辦法證明它載入的是哪一個版本**。

**⑥ 的基準數字只能用 `pytest --collect-only` 算，不可以用 `grep -c "def test_"`。**
2026-09-21 A 犯過這個錯：grep 得到 1,034、實際收集到 **1,084**，差的 50 題是
7 個檔的 `@pytest.mark.parametrize` 展開出來的。基準寫低了的後果不是「數字不好看」，
是**少掉 50 題也不會有人發現** —— 而「少於基準」正是唯一能抓到「有東西沒被跑到」的訊號。
基準數字本身就是一道守門，**守門寫錯了跟沒有守門是一樣的**。

**基準會隨每一輪新增的測試往上走，要比的是「上一輪的數字」不是一個寫死的常數。**
時間軸：本期起點 **1,084** → 第 1 輪加 C 的 27 題後為 **1,111**
（B 實跑 1,055 過／55 skip／1 紅，合計 1,111，數字對得上）。
**每一輪把新的總數寫進自己的視窗檔**，下一輪才比對得了。

```bash
cd backend && git ls-files tests/ | grep -c "\.py$"          # 檔數
python -m pytest tests/ --collect-only -q --basetemp=<固定路徑> | tail -1   # 題數
```

---

## §5 · 硬規則

1. **不准用會動的名字。** `HEAD~1`、`$(git rev-parse HEAD)`、`ls -t | head -1`、
   `sorted(glob(...))[-1]` 一律禁止 —— 在共用工作目錄裡，它們會指到另一個視窗的東西，
   而失敗的樣子是「成功了，只是動到別人的」。要講哪一個 commit 就寫完整 SHA。
2. **commit 前先 `git status`，只 `git add` 到檔案層級，而且 `git commit` 一律帶 pathspec：`git commit -m ... -- <路徑>`。**
   `git add <目錄>` 等同那個目錄下的 `-A`，會把另一個視窗正在改的東西一起帶走。
   🔴 **而且「只 add 自己的檔」防不了真正的坐池：`git index` 是共用狀態，`git commit` 送出的是**整個 index**，不是「我剛 `add` 的那些」。**
   ⇒ 別人暫存好的檔會進你的 commit，而你完全不會發現。
   **A 已實測確認**（沙箱 repo，2026-09-21）：
   ```
   兩人各自 add 一個檔 → git commit -m "..."        ⇒ commit 含兩個檔 ❌
   兩人各自 add 一個檔 → git commit -m "..." -- b.txt ⇒ 只含 b.txt，
                                                    a.txt 仍留在 index ✅
   ```
   （這個坑是 MSP 安控平台視窗3 告知的，它們真的踩到了：某個 session 明確單檔 `add`，照樣把別人已暫存的 12 個檔送進自己的 commit。）
3. **跑 pytest 一律帶 `--basetemp`，而且要「固定但分用途」**：
   ```
   全量回歸    --basetemp=...\motrix-pytest-<視窗>-full
   臨時單檔跑  --basetemp=...\motrix-pytest-<視窗>-adhoc
   ```
   固定 ⇒ 不會指到別人的；分用途 ⇒ **同一個視窗的兩次跑也不會互刪**。
   ⚠️ 2026-09-21 視窗 C 實測：**pytest 會把 `--basetemp` 整個刪掉重建**，觸發時機是**第一次有測試用到 `tmp_path`／`tmp_path_factory`**（不是 session 開始，視窗 C 實測更正）。全量回歸跑到 86% 時另跑一支單檔測試，
   把它正在用的 `motrix_app0/motrix_erp.db` 刪掉了。
   ⚠️ **這一句的精確度有影響**：照舊的「session 開始」說法，下一個人寫一支不碰 `tmp_path` 的測試去驗證，會看到目錄**沒有**被刪，並據此認定這條規則是假的。
   （C 自己就差點這樣推翻自己的假設；換成 `def test_x(tmp_path)` 就刪了。）
   **規則本身不變**——本專案 `conftest.py::_app` 幾乎一啟動就`tmp_path_factory.mktemp()`，實際效果等同「一跑就刪」。
   🔑 **描述機制寫錯，會讓下一個人把對的規則当成錯的而拿掉。**
   ☠️ **這條的失敗模式比 CPU 那條更糟**：CPU 競爭讓斷言不成立（吵、看得見）；
   basetemp 被刪是**檔案在腳下消失**，錯誤訊息指向資料庫，**看起來像真的有 bug**。
   （原本只要求「固定」是 A 為了防「會動的名字」，卻製造了這個洞——**兩個都要防**。），否則測試全過也會回非 0，且 `%TEMP%` 會被吃掉
   1～2 GB／次不自動清（見 `MOTRIX-ERP-QUICK.md` §11）。
   🔑 **「我沒撞到」跟「這個做法沒問題」是兩件事**（視窗 B 回頭自查後講的）。
   B 先前全量與單檔也用同一個 basetemp，只因為那兩次回歸期間剛好沒再跑 pytest 而避開——**靠運氣，而運氣不是防線**。
4. **不要動正式機。** 這是開發機（`C:\Users\hichan\Desktop\MOTRIX-ERP`）。
   打包與部署是使用者自己的動作，任何視窗都不要自己跑 `apply_update.ps1`。
5. **全量回歸一次只能有一個人跑**，而且跑之前要在自己的視窗檔寫一行「我開始跑了」。
   共用工作目錄防的是**檔案**撞車，但 **CPU 也是共用資源**。
   2026-09-21 踩過：A 為了回答使用者問題開了背景全量回歸，與 B 的同時跑，
   把 `test_semaphore_caps_concurrent_holders`（斷言是「應該有一批真的頂到上限」）搞紅了。
   ☠️ **失敗的樣子跟「真的有 bug」一模一樣**——若不是 B 順手註明
   「當下有三個 pytest 在跑」，這一輪會有人去追一個根本不存在的併發 bug。
6. **收工一定要更新自己的視窗檔。** 沒寫下來的結論等於沒發生 —— 下一棒讀不到。
7. **`.ps1` 含中文一律存 UTF-8 with BOM**，`.bat` 一律 CRLF（PS 5.1 會用 CP950 讀壞，
   而且是非終止型錯誤，守門判斷式會被直接跳過，見 §1.1）。

---

## §5b · 通則：狀態旗標不可以蓋過真實狀態

**真實狀態**（庫存夠不夠、授權過期沒有）是**算出來的**、會自己更新；
**旗標**（`received`、快取的值）是**人設的**、會過期。
讓過期的旗標去抑制算出來的事實，失敗的樣子是**「它安靜地不再提醒你」**。

2026-09-21 一天內踩到兩次，兩次都是 **A 寫的規格**、都是寫手拓到的：
- `received` 永久抑制採購建議 ⇒ 料號「只能被採購一次」（視窗 B）
- 快取鍵只有 mtime ⇒ `days_left` 永遠停在第一次的值，**年費授權靜默變成永久授權**（視窗 B）

跟〈降級之後它還是會動〉同一家族：**壞掉會被報修，安靜地少做一件事不會。**

---

## §5c · 通則：證據存在、而且是真的，只是它證明的不是你以為的那件事

**這是 2026-09-21 一天內四次失敗的共同形狀**（視窗 B 講出來的）：

| 手上的證據 | 是真的嗎 | 它證明的是 | 我以為它證明的是 |
|---|---|---|---|
| 第 1 輪⑤⑥全綠 | ✅ 真的 | 第 1 輪的 27 題沒問題 | ❌ 第 1、2 輪都結案了 |
| 全量回歸 1,111 全綠 | ✅ 真的 | **掛 middleware 之前**的樹沒問題 | ❌ 含 middleware 的樹沒問題 |
| 「單獨跑那題是綠的」 | ✅ 真的 | 它在無競爭時會過 | ❌ 它是 flaky |
| 「我 13 個 commit 都沒夾帶」 | ✅ 真的 | 那 13 次時機湊巧 | ❌ 不帶 pathspec 沒問題 |

⚠️ **這比「證據是假的」難發現得多。** 假證據會被質疑；
**真證據會讓人停止質疑**——而停止質疑的那一刻，錯誤就定型了。

**怎麼防**：拿到一個綠燈時問一句——

> **「它是什麼時候、對哪一個版本、量出來的？」**

三個答案有一個說不出來，那個綠燈就還不能用來下結論。
（C 用 sha256 前後對比算出「那次回歸跑的是 10:11:25 之前的 `main.py`」，
就是把這三個問題真的答出來的樣子。）

---

## §5d · A 是排程器，不是觀察者——**idle 不等於做完，idle 等於在等指令**

**這三個視窗都是互動式 session：收到訊息才動，動完就停。**
它們**不會自己接著做下一件事**，即使上一個回合明確說了「接下來我要做 X」。

2026-09-21 下午因此停了兩次，每次約 40 分鐘：
- 第一次：A 聯絡不到 C（地址找錯），C 做完⑤⑥寫好報告就停住
- 第二次：C 回覆 A 說「接下來我開始寫測試」，**然後回合結束**。
  A 以為它在寫，實際上它 idle 了 20 分鐘，檔案一個字都沒有

⚠️ **失敗的樣子是「很安靜」——安靜跟一切正常長得一模一樣。**
A 靠檔案 mtime 猜「它在不在動」猜了整個下午，**而那個訊號根本分不出
「正在寫」與「停住了」**。

🔑 **`ListAgents` 的 `busy` / `idle` 才是正確訊號**：
- `busy` = 真的在跑，**不要打擾**
- `idle` = **回合結束了，在等下一個指令**，不管它上次說要做什麼

**A 的義務**：每一輪先看 `ListAgents`。
**看到該做事的視窗是 `idle`，就是 A 漏發指令了，立刻補。**
不要用 mtime、不要用「它應該還在忙」、不要等——**那些都是猜**。

---

## §5e · 不要用 `git checkout --` 還原檔案

這個 repo `core.autocrlf=true` 且**沒有 `.gitattributes`**，
而磁碟上的 `.py`／`.html` 是 LF。
⇒ `git checkout -- <某支 .py>` 會把它拉回 git 認為的樣子（**CRLF**），
`git diff` 看不出來，**但位元組變了、雜湊變了**。

> **「還原」這個詞在這裡會騙人：它還原成 git 認為的原樣，不是你動手之前的原樣。**

**要還原就先複製一份副本，用副本蓋回去。**
做突變測試時**優先用 monkeypatch／`-p` 注入，根本不要碰磁碟上的檔**。

⚠️ 真正的修法是加 `.gitattributes`，但那會重寫整棵樹的位元組，
**已排成獨立一輪**（條件見 `docs/windows/STATE.md` 第十四次裁決）。

---

## §5f · 🔴 這個架構會造出一個「無人可查核的時段」，而 A 正好站在裡面

**視窗 C 於 2026-09-21 指出的，是本協定自己製造的失效模式：**

> 為了讓測試誠實，**C 被限制不能讀實作**；
> 為了讓量測乾淨，**B 被要求停工**。
> **兩道各自正確的限制疊起來，就造出了一個
> 「只有 A 看得到實作、而且沒有人能查核它的敘述」的時段。**

A 在那個時段講的任何關於實作的話，**都會被下游當成事實**——
C 會照著寫測試，B 會照著改碼。

**實際發生過（2026-09-21）**：A 宣稱「若呼叫端用 UTC 的今天，
台灣每天有 8 小時會算錯一天」，**寫得像既存缺陷**。
查證後根本不存在（程式用 `date.today()`，取的本來就是本地日期）。
**當下 C 不能讀實作、B 停工中，兩邊都沒有辦法反駁。**

### 規則：在那種時段講「實作現在是怎樣」，要附查證的指令與輸出，不是結論

❌ 「它用的是 UTC」
✅ 「我跑 `grep -n 'today' backend/routers/inventory.py` 得到 `192: today = date.today()`」

> **「我查了 X 指令得到這幾行」跟「它是 Y」是兩種不同強度的東西。**
> 附上指令與輸出，下游即使不能讀實作，**也判斷得出這句話的強度**。

⚠️ 同理適用於**提風險**：**提風險時要同時說「我還沒查證」，或者自己先查。**
在沒有人能反駁的位置上，**一個沒查證的風險會變成沒有根據的事實**。

---

## §5g · 證據品質 ≠ 證據範圍

鐵則 (d)（拿到綠燈先問「什麼時候、哪個版本、什麼條件下量的」）查的是
**證據品質**——「我看到的這個綠燈可信嗎」。

它**查不到**另一件事：**證據範圍**——「我該看的都看了嗎」。

> **範圍的缺口不會讓任何一項證據變得可疑——
> 它讓你「不會想到要去找那一項證據」。**（視窗 C 的講法）

2026-09-21 A 因此結錯兩次案：
- 用第 1 輪的⑤⑥結了第 2 輪（第 2 輪的測試從沒被寫過）
- 結第 3 輪時三個品質問題都答得出來，**但沒查「這一輪派下去的事都做完了嗎」**

⇒ **鐵則 (i)：結案前要另外問一次「這一輪派下去的事都做完了嗎」。**
那不是 (d) 的一部分，是獨立的一題。

---

## §5h · 驗「可替換的接縫」要三次，不是兩次

**視窗 B 於 2026-09-21 示範的方法**（驗 `procurement.today()` 這個接縫）：

```
未換接縫        eta = 2026-09-26   （今天 + 5）
換掉 today()    eta = 2030-01-06   ← 會動，所以接縫是真的
還原之後        eta = 2026-09-26   ← 精準回到原值
```

> **B 的話：「第三行才是關鍵。只驗『換了會動』證明不了它沒有副作用。」**

**第三次（還原後要精準回到原值）證明的是「純替換、沒有殘留」。**
少了它，一個「換得掉但會污染後續呼叫」的接縫也會通過前兩行的檢驗——
而那種污染會在**下一題測試**裡爆出來，**而且看起來像那一題自己的問題**。

⇒ 這也適用於**突變測試**：植入 → 驗紅 → **移除 → 驗回到原本的綠**。
第三步不是形式，它排除的是「突變留下了殘留」這一類假訊號。

---

## §5i · 誰有權查證「實作現在是怎樣」——把它寫成授權，不只是習慣

§5f 說 A 要附查證的指令與輸出。**視窗 B 指出那還不夠**：

> 「當時我停工、C 不能讀實作，**不是我們不想查，是我們被規則擋住了**。
> 分工本身製造了一個沒有人有權查證的區域，而風險宣告正好落在那裡。
> ⇒ 它需要的不是『我更小心』，是**把『誰有權查證這一類主張』寫成協定的一部分**。」

**規則**：

1. **A 對「實作現狀」的任何主張，若會變成驗收條件或工作項，B 有權（也有責任）複驗。**
2. 🔑 **B 停工期間仍然可以被要求做唯讀查證，那不算開工。**
   停工是為了讓量測的樹不動；**讀檔不會讓樹動**。
3. **B 複驗不同意時，以 B 的查證為準**——它是唯一能讀實作又不受 C 那道限制的人。
4. ⚠️ **C 不要因為「A 說了」就把某個實作行為寫進測試的預期值**；
   §3 沒寫的，回頭問 A 要查證輸出。

### 📌 附帶：待命不等於零責任（B 自己講的）

> 「我把『等派工』執行得很乾淨，**乾淨到你漏派的那一件就這樣躺了一整輪**。」

⇒ **進入待命前，回頭掃一次那一輪的裁決裡有沒有「寫到我、卻沒變成工作項」的句子。**
A 會漏派；**而最不可能發現 A 漏派的時刻，正是被漏派的人已經停工的時候。**


### 📌 底傑掃描要**對照 STATE 而不是對照對話**（視窗 B 提）

進待命前回頭掃那一輪的裁決時，**用 `grep` 掃 `STATE.md` 裡寫到自己的句子**，不要靠回想對話：

```bash
grep -n "請 B\|B 要\|派給 B\|由 B\|B 補\|B 加\|B 動" docs/windows/STATE.md
```

> **訊息會漏細節，而漏掉的那一格正好不會出現在我腦中的清單裡
> ——我不會去找一個我不知道存在的句子。**（B）

⚠️ 2026-09-21 實際掃出兩件，而且成因不同：
- 一件是**執行到一半**（做了接縫、沒做確認）——**這種最難發現，因為那一項看起來已經完成了**
- 一件是**派工管道不一致**（訊息裡沒寫、STATE 裡有寫，對方只讀了訊息）

---

## §6 · 停機條件

**一輪產出的待決事項多於解決的，就是負產出；連續兩輪負產出就停下來找 A。**

迴圈的目標函數是「**可出貨**」，不是「正確性」。每一輪都成功、而整體離出貨愈來愈遠，
問題不在任何一輪裡，在派工的方向上。

---

## §7 · 兩條端到端細線

這一期只做兩條線，**每一輪的產出都必須站在某一步上，站不上去的不做**。
細線的定義與逐步驗收條件見 [`SELLABLE-AND-MOBILE-SPEC.md`](SELLABLE-AND-MOBILE-SPEC.md)。

- **細線 1 — 授權底座**：能把這套系統賣給第二家公司並裝起來。
- **細線 2 — 手機專用頁面**：現場人員用手機完成一筆真實工作並回到桌面版看得到。

**線 1 先做完再做線 2**（只有一個寫手，平行只會製造交接成本）。
線 1 進行中，C 在等待時做線 2 的驗收測試骨架 —— 不寫產品碼。
