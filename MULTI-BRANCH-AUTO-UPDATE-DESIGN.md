# 多分公司架構＋自動核版更新（遊戲式更新）— 架構規劃書

> 狀態：**規劃中，尚未施作、尚未列入排程**（2026-08-31 與使用者討論定案方向，僅供之後真的要做分公司擴充時參考）。本文件記錄的是「該怎麼做」的設計，不是「已經做了」的紀錄——動工前務必先回來重讀一次，因為到時候 `apply_update.ps1`／§15 流程本身可能已經有變動。

## 0. 背景與決策摘要

使用者提出「後續系統可能架設到分公司」，討論分兩層：

1. **分公司連線架構**：集中式（所有分公司 VPN 連回總公司單一正式機）vs. 分散式（每個分公司自己跑一台，各自 SQLite）。已建議**集中式＋site-to-site VPN**（例如 WireGuard，把各分公司區網接進總公司區網當成同一個虛擬網段），因為現有架構是單機 SQLite（WAL 模式，適合 LAN 內多人讀寫，沒有多主複製機制），分散式代表要另外解決「各分公司資料怎麼彙總」這個目前完全不存在的問題，工程量大很多。
2. **遊戲式自動更新**：使用者接著問「地端開啟或時間到自動跟伺服器核對版本，版本不符就自動下載安裝」。這個問題本身**隱含了分散式（至少是每個分公司有自己在跑的本機服務）的前提**——純集中式架構下所有人直接連同一台伺服器，沒有「地端」這個概念可言，也就沒有多節點更新分發的問題（只剩一台正式機，套用§15既有流程就夠）。**這是本文件動工前第一個要跟使用者重新確認的分岔點**：如果最終走純集中式，本文件多數內容不適用，直接跳過；如果分公司端仍會各自跑一份本機服務（例如考慮到分公司斷網時仍要能繼續作業），才需要下面這套機制。

| 決策點 | 目前傾向 | 備註 |
|---|---|---|
| 分公司連線模式 | 集中式＋VPN（單一正式機） | 若分公司要求斷網仍可運作，才會走分散式，屆時本文件才有意義 |
| 更新檢查/下載 | **自動**（比照遊戲更新體驗） | 沿用既有 `heartbeat_job.py` 輪詢模式，不 import app、獨立執行 |
| 更新套用（apply） | **不完全無人值守** | 保留排程窗口（比照每日 02:00 備份）或人工遠端核准兩種選項，不做「偵測到不符就立刻在營業中套用」 |
| 套用機制本體 | **完全重用** `apply_update.ps1` | 現有 db 快照／migration 乾跑／程式碼快照／健康檢查／自動回滾全部照用，新增的只有「誰、何時觸發」這一層 |
| 新版本發佈到分公司頻道 | **人工核准（canary）**，不是打包完自動推送 | 見 §4 風險與緩解 |

---

## 1. 為什麼不能直接套用遊戲更新的無人值守模式

`apply_update.ps1` 目前刻意設計成半自動（腳本本身文件字串：「觸發方式是半自動——一鍵執行，但套用前仍需操作者手動確認一次，不做無人值守全自動」），這不是偷懶沒做完，是有意的安全閘門。原因：

- **健康檢查抓不到邏輯 bug**：`GET /api/ping` 有回應、`logs/server.log` 沒有 traceback，只代表服務「activate 起來了」，不代表邏輯正確。本次 session 實際發生過的例子——`dashboard.py::dashboard_monthly()` 用裸的 `deal_tag` 查詢漏算舊格式報價單——伺服器完全正常運作、健康檢查一定會過，但金額算錯了。這類 bug 自動回滾機制完全抓不到。
- **遊戲更新壞了頂多重開**，這裡壞了是「悄悄算錯錢／悄悄漏資料」，而且如果無人值守推播到**所有分公司同時**、又剛好在營業時間，會變成同時間全部分公司的帳都不準，比單一分公司出包難處理得多。

所以遊戲更新模式裡「自動核對＋自動下載」這兩步驟可以照搬，但「自動套用」這一步要多包一層閘門。

---

## 2. 元件設計

### 2.1 中央版本發佈端點（新增，掛在總公司正式機上）

新增 `routers/update_distribution.py`（比照現有 router 慣例掛進 `main.py`），提供：

| 端點 | 權限 | 用途 |
|---|---|---|
| `GET /api/update-channel/latest` | 需帶分公司專用 API key（非一般使用者登入） | 回傳目前**已發佈**版本的 `{commit, version, packageUrl, sha256}` |
| `GET /api/update-channel/package/{commit}.zip` | 同上 | 下載對應的部署包（`build_deploy_package.ps1` 產物） |
| `POST /api/update-channel/publish` | superadmin only | **人工核准**：把某個已在總公司跑穩的 commit 標記為「可發佈給分公司」，寫入上面 `latest` 回傳的內容 |

`publish` 這個動作刻意獨立於 `build_deploy_package.ps1` 之外、且需要 superadmin 手動觸發——不是打包完就自動變成分公司會抓到的版本，讓總公司自己先當「金絲雀」（canary）跑一段時間再決定要不要推廣出去（見 §4）。

### 2.2 分公司端檢查/下載代理（新增，獨立腳本）

新增 `backend/tools/update_checker.py`，比照現有 `heartbeat_job.py` 的模式——獨立執行、不 import `main.py`，這樣即使 ERP 服務本身掛了，這支腳本照樣能跑（也照樣能在服務掛掉時把「服務掛了」這件事透過既有 email 通知機制回報）。

```
每次執行（由 Windows 排程觸發，見 2.3）：
  1. 讀本機 backend/.deployed_commit.json（§15.3 既有機制，已經存在）
  2. GET <總公司端點>/api/update-channel/latest（走 VPN）
  3. 若 commit 相同 → 結束（沒有更新）
  4. 若不同：
     a. 下載 packageUrl 對應的 zip 到 deploy_packages/incoming/
     b. 驗證 sha256
     c. 寫一個 staging marker（例如 deploy_packages/incoming/READY.json，記 commit/下載時間）
     d. （可選）寄信通知 superadmin：「分公司 X 偵測到新版本，已下載待套用」
  5. 不執行任何 apply 動作——套用交給 2.3
```

觸發時機兩種都保留，不互斥：
- 服務啟動時跑一次（`autostart.bat` 在拉起 uvicorn 之前先呼叫一次，最多耽誤幾秒）
- 排程比照 Heartbeat（新增 Windows 工作排程 `MOTRIX ERP Update Check`，例如每 4 小時一次）

### 2.3 排程套用窗口（新增，薄薄一層包在既有 `apply_update.ps1` 外面）

新增 `backend/tools/scheduled_apply.ps1`：

```
每日固定時間觸發（建議接在既有 02:00 每日備份之後，例如 02:30，
  這樣「備份先做完」跟「套用前的 db 快照」時間點不會打架）：
  1. 檢查 deploy_packages/incoming/READY.json 是否存在且尚未套用過
  2. 若無 → 結束
  3. 若有 → 呼叫既有 apply_update.ps1 -PackagePath <staging路徑> -Yes
     （-Yes 這個既有旗標本來就存在，文件字串寫「僅供自動化測試，正常
      人工執行不要加」——這裡是刻意、經過設計討論的例外用法，不是
      繞過安全機制，因為前面 db 快照/migration 乾跑/健康檢查/自動回滾
      這整套安全機制完全沒有被跳過，只是把「誰按確認鍵」從人工改成
      這支排程腳本）
  4. 套用完不論成功/失敗都寄信通知 superadmin（成功：新版本號；
     失敗：已自動回滾＋快照路徑，等同現有 apply_update.ps1 失敗時
     人工執行會看到的資訊）
  5. 刪除/歸檔 READY.json，避免重複套用
```

這一步刻意**不新增任何套用邏輯**——完全重用 `apply_update.ps1` 既有的 db 快照／migration 乾跑驗證／程式碼回滾快照／停服／健康檢查／自動回滾，新寫的程式碼只有「幾點、要不要套用」這一層薄的排程判斷。

---

## 3. 重用 vs. 新增總覽

**完全重用（零修改）：**
- `apply_update.ps1` 全部既有邏輯（db 快照、migration 乾跑、程式碼快照、停服、健康檢查、自動回滾）
- `backend/.deployed_commit.json` 既有版本追蹤機制
- `helpers/email_notify.py` 既有通知基礎設施
- `heartbeat_job.py` 的「獨立腳本、不 import app、排程觸發」設計模式

**新增：**
- `routers/update_distribution.py`（總公司端，3 個端點）
- `backend/tools/update_checker.py`（分公司端，檢查+下載，不套用）
- `backend/tools/scheduled_apply.ps1`（分公司端，排程窗口內呼叫既有 `apply_update.ps1 -Yes`）
- 2 個新 Windows 排程工作（`MOTRIX ERP Update Check`／`MOTRIX ERP Scheduled Apply`），比照現有 §1.1／§1.2 三個排程工作的建置方式
- 分公司對總公司端點的認證機制（API key 或憑證，細節未定，見 §5 待決事項）

---

## 4. 風險與緩解

- **健康檢查抓不到的隱藏邏輯 bug（見 §1）**：緩解方式是把「發佈」（§2.1 的 `publish` 動作）跟「打包」分開——新版本先在總公司正式機用既有 §15 人工流程套用、實際跑幾天確認沒問題，才由 superadmin 手動呼叫 `publish` 讓分公司排程檢查得到。總公司本身就是自然的金絲雀（canary），不需要另外建置測試環境。
- **VPN 斷線時檢查失敗**：`update_checker.py` 這類網路失敗直接視同一般輪詢失敗（跟 `heartbeat_job.py` 現有的失敗處理邏輯一致），下次排程重試即可，不需要特殊處理。
- **多分公司同時套用同一個壞版本**：因為 §2.1 的「發佈」是總公司先自己驗證過才手動核准，理論上能大幅降低這個風險發生的機率；但如果還是發生了，需要有「緊急撤回」手段——`publish` 端點應該同時支援把 `latest` 指回上一個 commit（分公司下次檢查時會看到「版本不符」，套用邏輯本身不分方向，一樣呼叫 `apply_update.ps1` 套用回舊版包，靠既有的套用/回滾機制處理，不需要另外寫「降版」的特殊邏輯）。
- **排程窗口太長沒被觸發**（例如分公司主機在 02:30 剛好離線/重開機中）：比照現有 Heartbeat 排程「無綁定登入、每 5 分鐘」的做法，`scheduled_apply.ps1` 建議也用「每天固定時間 + 錯過視同下次窗口再檢查」而非嚴格單次觸發，避免主機臨時重開造成整天都沒套用到。

---

## 5. 待決事項（下次要動工前，先跟使用者確認）

1. 最終真的走分散式（分公司各自跑本機服務）還是純集中式？如果是純集中式，本文件大部分內容可以整份跳過，只需要考慮 VPN 架構本身。
2. 分公司對總公司 `update_distribution` 端點的認證方式——固定 API key（簡單，但外洩風險要另外處理）還是憑證/mTLS（更安全，建置複雜度較高）？
3. 排程套用窗口的確切時間，要不要可設定（例如允許各分公司自訂避開自己的營業時間）？
4. 分公司之間的 SQLite 資料是否需要彙總回總公司（跨分公司報表）？如果需要，這是完全獨立於「更新機制」之外的另一個大工程（資料同步/複製），本文件不涵蓋，需要另外立案討論。

---

## 6. 與現有文件的關係

- 執行面細節（正式機/開發機路徑、既有排程工作清單、`apply_update.ps1` 現有行為）以 `MOTRIX-ERP-QUICK.md` §1.1／§1.2／§15 為準，本文件只補充「多分公司」情境下要新增的部分，不重複抄一份。
- 真的要動工時，這份文件的角色跟 `NETWORK-PLAN-MODULE-DESIGN.md` 一樣——先讀本文件掌握決策脈絡，再開始寫程式；完工後應該把「已完成」的部分搬進 `MOTRIX-ERP-QUICK.md` 正式章節，本文件則保留當歷史規劃紀錄。
