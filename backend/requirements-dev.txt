# 開發/測試專用相依，正式機執行 ERP 服務不需要——不要加進 requirements.txt。
#
# playwright：backend/tests/test_e2e_playwright_2026_09_07.py 用的真實瀏覽器
# 端對端測試。裝好套件後還需要額外抓瀏覽器執行檔（不在 pip 套件裡）：
#   pip install -r requirements-dev.txt
#   playwright install chromium
# 沒裝的環境執行 `pytest tests/` 時，該測試檔會自動 skip（pytest.importorskip），
# 不影響其餘測試或 build_deploy_package.ps1 既有的「先跑 pytest 再打包」流程。
playwright>=1.40.0
