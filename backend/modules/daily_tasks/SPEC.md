# M12 每日任務 · 規格條件

> 格式同 `modules/tender_radar/SPEC.md`（MODULE-GUIDE §5）；`backend/tests/test_spec_coverage_2026_09_21.py` 讀 `STATE.md` 加上各模組的 `SPEC.md`。

## 規格條件

本模組沒有專屬編號。依據：2026-09-26 在拿掉 `modules/daily_tasks` 與 `modules/netplan` 的樹上跑 `test_spec_coverage_2026_09_21.py`：15 passed
（STATE.md 裡沒有只由本模組測試命名的編號）。之後新增本模組的規格條件，寫在這一節。
