# M08 營運分析 · 規格條件

> MODULE-GUIDE §5。**拿掉本模組時，本檔與本模組的測試一起消失**；`test_spec_coverage` 讀 `STATE.md` 加上各模組的 `SPEC.md`。
> ⚠ 2026-09-26 搬遷時**規格編號尚未自 STATE.md 分出**：營運報表相關的編號仍在 `docs/windows/STATE.md`（待辦，見本模組 CHANGELOG 與 ROADMAP）。
> 分出時比照 `modules/tender_radar/SPEC.md` 的做法：只有本模組測試用它命名的編號才搬，撞號的只搬報表那一行。

## 規格條件

（尚未分出）

## 範圍

（尚未分出）

## 登記

（尚未分出）
