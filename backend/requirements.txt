# Supported Python: >= 3.11 (lower bound only, no upper bound -- user ruling 2026-09-25:
# do not tie the system to one Python version). Tested on 3.11, 3.12, 3.13.
# Keep the same number in tools/platform/project_env.py PYTHON_MIN.
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.0.0
aiofiles>=23.0.0
pyotp>=2.9.0
qrcode>=7.4.0
boto3>=1.34.0
openpyxl>=3.1.0
Pillow>=10.0.0
webauthn>=2.1.0
# 2026-09-11: imported directly by routers/daily_tasks.py::_read_serving_cert
# (certificate expiry check). It used to be only a transitive dep of webauthn;
# now that we use it ourselves it is declared explicitly.
#
# NOTE: this file must stay pure ASCII (2026-09-22, HC2a).
# pip_audit's parser reads it with the system locale encoding (cp932 here),
# so a single CJK comment makes it raise UnicodeDecodeError -- which meant
# dependency vulnerabilities had never been scanned at all. The failure was
# silent: the tool refused to start, and nobody was waiting for its output.
cryptography>=42.0.0

# 2026-09-23 (JV5): merging PDF attachments into the exported voucher.
# Edge headless can print HTML to PDF but cannot merge existing PDFs.
# pypdf is pure Python (py3-none-any wheel); on Python >= 3.11 it has
# zero transitive dependencies. Range, not a pin -- see the version
# adaptation rule.
pypdf>=6.0.0
