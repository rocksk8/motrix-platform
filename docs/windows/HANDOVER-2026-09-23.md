# 交件：使用者離線那一段（2026-09-23）

> 🔴 **這份檔案存在的理由**：使用者離線前逐字說「**全數都做完，我要離開電腦，由你們安排測試跟決策**」，
> 並要求回來時交**兩份**。而那句授權原本**只存在於對話裡** —— 已落檔在 `STATE.md §339`。
> 📌 這份檔案**在他回來之前就建立**，理由是 `STATE.md §343`：
> ☠️ **只存在於主持人佇列裡的東西，在清單上與「不存在」長得一樣。**

⚠️ **狀態：編寫中。** 每一節底下標了「已定稿 / 待補」。

---

## 這一包有五份文件，照這個順序讀

```
① 本文件（HANDOVER）              讀約 30 分
   回答：做完了什麼，以及**四件不修就不能出貨的**
   要您做：先看 §0a／§0b（約 10 分），那四件都要您裁

② TEST-CONFIDENCE.md              讀約 8 分
   回答：這次的測試結果，以及**它能保證到哪裡**
   要您做：不必做任何事，讀完再看 ③④

③ KNOWN-GAPS.md                   讀約 15 分
   回答：我們知道自己沒查的是什麼（**21 條，只有您查得到**）
   要您做：在正式機上查那 11 條，其餘 10 條是回答問題

④ PENDING-RULINGS.md              讀約 25 分
   回答：**18 條**待您裁示，而每一條都已經有一個決定
   要您做：**只回不同意的那幾條；全部不回也可以**

⑤ TEST-BASELINE-2026-09-23.md     **不必讀**
   回答：② 那些數字的原始紀錄（哪一支題、跑多久、在哪個版本）
   要您做：只有在您想查證 ② 的某個數字時才打開
```

### 🔑 而順序有依賴，不是由短到長排的
```
① 先 => 四件出貨阻擋決定「**能不能出**」，其餘四份都在講「出了之後怎樣」
② 再 => 沒有它，您會把 ③ 的 21 條當成「還有 21 個 bug」；
        而它們是**沒有人去問過的問題**，不是已知的缺陷
③ 在 ④ 之前 => ③ 是**事實**（正式機現在是什麼狀態），
        而有幾條答完會讓 ④ 的選項變少 —— 反過來讀會多裁一次
⑤ 最後 => 它是**證據**，不是結論
```
> ### ☠️ 而若不給順序，最可能發生的是**從最長的那一份開始讀** ——
> ### 而那是 `PENDING`，**最需要前面三份當背景的一份**。

### ⚙️ 而這張表裡哪些數字是量的、哪些是估的
```
字數／行數      ✅ **量的**（wc）
21 ／ 18 ／ 4   ✅ **量的**，而且逐條印出來看過
讀約 N 分       ⚠️ **估的**（字數 ÷ 250 字/分）
「做多久」      ❌ **答不出來** —— 那取決於您的環境，我們不猜
```

---

## 0. 先講三件他回來會馬上想知道的

```
① 正式機**沒有被碰過**。沒有打包、沒有部署。（§145：A 只做到匯出升級檔為止）
② 沒有任何人輸入過密碼。666／6667 沒有被砍。
③ 🔴 每一個**原本要問他**的裁定，規格裡都標了 `⏳ 待使用者回來確認`，
   並寫明：**我裁了什麼 ／ 依據 ／ 他選另一個要改哪裡**
   => 他不必把整段重看一遍，只要找那個標記
```

---

## 0a. 🔴🔴🔴 **最大的一件：出貨包裡有我們的整份內部工程文件**

```
build_deploy_package.ps1:670  git archive --format=tar -o $tarPath $commit
=> 匯出**整個 repo 已追蹤且已 commit 的內容**，沒有 .gitattributes / export-ignore
```
### ⚙️ 而這**不是推論，是打開已經產出的包看到的**
```
deploy_packages/20260922_184908_c5b1e84/docs/windows/STATE.md   ✅ 在
deploy_packages/20260922_200604_7bc1fb8/docs/windows/STATE.md   ✅ 在
兩個包都含 SCOPE.md 與 MULTIWIN-PROTOCOL.md
⚙️ 包裡的 STATE.md：**20,341 行／583,698 字元**
⚙️ 內含：「私鑰」26 次 ／ license.key 15 次 ／「客戶」71 次
```
> ### ☠️ **每一個出貨包裡，都有我們對這個系統每一個已知缺陷的完整內部討論。**

### ⚠️ 而嚴重度據實說
```
✅ **目前沒有任何外部客戶收到過** —— 這兩個包只上過正式機（公司自己的機器）
🔴 而它在**出貨那一刻**發生，而本期目標正是「把 ERP 變成可販售的產品」
🔴 且今天的 STATE.md 比包裡那一版又多了：demo 帳號密碼、公司統編、員工姓名、
   以及下面四件出貨阻擋的完整說明
```
📌 最鋒利的一句：**這份交件檔自己，也會被打包進出貨包。**

### 🔴 而 2026-09-23 晚間的進度：**第一次修它沒有修好，而守門說它好了**
```
⚙️ .gitattributes 加了 export-ignore，而 `docs/windows/ ` 與 `backend/tests/ ` 這兩條
   （**尾巴加 /**）對裡面任何檔案**都不生效** —— git check-attr 實測全部 unspecified
✅ 單檔規則（約 29 條）正常
☠️ 而我們為它寫的守門，用的是 **gitignore 式**語意 => **它不可能抓到這個問題**
```
> ### 🔑 **設定存在、守門通過、而事情沒有發生。**
⇒ 已派修（改 `**` 寫法 ＋ 守門改成呼叫 `git check-attr` 問 git 本人）。

### 🔑 為什麼四個視窗盤點了一整天都沒碰到它
> 我們問的是「**我們把公司資料寫到哪裡**」——
> ☠️ 而答案是「**我們沒有寫。是出貨機制把整個 repo 帶出去了。**」

⇒ ⏳ 待你裁：我裁**加 `.gitattributes` 的 `export-ignore`**，
  而**驗收要打開產出的包去看**（釘「包裡不存在 `STATE.md`」，不要釘「設定檔裡有那一行」）。

---

## 0b. 🔴🔴 **四件出貨阻擋 —— 這是這一段最重要的發現**

> 全部在「把 ERP 變成可販售的產品」這條線上，而**四件都不是有人去找才找到的**：
> 都是在做別的事情的路上撞到的。🔑 **那本身就是一個訊號。**

### ① `IA1`｜出廠管理員的身分寫死在產品碼裡
```python
# backend/helpers/startup.py  init_default_admin()   ← **每次啟動都跑，不是 migration**
INSERT INTO users (...) VALUES ('jeff', '黃玉龍', 'superadmin', 'jeff@miactw.com', ...)
```
🔴 而**不只是預設值** —— `'jeff'` 寫在**存取控制邏輯**裡：
```
routers/auth.py:1548  delete_user()        username=='jeff' => 400「不可刪除超級管理員帳號」
routers/auth.py:1582  toggle_user_active() username=='jeff' => 400「不可停用」
frontend/pages/users.html ×5                配合隱藏刪除/停用按鈕
```
> ### ☠️ **客戶的系統裡會有一個永遠刪不掉、停不掉、名字叫 `jeff` 的帳號。**
```
⚠️ 客戶改成自己的名字之後**不會**被改回去（兩句 UPDATE 都有 WHERE 守衛）
   而**把顯示名或 email 清空的話會被填回「黃玉龍」／jeff@miactw.com**
✅ 安裝流程本身是想過的（隨機臨時密碼、憑證檔、must_change_password=1）=> 只要改身分三個值
```

### ② `IA2`｜一個客戶不知道的 superadmin，密碼是一個公開數字
```python
# backend/helpers/startup.py  init_demo_account()   ← main.py:528 **無條件呼叫，沒有開關**
VALUES ('demo', ?, '展示帳號', 'superadmin', ?, 1, ?, 0)   # must_change_password = 0
_hash_pw("60575481")                                       # 密碼 = **公司統一編號**
logger.info("已建立展示帳號（demo），密碼 60575481")         # 🔴 明文寫進 logs/server.log
```
```
⚙️ 三件都查過，三件都空：① 有開關嗎 => 沒有 ② 登入頁看得到嗎 => 看不到 ③ 授權底座處理了嗎 => 沒有
🔑 而那個密碼**印在客戶自己匯出的 PDF 頁尾上**
```
> ### ✅ **資料隔離是真的**（demo 登入後路由到隔離 DB）⇒ ❌ 不是「客戶資料會外洩」
> ### ✅ 是「**客戶的系統裡有一個他不知道的 superadmin，而它的密碼是一個公開數字**」
> ### 🔑 **真正的問題不是它能做什麼，是它不該在那裡。**

### ③ `WL7`｜兩份報表印我們的公司名與統編，而它們**完全不讀 `company_profile`**
```
backend/routers/reports.py           => 營運報表 Excel／PDF、銷項發票清單
backend/network_plan_export.py       => 網路架構規劃書 Excel／PDF
⚙️ grep -c company_profile 兩支都是 **0**（實跑過）
```
> ### ☠️ **客戶不管怎麼改公司設定，這兩份報表永遠印允碩的公司名與統編。**
⚠️ 而它**不是**「尚未設定公司抬頭」那個 bug（`JV9`，那是有設定而讀不到）——
  這一支是**根本沒有去讀**。
⚠️ 而**只換那兩個常數不夠**：`network_plan_export.py:380/:481` 把公司名／email／電話／統編
  **直接內嵌在 f-string 裡**。

### ④ `JV27`｜一支重複實作讓一個安全設計**不可達**
```
routers/vouchers.py:153 _appr_of()  <= 獨立於 helpers/voucher.py _chain_tiers() 的第二份解析
它在 read_voucher() 裡搶在 signatures_of() 之前 raise HTTPException(400)
=> approval_json 壞掉時，整張傳票**連分錄都讀不到**
```
🔑 而 `signatures_of()` 的 docstring 寫著「**版面壞掉不該讓整張單讀不出來**」——
  ☠️ **那個設計今天不可達，而沒有任何東西會說。**
⚠️ 它目前用 `xfail(strict=True)` 追蹤 ⇒ **缺陷存在時測試是「通過」** ⇒ 打包關門會說綠。

---

### ⏳ 這四件都要你裁，而規格裡各有一節寫了「我裁了什麼／依據／你選另一個要改哪裡」
📌 相鄰的 `MG5`（`db.py` `_m008` 也寫死同一組資料）**留在 `NEXT`**：
  它是一次性 migration，且**全新安裝時三句 UPDATE 都是 0 列**（有 code 證據）。

---

## 1. 第一份：**做完了什麼**

### 1a. A 的版本 —— **有偏誤的那一份**（從裁示與交件回報編的）
```
B（產品碼）  EM3 ／ QS1-a ／ BN14 ／ BN17 ／ BN18 ／ BN16 ／
             PK1（.gitattributes ＋ verify_package 不變量）／ WL7 ①② ／
             version_manifest 精簡 ／ backend/tools 分類落地 ／ docs/reference 排除
C（只寫題）  QL26 ／ AT1 ／ EM5 ／ BN17 題 ／ BN15 題 ／ JV20 誘餌 ／ e2e 選擇器 ／
             隔離修復 ／ test_ac1 判準 ／ JV25 題 ／ _export_ignore_state 四題 ／
             EM3 舊檔刪除 ／ BN17 舊檔改名
A-2（規格）  SPEC-QS1-a／BN17／EM7／EM13／MG3／BN18／IA1／WL7 ／
             AUDIT-SPEC-COVERAGE ／ PENDING-RULINGS ／ KNOWN-GAPS ／ TEST-CONFIDENCE ／
             refcheck 工具 ／ 協定 §6b–§6g
D（唯讀）    28 筆三欄表 ／ 五件抽驗 ／ 隔離掃描 ／ 測試母體對齊 ／ 公司資料盤點 ／
             PK1 排除清單 ／ tools 28 檔分類 ／ export-ignore 驗證 ／ exit code 驗證 ／
             證據強弱表
A（派工）    STATE §334–§362 ／ 12 個新編號 ／ 本文件 ／ TEST-BASELINE
```
> ### ⚠️ **這一份是有偏誤的**：它記的是「我以為交了什麼」。
> ### 🔑 而今天已經證明過那份記憶會與事實脫節（`STATE.md §343`）——
> ### **所以下面那一份才是主要的。**

### 1b. D 的版本 —— **從 585 個 commit 獨立編的**
```
母體：今天（09-23）**585 個 commit**，主旨行提到的編號共 **142** 個
扣除：L2（不是編號，是「階層第二層」）／RP4（排程狀態，不是證據強度）
=> **母體 140**
```
```
① live 突變        **18**
② 資料建構          **7**
③ 僅結構複讀         0
④ 只是綠的         **114**
⑤ 沒有題             0
JV22（混合）         1   <= 四個內部檢查點裡 3 個只結構複讀、1 個有真突變
=> 18 + 7 + 114 + 1 = **140** ✅
```
> ### ☠️ **有具體證據（①或②）的是 25 / 140 —— 不到兩成。**

#### ① live 突變（18 項，附證據原句）
```
PK1   取出被刪掉前的 _pattern_covers() 跑在同一組誘餌上，兩個都回 True
      ——與現在斷言方向相反，證明對舊實作真的會紅（bd4319c）
AL1   補完之後 A4 殺得掉（突變後那一題紅，其餘 13 綠）（1dcc8b3）
BN1   7/7：P1 濾掉 ok=false KILL ／ P2 拿掉 is_active=1 KILL … P7 上界放寬 KILL（1bcccef）
EM3   不需要注入——自己正好紅在一個真缺陷上（D 親驗）
BN14  同一 db 路徑連續呼叫兩次，第二次在 _seed_group() 炸開；
      改成每次全新隔離庫後兩次都落在成員 FK 那行（24f370c）
DM1   把 DEMO_FILTERED_CLEARS 清空（整張保留）=> 被抓到（163cb11）
MD1   突變 27/27，全部紅在自己的斷言上（7fa7f1d）
FN1   10 題全綠（突變 13/13）（d8ce924）
BN15  5/5 ＋ 突變自檢 3/5 核心行為正確變紅（00d6b22）
BI1   6/6：M1 created_by 寫空字串 KILL … M6 空人員改成靜默放行 KILL（74c47f0）
AT1   monkeypatch _tagged_file_entries() 回空清單，①②③真的會紅，④負對照仍綠（cfd81e4）
BG1   突變 7/7（5806ef3）
EM5   各自 mutation ＋ 負對照，並繞過 _appr_of() 單獨驗 signatures_of()（0380051）
A4    AL1 突變套件子項　／　P6  BN1 突變套件子項（1bcccef/020f804）
JV18  D 親驗：1 failed / 6 passed，斷言訊息與注入的壞行為逐字對上，其餘 6 題沒被誤傷
JV23  D 親驗：雙向各自精準（四個方向全對）　／　BN13  同批
```

#### ② 資料建構（7 項）
```
JV3 ／ EM9 ／ JV12 ／ JV14 ／ JV19 ／ QL26 ／
QL24（紅：空據點預設展開；綠：已填的仍展開＝正對照；綠：手動收合仍有效＝反向控制）
```

#### ④ 只是綠的（114 項，只列編號）
> 🔑 **它們沒有證據，而那就是它們的內容。**
```
BN8 CA1 JV5 QS1 AC1 AS2 BN6 BN18 EM13 JV7 BN11 BN7 JV11 EM1 EM2 JV2 WL1 BN17 IA1
JV20 EM10 JV21 QL25 BN5 JV10 JV1 JV25 EM7 EM12 BN12 QN1 AS4 JV9 WD1 FN5 FN2 T100
WL7 BN16 JV24 EM11 BN2 JV8 AR1 VP6 IA2 MG3 UP1 BN10 BN3 JV16 JV17 BR1 BN9 AS1 QA3
AI1 AC2 BK31 HG1 JV27 JV26 QE1 PX1 BR4 AS3 EM4 BK33 RP3 QA2 BK32 QA1 GC9 CL1 FN3
UI10 DB1 VP8 PK2 BN19 MG5 RT1 QS1a MG4 UP3 UP2 SL1 JV15 JV13 EM8 BN4 GW1 GW2 GW3
JV4 JV6 GT2 DV2 FN7 FN6 VP7 VP3 RP2 UI8 V93 V94 V95 R8 GT1 FN4 SP1 GC9b A2 CL2
```

### 1c. 並排比對 —— **差異本身就是產出**
```
🔑 兩份的**母體不同**：A 的是「我派了什麼」（約 50 項），D 的是「commit 提到什麼」（142 個編號）
=> 兩者不是同一種清單，不能逐項對消
```
#### ✅ 而並排今天實際抓到三件
```
① `QS1a` —— A 的清單裡它是「做完的」，而 SCOPE 裡它**不存在**
   （子題繼承了母題的桶子，而做的人不會注意到）
② `WL1` 撞名 —— A 發了一個既有編號，而那是 A-2 用第二把尺算交集抓到的
③ 關鍵字初篩把 `GT1` 判成「沒有題」（它最新的 commit 就是補題那一筆），
   而把 `A2` 判成「驗過了」（那個 commit **原話寫著「沒驗到，要補」**）
```
> ### ☠️ ⇒ **「提到」不等於「做了」，而自動掃描分不出這兩者。**
> ### 🔑 而那 25 個有證據的裡，**有 5 個是靠當時寫下來的記錄才留住的** ——
> ### **證據不能事後從 commit message 重建，它必須在驗的當下被寫下來。**

## 2. 第二份：**哪些是驗過的、哪些只是現在是綠的**

### 2a. 證據強弱的分級（已定稿）
```
① live 突變     —— 真的把實作改壞過，而那一題**真的紅了**（最強）
② 資料建構      —— 用資料重現缺陷情境，斷言指向產品行為
③ 僅結構複讀    —— 兩次獨立讀原始碼，結論一致（**沒有執行過任何反例**）
④ 只是綠的      —— 它通過，而**沒有人證明過它會紅**
⑤ 🔴 沒有題     —— 它不紅，是因為**沒有東西在看它**
```
> ### ☠️ ④ 與 ⑤ 在任何一份「全綠」報告上**長得一模一樣**，而它們是兩件事。

### 2b. 🔴 已出貨而**完全沒有題**的編號（⑤）
```
盤點方法：三把尺量出 8／81／23，**兩側都驗證過是錯的** => 報**可靠下限**
下限 = 11（A-2 盤點，STATE.md §339 + docs/windows/AUDIT-SPEC-COVERAGE.md）

🔴 其中**已經出貨**的三個（最優先補題，已派 C）：
   QL26  公司基本資料的儲存按鈕
   AT1   報價單送審後看得到附件
   EM5   我們結案說「已修」
   其餘八個：P0-0 P0-1 P0-2 JV4 JV6 JV15 AS3 BI1
```
> ⚠️ **這是下限不是答案。** 一份標明「這是下限」的清單，比一個標明「這是答案」的數字有用。

### 2c. 已經做過 live 突變抽驗的（①，已定稿）
```
JV18        1 failed / 6 passed —— 斷言訊息與注入的壞行為逐字對上，其餘 6 題沒被誤傷
JV23 + BN13 雙向各自精準（四個方向全對）
EM3         **不需要注入** —— 它自己正好紅在一個真缺陷上
JV22 地基   ④有真突變；①②③**只有結構複讀**（D 自己明說證據較弱）
```
⚠️ 而 `EM3` 那個自證**有到期日**：那一處被修掉之後，這個自證就消失了。

### 2d. ✅ **跑了一次全量 —— 而結果是 30 紅**

```
HEAD **e70527e**（跑前跑後相同，`git status --porcelain` 也相同）
not_e2e   **30 failed / 2,440 passed / 53 skipped / 1 xfailed**   520 秒
e2e        **2 failed /    73 passed /  2 skipped**               387 秒
```
> ### 🔑 **這是今天唯一一次「從頭到尾沒有人動工作樹」的量測。**
📌 逐支名單與分類在 **`TEST-BASELINE-2026-09-23.md`**；
  「這些綠燈能保證到哪裡」在 **`TEST-CONFIDENCE.md`**。

#### 🔑 而這一節原本寫的是「**今天沒有跑過一次全量**」
```
那句話當時是對的，而成因不是誰疏忽：
   **沒有人被派去跑全量，而每一輪的局部回歸都是綠的**
☠️ 〈迴圈的目標函數〉：**每一輪都成功，而整體的可出貨性沒有被量過**
```
> ### ✅ **而現在它被量過了 —— 那正是這一條該有的結局。**

#### ⚠️ 而 30 紅的組成
```
**6 支** 來自 test_spec_coverage —— 成因已知：**今天新增 12 個編號而題還沒寫**
   🔑 ⚠️ **不要讀成「預期內」，它是一張欠條**
   🔴 而其中 **1 支是那支守門自己的 parser bug**（含「THIS」字樣的標題都被當成進入區塊）
**24 支** 已由 C 逐支分類：
   ⓐ 知道為什麼紅（要舉證）／ⓑ 產品缺陷（要發編號）／ⓒ 題過期／ⓓ 不認得
   ⚠️ **四類的各自支數我還沒核對**（C 的回報裡那四個數字加起來不等於 24）
   => 🔑 交件時這一格若還沒對上，**就照這樣寫，不要填一個我沒核過的數字**
**2 支** e2e —— 兩支都是**早已知道而尚未解決**的
   （admin 空狀態三件只有一件、待您裁 ／ JV25 iframe 差 5.8px）
```
> ### 📌 而 `ⓑ 產品缺陷`那一類裡，目前確認的是
> ### **`BN14` 今天新建的兩張群組表沒有登記進每日備份與 demo 重置的分類表。**
> ### ✅ 而**既有的六張獎金表一直都在備份裡** —— 一度回報「獎金資料整個不在備份範圍」是錯的
> ### （那次 grep 的是一個**不存在的路徑**）。

#### ⚠️ 而兩件關於「數字怎麼讀」的，仍然成立
```
① **同一份報告裡的幾個數字，必須出自同一次量測並釘 HEAD**
   ⚙️ 實例：同一支 `--collect-only` 幾分鐘內量到 **2,569** 與 **2,575**
      => 不是 pytest 不穩定，是**工作樹是共用的，母體在兩次量測之間變大了**
② 🔴 而更早的基準 **1,440 passed / 53 skipped**（09-22 03:33）**沒有原始紀錄**
   那個包已被 `KeepPackages = 2` 輪替掉（您 2026-09-15 要求的行為，不是 bug），
   而 `build_history.jsonl` 是那之後才加的
   => 🔑 **那個數字現在只存在於 A 的記憶裡，沒有任何東西可以核對它。**
```

### 2e. 隔離掃描（已定稿）
```
問題：有幾支測試繞過隔離 fixture、直接打真實共用開發庫？
答案：**一支，不是一類**（已修）。全母體 2569 掃過，第二個 = 0
```
🔑 而它是 B 做別的事時**撞到自己上一輪的殘留列**才發現的 ——
  ☠️ 那一輪若沒撞到，這個洞今天不會被任何人看見。

---

## 3. 🔴 要您裁的 —— 在 `PENDING-RULINGS.md`（**18 條**）

```
① 出貨阻擋 5 ／ ② 資料與遷移 4 ／ ③ 獎金 6 ／ ④ 檔案與紀錄 3
```
```
✅ 每一條都有三行：**我裁了什麼 ／ 依據 ／ 您選另一個要改哪裡**
✅ **全部不回也可以** —— 那表示您同意目前的決定
📌 而另有 **10 條**是純工程權衡，我們自己裁了（同樣寫了三行，在各自規格裡）
   判準：**這個決定需要知道您的業務怎麼運作，或要您承擔商業後果嗎？**
```

## 4. ⚠️ 我們自己知道的弱點（不等他問）
```
① 🔴 「全綠」不代表「驗過」—— 見第 2 節。這一段新增的題有一部分是**寫完就綠**的
② 🔴 今天有**兩次**守門誤報，而 B 最省力的反應是**改寫註解去迎合尺**（已恢復並改尺）
   ☠️ => 「守門會被維護」這個前提，今天的證據是反的
③ ⚠️ 規格覆蓋率的四類切分，對「用對照表登記」的那些一律判錯
   （表裡只有 UI8 一列，**而表外可能還有**）
④ ⚠️ D 的掃描器有過三個版本，前兩版都是假陽性（74 => 3 => 0）
   🔑 若只跑第一版就交件，會派人去改 74 支**本來就對的**測試
```
