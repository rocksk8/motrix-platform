# `AUDIT-SPEC-COVERAGE` · `SCOPE.md THIS` 的規格覆蓋率盤點

> 座標：`c8ae2a7`
> 作者：視窗 A-2 ／ 2026-09-23
> 委託：A（「先給清單，我排順序 —— **不要順手補齊**」）

---

## §0 🔴 先更正委託裡的一個前提

> A 寫：「我懷疑 **`SPEC-BN17` 沒有寫**（我排它第 2，而你回報時是從第 5 開始）」

```
⚙️ `docs/windows/SPEC-BN17.md` **存在**（commit `5f4ff1b`，`eb953d3` 修過節次順序）
🔑 而 A 的推論方式是對的（從我的回報順序推），**只是那個前提錯了** ——
   我當時的回報**先講了 EM7／EM13**，因為 BN17 是上一輪交的
📌 ⇒ 〈你的輸出是別人的輸入〉：**我的回報順序讓他推出一個錯的結論。**
```

---

## §1 母體：**180 個編號**

```
⚙️ 來源：`SCOPE.md` 的 `## ✅ THIS` 區塊（它已經是彙總表，
   後面的追加區塊是歷史紀錄且已合併進來 —— 抽驗 `AS`／`JV8` 一致）
⚙️ 自檢一：各族宣告數相加 = **180**；實際列出的編號數 = **180** ✅
⚙️ 自檢二：區塊內**沒有被解析到的行 = 0**
```

### ☠️ 而第一把尺漏掉一整行，**而加總自檢說「無不符」**

```
我的正則 `^([A-Z]+)\((\d+)\):` 要求族名**全大寫字母**
=> `P0(4): P0-00 P0-0 P0-1 P0-2` **不匹配** => 那一行整個沒進計算
🔴 而加總自檢回報「宣告 vs 列出：無不符」—— **因為兩邊都少了同樣的 4 個**
```

> ### 🔑 **加總自檢只驗它看得到的東西。**
> ### ⇒ 所以要配一個「**有幾行沒被解析到**」的計數 —— 那一格才抓得到整行漏掉。

---

## §2 四類（A 指定的分法）

```
① 有獨立規格檔                = **39**
② 沒有獨立檔，內容在別份規格裡 = **20**
③ 無規格，而測試裡有它的題     = **98**   ⚠️ 見 §3：這個數字不可靠
④ 無規格也無題                = **23**   ⚠️ 同上
⚙️ 加總 39 + 20 + 98 + 23 = **180** ✅
```

---

## §3 🔴🔴 而 ③／④ 的切分**我量不準** —— 三把尺量出三個答案

| 尺 | 判準 | ④ 的數量 |
|---|---|---|
| v1 | 測試檔裡**出現過**這個編號 | **8** |
| v2 | 測試檔名含它，或 `# ID ·` 開頭的題目標題 | **81** |
| v3 | v2 ＋ **任何 `#` 註解行裡出現它** | **23** |

### ☠️ 兩側都驗證過是錯的

```
v2 太窄（假陰性）⚙️ 抽驗被判成 ④ 的：
   `# GB2 / GB3 · 計數點要對得上帳單`     <= **合併標題**，一題涵蓋兩個編號
   `# GC1 / GC2 / GC3 · …`
   `# §53 · \`RP1\`–\`RP3\`：…`            <= **範圍形式**
   => 🔴 它們**都有題**，而 v2 說沒有
   ☠️ 我差點報「81 個沒有題」—— 而那會讓 A 去排 58 件不存在的工作

v3 太寬（假陽性）⚙️ 抽驗被判成 ③ 的：
   `JV2` -> `test_bonus_award_approval_2026_09_23.py`
   而那個檔案是**在引用 JV2 的行為**（「`JV2` 就是這麼壞的：…」），
   **不是 JV2 自己的題**
   `GC9b`／`VR7`／`VR8`／`VR10` -> `test_spec_coverage_2026_09_21.py`
   => 那是**覆蓋率測試**，不是它們各自的題
```

> ### 🔑 **我在兩個方向之間震盪，而每一次都有「看起來正確」的理由。**
> ### 📌 〈判準的寬窄都會騙人〉：**太寬給你綠燈，太窄讓你去否定別人。**

### ✅ ⇒ 不追求一把完美的尺，**報一個可靠的下限**

```
🔑 v1 是**最寬**的尺 => 它判為 ④ 的，其他尺一定也判為 ④
⇒ **三把尺的交集 = v1 的 8 個** = 一個**可靠的下限**
⚙️ 已驗證 8 ⊂ 23（v3 的 ④ 含 v1 的全部 8 個）
```

---

## §4 ✅ **④ 的可靠下限 = 8 個**（連在測試檔裡被提到都沒有）

```
QL26   P0-1   P0-2   JV4   JV6   AT1   EM5   BI1
```

> ### ⇒ 這 8 個是「**沒有規格、沒有題、而且沒有任何測試提到它**」。
> ### 🔑 **這是我唯一有把握的一份清單。**

### 🟡 而 v3 多出的 15 個要**人看一眼**

```
VR6  BK8  BK24  BK26  BK29  GB14  GB18  GB19  GB21  GC4
P0-00  P0-0  UI8  JV15  AS3
```
```
它們的狀態是「**有被提到，而沒有一個以它為標題的題**」
⇒ 可能是：ⓐ 被合併進某一題而標題沒列它
         ⓑ 只在說明裡被引用，實際沒有題
🔴 而 ⓐ 與 ⓑ **在我的尺上長得一樣** => 這 15 個要打開看
⚠️ 而**我沒有打開看** —— A 交代「先給清單」，而逐一打開是另一份工作
```

---

## §5 ⚠️ ③ 這一類 A 說「最危險」，而我要補一句

> A 寫：「**題在、規格不在 => 下一個人看不到判準，只看得到斷言**」

```
✅ 成立。而 ③ 裡**絕大多數是 2026-09-22 以前的批次**
   （VR／BK／MN／GB／GC／TD／QL／RP／BR）
🔑 那些是**在 A-2 這個視窗存在之前**做的 ——
   當時的工作方式就是「A 派工 -> C 寫題」，沒有規格這一層
⇒ ⇒ **它們不是「漏掉規格」，是「在規格這個制度之前做的」**
```

> ### 🔑 ⇒ 處置不該是「回頭補 98 份規格」。
> ### ⚠️ 而 A 的顧慮**仍然成立** —— 只是它的形狀是
> ### 「**這 98 個的判準只存在於測試的 docstring 裡**」。

```
📌 而那其實**不算最糟** —— 那些 docstring 寫得很詳細（我讀過好幾支）
⇒ 建議：**不補規格**，而是確認那些題的 docstring **說得出「為什麼」**
   ⚙️ 那是一個可以抽驗的東西，而不是 98 份文件的工作量
⚠️ 而**我沒有抽驗過它們** —— 這一句是從我讀過的幾支推的
```

---

## §6 我沒查什麼

```
① 🔴 那 15 個「要人看一眼」的，**我沒有打開看**（§4）
② 🔴 ③ 那 98 個的 docstring 品質 —— **沒有抽驗**（§5 是從我讀過的幾支推的）
③ ② 那 20 個「內容在別份規格裡」我**沒有驗證它真的在**
   ⚙️ 判準只是「別份規格裡出現 `\`XX\`` 這個字串」
   ☠️ 而「被提到」與「內容在裡面」是兩件事 —— **與 ③ 的假陽性同一種錯**
   ⇒ 這 20 個的可靠度**與 ③ 相同，也就是不高**
④ `NEXT` 與 `EXEMPT` 完全沒看（A 只問 `THIS`）
⑤ 測試檔裡的題與 `SCOPE.md` 的編號**是不是同一個意思** —— 沒驗
   ⚠️ 例如 `AS4` 出現在 `test_ac1_write_actions` 裡，而那可能是巧合
```

---

## §7 ⏳ 待使用者回來確認

```
✅ **無。**
🔑 本檔是一份盤點，**沒有做任何決定** —— A 交代「先給清單，我排順序」。
⚠️ 而 §5 的建議（不補 98 份規格，改為抽驗 docstring）是一個**建議**，
   它要 A 裁，而不是使用者。
```
