# `SPEC-PK1` · 出貨包裡有我們的整份內部工程文件

> 座標：`26598c1`（工作樹：本檔為新增）
> 作者：視窗 A-2 ／ 2026-09-23
> 🔴 **出貨阻擋**（A 發編號並進 `SCOPE THIS`）
> ⚠️ 嚴重度據實：**目前沒有任何外部客戶收到過**（兩個包只上過正式機）

---

## §1 ✅ A 的事實我複驗過，**而包裡的東西比他列的多**

```
tools/build_deploy_package.ps1:670
    git archive --format=tar -o $tarPath $commit
=> 匯出**整個 commit 已追蹤的內容**
```

### ⚙️ 我打開 `deploy_packages/20260922_200604_7bc1fb8/` 實數

```
.md 總數 = **45**
   根目錄        **27**（`AGENT-HANDOFF-TEMPLATE.md`／`GITFLOW.md`／`DR-SOP.md`…）
   docs/windows  **12**  <= 🔴 **這一層是我們的工作檔**
   其他           6
```

```
🔴 docs/windows 裡有：
   STATE.md（**20,341 行**）／SCOPE.md／BACKLOG.md
   **A-2.md ／ B.md ／ C.md ／ D.md**  <= ☠️ **四個視窗的工作檔**
   AUDIT-2026-09-22.md ／ HEALTH-2026-09-22.md ／ PIP-AUDIT-2026-09-22.md
   PIPELINE.md ／ DELIVERY-NOTE-DRAFT.md
```

### 🔴 而 A 沒提到的兩件

```
① `backend/tests/` = **203 個檔案**在包裡
   🔑 而測試的**函式名就是缺陷的名字**，docstring 更是逐條寫著
      「這個缺陷長什麼樣」「為什麼會發生」「少了它會怎樣」
   ☠️ 那比 `STATE.md` **更精確**地描述了每一個缺陷
② `.claude/hookify.shell-quoting-python-dq.local.md`
   => **我們自己的 AI 工具設定**也被打包了
```

⚙️ 而**沒有**的（確認過）：`.db` 檔 0 個 ✅；包總大小 40MB。

> ### ☠️ **每一個出貨包裡，都有我們對這個系統每一個已知缺陷的完整內部討論。**

### 📌 而 A 那句最鋒利的話**成立**，我驗過

> 「**你正在寫的那幾份 `SPEC-*.md`，它們自己也會被打包進出貨包。**」

```
⚙️ 那個包是 09-22 的 => docs/windows 只有 12 檔
⚙️ 而今天（09-23）我寫了 40+ 份 `SPEC-*.md`
=> 🔴 **下一個包會多四十幾份「這個系統哪裡有問題」的規格**
```

---

## §2 🔴 而 A 的敘述有一格**不精確，而那個差別會害到 B**

> A 寫：「沒有 `.gitattributes` / `export-ignore`」

```
⚙️ 實查：`.gitattributes` **存在**（3,217 bytes，`Sep 21 23:29`）
⚙️ 而 `grep -c export-ignore` = **0**
```

> ### ⇒ 結論一樣（沒有 export-ignore），**而前提相反**。

```
☠️ 「沒有 .gitattributes」  => B 會**新建一個**
✅ 「有而沒有那個功能」      => B 要**加進既有的**
🔴 而新建一個會覆蓋掉裡面那 **30 行換行規則**：
   `*.py text eol=lf` ／ `*.ps1 text eol=crlf` ／ `backend/tests/fixtures/** -text`
   ／ 12 種 binary 標記
=> ☠️ **全 repo 的行尾設定壞掉**，而它的症狀是「所有 .ps1 在正式機上跑不動」
```
🔑 ⇒ 規格要明著寫「**加進既有檔案的結尾**，不要新建」。

---

## §3 ✅ A 裁的三件

```
① 加 `.gitattributes` 的 `export-ignore`
   ⏳ 而「出貨要不要附文件」待使用者確認 —— 見 §6①
② 🔴 驗收釘「**包裡不存在 `STATE.md`**」，**不要**釘「`.gitattributes` 裡有那一行」
   ☠️ `export-ignore` **只影響 `git archive`** —— 改用別的打包方式它會**靜默失效**
   🔑 而這一件就是「**打開產出物**」才證實的，不是讀腳本
③ ⚠️ 排除清單**不可以一律排除 `backend/tools/`** —— 有些是正式機維運要用的
   （D 已確認 `tools/` 被打包是**刻意的**）
```

---

## §4 🔴 A 要我判的那一格：**`backend/tests/` 可以排除，而代價是零**

> A：「排除它之後，**客戶現場出問題時我們也跑不了測試** => 這一格寫成 ⏳，把兩邊的代價都寫出來」

### ⚙️ 而我去查了「現場跑得了測試嗎」—— **答案是跑不了**

```
⚙️ 包裡的 `backend/requirements.txt`：**沒有 pytest**（也沒有 playwright）
⚙️ `docs/DEPLOY.md` 裡 grep「pytest」「測試」= **0 筆**
   => 部署流程**沒有任何一步**會在正式機上裝測試工具
```

> ### 🔑 ⇒ **「現場跑測試」這個能力今天就不存在。**
> ### ⇒ 排除 `backend/tests/` **不會失去任何東西**。

```
📌 ⇒ 這一格**不必寫成 ⏳** —— 它不是一個取捨，是一個**被誤以為有代價的動作**
☠️ 而我差一點照 A 的交代把它寫成待確認 ——
   🔑 那會讓使用者去裁一個**兩邊不對等**的選擇題
      （一邊是「洩漏 203 個缺陷的名字」，另一邊是「失去一個不存在的能力」）
```

### ✅ 而 A 派給 D 的那個前置，**我順手查掉了**（D 已獨立驗證，結果一致）

```
🔴 A 的顧慮：「排除 tests／requirements-dev 會不會讓
   `verify_package.py` 在客戶端壞掉？那是**安裝流程的一部分**」

⚙️ backend/tools/verify_package.py：
   ✅ 只用**標準函式庫**（argparse／hashlib／io／json／os／re／sys）=> **零外部相依**
   ✅ :399-401 `tests = [...]` 之後**只是 print**：
      「backend/tests/ 檔案 %d 個**（不在包裡）**」
      🔑 那個「（不在包裡）」字串**就是為這個情況寫的**
   ✅ 而 `R.fail` 只出現在 `.git/` 那一段（:405），**不在 tests 這一段**
```

> ### ⇒ **排除 `backend/tests/` 不會讓驗包壞掉 —— 而它早就預期了這個情況。**

```
📌 ⇒ 這一格**不寫成 ⏳**
⚙️ 而 `apply_update.ps1` 也不引用 `backend/tests`（grep = 0 筆，已查）
```

---

## §5 處置

### ① 🔴 排除清單**在 `SCOPE.md` 最後一節** —— 本規格**指向它，不複製**

```
⚙️ D 量、A 裁，落在 `SCOPE.md`（commit `2328b05`）：
   ❌ 明確排除 **43 項**（逐類列名）
   ✅ 明確保留 DEPLOY／DR-SOP／HTTPS-DEPLOY-CHECKLIST／firewall_setup.bat／
      start_server.ps1／requirements.txt／`backend/tools/`（**不可一律排除**）
   🔴 三件裁決：CHANGELOG 排除／PASSKEY-CA-ROLLOUT 整份排除／version_manifest 待查
```

> ### ☠️ **不要在這裡再抄一份。**
> ### 🔑 兩份清單一定會漂移，而漂移的那天**沒有人知道哪一份是真的** ——
> ### 而這一件的後果是「**有一份內部文件出去了**」。

```
📌 那正是我今天在 `EM13 §6c` 用過的判準的**反面**：
   那裡「資料驗得完 ⇒ 第二份可以接受（配逐字守門）」
   🔴 而**這裡不行** —— 差別是**失敗的代價**：
      EM13 漂移 => 訊息裡的模組名字不對（看得見、報得修）
      PK1  漂移 => **一份內部文件被打包出去**（看不見、收不回來）
🔑 ⇒ 判準要合起來用：**驗得完 ＋ 驗會被維護 ＋ 失敗看得見** ——
   而 `PK1` 第三項就不成立
```

### ①b 🔴🔴 **判準比清單重要：排除任何一個檔案之前，先問「執行期有沒有人讀它？」**

> A `9716130`（比 `2328b05` 晚的一則）

```
☠️ 對**會被讀**的檔案，「排除」產生的是**安靜的降級** ——
   東西還在跑，只是少了一塊，**而沒有人會報修**
```

#### ⚙️ 而它有一個現成的實例：`version_manifest.json`（**第 44 項**）

```python
# routers/auth.py:288-308  GET /api/system/version
entries = json.load(open(version_manifest.json))   # 🔴 **每次請求都重新讀磁碟**
return {"version": latest["version"], "date": latest["date"]}
```
```
✅ 端點只回**最新一筆**的 version + date（完整異動細節從沒回給客戶端）
🔴 而整份拿掉 => 進 except => **登入頁的版本號安靜消失**
   ⚠️ 那個失敗模式**它的 docstring 自己寫著**，還引用了一次真的發生過的案例（`1c8f2e8`）
```

> ### ⇒ 處置是「**打包時產生精簡版**」（只留最新一筆 version/date）取代整份出貨 ——
> ### 🔑 **那是建置流程新增一步，不是 `.gitattributes` 排除清單處理得了的。**

```
📌 ⇒ **「排除」與「精簡」是兩種處置**，而排除清單那個形狀只表達得出前一種
☠️ 一個把它當成第 44 項寫進 export-ignore 的實作，
   會讓每個客戶的登入頁**少一行版本號**，而沒有人會回報
```

#### 🔴 ⇒ 那 43 項要標成「**待過判準**」，不要寫成已定稿

```
✅ 已過判準的：9 份選型研究材料（D 已查證「**不被任何 runtime 程式碼讀取**」）
🔴 其餘 **34 項還沒逐一過** —— A 已派 D 分類哪些需要查
⚠️ 而 `SCOPE.md` 那份清單**是可以動的** —— B 動工前要確認它的狀態
```

### ② `.gitattributes` **加在既有檔案的結尾**，不要新建（§2）

```gitattributes
# ── 出貨包不含內部工程文件（PK1，2026-09-23）─────────────────────────
# ⚠️ export-ignore **只影響 git archive**。改用別的打包方式時它會靜默失效，
#    所以驗收釘的是「包裡不存在」而不是「這裡有這幾行」（§3②）。
# 📌 清單的唯一真相在 docs/windows/SCOPE.md 最後一節，**這裡是它的實作**。
```
```
🔴 逐項照 `SCOPE.md` 那 43 項寫，**而順序與分類要一致** ——
   ☠️ 否則下一個人對帳時要重新配對，而那正是漂移開始的地方
```

### ②b ✅ A 的兩則新裁示（`321305c`）

```
✅ `PK1` 是**新發現**，不是漏掉的舊裁示
   ⚙️ `git log -S INTERNAL_DOC` => `ab8e700` **通篇沒提過出貨**
   🔑 那一格值得記：**「是不是我們早就知道而漏了」本身是可查的**，
      而查它的成本很低（一次 `-S`）
✅ `backend/tools/` 28 檔分類定稿：甲排除 **18**（**含 `verify_package.py` 本身**）／
   保留 6 ／需裁 4
   🔑 而排除 `verify_package.py` **順便關掉了 `INTERNAL_DOC` 那張表的檔名洩漏** ——
   📌 **一個處置關兩個出口，比兩個處置各關一個好**
```

### 🔴 ②c 驗收新增：**包裡不存在任何 `*_private_key*.pem`**

```
⚙️ 正對照：`_license_private_key_dev.pem` **在磁碟上而 git 沒追蹤**
☠️ 而 `git archive` 匯出的是**已追蹤**內容 => **那條 `.gitignore` 是唯一一道**
```
> ### 🔑 ⇒ 「唯一一道」就是釘它的理由 ——
> ### ☠️ 而 `.gitignore` 是**人可以繞過的**（`git add -f`），
> ### 所以驗收要看**產出物**，不是看 `.gitignore` 有那一行。

📌 與 §3② 同一條：**釘「包裡不存在」，不要釘規則檔裡有那一行。**

### ③ ✅ 而 `deploy_packages/` **已經被擋著**（查掉了）

```
⚙️ `git check-ignore -v deploy_packages/` => `.gitignore:58:deploy_packages/`
⚙️ `git ls-files deploy_packages/ | wc -l` => **0**
=> ✅ 這一條**不必做**
🔑 而我原本把它列成「§7③ 沒查」——它三秒就查得完
```

---

## §5b 🔴 驗收：**釘「包裡不存在」，而總數用另一把尺**

```
① 🔴 打一個包 -> 解開 -> `docs/windows/STATE.md` **不存在**
   ☠️ **不要**釘「`.gitattributes` 裡有那一行」——
      `export-ignore` 只影響 `git archive`，換打包方式會**靜默失效**（§3②）
   🔑 而這一件本身就是「**打開產出物**」才證實的，不是讀腳本
② ✅ 負對照：`DEPLOY.md`／`DR-SOP.md`／`backend/tools/` **仍然在包裡**
   ☠️ 少了 ②，「把整個 docs/ 與 backend/ 排除」也會讓 ① 綠
③ 🔴 **加總自檢，而兩個數字要來自不同的尺**：
   **排除數 ＋ 保留數 ＝ 用另一種方式數出的包內檔案數**
④ 🔴 **不變量：排除清單 ∩ `verify_package.py` 的 `MUST_EXIST` = 空集合**
   ⚠️ 釘**交集為空**，**不要**把今天的兩個值（`autostart.bat`／`DEPLOY.md`）抄下來
   🔑 `MUST_EXIST` 會長，而抄下來的那一份會在它長的那天靜默失效
   ☠️ 而失效的樣子是「驗包過了」——因為交集檢查換成了兩個過期的字串比對
⑤ ✅ 而 `backend/tests/`／`requirements-dev.txt` 的排除**已驗證安全**（A 複驗）：
   `verify_package.py` 不 import pytest、不 subprocess 叫 pytest、`MUST_EXIST` 裡沒有它們
   `DEPLOY.md` 全文搜 `verify_package`／`pytest`／`requirements-dev` **都是 0 命中**
```

### 🔑 而 ③ 的形狀是 A 今天實際抓到錯誤的那一個

```
⚙️ D 報總數 **34**，正確是 **43**（最後那 9 沒加進去）
✅ 而抓到它的是：包的根目錄有 **27 份 .md**（**另一次量測**），
   而排除 13 + 9 = 22、保留 5 => **22 + 5 = 27** ✅
```
> ### ☠️ **同一把尺相加的加總不會翻臉** —— D 的 34 就是那樣來的。
> ### 🔑 ⇒ 驗收要寫「**排除數 ＋ 保留數 ＝ 另一把尺數出的總數**」，
> ### 而**不是**「排除清單有 43 項」。

```
📌 今天第三次成立（`MG4` 的 34=30+4 幽靈、`AUDIT` 的 180 漏掉 P0 那一行、這一次）
⚠️ 而 `AUDIT` 那次**加總說「無不符」而它是錯的** ——
   🔑 因為兩邊都少了同樣的 4 個 => **加總只驗它看得到的東西**
```

---

## §6 ⏳ 待使用者回來確認

```
① 🔴 **出貨要不要附文件**
   我裁（沿用 A）：**加 export-ignore，出貨包不含 `docs/windows/`**
   依據：那一層是四個視窗的工作檔與 `STATE.md`（20,341 行的內部討論）
   ⚠️ 而他若要「出貨附文件」——
      🔑 A 已指出：那要**分兩份**（**給客戶的手冊** ／ **我們自己的工程紀錄**），
         **不是把現有的挑一挑**
      ☠️ 挑一挑的失敗方式很具體：下一份新文件預設會被帶出去，
         而**沒有人會記得回來挑它**
   ⇒ **若他要做客戶手冊**：那是一個新的產出，不在本規格範圍

② ✅ **根目錄那 27 份已由 D 逐份判、A 裁**（`SCOPE.md`）——
   排除 22（13 ＋ 9 份選型研究材料）／保留 5
   🔴 而 A 的兩件裁決依據要留著：
      `CHANGELOG.md` 排除（抬頭是我們公司、指向內部 QUICK.md）
      `PASSKEY-CA-ROLLOUT.md` **整份**排除 —— 而依據是**失敗方向**：
      ```
      ✅ **排除的失敗會被報修**（客戶少一份文件會來問）
      ☠️ **保留的失敗不會**（我們的執行紀錄躺在客戶機器上，沒有人會告訴我們）
      ```
      🔑 正確處置是**拆檔**，而那是工作不是決定 => 本輪取安全側
   ⏳ 而**兩份的「另生成一份給客戶的」都還沒做** —— 那是新產出，不在本規格

③ ✅ **`backend/version_manifest.json` 已裁：精簡，不是排除**（A `9716130`）
   ⚙️ 端點每次請求都讀磁碟，而整份拿掉 => **登入頁版本號安靜消失**
   ⇒ 打包時產生只含最新一筆的精簡版
   🔑 而它是**建置流程新增一步**，不是排除清單處理得了的（§5①b）
   ⏳ 而**精簡版要含哪幾個欄位**仍未定 —— 端點今天只用 `version`／`date`，
      ⚠️ 而「今天只用兩個」不等於「精簡版只要放兩個」——
      ☠️ 若客戶端日後要顯示更多，那會變成一個查不到來源的缺欄位

④ ✅ **無**其餘 —— §4（`backend/tests/`）**不是取捨**，見該節
```

---

## §7 我沒查什麼

```
① ✅ 根目錄 27 份由 **D 逐份判過**（我沒讀，而**不必重做** —— `SCOPE.md` 是唯一真相）
② ✅ **已查**：`apply_update.ps1` 不引用 `backend/tests`（grep = 0 筆）
   ＋ `verify_package.py` 只 print 不 fail（§4）
③ ✅ **已查**：`deploy_packages/` 被 `.gitignore:58` 擋著，`git ls-files` = **0**
   📌 我原本寫「我的記憶暗示它是被擋著的」—— 🔑 **暗示不是查過**，而它三秒就查得完
④ 兩個包**有沒有真的送到過客戶手上** —— A 說沒有（只上過正式機），
   而**我沒有獨立驗證**
   ⚠️ 若送過，那是一個已經發生的外洩，而處置完全不同（要通知）
⑤ `git archive` 以外還有沒有別的打包路徑 —— **沒查**
   🔑 而 §3② 的「釘包裡不存在」正是為了不依賴這個答案
⑥ 🔴 **D 對帳時新發現的三個前端檔**（`login.html`／`index.html`／`quotation-form.html`）
   ⚙️ 我已裁：**它們屬於 `WL1` 不是 `PK1`** —— 那是「產品畫面上寫死我們是誰」，
      而 `PK1` 是「打包機制把整個 repo 帶出去」，**兩個不同的層**
   🔴 而 `login.html:203`「允碩整合集創 · **內部管理平台**」要分出來：
      它**不是公司抬頭**，是**產品定位描述** ——
      ☠️ 沒有一個值可以填（填「客戶的內部管理平台」也不對，他買的是產品）
      ⇒ 處置是**整句換成產品名**，不是換設定值
   ⚠️ 而**優先序高於其他** —— 它在登入頁上，客戶每個員工每天都看到
```
