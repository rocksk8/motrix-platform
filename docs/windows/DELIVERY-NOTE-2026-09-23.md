# 本次更新說明（2026-09-23）
> 部署與回滾都是你自己按。這一包**會動到資料庫**，請先看第四節。

---
# 一、你打開系統會看到的

## 1. 會計科目
新增一頁「會計科目」，裡面已經匯入**經濟部《商業會計項目表》112 年度版的 547 個項目**。
```
· 公告的那 547 個是**唯讀的**（改不了、刪不了，連超級管理員也不行）
· 你自己加的科目可以改、可以停用
· 這一版**只能看**：新增與停用的按鈕還沒做
```
⚠️ 你會看到「**勞務成本**」掛在「**銷貨成本**」底下 —— **那是官方表本身的形狀，不是錯的**。
我們照抄官方公告的結構，沒有自己調整。

## 2. 傳票（**畫面下一輪**，底層與讀寫介面已完成）
資料結構已經建好，包含你要的：
```
· 傳票號碼、日期（可指定，預設今天）、狀態由系統管
· 過帳前檢查借貸平衡，不平衡不准過帳
· 過帳後鎖住，要修正只能作廢重開，兩張都留著且看得出取代關係
· 每一次修改都留紀錄（誰、什麼時候、改前什麼、改後什麼）
· 摘要範本、附件、簽核流程的資料結構
· 已經可以用 API 讀取與修改草稿傳票，而**只有有「出納」或「財務」權限的人**叫得動
  （七種身分實測過：沒有那兩個權限的一律拒絕，**含管理者**）
```

## 3. 側欄
「財務」底下的項目調整過，**出納現在有自己的入口**（之前它沒有，只能從營運報表那一項進去）。

---
# 二、你看不到、但確實變了的

## 🔴 資料庫改版：92 → 98
```
93  會計科目表（＋公告項目唯讀的保護）
94  匯入 547 個公告項目
95  傳票的五張表
96  科目的停用旗標
97  獎金分潤的五張表
98  編寫紀錄加「保留期」欄位
```
⇒ **部署時會自動升級，不需要你做什麼。**
⚠️ 而它表示：**如果你回滾，資料庫也會一起回到升級前** —— 升級後新建的資料會消失。

## 其他
```
· 部署工具的失敗判定修好了（先前「不健康的部署」可能被記成成功）
· demo 帳號重置的清除範圍修正（先前會讓不同 demo 使用者的資料互相看到）
· 備份守門、測試守門一批修正（你不會感覺到，但它們會在下次出錯時說話）
```

---
# 三、🔴 這些**不可以**當成已經驗證過

| 項目 | 目前的證據到哪裡為止 |
|---|---|
| **傳票操作** | 資料結構、規則與**讀寫介面**都有測試守著，**而畫面還沒做** ⇒ 你這一包還不會在螢幕上看到傳票 |
| **獎金分潤** | 資料表、計算規則與 API 都有測試守著，**而畫面只做到看得到** ⇒ 還沒有人實際發過一筆 |
| **會計科目頁** | **只能看**。新增、編輯、停用都還沒做 |
| 547 個公告項目 | 是從官方 PDF 解析出來的，而**112 年版之後有沒有更新版，我們沒有查證** |
| 勞務成本的位置 | 對過官方 PDF，**是官方表原本的形狀** —— 不是缺陷 |

---
# 四、🔴 部署前請先確認一件事：**備份**

## 我查到的（事實）
```
本機雲端硬碟 G:\我的雲端硬碟\系統存檔\每日備份
  2026-09-21   44 個檔  **完成標記：有**
  2026-09-22   44 個檔  **完成標記：有**
  2026-09-23   48 個檔  🔴 **完成標記：沒有**
  資料庫檔案   09-22 是 8.9 MB ／ **09-23 是 10.0 MB**
```
⇒ **今天的備份有跑、資料有進去**（檔案數與資料量都比前兩天多）。
🔴 **而「備份完成」那個標記沒有出現，前兩天都有。**

## ⚠️ 那個標記是什麼
```
它是備份程式跑完最後一步才寫下的一個空檔案。
那道關卡**專為 8/30、8/31、9/3 那三天的空備份而做** ——
有檔案不代表備份完整，所以才需要一個「我確實跑完了」的簽名。
```

## 🔴 而成因分不出來，證據在正式機上
```
可能是備份程式最後那道自我檢查沒過，
也可能是中途出了例外而被外層吞掉。
⇒ **兩種在雲端硬碟上長得一模一樣。**
```

## ⇒ 請你在**正式機上**看一眼再部署
```
① 系統設定裡的「備份警示」（backup_alerts）有沒有今天的新紀錄
② 或看 server.log 裡今天的備份那一段
```
⚠️ 兩個理由，而第二個與異常無關：
```
① 今天的備份沒有完成簽名 —— 它可能是完整的，而**沒有東西證明它是**
② **這一包會動資料庫** —— 出事時回滾會依賴備份
```

# 五、部署之後建議做的

```
① demo 帳號**連續登入兩次** —— 這次改了 demo 重置，而問題只在第二次才會出現
② 打開「會計科目」看一眼 547 個項目有沒有正常顯示
③ 側欄「財務」底下確認出納的入口出現了
```

---
# 六、還在等你或會計師的（不影響這次部署）

```
① 「應付獎金」要記在哪個科目 —— 公告表裡沒有這個項目
   （現成的有 2191 應付薪資、2197 其他應付費用）
② 「誰查看過傳票」這類紀錄算不算會計帳簿 —— 決定要保存 10 年還是可以清理
③ 《商業會計項目表》112 年版之後有沒有更新版
④ 月結關帳後，還能不能補登那個月份的傳票
```
📌 這四題在 `docs/ASK-ACCOUNTANT.md`，可以直接轉給會計師。

---

## ⚠️ 驗包檢查的射程（不是缺陷，而你要知道）

升級檔裡有 **471 個檔**，而自動檢查**真的看過內容的只有 7 個**：
```
算過雜湊    5 個（地圖元件）
解過內容    2 個（資料庫程式、來源清單）
其餘 464 個 只檢查「在不在」，不檢查內容
```
☠️ 實測的一個例子：把開機啟動檔裡的**正式機路徑改成錯的**，
其餘一字不動 ⇒ **驗包全綠**，而那台機器開機起不來。

⇒ 本輪已補上那一個檔的內容檢查；**其餘的檢查仍然只能告訴你「檔案在」**。
🔑 所以：**驗包過了不等於包是完整的**。
