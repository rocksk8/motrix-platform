# 規格覆蓋率守門擋下建包 —— 31 個編號的分類（2026-09-23 夜）

> ⚙️ 視窗 A-2 ／ 依據＝**跑 `test_every_declared_condition_has_a_test` 拿到的清單**
> ⚠️ 不是我自己寫的解析器 —— 見文末「我量錯過一次」。

---

## 🔑 一句話：**守門列了 31 個，而真欠帳是 5 個（可能 3 個）**

| 類別 | 數量 | 證據是什麼 | 硬度 |
|---|---|---|---|
| **丁** 撞名，守門**設計上**不給信用 | **9** | 守門自己的 `ambiguous` 計算 | 🟢 **硬** |
| **丙** 有題而題名不帶編號 | **9** | 測試檔**內文**命中 | 🔴 **軟，未逐支打開** |
| **乙** 產品做了而零題（真欠帳） | **5** | 產品碼命中 | 🟡 2 個已查實／3 個軟 |
| **甲** 沒做 | **8** | 測試與產品碼都零命中 | 🟡 1 個已查實／7 個軟 |
| 合計 | **31** | 加總自驗 31 = 31 ✅ | |

---

## 🔴 ① `BN17` 有 10 支 `test_bn17_*` 的題，而守門說它「沒有人管」

> ### **守門那句訊息，字面上是錯的。**

```
⚙️ 機制在 _implemented()：
   ambiguous = {題出現在 >1 個檔}  ∪  {規格裡宣告 >1 行}
   return set(where) - ambiguous - MISCREDITED
⇒ BN17 的題在兩個檔 => **被扣掉不給信用**
```
🔑 ⇒ **丁類登記進 `AMBIGUOUS_ACK` 不是繞過，那正是那個機制的設計用途。**

### ⚙️ 丁類 9 個（證據硬：守門自己算的）

```
題檔撞名（5 個）
   AC1   3 檔  test_ac1_write_actions ／ test_bonus_empty_state ／…
   BN17  2 檔  test_bn17_award_reject_record ／ test_bonus_award_reject_edit_log
   EM5   2 檔  test_em5_reload_flag ／ test_em5_voucher_chain_unreadable
   JV16  2 檔  test_voucher_attachment_download ／ test_voucher_attachment_list_in_modal
   JV7   2 檔  test_e2e_voucher_summary ／ test_voucher_summary_sources

宣告撞名（4 個）—— **成因不同，登記理由要分開寫**
   BR1 BR2 BR3 BR4   規格裡同一個編號宣告了**兩行**
```

---

## 🔴 ② 乙類 5 個 —— **不可以登記掉**

```
BN11  產品碼 10 處（helpers/bonus.py 的 SETTLEMENT_FIELDS／精算明細列定義）
BN12  產品碼 10 處（同上）
JV13  產品碼  2 處（voucher.py:329 signatures_of() 的 by 存 username）
BI1   ✅ **已查實：產品真的修了**
      routers/bonus.py:169 `user = _require_user(authorization, require_superadmin=True)`
      條文說的「`user` 未綁定 ⇒ NameError 500」**已經不存在** ⇒ 確定是乙
EM13  🔴 **已查實：產品沒改 ⇒ 它其實是甲類**
      條文說「僅管理員、出納或財務可查閱」要改，而那 5 處**全部還在**：
      backend/routers/cashier.py:39 ／ frontend/js/cashier.js:179,187
      ／ frontend/js/reports.js:1351,1359
```
> ### ⇒ **修正後：乙 = 4 個（BN11 BN12 JV13 BI1）／甲 = 9 個**
> ### ⚠️ 而上面那張表的數字是**分類當下**的，這一格是後來查實的 —— 兩個都留著。

---

## 🔴 ③ 丙類是**最可能有假的一格** —— 而我看了一眼之後，它更可疑

```
⚠️ 我只驗到「測試檔內文提到它」，**沒有逐支打開確認那支題在驗它**。
🔑 而高命中數最可疑：**命中很多的時候「我查過了」的感覺最強。**
```

### ⚙️ 而停止查證前我印了命中行，看到的訊號如下（**部分證據，不是結論**）

```
🟢 看起來真的在驗它
   AI1  test_custom_account_items 的 docstring：「`CA1` · 自訂會計科目…，`AI1` 併入」
   WD1  test_wording_guards 的 docstring：「`WD1` · 通知文案的守門」

🔴 看起來**只是註解引用**，沒有題在驗它
   BN16 命中在 `test_ac1_*` 的註解：「`BN16`（§6）合法地把它改成…」
   EM1  三處全是「會被 `WD1`／`EM1` 改掉」這種**提醒別人不要釘文字**的註解
   JV21 命中在註解：「`JV21` 沿用同一支端點多回第三個頁籤」

☠️ 而有兩個訊號指向**別的類別**
   JV14 命中行逐字：「`JV14` **已結案：使用者目視確認紙本**」
        ⇒ 那是 **EXEMPT** 的形狀（目視驗證），不是丙
   JV22 命中行逐字：「**這一支紅著是 `JV22 §3` 的 TRIGGER 沒有人做**」
        ⇒ 測試檔**自己明說沒有人做** ⇒ 它是甲或乙，不是丙

⚪ 沒看出來
   BN10 命中在「使用者驗 `BN10` 回報：獎金單只有編號沒有案件名稱」
   EM11 命中在「✅ 已修——`EM11` 註解＋」
```
> ### ⇒ **丙類 9 個裡，至少 5 個的分類是可疑的**（BN16／EM1／JV21／JV14／JV22）。
> ### ☠️ **登記 `NAMED_ELSEWHERE` 等於宣稱「這個編號有人在守」** ——
> ### 而若那支題其實沒在驗它，這一包就宣稱了一件不實的事。

---

## ⚪ 甲類 8 個（分類當下）

```
EM2 EM6 EM7 EM8 JV26 JV4 JV6 PX1
＋ EM13（後來查實，見 ②）
```
✅ `JV6` 是確定的：**規格 2026-09-23 才寫（`4027c01`），B 還沒做**。
⚠️ 其餘 7 個**我沒查它們為什麼沒做** ⇒ 進 `PENDING` 時理由要各自寫，不要寫一句共用的。

---

## ⚠️ 而我量錯過一次 —— 這一段要留著

```
我第一次自己組了一把尺：declared ∩ THIS - implemented
=> 量出 **26 個**，而且編號與守門的不同
   （我多了 GC2 MN3 VR10 VR8，少了 AC1 BN17 BR1-4 EM5 JV16 JV7）
✅ 正確做法：**跑那支測試，用它印出來的清單**
```
> ### 🔑 協定 `§6j③`：**換了一把尺 ⇒ 答案與權威來源相反，而兩邊都「查過了」。**
> ### ☠️ 而它與「判準太寬／太窄」不同：**再查幾次都一樣，因為那把尺自己是一致的。**
> ### ⇒ 抓到它的唯一方法是**與權威來源對答案**。

---

## 📌 這份文件本身的限制

```
① 丁類的證據**硬**（守門自己算的），其餘三類的證據**軟**（字串命中）
② 丙類**沒有逐支打開** —— 而上面 ③ 那幾個訊號是「停止查證」之前順手看到的，
   ⚠️ 它們是**線索不是結論**
③ 乙/甲 只有 BI1／EM13 查實，其餘 11 個是從「產品碼有沒有命中」推的
   ☠️ 而產品碼命中**可能只是註解引用**（EM13 就是這樣被翻過來的）
```
> ### 🔑 ⇒ 下一個人要登記任何一條之前，**先看它在哪一類、那一類的證據有多硬**。
