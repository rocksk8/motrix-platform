# `SPEC-MG3` · `_m008` 改到一半就停下，而狀態被永久化

> 座標：`cda3719`（工作樹：本檔為新增）
> 作者：視窗 A-2 ／ 2026-09-23
> 來源：`QS1 §2b` 的洞 A 那一族，D 量完後 A 發編號。

---

## §1 🔴 因果鏈比「迴圈裡吞例外」更長 —— **它會被 commit，而版號照樣推進**

```python
# db.py:955  _m008_fix_legacy_owner_names(conn)
for r in rows:
    try:
        d = json.loads(r["data_json"] or "{}")
        if d.get("ownerName") in old_names:
            d["ownerName"] = correct
            conn.execute("UPDATE customers SET data_json=? WHERE id=?", …)
    except Exception:
        pass                      # 🔴 ① 吞掉，而**迴圈繼續**
…
conn.commit()                     # 🔴 ② 最後照樣 commit
```
```python
# db.py:854  執行迴圈
for i, fn in enumerate(_MIGRATIONS, start=1):
    if i <= current: continue
    fn(conn)                      # 🔴 ③ **沒有 try、沒有交易包裝**
    _set_version(conn, i)         # 🔴 ④ 而版號照樣 +1
```

### ⇒ 四步合起來

```
① 第 N 筆的 JSON 壞了 => 跳過，而**前面已改、後面繼續改**
② 走到最後 `conn.commit()` => 「**改了一半**」**被寫進磁碟**
③ 沒有例外往上傳 => 沒有任何一層知道出過事
④ `_set_version(conn, 8)` => **下次啟動不會重跑**
```

> ### ☠️ **那一批客戶永遠停在「有些改了、有些沒改」，而重跑救不了它 ——
> ### 因為版號已經推進。**

```
🔑 而它與 `QS1 §2` 的洞 B（比對到 0 筆）**不同**：
   洞 B 是「什麼都沒做」=> 重跑一次就好
   🔴 這一支是「**做了一部分**」=> 重跑也只會從已經改過的狀態再跑一次
⚠️ 而 `_m008` **是冪等的**（改過的 `ownerName` 不在 `old_names` 裡了）
   => 重跑**不會壞**，只是**也不會補**那些壞掉的 JSON
```

---

## §2 ⚙️ 而 `_m008` 還有**第二層問題**：它把一個客戶的資料寫死在產品碼裡

```python
old_names = ("jeff", "Jeff", "Jeff 管理員", "jeff管理員", "jeff超級管理員", "Jeff超級管理員")
correct = "黃玉龍"
…
conn.execute("UPDATE users SET email='jeff@miactw.com' WHERE username='jeff' AND …")
```

### 🔴 而它對**新裝的客戶也會跑**

```
版號從 0 跑到 103 => 每一個新客戶都會執行 `_m008`
```

#### ☠️ 而其中一句**真的可能誤傷**

```sql
UPDATE quotations SET sales_person='黃玉龍' WHERE sales_person IN ('jeff','Jeff',…)
```
```
⚙️ 而 `QS1` 已經證明 `quotations.sales_person` **會存 username**
   （`MQ-202607-059`／`-060` 的 sales_person 是 **'test3'**）
=> 🔴 一個新客戶若有業務的帳號叫 `jeff`，
   他的報價單會被這支 migration 改成「**黃玉龍**」
```

> ### 🔑 **那不是假想 —— 「sales_person 存 username」這件事有實例。**

```
⚠️ 而另外兩句是安全的：
   `WHERE username='jeff' AND display_name IN (…)`   => 要同時滿足兩個條件
   `WHERE username='jeff' AND (email='' OR …)`       => 同上
✅ 新客戶不會有 username='jeff'（除非他真的建了一個）
```

📌 ⇒ 這一層與 §1 **是兩件事**，而它們**都在這一支函式裡**：
```
§1 迴圈吞例外 => 改一半而不知道
§2 一次性的資料修補留在 migration 鏈裡 => 對每一個新客戶跑一次
🔑 而 §2 的修法**不會**順便修好 §1（反之亦然）
```

---

## §3 處置：**三件，而第一件與 `MG4` 是同一支 helper**

### ① 迴圈要有計數器，而**失敗要中止**

```python
skipped = []
for r in rows:
    try:
        …
    except Exception:
        skipped.append(r["id"])
        continue
if skipped:
    raise MigrationIncomplete(          # 🔑 **自己的型別**，不是 RuntimeError（見下）
        "m008：%d 筆 customers 的 data_json 無法解析，id=%s。"
        "**這支 migration 未完成，版號不會推進。**" % (len(skipped), skipped))
conn.commit()
```

```
🔑 為什麼是 **raise** 而不是「記一筆 log 繼續」：
   ✅ raise => `_set_version()` 不執行 => **版號停住** => 修好資料後重跑
   ❌ 繼續 => 版號推進 => **那幾筆永遠不會再被處理**
☠️ 而這一格與 `EM7` 的裁定**相反**（那邊是「計數＋結束報一次，不中止」）——
   🔑 差別在**可不可以重來**：
      EM7 的迴圈是**每天跑的排程** => 中止會讓整批停擺，而明天還會再跑一次
      migration **一個版號只跑一次** => 不中止就等於**永久放棄那幾筆**
```

> ### 📌 ⇒ 判準：**「跳過一筆」可不可以接受，取決於「還有沒有下一次」。**

### 🔴🔴 而**光是 raise 不行** —— 查完呼叫端之後處置要改

```
⚙️ `main.py:525  init_db()` 是**模組層級的呼叫**（不在函式裡、**沒有 try**）
=> `_m008` 拋例外 => 模組 import 失敗 => **uvicorn 起不來**
```

> ### ☠️ **一筆壞掉的 `data_json`，會讓客戶的整個系統開不了機。**

```
🔑 而我原本的理由（「一個看得見的失敗比一個看不見的成功好」）**仍然成立** ——
   只是「看得見」不該等於「**完全不能用**」
📌 ⇒ 〈一個限制的代價，要用「他被迫改走哪條路」來算〉——
   而這裡他被迫走的是「打電話說系統壞了」
```

### ⇒ 第三條路：**版號不推進，而系統照常起來**

```python
# db.py:854  執行迴圈
for i, fn in enumerate(_MIGRATIONS, start=1):
    if i <= current: continue
    try:
        fn(conn)
    except MigrationIncomplete as e:      # 🔑 **自己的例外型別**
        logger.error("migration %d (%s) 未完成：%s", i, fn.__name__, e)
        break                              # 🔴 **不 set_version，而且不再往下跑**
    _set_version(conn, i)
```

```
✅ 版號停在 i-1 => **下次啟動會重跑** => 修好資料後自動補上
✅ 系統照常起來 => 客戶能用
✅ 而 `migration_log`（`MG4`）裡查得到「第 8 版沒跑完，是哪幾筆」
```

### 🔴 而 `break` 之後**不可以繼續跑後面的 migration**

```
☠️ 繼續跑的話，系統會在一個「**第 8 版沒做完，而第 9~103 版做了**」的狀態
   => 而版號會說它是 8 => **版號與實際狀態不一致**
🔑 那比「停在第 7 版」危險得多：**版號是所有重跑邏輯的依據**
```

### ⚠️ 而**只有回填型**適用這一條

```
🔴 DDL 型失敗（ALTER／CREATE 失敗）**必須擋住啟動** ——
   schema 不完整而系統起來 => 到處 500，而使用者看到的是隨機的錯誤
✅ 回填型失敗（資料改一半）=> schema 是完整的 => 系統起得來
🔑 ⇒ 判準：**這一支失敗之後，schema 還完整嗎？**
   完整 => 起來（本條）／不完整 => 擋住（維持今天的行為）
⚠️ 而那要靠 **`MigrationIncomplete` 這個型別**來分 ——
   ☠️ 用 `except Exception` 會把 DDL 失敗也放行，而那是更糟的那一邊
   📌 〈用自己的例外型別，讓呼叫端分得出「規則擋下來」與「資料庫壞了」〉
      （`helpers/edit_log.py:46` 的 `MissingOldValue` 同一個做法）
```

### ② 回報走 `MG4` 的 `report_backfill()`

```
⚙️ 而 §1 的 raise 與 MG4 的回報**不衝突**：
   raise 之前先 report_backfill(conn, 8, "_m008", changed=n, skipped=len(skipped), details=…)
   ⚠️ 而那一筆**要在 raise 之前 commit** ——
   ☠️ 否則 raise 之後整個交易回滾，**連「為什麼失敗」都沒留下**
   🔑 那是一個需要**獨立連線**或先 commit 再 raise 的細節，B 要注意
```

### ③ 🔴 §2 那一層：**要 A 裁**（見 §5②）

---

## §4 驗收（`AC1`）

```
① 後端  `_m008` 迴圈計數 ＋ 失敗時丟 `MigrationIncomplete`；
        執行迴圈接住它、**不推進版號、`break`**；回報走 `report_backfill()`
② 前端  無異動
③ 頁面  ⓐ 造一筆 `customers.data_json` 是壞 JSON 的列 -> 從版號 7 啟動
           => 🔴 **系統照常起來**，而 `schema_version` **仍然是 7**
           🔑 ⓐ 是核心，而它有**兩個**斷言 ——
              改之前版號會變 8（那筆永遠不再被看），
              而「單純 raise」的實作會讓**系統起不來**
           ☠️ 少了「系統照常起來」那一半，raise 版本也會綠
        ⓑ 而 `migration_log` 裡**查得到**那一筆（含壞掉的 id）
           ☠️ 少了 ⓑ，例外之後交易回滾 => 連為什麼失敗都沒留下
        ⓒ 把那筆 JSON 修好 -> 重新啟動 => 版號變 8，**而且後面的 migration 也跑了**
           🔑 ⓒ 驗的是「修好資料後自動補上」—— 那是這個處置的全部意義
        ⓓ 負對照：全部 JSON 正常 -> 版號正常推進，**不中斷**
        ⓔ 🔴 負對照二：一支 **DDL 失敗**（例如 ALTER 撞到鎖）
           => **仍然要擋住啟動**
           ☠️ 少了 ⓔ，`except Exception` 的實作會把 DDL 失敗一起放行 ——
              而那會讓系統在 schema 不完整的狀態下起來
        ⓕ 冪等：連跑兩次結果相同（既有 `test_u10` 已經在驗）
```

---

## §5 ⏳ 待使用者回來確認

```
① ⏳ **失敗時怎麼辦**
   我裁：**版號不推進、`break` 不再往下跑、而系統照常起來**（§3① 第三條路）
   依據：`main.py:525` 的 `init_db()` 沒有 try ⇒ 單純 raise 會讓**系統開不了機**，
        而「一筆壞 JSON ⇒ 客戶完全不能用」的代價**大於**它要防的事
   ⚠️ 而它需要一個 `MigrationIncomplete` 型別，**只放行回填型** ——
      ☠️ DDL 型失敗仍然要擋住啟動（schema 不完整而系統起來 => 到處 500）
   🔑 而「下次啟動會重跑」這條路**要有人知道它在等什麼** =>
      那就是 `MG4` 的 `migration_log` ＋ 它的 superadmin 入口
      ☠️ **少了 `MG4`，這一件只是把「沒有人知道」搬到另一個地方**
   ⇒ **若他要維持今天的行為**（吞掉、繼續、版號推進）：
      那要接受「那幾筆永久遺失且查不到是哪幾筆」——
      而那正是 §6② 說「正式機當初跳過幾筆**永遠查不到了**」的成因

② 🔴 **`_m008` 裡寫死的「黃玉龍」「jeff@miactw.com」要不要處理**（§2）
   我裁：**本輪不動，而要登記**
   依據：改它等於**改寫已經跑過的歷史** ——
        〈凍住的歷史不要呼叫活的程式碼〉的鄰居：
        **一支跑過的 migration 改掉之後，跑過的人與沒跑過的人得到不同的結果**
   ⚠️ 而它的風險是真的（新客戶的業務若叫 `jeff`，報價單會被改名）
   ⇒ **若他要處理**：正確的做法是**新增一支 migration 把它改回來**，
      而不是改 `_m008` 本身
      🔑 而那需要知道「**哪些列是被 `_m008` 改的**」—— 而今天**沒有那個紀錄**
      ☠️ 那正是 §1 要修的東西 ⇒ **兩件事在這裡接上了**
```

---

## §6 我沒查什麼

```
① `customers.data_json` 今天有沒有壞掉的 —— **沒查**
   ⚙️ 而開發機的版號已經是 103 => `_m008` **早就跑過了**
   ⇒ 就算今天有壞的，也看不出當初跳過了幾筆（**沒有紀錄**，那正是這一件）
   🔑 ⇒ 這一件是「**往後不會再發生**」，不是「回頭補」
② 正式機當初跑 `_m008` 時跳過了幾筆 —— **查不到也不碰正式機**
   ☠️ 而那個數字**永遠查不到了** —— 那是這一件最貴的部分
③ 其他 migration 有沒有同樣的「迴圈吞例外 ＋ 最後 commit」形狀
   ⚙️ D 量過：`db.py` 的靜默 handler = 3 個，而**逐筆迴圈裡的只有 `_m008`**
   ⚠️ 而那是 D 量的，**我沒有複驗**
④ ✅ **已查，而它改變了處置**：`main.py:525 init_db()` 是**模組層級呼叫、沒有 try**
   => 拋例外 = **uvicorn 起不來**
   🔑 ⇒ §3① 從「raise 就好」改成「版號不推進而系統照常起來」
   📌 而我原本把它列成「我沒查什麼」的第四項 ——
      ☠️ **它其實是處置的地基**，不是一個補充問題
⑤ ✅ **已推完，而判準成立**：`break` 之後後面 95 支不跑，對**全新安裝**是什麼狀態？
   🔑 答案是**這個情況不會發生** ——
   ```
   全新安裝時 `customers` 是空的 => `_m008` 的 `rows` 是空 list
   => 迴圈跑 0 次 => `skipped` 是空 => **不可能丟 MigrationIncomplete**
   ```
   > **回填型 migration 在全新安裝時必然是 no-op ——
   >  因為那時候沒有資料可以回填。**
   ⇒ ⇒ 「回填型可以放行」對兩種情境**都成立**，不必分開處理
   ⚠️ 而**混合型**（`_m051`／`_m062`／`_m090`，種子 ＋ 回填）要注意：
      種子那一半**會**執行，而種子失敗**不是** `MigrationIncomplete`
      => 它照舊擋住啟動 ✅ 那是對的
   📌 而我原本寫「這一格 B 動工前必須解決」——
      ☠️ 它其實五分鐘就推得完，而我當時把它交出去了
      🔑 〈量它不等於修它〉：**推演也是一種量，而它通常比修便宜**
```
