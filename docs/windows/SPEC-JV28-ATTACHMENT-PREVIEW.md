# `SPEC-JV28` · 傳票附件要能預覽，不能只有檔名

> 座標：master `3370d74`（正式機 `46dc6ae`）／作者：hichan-0a ／ 2026-09-24
> 使用者逐字：「**在傳票上，已上傳檔案要能夠預覽，只有名稱無法辨別**」
> 前一輪：`SPEC-JV16-JV17.md`（取檔端點＋預覽窗內可點清單）——本檔是它沒解決的那一半。

---

## §1 現況（讀碼，HEAD `3370d74`）

| 位置 | 現況 | 問題 |
|---|---|---|
| `voucher.html:461-475` 編輯頁附件清單 | `<span class="vc-att__name" x-text="a.filename">` | **只有檔名，不能點** |
| `voucher.html:630-640` 預覽窗附件清單 | `<a @click.prevent="openAttachment(a)">` | 可點 |
| `voucher.js:395-409` `openAttachment()` | `fetch`（帶 Authorization）→ blob → **`window.open(url)`** | `window.open` 在 `await` 之後 ⇒ 已不是使用者手勢 ⇒ **可能被彈出視窗封鎖**；且開新分頁，離開傳票畫面 |
| `vouchers.py:1310` `GET /{id}/attachments/{file_id}` | `FileResponse(mime, filename)` | 端點本身可用；前端走 blob 所以 Content-Disposition 不影響 |

> ⚠️ 未實測「被封鎖」——這是依程式形狀推斷。實作前先在 e2e 裡重現（⑤ 的第一支題）。

## §2 要做的

1. **頁內預覽窗**（取代 `window.open`）：點附件 ⇒ 同一頁開一個預覽 modal。
   - 圖片（`image/png`、`image/jpeg`、`image/gif`、`image/webp`）⇒ `<img>` 顯示原圖，可縮放到視窗大小。
   - PDF（`application/pdf`）⇒ `<iframe>` 內嵌顯示。
   - 其他類型 ⇒ 顯示檔名、大小、類型，與「下載」鈕（**不內嵌**）。
   - modal 內有「下載」鈕（所有類型），以及上一個／下一個（同一張傳票的附件間切換）。
2. **編輯頁清單**：檔名改成可點（開同一個預覽窗）；**圖片附件顯示縮圖**（約 48px，延遲載入）。
3. **預覽窗內的附件清單**（JV16 那份）改用同一個預覽窗，不再 `window.open`。
4. blob URL 用完要 `revokeObjectURL`（關閉 modal 或切換附件時），不可累積。

## §3 🔴 安全界線（不可以省略）

- **只有 §2-1 列出的 MIME 可以內嵌**。`image/svg+xml`、`text/html`、`application/xhtml+xml` 及任何未列出的類型 **一律只給下載**——SVG／HTML 內嵌等於在我們的網域執行上傳者的腳本。
- MIME 判斷以**伺服器存的 `mime`＋副檔名兩者都符合**為準；只看其中一個就不內嵌。
- blob 建立時明確指定 `type`，不讓瀏覽器猜。
- 取檔權限不變：沿用 `_require_voucher_access`，不新增公開路徑、不加 `?token=`（FX21 已拿掉）。

## §4 不做

- 不改 `preview_html()`（理由見 `SPEC-JV16-JV17.md` §1）。
- 不做 Office 檔（doc/xls）線上預覽——下載即可。
- 不改附件上傳、刪除、併入 PDF 的邏輯。

## §5 驗收（題先紅，觀測點打在畫面上真的看得到的東西）

| # | 題 | HEAD 上應該 |
|---|---|---|
| ① | 點編輯頁的圖片附件 ⇒ 頁內出現預覽 modal，`<img>` 的 `naturalWidth > 0` | 紅（檔名不能點） |
| ② | 點 PDF 附件 ⇒ modal 內 iframe 的 src 是 blob、type 是 pdf | 紅 |
| ③ | 圖片附件在清單上有縮圖（`naturalWidth > 0`） | 紅 |
| ④ | 上傳一個 `.svg`（與一個偽裝成 `.png` 但 mime 是 `image/svg+xml`）⇒ **不內嵌**，只出現下載鈕 | 突變「放行 svg」⇒ 紅 |
| ⑤ | 預覽窗（JV16 那份）點附件 ⇒ 同一個頁內 modal，**沒有開新分頁**（page 事件 `popup` 0 次） | 紅 |
| ⑥ | 關閉 modal 後 blob URL 已 revoke（對照：不 revoke 的突變 ⇒ 紅） | — |
| ⑦ | 非本人／無權限帳號取檔 ⇒ 403（既有權限不退） | 綠（凍結組） |

使用者可見的改動 ⇒ **要附頁面實測截圖或 e2e 輸出**，不能只有後端綠。

---

# `JV29`～`JV34` · 傳票依台灣法規與使用者體驗修正（2026-09-24，接在本檔，使用者要求不新增文件檔）

> 使用者逐字：「我不懂傳票，由你依據台灣的法規跟傳票功能，結合使用者體驗去修正」；
> 「我點傳票的摘要他也能自動帶入已上傳檔案跟支出項，不用每次來回點閱」。
> 表單裁示（2026-09-24）：四組全做（法規缺口、資料錯誤、摘要面板、輸入與列印）；傳票類別「自動判斷，可手動改」。
> **這一輪只改碼、不建包**，使用者確認後才上正式機。

## 法規依據（law.moj.gov.tw，2026-09-24 查）
```
商業會計法 §17  記帳憑證：收入傳票／支出傳票／轉帳傳票
商業會計處理準則 §6  記帳憑證內容：商業名稱、傳票名稱、日期、傳票號碼、會計項目名稱、摘要及金額，並經相關人員簽名或蓋章
商業會計法 §35  記帳憑證應由負責人、經理人、主辦及經辦會計人員簽名或蓋章
商業使用電子方式處理會計資料辦法 §5  簽章得以「權限密碼設定之控制程序」替代（摘要，未逐字核對）
商業會計處理準則 §13  影響總數之錯誤應另開傳票更正（現行作廢重開已符合）
```

| 編號 | 內容 | 驗收（題先紅） |
|---|---|---|
| **JV29** 傳票類別（§17、準則§6） | `category` 改為「收／支／轉」三選一，**自動判斷可手動改**：分錄含現金／銀行存款類科目（科目代號 11xx 的現金及約當現金，實作前查 `account_items` 定出清單並寫進題）且淨額在借方＝收入、在貸方＝支出、無＝轉帳；手動改過的不再被自動覆蓋。畫面顯示；**PDF 標題印「收入傳票／支出傳票／轉帳傳票」**（取代「傳　票」）。既有資料 `'轉'` 保持轉帳 | 三種分錄各一題看 PDF 標題；手動改後再編輯分錄不被覆蓋 |
| **JV30** 🔴 簽核權限（§35＋電子辦法§5） | `approve`／`send_back`：①操作人必須是**當前層**的簽核人（含代理人，走 `tiered_approval.resolve_tier_approvers`），否則 403 並說明「這一層的簽核人是 ○○」；②**製票人不可核准自己的傳票**（任何一層）；③superadmin 是否例外：**不例外**（照組織流程，比照 09-15 第八輪） | 非簽核人按核准⇒403；製票人核准自己⇒403；正確簽核人⇒成功；代理人期間內⇒成功（對照組） |
| **JV31** 記帳簽章（§35） | 過帳記錄 `posted_by/posted_at`，PDF 與畫面簽章欄加「記帳」；畫面簽章欄依實際層數顯示，不固定 3 格 | 過帳後 PDF 有記帳人姓名；4 層流程畫面顯示 4＋製票＋記帳 |
| **JV32** 金額輸入 | 接受 `1,000`、全形數字、前後空白；小數**擋下並說明**「金額以新台幣元為單位，不可有小數」（不靜默截斷，前後端都擋）；同一行借貸都填、負數 ⇒ 擋下並指出第幾行 | 貼「1,000」存回＝1000；「12.5」⇒ 422 訊息；後端 `int()` 截斷路徑拿掉 |
| **JV33** 摘要帶入面板 | 點（focus）任一行摘要 ⇒ 側邊面板**同時**列出：本傳票已上傳檔案（含 JV28 縮圖，點可預覽）＋可帶入的支出項（沿用現有「摘要來源」API：案件／已上傳檔案／支出項），**不用切分頁**；點一項 ⇒ 摘要空白則填入、非空則以「；」接在後面；支出項另有「連金額一起帶入」按鈕（僅在該行借貸都空白時可按） | 點摘要後面板兩區同時可見；點支出項後該行摘要＝預期字串；金額帶入只在空白行 |
| **JV34** 輸入與列印 | ①會計科目改為可搜尋選單（代號或名稱，含停用標示）；科目名稱欄改唯讀 ②最後一行按 Enter 新增一行；「補平差額」鈕把差額填進目前行 ③PDF：表頭列加「附件 N 張」、頁碼「第 x／y 頁」、跨頁重印表頭 ④清單：關鍵字（號碼／摘要）、日期區間、狀態篩選 ⑤作廢時可勾「作廢並重開」（後端已支援 `reopen`） | 各一題；PDF 題用多行分錄觸發跨頁 |

界線：
- 不改編號規則、不改作廢重開的語意（已符合準則 §13）。
- 不動 `preview_html()`。
- 簽核權限只收緊傳票的 `approve`／`send_back`，**不動其他單據**。
- `db.py` 若需要新增欄位（`posted_by`、`category_manual`），先宣告範圍再動（鎖定檔）。
- 順序：**JV28 完成後**再做；建議 JV30 → JV32 → JV29 → JV31 → JV33 → JV34（先補控管漏洞與資料錯誤）。

### JV30 補充裁示（2026-09-24 使用者表單）
- 「製票人剛好是某層簽核人／只有一人有權限」⇒ **「依組織流程允許自簽」**：該層在組織流程被標為 `selfApproval` 時製票人可以自簽（與其他單據一致、不卡死）；其他情況製票人不可核准自己的傳票，且核准者必須是當層簽核人（含有效代理人）。
- 未設定簽核流程（內建兩層）時：只套「製票人不可核准」；其餘維持模組權限。
- 使用者**未選**「全單據禁止自己核准」⇒ 共用 `check_approve_permission` 不改；本項只作用於傳票。
