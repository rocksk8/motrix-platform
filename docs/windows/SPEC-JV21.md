# `SPEC-JV21` · 傳票摘要要能帶入案件底下的支出項

> 座標：`a719963`（工作樹：本檔為新增）
> 作者：視窗 A-2 ／ 2026-09-23
> 使用者原話：
> 「**摘要部分帶入案件的內容**，例如我點選案件，這裡面有甚麼**支出項有發票的**
> 都需要能帶入，例如**承攬商有發票有支出**、**叫料有發票有支出**，
> **點選案件內的案件可以帶入子項目**」

---

## §1 現況：`summary-sources` 列的是**案件本身**與**檔案**，沒有「支出項」

```
頁籤① 案件         SELECT quote_no, customer_name, project_name, status FROM quotations
                   => 只有案件本身
頁籤② 已上傳檔案   case_attachments(conn, quote_no)
                   => 列的是**憑證檔案**，不是**支出項目**
```
🔴 ⇒ 使用者要的是**第三種東西**：案件底下的**各筆支出**（有金額、有發票）。

---

## §2 ① 「支出項有發票」在資料層是什麼 —— 🔴 **三個來源，三種「有發票」**

### 用結構圈母體（`PRAGMA table_info`，不用名字猜）

```
⚙️ 全 DB 97 張表裡，**同時**有「金額型欄位」與「發票型欄位」的 = **1 張**
   contractor_dispatches   金額 total_amount ／ 發票 invoice_no, invoice_files_json
```

### 🔴 而 A 點名的五張候選裡，**有一張不存在**

```
contractor_dispatches   ✅ 存在  金額✅ 發票✅（**欄位**）
case_extra_expenses     ✅ 存在  金額✅ total_cost   發票❌（**沒有發票欄位**）
invoice_vouchers        ✅ 存在  金額✅ amount       發票❌
payment_requests        ✅ 存在  金額✅ amount       發票❌
🔴 material_orders      **這張表不存在**
```

### 🔑 「有發票」**不是一個欄位** —— 三個來源各自不同

| 來源 | 金額在哪 | 「有發票」怎麼判 |
|---|---|---|
| **承攬商派工**<br>`contractor_dispatches` | `total_amount`（＋`items_json`／`personnel_json`，見 ⚠️） | `invoice_no` **欄位** ／ `invoice_files_json` **附件** |
| ~~**叫料**~~<br>🔴 **見 §2b：它沒有金額** | ~~`qty × unitCost`~~ | `invoiceFiles` 陣列（**而 0 筆資料在用**）|
| **案件額外支出**<br>`case_extra_expenses` | `total_cost` **欄位** | `files_json` **附件** ＋ `doc_no`（憑證單號） |

⚠️ **叫料不是一張表** —— 它在 `quotations.data_json.**caseRecord**.materials`
（⚠️ **不是 `data_json.materials`** —— 我上一版寫錯了層），
而它的發票走 `POST /api/quotations/{no}/materials/{idx}/invoice-files`。

---

## §2b 🔴🔴 **更正：叫料「沒有支出金額」** —— 這一列是我推的，不是查的

### 實測（`caseRecord.materials`，13 張單、6 個品項）

```
出現過的鍵：id ／ name ／ model ／ qty ／ unit ／ ordered ／ arrived ／
            devices ／ note ／ supplier
🔴 **`cost`／`price`／`amount`／`total` 一個都沒有**
```

> ### ⇒ **叫料是「物流追蹤」（訂了沒、到了沒、序號），不是支出記錄。**

☠️ 而我上一版寫「叫料的金額 = `qty × unitCost`」——
**那是從端點路徑推的，沒有任何資料佐證，而它是錯的。**
🔑 〈先問欄位在不在，再問可不可以用〉：**我跳過了「那個欄位在不在」。**

### 而「材料的支出」實際上記在**兩個別的地方**

```
case_extra_expenses.category   「材料」3 筆 6,525 ／「運費」2 筆 2,750
                                「工時」1 筆 1,200 ／「其他」1 筆 15
settlement.items[].actualTotalCost   原始報價品項的**實際成本**
```

### ✅ 使用者裁（2026-09-23）：**他指的是「額外支出」裡的材料費**

> 選項原話：「那些本來就有金額也有發票檔
> （材料 3 筆 6,525／運費 2 筆 2,750／工時 1 筆 1,200）」

```
⇒ `JV21` 現有範圍**已經涵蓋** —— **不用動叫料**
```

### 🔴 而 `JV6` 會讀**同一批資料** —— 那是設計不是重複（2026-09-23 補）

使用者裁「購料發票＝叫料／材料費」之後，`JV6` 的第三種來源就是這裡。

```
同一筆 case_extra_expenses 的材料費：
   JV21（本件）->  **摘要文字**（voucher 的摘要欄，人可再編輯）
   JV6         ->  **分錄列**（voucher_lines：金額 ＋ 科目留空）
```
> ### 🔑 **同一個來源餵兩個產出，是設計不是重複。**
> ### ☠️ ⛔ **不要把其中一個做成另一個的呼叫** —— 那會把兩種產出綁死
> ### （使用者改了摘要，分錄金額跟著變；而 `JV7` 的裁定是「帶入是**起點不是終點**」）。

```
⚠️ 而兩者的範圍**不一樣**，不要互相參照：
   JV21  case_extra_expenses **全部**（材料 3／運費 2／工時 1／其他 1）
   JV6   **只有 category='材料'**（他的原話只說了「購料」）
```
📌 判準（`SPEC-JV6 §1`）：**產出物落在哪張表的哪幾欄，不是「從哪裡撈」。**

### 🔴 而「**為什麼不是那個讀法**」要留著

> **叫料（`caseRecord.materials[]`）沒有任何金額欄位** ——
> 它是**物流追蹤**（訂了沒、到了沒、序號），**不是支出記錄**。
> 使用者 2026-09-23 確認他指的是 `case_extra_expenses` 的材料費。

```
☠️ 下一個人看到「**叫料有發票有支出**」這句使用者原話，
   **會去幫叫料加金額欄位** —— 而那是一個沒有人要的功能
🔑 〈更正要留著錯的那一列〉的前瞻版：
   **把「為什麼不是那個讀法」寫下來，比只寫對的讀法有用。**
```

📌 而 `/api/quotations/{no}/materials/{idx}/invoice-files` **端點存在而 0 筆資料在用** ——
⚠️ 那是「**有人也想過，做了一半**」的痕跡，一併記著。

### ～ 以下是裁定前的兩種讀法（留著）～

```
甲 他指的是 `case_extra_expenses` 裡 category='材料' 的那些
   ✅ 那些**有金額、有附件** => JV21 已經涵蓋（就是「額外支出」那一支）
乙 他認為**叫料本身就該有金額**
   🔴 那是**一個新需求**（叫料要加成本欄位），**不是 JV21**
```

📌 ⚠️ 而 `/materials/{idx}/invoice-files` 這支端點**存在而 0 筆資料在用** ——
⇒ 「叫料可以掛發票」這個功能**做了而沒有人用過**，那本身也是一個訊號。

### ⚠️ 而 `contractor_dispatches.total_amount` **不是那一筆的總額**

```
`ACC-BN6 §3` 已經踩過這一顆：
   grandTotal = (items 小計 × (1+稅率)) ＋ **外包人員費用**
   而 total_amount 少了稅、也少了人員
☠️ 帶入摘要若用 total_amount，**金額會比實際少**
⇒ 用 `vendor_contractors.py:147` 已經算好的 `grandTotal`
```
🔑 **這個坑今天已經有人踩過並記下來了** —— 不要再踩一次。

### 🔑 而「案件底下有哪些憑證來源」**已經枚舉過了**

```python
# helpers/voucher_attachments.py:54  SOURCE_TYPES
"quotation_signed"    # 報價單回簽檔
"case_update"         # 案件動態
"payment_item"        # 收款項目的發票影本
"material"            # 叫料清單的附件
"material_invoice"    # 叫料清單的發票／包裝清單
"extra_expense"       # 案件額外支出
"invoice_voucher"     # 開票申請
"contractor_dispatch" # 承攬商派工
"contractor_invoice"  # 承攬商發票
```

> ### 📌 **`JV21` 要的「支出項」是這份清單的子集 ＋ 金額。**

```
✅ 是支出      contractor_dispatch ／ contractor_invoice ／ material ／
              material_invoice ／ extra_expense
❌ 不是支出    payment_item（**收款**，發票是開給客戶的）
              quotation_signed ／ case_update（不是支出）
⚠️ 待判        invoice_voucher（開票申請 —— 那是**開給客戶**的票，不是我們的支出）
```
🔑 ⇒ **不要新寫一份來源清單** —— 沿用 `SOURCE_TYPES` 並標出哪幾種是支出。
📌 而 `case_attachments()` 已經在跑這份清單 ⇒ `JV21` 是**同一批來源、換一種輸出**
（回「項目＋金額」而不是「檔案」）。

---

## §3 ② 🔴 「點選案件內的案件」—— **子案件不存在，這句話要回去問**

### 實查

```
quotations 共 **31 個欄位**
⚙️ parent／child／sub／root 命中 = **0 個**
=> **沒有子案件這種結構**
```

### 兩個候選讀法，**我不猜**

```
甲 「案件內的案件」是口語／筆誤，指的是「案件內的**項目**」
   => 兩層：案件 -> 該案件底下的支出明細
   ✅ 與同一句的前半「這裡面有甚麼支出項有發票的都需要能帶入」一致
   ✅ 與「可以帶入**子項目**」也一致（子項目 = 支出明細）

乙 真的有第二層案件概念
   ⚠️ 系統裡最接近的是 `dev_cases.converted_quote_no`（業務開發案 -> 報價單）
      —— 但那是**前身**不是**子案件**
```

### ✅ 使用者裁（2026-09-23）：**三層 —— 支出底下還有品項**

> 例：一張派工單底下有好幾個品項，**要能點進去挑單一品項帶入**。

```
第一層  案件（quote_no）
第二層  支出（一張派工／一筆叫料／一筆額外支出）
第三層  **該筆支出底下的品項**
```

> ### 🔴 而第三層在**三個來源上長得不一樣** —— 規格要分開寫，不要假設一致

```
承攬商派工   items_json 的陣列 ＋ personnel_json 的陣列   <= **兩種品項**
叫料         data_json.materials[i] 本身就是一筆         <= 它**已經是**第三層
額外支出     case_extra_expenses 一列就是一筆            <= **沒有第三層**
```
☠️ ⇒ 「每一種都展得開」是錯的假設：**額外支出展不開，叫料的第二層與第三層是同一層**。

⚠️ **這一段（第三層）的細節尚未寫完** —— A 指示「做完 `cashier` 再回來補」。
📌 ⇒ 本規格目前**只涵蓋前兩層**，第三層的資料形狀要另外一輪。

---

## §3b 第三層的資料形狀 —— **三個來源分三段，不要假設一致**

### ① 承攬商派工：**第三層有兩種，而它們的鍵完全不同**

```
contractor_dispatches.items_json[]
    {id, description, qty, unit, unitPrice, amount, note}
contractor_dispatches.personnel_json[]
    {id, name, amount, note}
```

```
🔑 兩種在畫面上都是「這張派工底下的一筆」，而：
   品項有 description／qty／unit／unitPrice   人員只有 name／amount
⇒ **不可以用同一個渲染器** —— 用了就得在其中一種上留空欄位
```

#### ☠️ 兩個實測到的陷阱

```
① `unitPrice` 的**型別不一致**：`6800`（數字）與 `"12000"`（字串）都有
   => 算金額前要 `float()`，⚠️ 而**空字串會丟例外**
② `items[].id` 是**前端產生的 float timestamp**（`1784650316777.8818`）
   `personnel[].id` 是**小整數**（2、3）
   ☠️ **兩邊的 id 語意不同，不可以混用** ——
      一個 `id=2` 在 items 裡幾乎不可能存在，而在 personnel 裡是常態
   ⇒ 帶入時的識別要用 `(dispatch_id, "item"|"personnel", index)`，**不要只用 id**
```

### ② 案件額外支出：**它自己就是第三層**

```
case_extra_expenses 的欄位：
    category ／ description ／ qty ／ unit ／ unit_cost ／ total_cost ／
    note ／ expense_date ／ doc_no ／ files_json
```

```
🔑 它**有 description／qty／unit／unit_cost** —— 品項該有的欄位都在
⇒ 「一筆額外支出」就是「一個品項」，**沒有再下一層**
```

> ### ✅ A 問的甲／乙 —— **選甲：展開後只有一層，不假裝有第三層**

```
甲 展開後只有一層（**畫面說實話**）              <= ✅ 選這個
乙 統一都展兩層，這一層只有一筆                   <= ❌
☠️ 乙的代價：使用者點開一個箭頭，看到**一筆與上一層一模一樣的東西**
   —— 他會以為自己點錯了，或以為系統壞了
🔑 **不要為了一致而製造一個空層。**
```

📌 ⚠️ 而**我上一版把這一格安在「叫料」頭上是錯的** ——
真正「第二層與第三層是同一層」的是**額外支出**，不是叫料
（叫料的問題是**根本沒有金額**，見 §2b）。

### ③ 叫料：🔴 **這一支現在寫不了**

```
`caseRecord.materials[]` 沒有任何金額欄位（§2b 實測）
⇒ 它進不了「支出項」這個清單 —— **不是形狀問題，是它不是支出**
⚠️ 而它**有第四層**（`devices[]`：序號／MAC）
   => 若日後補了金額，它會是**四層**不是三層
```

⇒ **等 §2b 那一題問完再決定**。本規格的第三層**只涵蓋 ①**。

---

## §4 ③ 帶入之後長什麼樣 —— 🔴 **不可以存來源 id**

`JV7` 的裁定逐字（`vouchers.py:312-320`）：

> 帶入只是**省打字** ⇒ 帶完之後那一格仍然要打得動，
> 而最終值是**使用者打的那個**，不是來源 id 再組一次。
> ☠️ 存來源 id、開啟時重組的實作，症狀是「使用者改完、存檔、關掉；
> **下次打開才變回來**」—— 中間隔了幾天，他不會把兩件事連起來。

### ⇒ 形狀

```
點一筆支出 => 把**一段文字**寫進當前那一行的摘要欄
           => 之後它就是一段普通文字，**系統不記得它從哪來**
```

### 文字內容（建議，A 可改）

```
承攬商派工   「{廠商名}－{範圍}　NT$ {grandTotal}」
叫料         「叫料－{品名}　NT$ {金額}」
額外支出     「{備註}　NT$ {total_cost}」
⚠️ **金額要帶**（使用者原話「有發票**有支出**」⇒ 他要的就是金額）
⚠️ 而**發票號碼**要不要帶：承攬商派工有 `invoice_no`，另外兩種沒有
   🔑 ⇒ **有的才帶**，不要為了一致而留一個空欄位
      （`describe_missing()` 的既有慣例：缺的留空並回報，不要編）
```

---

## §5 ④ 閘門 —— **顧慮變大了，而處置不變**

`summary-sources` 的 docstring 已經警告過：

> 這裡會列出**案件**（客戶名、報價單號）—— 那是業務資料不是傳票資料。
> ☠️ 閘門放鬆的話，等於**從一個記帳畫面繞過去看客戶清單**。

```
🔴 而 JV21 多列了**每一筆支出的金額** => 那個顧慮從「看得到客戶清單」
   變成「**看得到每個案件的成本結構**」
```

### ✅ 處置：**閘門不放鬆，而且要明著寫「為什麼不放鬆」**

```
維持 `_require_voucher_access(_require_user(authorization))`
   —— 與其餘 14 支傳票端點**同一道**（`SPEC-JV16 §2` 已驗過全部一致）
⚠️ 而 `JV21` **不要**因為「只是列個摘要來源」就降級成 `_require_user`
   🔑 那正是 `vouchers.py:60` 那段註解在擋的事
```

### ✅ `canSeeFinancial()` 那一層 —— **使用者已裁（2026-09-23）**

```
案件管理頁的「額外支出」頁籤有 canSeeFinancial() 這一層額外閘門
   （role ∈ superadmin/admin/sales 或有 financial_view 模組）
而 JV21 會讓「有傳票權限但沒有 financial_view」的人看到同樣的金額
```

> ### ✅ **使用者裁：有傳票權限就看得到。**
> 原話：「**記帳就是要抵那筆金額，看不到金額就做不了事**」

```
⇒ 等於確認：**傳票權限本身就含「看得到案件成本」**
🔴 而這一句要明著寫在規格裡（含日期）——
   ☠️ 否則下一個做**安全稽核**的人會把它當成缺陷**收緊回去**
   🔑 而收緊之後的症狀是「記帳的人看不到金額」，
      **而他不會知道那是被誰、依據什麼收緊的**
```
📌 〈守門守的對象被搬走〉的鏡像：**裁示翻面而守門不會自己翻面** ——
這裡是**先寫下依據，讓日後的收緊有東西可以撞**。

---

## §6 ⑤ `JV18` 的「已計算」標記 —— 🔴 **套不上，而依據是 `JV7`**

```
JV18 的標記靠的是 voucher_attachments 的 (source_type, source_doc_no, source_file_id)
   => **因為附件是被複製進來的，系統記得它從哪來**
JV21 的帶入寫的是**一段文字**，而 JV7 明著裁**不存來源 id**
   => **沒有任何連結可以查「這筆支出被哪張傳票用過」**
```

> ### ⇒ **套不上 —— 不是不想做，是沒有連結。**

### ⚠️ 而「要做就得違反 `JV7`」這件事要寫出來

```
若日後真的要標，只有兩條路：
甲 存來源 id  => **違反 JV7**（症狀：改完存檔關掉，下次打開才變回來）
乙 另外開一張「這張傳票參照了哪些支出」的關聯表，**與摘要文字無關**
   ✅ 它不會回頭覆蓋使用者打的字
   ⚠️ 而它要有人去維護（使用者改了摘要，關聯還在）
      => 那個關聯的語意變成「**他當初點過什麼**」不是「這張傳票記了什麼」
      🔑 而那兩件事不一樣 —— **不要用同一個欄位表示**
```
📌 ⇒ 本規格**不做標記**，而把乙寫下來當作日後的路。

---

## §7 ⚠️ 新增端點的**路由順序坑**（A 點名）

`vouchers.py` 在那條動態 GET 之前有一段寫在碼裡的警告：

> 要加**靜態** GET 路徑的人：**宣告在這一行之前**。
> 🔑 **422 讀起來像「我參數傳錯了」，而實際是「這條路還沒做」** ——
> 查的人會去翻自己的呼叫端，而問題在這個檔案的行號順序上。

```
🔴 JV21 若新增 `GET /api/vouchers/case-expenses`（或任何靜態路徑）
   => **必須宣告在 `@router.get("/{voucher_id}")`（:398）之前**
⚙️ 而驗收要有一題：打那支新端點**不可以回 422**
   🔑 釘 422 不是釘 404 —— 那才是這個坑的症狀
```

📌 **而更好的做法是不新增端點**：`summary-sources` 已經吃 `quote_no` 參數，
多回一個頁籤的資料即可 ⇒ **連這個坑都不必踩**。

---

## §8 驗收（`AC1`）

```
① 後端  summary-sources 多回一個「支出項」頁籤，
        來源沿用 SOURCE_TYPES 的支出子集，金額用 grandTotal 不用 total_amount
        閘門維持 _require_voucher_access
② 前端  第三個頁籤；點一筆 => 文字寫進當前行的摘要，**之後可以自由編輯**
③ 頁面  ⓐ 用 `MQ-202608-007`（4 筆派工）=> 支出頁籤列出 4 筆
           金額分別 187,992／63,000／16,275／**2,500**
           🔑 最後那筆 items=0、只有人員 —— 用 total_amount 會變成 **0**
        ⓑ 用 `MQ-202608-003` => 額外支出 1 筆 NT$3,500
        ⓒ 帶入後**改掉那段文字** -> 存檔 -> 關掉 -> 重開
           => **仍然是他改的那段**（`JV7` 的驗收）
        ⓓ 新端點（若有）**不回 422**
        ⓔ 沒有 cashier／finance 模組的帳號 -> **403**
```

---

## §9 我沒查什麼

```
① ~~「案件內的案件」沒有答案~~ => ✅ **使用者裁三層**（§3）
   🔴 **而第三層的資料形狀我還沒寫**（A 指示排在 `cashier` 之後）
   ⇒ 本規格現在**只涵蓋前兩層**，B 不可以照它做第三層
② `invoice_voucher`（開票申請）算不算支出 —— **我判它不算**（開給客戶的票），
   而**沒有查它的金額方向** ⇒ 待確認
③ ~~canSeeFinancial() 的寬窄不一致要 A 裁~~ => ✅ **使用者裁：有傳票權限就看得到**（§5）
④ 摘要文字的**長度上限** —— 沒查那一格有沒有限制，而帶入一筆長品名可能爆版
⑤ 一筆支出**被帶入兩次**（同一張傳票的兩行）會怎樣 —— 沒查，而那是使用者
   可能會做的事（分錄借貸兩行）
⑥ `material` 那一種的「金額」我寫 `qty × unitCost`，而**沒有確認**
   `data_json.materials[i]` 的實際欄位名 ⇒ B 開工前要先看一眼
```
