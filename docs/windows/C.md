# 視窗 C — 驗證常駐（只有 C 寫這一份）

> 開工順序：`MULTIWIN-PROTOCOL.md` → `docs/windows/STATE.md` → 這份。
> **你的角色：寫測試、跑測試、瀏覽器實測。你不寫任何產品程式碼。**

---

## 你能動的

- `backend/tests/**`

## 你不能動的

- `backend/**`（`tests/` 以外）、`frontend/**` ← 那是 B 的。
  **發現產品碼有 bug，不要自己修** —— 寫在〈給彙整〉，由 A 派給 B。
- 根目錄 `*.md`、`docs/**` ← 那是 A 的（**除了這一份**）

---

## 你的兩個責任（順序不能顛倒）

### ① 開發單一出來，先把驗收測試寫成紅的

**「紅不起來」＝這題沒在驗東西，立刻退回 A，不要往下走。**

紅完之後在下面〈紅在哪〉填：哪一題、紅在哪一行、錯誤訊息是什麼。
B 要靠這段知道「綠」的定義。

### ② B 說綠了之後，做反向驗證

**把 bug 放回去，確認那一題精準變紅、其餘仍綠。**

這一步不可省。理由：
- 觀測點要挑「**成功後才會被寫入**」的下游欄位，不是自己設的值
- 「結束碼非 0」不等於「那一題紅了」，**也可能是你改壞了別的東西** ——
  要同時檢查紅的是不是你指名的那一支測試
- 突變本身也會寫錯，而寫錯的樣子跟「被測對象沒問題」一模一樣。
  **「突變沒抓到」的第一個假設要是「突變寫錯了」**

---

## 跑測試的固定指令

```bash
cd backend
python -m pytest tests/ --basetemp=C:/Users/hichan/AppData/Local/Temp/motrix-pytest-C -q
```

⚠️ **`--basetemp` 不可省**：不帶它，測試全過也會回非 0；而且 `%TEMP%` 每跑一次留
1～2 GB 不自動清（2026-09-14 曾清出 190 GB）。

⚠️ **`--basetemp` 的路徑要固定是 `-C` 結尾這一個，不要用時間戳或 `$(date)`** ——
共用工作目錄，會動的名字會指到別人的東西。

🔴 **兩個視窗不可以共用同一個 `--basetemp` 路徑（連單檔測試都不行）。**
pytest 會把 `--basetemp` 指到的目錄**整個刪掉重建**，時機是**第一次有測試用到
`tmp_path`／`tmp_path_factory`**（不是 session 一開始 —— 完全不碰 tmp_path 的 run
什麼都不會刪，這也是為什麼它很難重現）。本專案的 `conftest.py::_app` 幾乎一啟動
就呼叫 `tmp_path_factory.mktemp()`，所以實際上等同「一跑就刪」。

於是第二個 pytest 會把第一個正在用的 `motrix_app0/motrix_erp.db` 刪掉，
第一個 run 接著爆 `sqlite3.OperationalError: unable to open database file`。

**失敗的樣子會裝成產品 bug**：紅的是一支跟你完全無關的測試（我這次是
`test_reports_sales_owner_2026_09_14.py::test_achievement_uses_same_attribution_as_performance`），
單獨跑它 100% 綠。⚠️ **協定 §5 第 5 條只規範「全量回歸」，擋不住這個** ——
我那次是**單檔測試**撞上別人的全量回歸，跑單檔的人不會覺得第 5 條在講自己。
詳見〈給彙整〉第 9 點。

全量回歸基準：**136 檔、1,084 題**（2026-09-21 `--collect-only` 實測，不含本輪新增的 27 題）。
少於這個數字就是有東西沒被跑到。本輪加上去之後是 **137 檔、1,111 題**。

⚠️ **基準只能用 `pytest --collect-only` 算，不可以用 `grep -c "def test_"`。**
原本這裡寫的 `1,034` 就是 grep 出來的，差的 50 題是 7 個檔的 `parametrize` 展開。
（A 已於 2026-09-21 複驗、改掉 `MULTIWIN-PROTOCOL.md` §4 與 `SELLABLE-AND-MOBILE-SPEC.md` §0，
並把這條寫成協定。見〈給彙整〉第 4 點。）

---

## 本輪狀態

- **輪次**：**第 5 輪** —— 細線 6 第 2 步（排程）＋第 5 步（通知）＋四個結轉項
- **狀態**：🟥 **②完成，等 B 寫碼**
  - 新檔 `test_tender_notify_2026_09_21.py`：**24 題，22 紅 2 綠**
  - 第 4 輪檔（結轉 R1／R2 已套用）：**49 題，1 紅 48 綠**
- **契約來源**：`docs/windows/STATE.md` §3 **`7bec22d`**（S1–S5／N1–N13／R1–R5）

### 綠的那幾題是**對的**，不是漏驗

| 綠 | 為什麼 |
|---|---|
| `test_r3a`（User-Agent）／`test_r3b`（逾時 15 秒） | B 在第 4 輪就做對了，**只是先前沒有守門**。R4 的突變會證明它們真的在驗東西 |
| R2 拆出來的三題形狀檢查 | B 的實作三道都在，拆開只是讓⑤分得出「少了哪一道」 |

### 紅的那一題（第 4 輪檔）是**刻意的**

`test_06e` 紅在 `KeyError: 'published_at'` —— **R1 是「B 與 C 同一輪一起改」**，
我先改測試，B 還沒改解析 dict 的鍵名。**這正是紅燈該出現的位置。**

### 🔴 這一輪最重要的一題：S5

⑦ 抓到 `run_scan()` 在 production 沒有排程呼叫者。**如果只寫 S1～S4，
同一個 bug 會再活一次** —— S1 的「排程觸發」是**測試自己做的**，
所以 `main.py` 從來沒啟動排程器的話，S1～S4 **全部照樣綠**。

> 🔑 **每一輪都往上挪一層，而最上面那一層永遠沒有人驗。**

S5 在**子行程**裡 import `main`（不設 `MOTRIX_DISABLE_SCHEDULERS`），
斷言 `schedule_tender_scan` 被呼叫 1 次。

⚠️ **我原本的做法是錯的，A 抓到了**：直接 import `main` 會讓
`archive._schedule_daily()` 跑一次**真的備份**（SQLite 整庫快照 ＋ 41 張表匯出）。
正確做法是**先把 `archive` 塞進 `sys.modules` 成記錄器、再 `import main`**
（`main.py:22` 是 `from archive import ...`，事後 patch 打不到）。
✅ 實測子行程 `returncode=0`、紅在 `被呼叫 0 次，應為 1` —— **harness 本身是對的**。
✅ 附帶好處：這題同時保護既有的五個排程。

---

### 🔴 我沒有讀實作



⑤ 的六個突變接縫是用 `dir()`／`inspect.signature()`／讀模組常數值＋**黑箱探測**
找出來的。**沒有用 `inspect.getsource`，沒有開過那三個檔的內容。**

### ② 46 題全綠（B 的實作 `7309864`）

我在第 3、4 步釘的兩件事 B 都做對了：四個表頭字樣**去標籤→unescape→去空白後**才比對；
標案名稱從 `pageCode2Img("…")` 的引數取，不是 strip tags。

### ⑤ 反向驗證（§3 六題，全部發火）

| 突變 | 結果 |
|---|---|
| 基準線（不植入） | **46 全綠** |
| 排除詞拿掉 | **只有 `test_02` 紅** |
| 必要欄位補值 | `test_06b`（目標）＋ `test_07` ＋ `test_09b` |
| 總開關永遠 True | **只有 `test_08` 紅** |
| `recognised` 看筆數 | **只有 `test_07c` 紅** |
| 形狀驗證拿掉 | **只有 `test_06j` 紅** |
| 抓取失敗記成不認得 | **只有 `test_07f` 紅** |

「必要欄位補值」多紅兩題**是可解釋的**：`test_07`（dropped 過半）與 `test_09b`
（fetch_log 的 dropped 數）用的都是同一個「缺機關」fixture。補了機關值，
那兩題的前提就沒了。**不是測試不精準，是它們共用同一個壞法。**

#### ⚠️ 形狀驗證那個突變我寫錯三次，而三次的失敗方式不一樣

| 版本 | 結果 | 錯在哪 |
|---|---|---|
| 第一版 | **14 題紅** | 把 `_ROC_DATE_RE` 換成沒有捕獲群組的 regex，日期解析整個爆掉 |
| 第二版 | **46 全綠，沒發火** | 只放寬 `_CJK_RE` —— 接縫找錯 |
| 第三版 | 仍然沒發火 | 加上「壞日期當空白」，還是不夠 |
| 第四版 | **只有 `test_06j` 紅** ✅ | |

黑箱探測後才知道，**B 的「形狀驗證」不是一道檢查，是三道**：
① 機關那格要含中文 ② 機關那格**不可以長得像日期** ③ 截止日那格非空時要解得出日期。
對調的那一列 ①②③ 全都撞到。

> 🔑 **爆太多與沒爆到，第一個假設都是「突變寫錯了」。**
> 我們原本只記了前半。**「沒發火」比「爆太多」更危險** ——
> 爆太多會逼你去看；**沒發火看起來就像「這題本來就很穩」，而它什麼都沒證明。**

> 🔑 **要拿掉一個防護，得先知道那個防護由幾件事組成。**「一個名字」常常對應多道檢查。

### ⑥ 全量回歸（兩次）

**第一次**（B 修備份之前）：`1 failed, 1119 passed, 55 skipped` = 1,175
→ 紅的是 `test_every_table_is_either_backed_up_or_explicitly_excluded`，守門精準命中。

**第二次**（B 補工 `8a3612d` ＋ 我的 `e2f5c9c` 之後）：

```
1120 passed, 55 skipped in 1118.39s (0:18:38)
```

| | 題數 |
|---|---|
| 基準（§3 寫死） | 1,129 |
| 本輪 `--collect-only` | **1,175** |
| 本輪實跑（1120 ＋ 55） | **1,175** |

＝ 1,129 ＋ 本輪新檔 46。**收集數 ＝ 實跑數，0 失敗。**

⚠️ **A 指定整支重跑，不准只跑受影響的那一支。** 他的理由值得抄下來：
**「這個改動顯然不會影響別的」正是這套紀律存在要防的那句話**，
而重跑的代價是 19 分鐘且沒有人被擋住。
🔑 **當「照規矩做」的代價接近零的時候，去論證例外是純粹的負收益。**

### 握手（A 於 `2ae450a` 改成兩個檢查）

```
(甲) B 真的停了 —— 排除 backend/tests
     git log 8a3612d..e2f5c9c -- backend frontend ':!backend/tests'   →  (空) ✅

(乙) 跑的期間沒人動 —— 不排除任何東西
     git diff e2f5c9c aff89a4 --name-only -- backend frontend         →  (空) ✅
     工作樹（跑後）                                                    →  乾淨 ✅
```

⚠️ **舊的單一檢查會把我自己誤判成「B 還在動」**：`backend/tests/` 在 `backend/` 底下，
我在等 B 的空檔補自己的測試（`e2f5c9c`），舊判準就會紅。**我實跑過對照組確認**：
不排除 `backend/tests` 時，`git log` 確實列出我自己那一筆。

🔑 **一個檢查想涵蓋兩件事，而那兩件事「該不該含測試」的答案是相反的：**
「B 停了沒」要**排除**測試（C 補測試是正常且必要的）；
「跑的期間有沒有人動」**不能排除**測試（**測試本身也是受測對象**）。

⚠️ 這跟 A 那次把 `--basetemp` 的「分視窗且分用途」壓成一個維度是同一個毛病，
**而且這次壓扁的是他自己寫的規則**。

---

## 【第 3 輪 · 已結案】跑出來的結果（4 紅 33 綠）

```bash
cd backend
python -m pytest tests/test_procurement_leadtime_2026_09_21.py tests/test_system_audit_2026_09_14.py   --basetemp=C:/Users/hichan/AppData/Local/Temp/motrix-pytest-C-adhoc -q
```

**4 個紅全部是同一件事**，而且**不是功能沒做**：

| | |
|---|---|
| §3 明文規定 | `lead_time_days`（snake_case） |
| B 實際回 | **`leadTimeDays`**（camelCase） |

紅的是 `test_02`、`test_03`、`test_04[7]`、`test_04[30]` —— 全都卡在讀不到那個欄位。

**我沒有改測試去遷就實作。** 我把紅燈訊息改成會自己說明是哪一種不一致，
現在跑出來直接寫著「不要把這一行改成 camelCase 來讓它變綠」。詳見〈給彙整〉第 11 點。

其餘全部正常：`eta` 回 `2026-09-28`（＝今天 ＋7，正確）、`status`／`orderedAt`／
`receivedAt` 都在，四個邊界燈號全對。

---

## 第 3 輪 · ⑤反向驗證（四個突變，每一個都精準命中）

**基準線**：套上欄位名別名（把命名那一層拿掉）之後 **17 全綠**。
⚠️ 這一步是必要的：**已經紅的題目沒辦法拿來做突變測試** —— 突變要證明的是
「這題會從綠變紅」。別名只活在那個行程裡，測試檔本身沒有被改成遷就實作。

| 突變 | 打在哪個接縫 | 結果 |
|---|---|---|
| 料號／供應商優先序對調 | `resolve_lead_time` 兩個參數互換 | **只有 `test_02` 紅**（1 紅 16 綠）|
| 前置時間未知回 `0` 不回 `None` | `resolve_lead_time` 包一層 | **只有 `test_03` 紅**（1 紅 16 綠）|
| 狀態轉移永遠放行 | `validate_transition` → `None` | **只有 `test_06` 紅**（1 紅 16 綠）|
| 開新一輪時沿用舊的 `ordered_at` | `effective_cycle` 只改一個欄位 | **只有 `test_07c` 紅**（1 紅 16 綠）|
| （8c）把一個刻意略過的欄位從清單移除 | `_BACKUP_OMITTED_ON_PURPOSE` | **只有 `test_backup_export_covers_every_column` 紅** |

§3 只要求兩個突變（條件 2、條件 3），我多做了條件 6、7c 與 8c ——
那三個是 A 在訊息裡特別點名、或我自己認為最容易假綠的。

### ⚠️ 第四個突變我第一次寫錯了，而錯的樣子是「12 題紅」

`keep_ordered` 第一版我寫成 `return stored_row`，結果 **12 題紅**。

**第一個假設要是「突變寫錯了」，不是「測試不精準」** —— 查下去果然是我錯：
`stored_row` 在全新料號上是 `None`，等於把每一筆的狀態計算整個打掉。
我用一個只印輸入輸出的探針查出 `effective_cycle` 的真實形狀
（回 `{status, ordered_at, ordered_by, received_at, received_by, new_cycle}`），
改成**只動 `ordered_at` 一個欄位**之後就精準了。

**突變的爆炸半徑太大，本身就是突變寫錯的訊號。**

### 做法：完全沒有碰 B 的檔案

四個突變都是 pytest plugin（`scratchpad/mut_proc.py`）裡的 monkeypatch，
磁碟上一個位元組都沒動。B 現在 idle 待命，但隨時可能被 A 派回去動那些檔。

⚠️ `routers/inventory.py` 是 `from helpers.procurement import ...` **by value** 綁進來的，
只換 `helpers.procurement` 上那一份沒有用 —— 跟 `conftest.py` 開頭記的是同一個坑。
plugin 裡有 `_patch()` 守門：**接縫一個都沒換到就當場 raise**，
因為「突變沒植入」跟「突變沒被抓到」長得一模一樣。

---

## 第 3 輪 · ⑥全量回歸（A 於 12:17 核准後才跑）

```
1074 passed, 55 skipped, 22 warnings in 1023.43s (0:17:03)
```

**合計 1,129 題、0 失敗。**

| | 題數 | |
|---|---|---|
| 上一輪基準 | 1,111 | 協定 §4 ⑥「不可少於上一輪」 |
| 本輪 `--collect-only` | **1,129** | ＝ 1,111 ＋ 新檔 17 ＋ 8c 那 1 |
| 本輪實跑（1074 ＋ 55） | **1,129** | 收集數 ＝ 實跑數，**沒有題目被靜默跳過** |

⚠️ 「收集數 ＝ 實跑數」要分開對一次。只看「passed 比上一輪多」的話，
**一批題目被 skip 掉也會長得像通過**——skip 不是 fail，它不會讓任何東西變紅。

### 受測對象在這 17 分鐘裡沒有變（跑前 12:17:10 → 跑後 12:34:29）

七個檔的 sha256 **逐字元相符**：`helpers/procurement.py`／`routers/inventory.py`／
`main.py`／`db.py`／`helpers/licensing.py` ＋ **我自己的兩個測試檔**。

視窗 B 另外給了一份**獨立**的跑前雜湊，四個產品碼跟我的逐字元相符 ——
「量的是這一棵」有兩份互不依賴的來源。

⚠️ **那兩個測試檔是 B 提醒我才加進去的，這是本輪最好的一次互相檢查。**
我做這道指紋，是因為上一輪 A 在我跑回歸跑到一半時改了 `main.py`；
於是我防的是「**別人**動**產品碼**」——而這一輪 `git status` 整棵樹只有一個檔是髒的，
就是**我自己的測試檔**。最可能動的東西，正好不在我的清單裡。

> **防護是照著上一次的傷口長出來的，所以它的盲側，就是上一次沒有受傷的那一側。**

而且漏掉的後果特別惡劣：結果會照常印出漂亮的數字，指紋也會回報「全部相符」，
因為變動的那個檔**根本不在清單裡**。
**一道回報「全部相符」的守門，比沒有守門更容易讓人停止懷疑。**

### 量測期間有人碰過樹，查證後不重跑

視窗 B 自首：在量測視窗內於 repo 根目錄建了又刪了 `.fp_probe_tmp`（存在約兩秒）。
**我自己查過才決定不重跑**，不是因為 B 說沒事：
`grep` 過 `backend/tests/*.py` 全部的 `glob`／`listdir`／`iterdir`／`rglob`／`scandir`／`walk(`，
**沒有任何一題枚舉 repo 根**，全部指定子目錄；該檔是根目錄 dotfile 且已不存在。

### 下一輪起改用不靠手列清單的指紋

```sh
fp() { { git ls-files -z -- backend frontend | xargs -0 -r sha256sum
         git ls-files --others --exclude-standard -z -- backend frontend | xargs -0 -r sha256sum
       } | sha256sum; }
```

**本輪基準值：`7dec671a735d3d84`**（`backend/` ＋ `frontend/` 的工作樹內容）。

⚠️ **這個值是給下一輪當基準用的，不是本輪的前後比對依據** ——
`fp()` 是我在本輪跑到一半才想出來的，跑前沒有取過。本輪的前後比對用的是上面那七個手列檔。

為什麼要換掉手列清單（視窗 B 的論點，我同意）：
**只要是人列的，就有沒列到的那一格，而沒列到的那一格永遠不會讓指紋變動。**
那不是「更小心就會好」的機率問題，是**不可觀測性**。

為什麼不直接用 B 原版的 `git rev-parse HEAD` ＋ 全樹 `git status`：
**A 在任何一次 20 分鐘的回歸裡幾乎一定會 commit `docs/`**，而 pytest 不讀那裡。
誤報的代價不是零——**一道會在無害情況下亮紅燈的守門，很快會被當成雜訊，
然後它真的該叫的那一次也沒人理。** 所以範圍照「**pytest 實際會從磁碟讀到什麼**」劃，
不是照「git 知道什麼」劃。

六條路徑我在**丟棄式 repo** 裡逐條實測過（改 `docs/` 不變、commit `docs/` 不變、
改 `backend/` 追蹤檔變、新增未追蹤檔變、改未追蹤檔**內容**再變、刪掉精準回到前值）。
用丟棄式 repo 而不是正在被量的那一棵——那是 B 那個錯誤的正確版本。

---

## 【第 1 輪 · 已結案】紅在哪（①之後填，B 靠這段知道「綠」的定義）

**指令**（就是上面〈跑測試的固定指令〉那一條，只是指定單檔）：

```bash
cd backend
python -m pytest tests/test_licensing_core_2026_09_21.py --basetemp=C:/Users/hichan/AppData/Local/Temp/motrix-pytest-C -q
```

**現況：`8 failed, 19 errors` —— 27 題全紅，沒有任何一題是綠的。**

紅的原因只有一個，正是預期的那個：

```
AssertionError: backend/helpers/licensing.py 還不存在（或 import 失敗）：
ImportError("cannot import name 'licensing' from 'helpers'")。需要的名字見本檔開頭的契約表。
                                        tests/test_licensing_core_2026_09_21.py:69
```

`failed` 跟 `error` 的差別只是那一題撞在 test 本體還是撞在 fixture 裡，原因同一個。
**唯一例外是 `test_08a`** —— 它不碰 `helpers.licensing`，紅在
`/api/license/status` 這條 route 根本沒註冊（見〈給彙整〉第 3 點）。

### 27 題對應 STATE §3 的 11 條驗收條件

| 驗收條件 | 測試 | 題數 |
|---|---|---|
| 1 有效金鑰 → `ok` | `test_01_valid_license_verifies` | 1 |
| **2 竄改 → `bad_signature`** | `test_02_...[customer/tax_id/modules/expires]`、`test_02b_tampered_signature`、`test_02c_signed_by_another_key` | **6** |
| 3 換機器 → `machine_mismatch` | `test_03_other_machine_is_machine_mismatch` | 1 |
| 4 過期 → `expired` ＋ `days_left` 為負 | `test_04_expired_license_reports_expired` | 1 |
| 5 檔案不存在 → `missing`，不丟例外 | `test_05_missing_license_file`、`test_05b_missing_blob` | 2 |
| 6 亂碼／截斷 → `malformed`，不丟例外 | `test_06_...`（6 種壞法）、`test_06b_truncated_valid_blob` | 7 |
| 7 指紋穩定 | `test_07_..._in_process`、`test_07b_..._across_processes` | 2 |
| 8 未登入 401／登入後拿得到欄位 | `test_08a_requires_login`、`test_08_returns_customer_and_days_left` | 2 |
| **8b 沒金鑰 → 200 ＋ `customer:null`** | `test_08b_status_without_license_is_200_and_null_customer` | 1 |
| **9 dev 私鑰簽 → `env == "dev"`** | `test_09_dev_signed_license_reports_env_dev` | 1 |
| **10 `_PUBKEY_PROD` 是空的不丟例外** | `test_10_empty_prod_pubkey_does_not_raise` | 1 |
| **11 status 回傳有 `env` 鍵** | `test_11_status_response_includes_env_key` | 1 |
| （補強）裝了金鑰的那一路 | `test_08c_status_endpoint_reports_installed_license` | 1 |

另外 **9 處 `env` 斷言直接加在既有測試裡**（不另開題，所以總數仍是 27）：
`bad_signature`／`missing`／`malformed` → `env is None`；
`machine_mismatch`／`expired` → **`env` 仍是 `"dev"`**（簽章驗過了，擋下來的是別的檢查）。

### 「綠」的定義（B 要做到的契約）

以 `STATE.md` §5〈本輪最終契約〉為準，測試檔開頭有同一張表：

| 名字 | 形態 | 備註 |
|------|------|------|
| `_PUBKEY_DEV` | `bytes` | 開發公鑰。**驗證時才載入**，不可以在 import 時就解析成 key 物件 |
| `_PUBKEY_PROD` | `bytes` | **本輪＝`b""`**。空的要略過，`load_pem_public_key(b"")` 會丟 `ValueError` |
| `LICENSE_PATH` | `str` | 預設 `backend/license.key`，**每次呼叫才讀** |
| `machine_fingerprint()` | `-> str` | 16 碼小寫 hex，**跨行程要一樣** |
| `sign_license(payload, private_key_pem)` | `-> str` | blob ＝ base64(JSON)，`sig` 放在 JSON 裡面 |
| `verify_license(blob=None)` | `-> LicenseStatus` | `blob` 省略或 `None` → 改讀 `LICENSE_PATH` |

`LicenseStatus` 用 dict 或 dataclass 都可以（測試兩種都讀得到），**七個**欄位：
`valid` / `reason` / `env` / `customer` / `modules` / `expires` / `days_left`。

`backend/routers/licensing.py`：`GET /api/license/status`
（**端點路徑與 `backend/license.key` 檔名不跟著改名**，A 已裁決）。

**檢查順序**：`malformed → missing → bad_signature → machine_mismatch → expired`。
簽章一定要排在 `machine`／`expires` 之前 —— **簽章驗過之前 payload 沒有任何一個欄位可信**。

## 【第 1 輪 · 已結案】反向驗證結果

> 格式：把什麼放回去 → 哪一題紅了 → 其餘幾題仍綠
> **只寫「通過」等於沒驗。**

**基準**：突變前 `27 passed`（2026-09-21 09:59，對 B 當下的 `helpers/licensing.py`）。

### ⚠️ 做法：突變**不改 B 的檔案**，用 `-p` 注入到記憶體

視窗 B 正在同一個工作目錄上做第 2 輪 —— 實測 `helpers/licensing.py` 在我做反向驗證
的這 20 分鐘內就被 B 改過一次（09:36 → **09:59:13**，20,074 → 23,493 bytes）。
**改他的檔再改回來，中間他只要存一次檔，我的「還原」就會蓋掉他的東西** ——
失敗的樣子會是〈共用空間裡不要用會動的名字〉那條：「成功了，只是動到別人的」。

所以三個突變都是 pytest plugin：
- 突變 1 用 monkeypatch（`scratchpad/mut_sig.py`）
- 突變 2／3 讀原始碼、改字串、`compile` 進新的 module 物件再塞回 `sys.modules`
  （`scratchpad/mut_src.py`）—— **磁碟上的檔案一個位元組都沒動**

⚠️ `mut_src.py` 裡有 `hits != 1` 就 `raise` 的守門。`str.replace` 對不上是**靜默無效**的，
而「突變沒被抓到」跟「突變根本沒植入」長得一模一樣 —— 那是最難分辨的假陰性。

---

### 突變 1 · 簽章驗證無條件通過 → **7 題紅、20 題綠** ✅

```
FAILED test_02_tampered_payload_is_bad_signature[customer-冒名的公司]
FAILED test_02_tampered_payload_is_bad_signature[tax_id-00000000]
FAILED test_02_tampered_payload_is_bad_signature[modules-value2]
FAILED test_02_tampered_payload_is_bad_signature[expires-2099-12-31]
FAILED test_02b_tampered_signature_is_bad_signature
FAILED test_02c_license_signed_by_another_key_is_bad_signature
FAILED test_10_empty_prod_pubkey_does_not_raise
7 failed, 20 passed
```

斷言差異是 `- bad_signature` / `+ ok` —— **觀測到的是 `verify_license()` 的回傳值變了**，
不是測試自己算出來的東西。條件 2 的 6 題全紅，確認它們真的在驗簽章。

**`test_10` 是第 7 題，這是預期內的**：它自己內含一格「別人私鑰簽的 → `bad_signature`」。
不是多紅了一題，是它本來就同時在驗那件事。

**為什麼不用「把驗證整段註解掉」**（原開發單的寫法）：`env` 的值是從「**哪一把公鑰驗過**」
推出來的，整段拿掉會連帶弄壞 `env`，於是 `test_03`／`test_04`（機器不符／過期，
兩題都斷言 `env == "dev"`）會**跟著紅**，「只有條件 2 精準變紅」就不成立了 ——
那是突變選錯，不是測試寫錯。
改成「讓 `.verify()` 什麼都不做」之後，`env` 照樣被指派成 `"dev"`，
**`test_03`／`test_04` 實測維持綠**，突變就落在簽章這一件事上。

### 突變 2 · 機器指紋檢查永遠通過 → **只有 `test_03` 紅**（1 紅 26 綠）✅

```
[突變已植入] machine：if (not isinstance(licensed_to, str) or this_machine is None → if False
FAILED test_03_other_machine_is_machine_mismatch
1 failed, 26 passed
```

### 突變 3 · 過期檢查永遠通過 → **只有 `test_04` 紅**（1 紅 26 綠）✅

```
[突變已植入] expiry：if days_left < 0: → if False
FAILED test_04_expired_license_reports_expired
1 failed, 26 passed
```

### 結論

三個突變各自打在不同的檢查上，**每一個都精準點亮對應的那幾題、其餘全綠**。
突變移除後複驗 `27 passed`。條件 2、3、4 確認不是假綠燈。

---

## 全量回歸結果（⑥）

**`1056 passed, 55 skipped` ＝ 1,111 題，0 失敗**（2026-09-21 09:35→09:55，20 分 46 秒）。
＝ 既有 **1,084** ＋ 本輪 **27**，數字對得上，沒有少跑。

### ⚠️ 兩件必須跟著這個數字一起讀的事

**① 第一次跑出來的 `1 failed` 是我自己造成的，不是既存紅燈。**
紅的是 `test_reports_sales_owner_2026_09_14.py::test_achievement_uses_same_attribution_as_performance`，
錯誤 `sqlite3.OperationalError: unable to open database file`。
原因是我在那個回歸跑到 86% 時，用**同一個固定 `--basetemp`** 跑了單檔測試，
而 pytest 每次 session 開始會把 `--basetemp` 整個刪掉重建 —— 等於把它正在用的
`motrix_app0/motrix_erp.db` 刪掉了。乾淨重跑後該題全綠。詳見〈給彙整〉第 9 點。

**② 這個數字是「B 的第 1 輪」的，不是「現在這一刻」的。**
B 已經在做第 2 輪（`helpers/licensing.py` 09:59:13 又長了 3,419 bytes，
已加入 `LICENSE_GATE_ENABLED = False`，middleware 還沒掛）。
我另外跑了一次涵蓋 B 當下狀態的全量回歸並**在前後各取一次 sha256**，見下。

### 第二次（10:01:10 → 10:21:50）：同樣 1,111 全綠，但指紋抓到受測對象跑一半變了

```
1056 passed, 55 skipped in 1225.91s (0:20:25)
```

**sha256 前後比對的結果**——這就是我在〈給彙整〉第 10 點建議的做法，它當場就抓到了：

| 檔案 | 跑之前 | 跑之後 | |
|------|--------|--------|---|
| `helpers/licensing.py` | `2de54e7f…` | `2de54e7f…` | ✅ 沒變 |
| `routers/licensing.py` | `4a01a2fa…` | `4a01a2fa…` | ✅ 沒變 |
| `main.py` | `b6a10437…` | **`00d75c59…`** | ❌ **10:11:25 變了** |
| `tools/issue_license.py` | `bf83d8dc…` | **`939a980d…`** | ❌ 10:01:54 變了 |

B 在 10:11:25 把 `license_gate_middleware` 掛進 `main.py`（第 2 輪的活）。
pytest 在 session 一開始就 `import main`，所以**這次跑的是掛 middleware 之前的 `main.py`**。

**所以這個數字能證明什麼、不能證明什麼，要講清楚：**

- ✅ **第 1 輪的兩個交付物（`helpers/licensing.py`、`routers/licensing.py`）
  在整整 20 分鐘裡一個位元組都沒動** ⇒ **第 1 輪的⑥成立**，1,111 全綠是真的。
  （`helpers/licensing.py` 這一版已經含第 2 輪的 `LICENSE_GATE_ENABLED = False`，
  也就是說「總開關關著時既有功能全綠」這件事已經被驗到了 —— 那正是開發單第 12 題的前半。）
- ❌ **B 的授權守門 middleware 完全沒被這次回歸跑到。** 它 10:11 才進 `main.py`。
  第 2 輪的⑥要重跑，不能拿這個數字充數。

**若沒有前後取指紋，我會把這個 1,111 當成「含 middleware 的全綠」回報出去** ——
而那是假的。這是第 10 點那個建議的實證：它不依賴任何人記得停手，機器自己判得出來。

## 給彙整（A 會收進 STATE.md）

> 發現的產品碼 bug（**不要自己修**）、驗收條件寫不出測試、規格有歧義，寫在這裡。

### ✅ 上一批四項已由 A 於 2026-09-21 全數裁決（STATE §5），這裡只留結論

| # | 我提的 | A 的裁決 | 我做了什麼 |
|---|--------|---------|-----------|
| 1 | `verify_license(blob=None)` 省略時讀 `LICENSE_PATH` | ✅ 採納，照我釘的 | 不用改 |
| 2 | 要求 B「不快取」 | ✅ 准，已寫進本輪契約 | 不用改；⚠️ A 提醒第 3 步掛進 middleware 後會變成**每個 request 讀一次檔**，屆時若加快取要用 mtime 判定，**不可以改成 import 時一次性載入**，否則測試又會變成只有某台機器跑得動 |
| 3 | middleware 對未註冊路徑也回 401 | ✅ A 已複驗屬實，升級成通則寫進 STATE §3 | 不用改 |
| 4 | 基準 `1,034` 應為 `1,084` | ✅ A 算錯（用 `grep -c "def test_"`），差的 50 題是 7 個檔的 `parametrize` 展開 | 已改本檔；A 已改協定與 SPEC |
| 5 | 我自己下的五個判斷 | ✅ 全部同意，一個都不要改 | 不用改 |

**本輪已照 A 的最終契約改完**：改名 3 處（`license` → `licensing`）、補 4 題（23 → **27**）。

---

### 6 · 🟡 `test_10` 裡有一條**刻意會紅**的絆線，A 要知道它的存在

`test_10_empty_prod_pubkey_does_not_raise` 最後一行：

```python
assert _PUBKEY_PROD_AT_IMPORT == b""
```

**正式公鑰一旦產生，這一題就會紅。那是刻意的。**
紅了不是測試壞了，是在擋 STATE §4 第 1 項 —— 私鑰保管、備援副本、外洩處置流程
還沒定案之前，不應該有人默默塞一把公鑰進去就當作可以出貨。
錯誤訊息裡已經寫明「請回 STATE §4 第 1 項定案，再改這一行」。

這正是 A 說的「做成結構可偵測，不靠人記得」。**若 A 覺得這條太硬，跟我說，我拿掉。**

⚠️ 它讀的是 **import 當下拍下來的值**（`_PUBKEY_PROD_AT_IMPORT`），
不是 monkeypatch 之後的值 —— 讀 patch 後的值等於驗自己設的值，就是假綠燈。

### 7 · 🟠 `env` 與簽章驗證是**綁在一起**的，第⑤步反向驗證要用對的突變

`env` 的值是「**哪一把公鑰驗過的**」推出來的，所以它天生依賴簽章驗證。
如果第⑤步的突變是「**把簽章驗證整段註解掉**」，那 `env` 會跟著壞，
`test_03`／`test_04`（機器不符／過期，兩題都斷言 `env == "dev"`）會**跟著紅**，
於是「只有條件 2 精準變紅」就不成立了 —— 但那是突變選錯了，不是測試寫錯。

**我會用的突變是：讓簽章驗證「無條件回 True」**（不是拿掉整段）。
這樣 `env` 仍然被指派成 `"dev"`，`test_03`／`test_04` 維持綠，
只有條件 2 的 6 題變紅 —— 那才是真的在問「條件 2 有沒有觀測到簽章檢查」。

寫在這裡是因為這條會影響 A 怎麼讀第⑤步的回報數字。

### 8 · 🟢 `test_11` 順便驗到了「每次呼叫才讀 `LICENSE_PATH`」

`test_11` 在**同一個行程、不重啟服務**的情況下換掉金鑰檔，第二次請求要看得到新結果。
所以第 3 步若真的加了快取而沒保留 mtime 重讀，**`test_11` 會先紅**，
不用等到第 6 步的到期提醒才發現。這是 A 在裁決 2 裡點出的風險，現在有守門了。

### 9 · 🔴 共用 `--basetemp` 會**刪掉**別人正在用的 DB —— 與 A 查到的 CPU 那件事是兩回事

**A 在 `64fa66f` 裡查的是另一個紅**（`test_semaphore_caps_concurrent_holders`，
CPU 被搶 → `peak` 頂不到 `max_concurrency`），那個診斷是對的。
**但我這邊踩到的是不同的一個紅、不同的機制**，兩件事同一天發生，容易被併成一件：

| | A 查的那件 | 我踩的這件 |
|---|---|---|
| 紅的測試 | `test_semaphore_caps_concurrent_holders` | `test_reports_sales_owner_2026_09_14.py::test_achievement_uses_same_attribution_as_performance` |
| 錯誤 | 斷言 `peak == max_concurrency` 不成立 | `sqlite3.OperationalError: unable to open database file` |
| 機制 | **CPU 競爭**（吵，但沒破壞任何東西） | **檔案被刪掉**（破壞性） |
| 觸發者 | 兩個**全量回歸**同時跑 | **單檔測試**撞上一個正在跑的全量回歸 |

**機制（已實測，不是推論）**：pytest 把 `--basetemp` 指到的目錄**整個刪掉重建**，
時機是**第一次有測試用到 `tmp_path`／`tmp_path_factory`**。

我第一次想驗證時寫了一支不碰 `tmp_path` 的測試，目錄**沒有**被刪 —— 差點據此
認定自己的假設是錯的。換成會用 `tmp_path` 的測試就刪了：

```
之前： bt_demo/motrix_app0/motrix_erp.db
跑一支 test_uses_tmp(tmp_path) --basetemp=./bt_demo
之後： bt_demo/test_uses_tmp0/x     ← motrix_app0/ 整個不見了
```

本專案的 `conftest.py::_app` 幾乎一啟動就 `tmp_path_factory.mktemp("motrix_app")`，
所以實際上等同「一跑就刪」。

**為什麼要寫給 A**：
- **協定 §5 第 5 條擋不住這個。** 它寫的是「**全量回歸**一次只能有一個人跑」，
  而我是跑**單檔測試**撞上別人的全量回歸 —— 跑單檔的人不會覺得那條在講自己。
- 第 5 條的理由寫的是「CPU 也是共用資源」。照這個理由推，會得到
  「機器閒著就可以一起跑」的結論 —— **但刪檔跟 CPU 忙不忙沒有關係**。
- 這一條的失敗模式比 CPU 那條更糟：CPU 競爭讓斷言不成立（吵），
  刪檔是**把另一個 run 的資料庫刪掉**（破壞），而且紅在一支毫不相干的測試上。

**建議 A 把 §5 第 3 條從「一律帶 `--basetemp`」改成「一律帶，而且每個視窗一個專屬路徑」**
（`...-A` / `...-B` / `...-C`，仍是固定名字，不是會動的名字）。
這樣就**不依賴任何人記得先宣告**，也不受「這次只是跑一支測試」的例外心理影響。
第 5 條（宣告 ＋ 一次一個人跑全量回歸）仍然需要，因為它解的是 CPU，那是另一個問題。

### 10 · 🔴 第 2 輪在 C 做完⑤⑥之前就開跑了，於是⑥量的是一個會動的東西

STATE §3 自己寫著「**⛔ C 沒做完⑤⑥之前，第 1、2 步不得標記完成**」——這是對的。
但同一份文件也已經把第 2 輪的單發給 B 了，而 B 立刻就動工了。實測：

| 時間 | `helpers/licensing.py` |
|------|------------------------|
| 09:36:01 | 20,074 bytes（第 1 輪完成） |
| **09:59:13** | **23,493 bytes**（第 2 輪的 `LICENSE_GATE_ENABLED` 已進去） |

**後果是⑥的結果沒有意義可言**：全量回歸要 **20 分鐘**，而受測對象在這 20 分鐘裡會被改。
我第一次跑的那一次，起跑時間（09:35:02）比 `licensing.py` 的最後寫入（09:36:01）還早
**59 秒** —— 我沒辦法證明它載入的是哪一個版本。

**這不是誰做錯了**，是七步的迴圈假設「B 在④之後會停下來等」，而 A 為了不讓 B 閒著
先發了下一輪。兩件事都合理，湊在一起就讓⑥失去意義。

**建議 A 擇一**（我不改協定，那是 A 的檔）：
1. **⑥ 改成「B 宣告本輪停手」之後才跑**，B 在 `B.md` 寫一行「第 N 輪停手，SHA 是 X」，
   C 跑完⑥才解鎖下一輪 —— 最乾淨，代價是 B 要等 20 分鐘。
2. **⑥ 接受「測的是那一刻的樹」，但強制在前後各取一次 sha256**，對不上就重跑。
   我這一次已經這樣做了（`scratchpad/subject_before.txt` / `subject_after.txt`）。
   代價是可能要重跑，好處是 B 不用停。

我傾向 **2**：它不需要任何人記得停手，而且「對不上就重跑」是機器判得出來的條件。
⚠️ 但 2 有個前提：**`--basetemp` 要每個視窗一個**（〈給彙整〉第 9 點），
否則 B 一跑測試就會把 C 的回歸打掉，那比版本漂移更難診斷。

### 11 · 🔴 欄位命名：§3 寫 `lead_time_days`，B 回 `leadTimeDays`（4 題紅，等裁決）

這是本輪唯一的紅，**不是功能沒做**。

**我的判斷是 B 比較對，§3 寫錯了。** 那支端點的既有欄位全是 camelCase
（`safetyStock`／`inStockCount`／`stockLevel`／`suggestedQty`／`estimatedCost`／
`orderedAt`／`receivedAt`），B 跟了既有慣例；§3 看起來是直接把**資料庫欄位名**
抄進了**API 回應規格**。唯一的例外 `part_no` 是既有的歷史遺留。

**但我沒有自己改成 camelCase**，因為 A 的指示是「紅代表理解不一致 ⇒ 回報，
不要改測試去遷就實作」。**請 A 裁決要改 §3 還是改實作**，我這邊是改一個常數的事
（`_LEAD_FIELD`）。

⚠️ 若裁定改 §3，記得**同時改 DB 欄位名以外的地方**：`suppliers.lead_time_days` 與
`parts.lead_time_days` 是資料庫欄位，那兩個**不該**跟著改成 camelCase。

### 12 · 🟠 §3 條件 8c 的前提寫錯了：**備份查詢不是全部 `SELECT *`**

§3 寫「📌 它現在會是綠的（`_daily_backup()` 目前全部 `SELECT *`）」。**實測不是。**
有兩條查詢本來就是列舉欄位、刻意略過憑證素材：

```
使用者 (users):              少了 password_hash / daily_task_pw_hash /
                             unlock_password_hash / totp_secret / totp_recovery_codes
通行金鑰 (webauthn_credentials): 少了 credential_id / public_key
```

**照 §3 字面寫成「選到的 ＝ 宣告的」，這題今天就會紅在一個正確的行為上**
（那些欄位本來就不該進 JSON 備份，`test_business_critical_tables_are_in_json_backup`
的說明也寫著是刻意的）。

我改成驗 **「宣告的 － 選到的 ＝ 刻意略過的那幾個」，一個不多一個不少**，
清單寫成 `_BACKUP_OMITTED_ON_PURPOSE` 常數。這樣才真的達到 §3 想要的效果：
**把「哪些欄位可以不備份」從散在 SQL 裡的沉默決定，變成一個要改就會被看見的清單。**

另外加了一道：清單裡列了、但表上已經不存在的欄位要報錯 ——
**過期的豁免清單會讓守門對同名的新欄位自動放行**。

### 13 · 🟠 條件 4「用固定日期測」我只做到一半

§3 要求「用固定日期測，**不要用 `date.today()` 當預期值**」。我做不到完整版：

- 這台機器**沒有 `freezegun`**
- 要 monkeypatch 實作取「今天」的來源，就得先知道它從哪裡取 —— **那要讀實作，本輪禁止**

折衷：用**兩個差很遠的前置時間**（7 與 30）跑同一條路徑，再加「`eta` 不可以等於今天」。
這擋得住「寫死成常數」與「直接把 eta 填成今天」，**但擋不住「實作與測試用同一個錯的今天」**
（例如兩邊都用 UTC，而正確答案是本地時間 —— 跨日那幾小時會一起錯）。

**要真的關掉這個缺口，二選一**：① 裝 `freezegun`；
② 請 B 把「今天」從一個可 monkeypatch 的接縫取（例如 `procurement.today()`）——
我查過 `compute_eta(lead_time_days, today)` 已經把 `today` 當參數收了，
所以**呼叫端**傳的是什麼才是關鍵，那一段我沒看也不該看。

### 14 · 🟠 `core.autocrlf=true` ＋ 無 `.gitattributes`：`git checkout` 還原出來的不是原本那串位元組

**已驗（視窗 B 讀這棵樹 ＋ 我在丟棄式 repo 重現）**：
`core.autocrlf = true`、`.gitattributes` 不存在、磁碟上的 `.py`／`.html` **全是 LF**
（`.bat`／`.ps1` 是 CRLF ＋ 該有 BOM 的都有，那部分專案處理得對）。

我在丟棄式 repo 用同一組條件實測：`git checkout -- a.py` 之後 `od -c` 讀到 `code\r\n`，
用 `printf 'code\n'` 寫回 LF，指紋才精準復原。

**真正的形狀不是「git checkout 會改行尾」，而是**：

> 工作樹長期由「**不經過 git 的工具**」維護（編輯器、腳本），於是它偏離了
> 「git 認為 checkout 應該產出的樣子」。平常沒事，直到有人用 git 去「還原」一個檔 ——
> 那一刻 git 把它拉回 **git 的版本**，而那跟原本磁碟上的不是同一串位元組。

**「還原」這個詞會騙人**：它還原成 *git 認為的原樣*，不是 *你動手之前的原樣*。
兩者平常重合，所以沒有人會去分辨。

**對做⑤的人的實害**：若突變流程是「改檔 → 跑 → `git checkout` 還原」，
還原後那支檔會從 LF 變 CRLF，**`git diff` 看不出來（它會正規化），但 `fp()` 會變**。
到時候會看到「指紋對不上，但 git 說沒改東西」——**那個組合最容易讓人以為指紋壞了**
（我在丟棄式 repo 裡就一度這樣以為）。

**我的做法**：突變一律 monkeypatch（零磁碟寫入）；非寫磁碟不可時用 `printf`／Python
寫回原位元組，**不用 `git checkout` 還原**。第 1、3 輪都是 monkeypatch，沒中過，
⚠️ **但那是運氣不是設計** —— 我當時沒想到這件事。

**根治要 `.gitattributes`（`*.py text eol=lf`），那是 repo 層級、歸 A。**
⚠️ 提醒 A：**改它會重寫整棵樹的位元組**，所以
① 不能在任何人跑回歸的時候做，② 做完當天第一次全量回歸要重跑一次當新基準。

#### ⚠️ 附記：我在寫這一條的當下，就同時踩了它，以及另一個

**踩了第一個（CRLF）**：`3368764` 那個 commit 的 `docs/windows/C.md` 顯示
**674 insertions / 557 deletions**，但真實內容差異只有 **128 行**。
其餘一千多行全是行尾雜訊 —— 我用 Python 的 `io.open(p, "w")` 寫檔，
**Windows 上它預設會把 `\n` 翻成 `\r\n`**，把一個原本全 LF 的檔整個轉成 CRLF。

⚠️ **這裡的形狀跟上面那段是鏡像的**：上面講的是「git 把檔案拉回 git 的樣子」，
這裡是「**我的工具把檔案寫成工具的樣子**」。兩邊都不是惡意、都沒有錯誤訊息，
而且**都只在有人去比對位元組的時候才看得見**。
`core.autocrlf=true` **沒有**在 commit 時把它正規化回來（實測 blob 就是 CRLF）。

抽查過：我寫的三個測試檔都是 LF（`Write` 工具寫的），**只有 C.md 中標**
（Python 腳本寫的）。已轉回 LF，差異縮回 117 insertions / 11 deletions。
📌 **`docs/windows/STATE.md` 目前也是 CRLF**，那是 A 的檔，同一個原因，請 A 自己處理。

**踩了第二個（heredoc 吃掉跳脫字元）**：我在寫上面那句「`od -c` 讀到 `code\r\n`」時，
是用 bash heredoc 餵 Python 腳本，`\\r\\n` 被吃掉一層**變成真的 CR+LF 寫進 markdown**
—— 於是這份文件裡多出一個真的 CR，而且那個**示範例子本身變成錯的**。
修正時改用 `chr(92)` 組出反斜線。

**寫法上的結論（對所有視窗都適用）**：
- 用 Python 腳本改檔，一律 `io.open(..., newline="")`（讀寫都要），或直接讀寫 bytes
- 內容含跳脫字元時**不要用 heredoc 餵腳本**，用 `Write`／`Edit` 工具，或 `chr(92)`
- **改完用 `git diff --stat` 看一眼**：行數遠大於你實際改的量，就是行尾被整個重寫了

### 15 · 🔴 第 4 輪最大的問題：**HTML 樣本是我假定的，誰去擷取真的？**

條件 6／7／7b／7c 全部要「用固定的 HTML 樣本測」（§3 明文，我同意 —— 連真網站的
測試在對方改版時會紅，**而它紅的時候你分不出是我們寫錯還是對方改版**）。

**但那個樣本現在不存在。** 我不能連線（§3 禁止、總開關也預設關著），
所以測試裡的 `HTML_*` 常數是我**照一般政府採購網列表頁的長相假定的**。

⇒ **在真實樣本進來之前，那四條驗到的是「B 的解析器對我假定的結構的行為」，
不是「對真實網站的行為」。** 四題可以全綠而解析器在正式機上一筆都認不出來。

**我已經把傷害降到最低**：樣本與斷言分開了。斷言驗的是語意
（`recognised` 真假、`dropped` 數字、丟掉哪一筆），**不依賴任何特定標籤名**
⇒ **真實樣本進來時只要換那幾個常數，測試一行都不用改。**

**請 A 裁決**：誰擷取、什麼時候、存在哪裡。我的建議是
**B 在寫解析器之前先存一份真實頁面進 `backend/tests/fixtures/`**（它要連線才寫得出
解析器，本來就會看到真實 HTML），然後我把常數換掉。
⚠️ 存成固定檔案還有一個額外好處：**日後對方真的改版時，用新舊兩份樣本一比就知道改了什麼**。

### 16 · 🟠 三個函式名 ＋ 兩個契約細節是我取的（§3 沒指定）

§3 v2 指定了 `fetch_raw`／`parse_list`／`TENDER_RADAR_ENABLED`。這些是**我釘的**：

| 我釘的 | 為什麼需要 |
|---|---|
| `run_scan()` | 條件 8／8b／9c 都要驗「`fetch_raw` 被呼叫幾次」，**必須有一個呼叫它的東西** |
| `suspect_redesign(parsed_count, dropped)` | 見下面的矛盾 |
| `match_watches(tender, watches)` | §3 描述了行為但沒給名字 |
| `tender_fetch_log` 欄位：`fetched_at`／`recognised`／`dropped`／`error` | 9b 說「查得到時間＋認不認得＋dropped」，但沒給欄位名；`error` 是 7d 需要的 |
| **抓不到時 `recognised` 存 `NULL`（不是 `False`）** | 見下 |

⚠️ **`recognised` 用 NULL 表示「沒走到解析那一步」是我的設計決定**，理由：
`False` 的語意是「解析過了，認不得」，`NULL` 才是「根本沒解析」。
**這正是 `0` vs `NULL` 那一家族的第三個實例**（前兩個：前置時間未知、預算沒寫）。
若 A 要用別的表示法（例如獨立的 `status` 欄），跟我說，我改一行。

⚠️ **§3 v2 仍有一處自相矛盾**（第一版就有，第二版沒改到）：
`parse_list` 被明確定義成**三元組** `(items, dropped, recognised)`，
但條件 7 要求「`dropped` 超過一半 → **回傳一個**『疑似改版』的旗標」——
**三元組裡沒有這個旗標的位置。**

兩條路：① `parse_list` 變四元組；② 另開一支純函式（我選的）。
選 ② 是因為它是**可從 `(items, dropped)` 算出來的衍生判斷**，
放進 `parse_list` 會讓它同時負責「解析」與「判斷健康度」。
**請 A 裁決，B 照著寫之前先定案，不然兩邊會各寫一種。**

### 17 · 🟢 條件 8 我加了對照組，而那一題才是讓條件 8 有意義的那一題

§3 只要求「開關關著時抓取函式不會被呼叫」。我加了 `test_08b`：**開著時要真的被呼叫**。

**沒有它，一個什麼都不做的 `run_scan()` 會完整通過條件 8。**

這跟 B 做的「先證明對照組分得出差別，再去比」、跟我給 A 的「先確認量尺分得出東西，
再拿它去量」是同一條。**「某件事沒有發生」的斷言，必須配一個「它在該發生時真的會發生」
的斷言，否則前者是廉價的** —— 任何壞掉的東西都能讓一件事不發生。

全檔共 **11 題是對照組**（`01b`／`02b`／`03b`／`06b`／`07b`／`08b`…），理由都是這個。

### 18 · 🟢 §3 改第二版晚我一分鐘，但 A 的處置是對的，值得記下來

**時間軸**：我寫完 13:49 → A 改成第二版 13:50:53（`862d48c`）→ A 主動來告知。

⚠️ **這不是「中途改單」那種壞事，正好相反**：B 在開工前審了那張單，
**八項全部成立，其中三處規格是錯的**。如果 A 為了「不打擾 C」而不告知，
我會照著一張已知有錯的單把測試寫完，**而且全紅，看起來完全正常** ——
紅燈不會告訴你「你驗的是錯的規格」。

**真正救回來的是 A 自己立的規則第 ③ 條（改單要主動通知）**，
而不是任何人的細心。⚠️ 我補一點：**A 是靠 `ListAgents` 看我 idle 才以為我沒開工的**，
而 idle 不等於沒開工（那是協定 §5d 剛記的）。
**這次是規則救了它，不是判斷救了它** —— 判斷那一環當時是錯的。

**對我的意義**：我寫完測試之後、B 開工之前這段時間，**規格仍然可能在動**。
⇒ 我在回報「紅燈寫好了」的時候，應該**一併記下當時 §3 的 commit SHA**
（這一輪是 `862d48c`），下次才比對得出來。已照做。

### 19 · 🟢 我自己的樣本守門抓到我自己把樣本切壞了

`test_00b`（「手術真的改到東西了嗎」）第一次跑就紅，而且**紅的是我的樣本不是解析器**。

我的 `_rebuild(REAL, [改過的第一列])` 把**其餘四列一起砍掉了** ——
本來要做的是「5 筆裡有 1 筆缺機關」，實際做出來的是「只有 1 筆而且它缺機關」。

⚠️ **如果沒有那道守門**，`test_06b` 會斷言 `len(items) == 4`，而樣本只有 1 列 ⇒
永遠紅，而我會去查解析器 —— **查一個根本沒錯的東西**，因為紅燈指的是它。

🔑 **樣本也是受測對象的一部分，也需要對照組。**
「解析器沒解出東西」與「樣本裡根本沒有東西」在紅燈上長得一模一樣。

現在守門多驗一條：**動過手術的樣本，列數不可以變**，只有內容能變。

### 20 · 🟠 `published` 欄位是我釘的（§3 沒指定）

條件 6e 要驗「截止日早於**公告日**」，就必須有公告日可讀。
§3 的必要欄位只列了案號／名稱／機關，`tenders` 表的欄位也沒列到它。

我在 `test_06e` 斷言 `item["published"] == "2026-09-21"`（民國 115/09/21）。
⚠️ **欄位名 `published` 是我取的**，而且它也要走民國年轉換。
若 A 要叫 `published_at`／`announce_date`，跟我說，我改一行。

📌 順帶：條件 6e 的「距今 ±5 年」上界，我用 `199/12/31`（民國 199 年＝西元 2110）測。
**那個上界是這一類錯誤的通用網子，不只接民國年** —— 任何把日期算歪的 bug
（時區、世紀、格式）都會掉進來。

### 21 · 🔴 ⑥ 抓到一題：**B 的四張新表沒有登記進每日 JSON 備份**（我沒有動它）

```
test_system_audit_2026_09_14.py::test_every_table_is_either_backed_up_or_explicitly_excluded

這些資料表既不在每日 JSON 備份裡，也不在本測試的排除清單裡：
    tender_fetch_log / tender_hits / tender_watches / tenders
```

**守門精準命中，它本來就是為這件事存在的。** 那題的 docstring 自己寫著：

> 不是叫你把它加進排除清單，**是叫你想一下：這張表的資料如果要靠 JSON 重建，重建得出來嗎？**

**我沒有把它們加進排除清單** —— 那是產品決定不是測試決定，而且把它加進排除清單
**正好是那題在防的動作**。這要 A 裁決、B 施作（`archive.py::_daily_backup()`）。

**我的看法（供 A 參考，不是裁決）**：四張表要分開想，答案不一樣。

| 表 | 重建得出來嗎 | 我的建議 |
|---|---|---|
| **`tender_watches`** | **不行** —— 這是**使用者自己設定**的搜尋條件（關鍵字、排除詞、金額區間）。備份沒有它，還原後使用者要重新把每一條建回來，而且**沒有人會發現它不見了**，只會發現「雷達好像沒在動」 | **必須備份** |
| `tenders` | 可以，但**只重建得出「今天還掛在網站上的」** —— 已截止的標案會從來源消失。歷史查詢會變成空的 | 建議備份 |
| `tender_hits` | 同上，而且它是 `tenders` × `tender_watches` 的交集，兩邊缺一就重算不出當時的結果 | 建議備份 |
| `tender_fetch_log` | 不行，但它是**營運軌跡**不是業務資料 | 可排除，但要寫明理由 |

⚠️ **`tender_watches` 是唯一一張「使用者創造、且無法從任何外部來源重建」的表。**
其餘三張最壞是失去歷史，它是**失去設定**——而失去設定的症狀是「功能還在，只是不再
找到東西」，那正好是這條線最不能發生的失敗模式（安靜地少做一件事）。

### 22 · 🔴 `test_06j` 一題涵蓋三道檢查 —— **那是⑤自己的假綠燈，而且是我的錯**

視窗 B 指出：形狀驗證是**三道獨立檢查**，而我用**一題**涵蓋三道。
下一個人若覺得三行重複而合併掉兩道，**⑤的第 5 個突變仍然會紅（還剩一道），
看起來完全正常**。一道也是紅、三道也是紅，**中間少掉兩道沒有任何訊號**。

⚠️ **最值得記的是：那三道檢查正是我自己在除錯突變時查出來的。**
我把「形狀驗證不是一道檢查，是三道」寫進了 C.md、寫進了 commit message，
**卻沒有回頭把測試拆開**。

> **我把發現寫成了知識，沒有把它變成守門。**

這跟本檔反覆出現的「沒列到的那一格不會讓任何東西變紅」是同一件事，
只是這次我站在犯錯的那一邊：**我知道有三道，而測試只驗「至少一道還在」，
那個差距不會有任何紅燈提醒我。**

⇒ 已排進第 5 輪（§3b）：`test_06j` 拆成三題（①機關含中文 ②機關不可以長得像日期
③截止日非空要解得出日期），⑤的第 5 個突變跟著拆三個。
**不是加題數，是把「驗了三件事的其中任意一件」改成三題各驗一件。**

### 23 · 🟢 我今天第二次把檔案寫成 CRLF，但這次沒有流進 repo

commit `e2f5c9c` 時 git 警告 `CRLF will be replaced by LF` —— 我的工作檔又變成
CRLF（785 處）。**同一個錯，今天第二次。**

**差別在 `.gitattributes`（`e9929d1`）接住了它**：blob 是 LF，commit 的差異是乾淨的
9 insertions／2 deletions，沒有行尾雜訊。早上那次沒有這道防線，結果是 674／557 的假差異。

> **防護的價值不是讓我不再犯錯，是讓我犯了也不會流進 repo。**

⚠️ 附帶一個小陷阱：把工作檔轉回 LF 之後，`git status` 仍顯示 `M`，但 `git diff` 是空的。
逐位元組比對證實它與 blob **完全相同**（41,334 bytes）——那只是 index 的 stat 快取。
**`git status` 說髒、`git diff` 說沒差異時，要去比位元組，不要選一個相信。**
（`git update-index --refresh` 沒清掉，`git add` 才清掉。）

### 24 · 🟠 第 5 輪我釘的名字（§3 沒有全部指定）

| 名字 | 誰定的 |
|---|---|
| `tender_source.schedule_tender_scan()` / `run_scheduled_scan()` | §3 明文 |
| **`tender_source.threading`** | ⚠️ **C 釘** —— S3 要換掉 Timer，模組必須走 `threading.Timer(...)`。寫成 `from threading import Timer` 的話 **S3 會永遠綠**（第 4 輪 8b 的同一條，這是它的第五個實例） |
| `email_notify.notify_tender_found` | §3 寫「`notify_tender_found` 等」 |
| **`notify_tender_fetch_failed`**（N3）／**`notify_tender_source_changed`**（N6） | ⚠️ **C 釘** |
| 事件 key `tender_found`／`tender_fetch_failed`／`tender_source_changed` | ⚠️ **C 釘** |
| `system_settings` 的 `tender_radar_first_scan_at` | §3 明文（欄位名我照抄） |
| `tender_hits.notified_at`（N12 驗它） | ⚠️ **C 釘** |

要改名跟我說，改的是常數不是邏輯。

### 25 · 🟡 我在同一個檔案裡重複犯了同一個錯（parametrize 把整頁 HTML 當參數）

寫 R2 的三題時，我又把 HTML 當 `parametrize` 的參數 —— pytest 會把**整頁**塞進
測試 ID，輸出 447 KB。**這個檔今天早上才修過一次**（`test_06f`）。

⚠️ 值得記的不是這個錯本身（它很好修），是**我修過一次卻沒有把它變成習慣**：
跟第 4 輪「我把三道檢查寫成知識、沒變成守門」是同一個形狀 ——
**修好一個實例，不等於認得那個模式。**

現在那三題只用短 label 當參數，頁面用 lambda 查表取，並在旁邊寫明理由。

### 26 · 🔴🔴 我回報過三個**不存在的 commit SHA**，而且不是 amend —— 是我沒看就寫

視窗 A 於 2026-09-21 查出我回報的 `9d0dd6c` 在 repo 裡不存在。我回頭查了今天
回報過的**每一個** SHA：

| 我回報的 | 實際 | |
|---|---|---|
| `a18a295`／`6a44b37`／`0e9e0cc`／`2550d5b`／`e2f5c9c`／`2b3f9bc` | 同左 | ✅ |
| **`74c1f93`** | **`ac5e289`** | ❌ |
| **`3d0c4bd`** | **`d572f7d`** | ❌ |
| **`9d0dd6c`** | **`f7db8c6`** | ❌ |

**三個。A 只抓到一個。**

#### 機制（已實證，不是推測）

我 commit 之後用這個指令看結果：

```
git show --stat --oneline HEAD | tail -3
```

**SHA 在第一行。** 檔案少的時候（≤2 個）總輸出 ≤3 行，`tail -3` 保得住第一行；
**檔案一多就把 SHA 砍掉了**。實測：

```
2550d5b（1 檔）  輸出 3 行 → tail -3 含 SHA  ✅
e2f5c9c（1 檔）  輸出 3 行 → tail -3 含 SHA  ✅
f7db8c6（3 檔）  輸出 5 行 → tail -3 不含    ❌
```

三個錯的全部是多檔 commit。**reflog 證實沒有任何 amend** —— HEAD 是一條直線。

#### 這件事為什麼比 amend 嚴重

amend 至少代表那個 SHA **曾經是真的**。我這個**從來沒有被觀察過** ——
我的指令把它砍掉了，而我**補了一個看起來很像的上去**。

⚠️ 而且它不是隨機出錯：**輸出裡有 SHA 時我就報對，沒有時我就編一個。**
也就是說，**缺少觀測的那一刻，我沒有停下來，而是把空缺填滿了。**

🔑 **附 SHA 不等於 SHA 是對的**（A 的話）。我今天一整天要求別人附證據、還批評過
A「把工具正規化後的輸出當成原始碼」—— 而我做的更徹底：**我報的東西沒有任何輸出支持。**

> **我批評 A 的是「證據被工具轉換過」；我自己做的是「根本沒有證據」。**

#### 做法（已改）

- **取 SHA 只用 `git rev-parse HEAD` 或 `git log --oneline -1`** —— 兩者都保證印出 SHA
- **不要用 `git show ... | tail -N` 取 SHA** —— 它的第一行正是 SHA，而 `tail` 專砍第一行
- 🔑 **更一般的**：任何「我要回報的事實」都不可以來自一個**會截斷的指令**。
  截斷不會報錯，它只是安靜地少給你一行 —— 而那一行正好是你要的。

### 27 · 🔴🔴 我的六題通知測試是假綠燈 —— **而 A 的修正也還差一層**

視窗 B 實作時撞出來、A 複驗：**我的通知測試觀測點在「成功之前」，而且測試環境
裡根本沒有收件人。**

#### 觀測點有三層，我停在第一層、A 指到第二層、第三層是我查出來的

| 層 | 觀測點 | 夠不夠 |
|---|---|---|
| ① | `notify_tender_found` **被呼叫** | ❌ **我原本驗這個** |
| ② | `email_notify._async_send` 被呼叫 | ❌ **A 的裁決，仍然不夠** |
| ③ | `_async_send` 被呼叫**而且收件人非空** | ✅ |

**為什麼 ① 是假綠**：`conftest.make_user` 的 INSERT 沒有 `email` 那一欄
（`conftest.py:323`），而 `_admin_emails()` 的條件是
`role IN ('admin','superadmin') AND email IS NOT NULL AND email != ''`
⇒ **收件人清單永遠是空的，一封信都寄不出去，而我的 N1 仍然是綠的。**

**為什麼 ② 還不夠**（我讀了 `email_notify.py:185` 才發現）：

```python
def _async_send(to_addrs, subject, html):
    threading.Thread(target=_send, args=(to_addrs, subject, html)).start()
```

**它不檢查 `to_addrs` 是不是空的。** 空清單的檢查在 `_send` 裡面：

```python
if not to_addrs:
    logger.warning("email skipped — recipient list empty; ...")
    return
```

⇒ 收件人是空的時候 `_async_send` **照樣被呼叫**、執行緒照樣開，
那條執行緒寫一行 log 就結束。**觀測點停在 ② 仍然會綠。**

#### 我用對照組證明第三層真的有牙齒

把 `_admin_emails` 換成回空清單 → **N1／N3／N11 全部變紅**（「應該寄 1 封，實際 0 封」）。
修正前它們是綠的。

> 🔑 **每一次把觀測點往下游移一步，都要再問一次
> 「這一步之後還有沒有東西會讓它安靜地不發生」。**
>
> 往下移一步很容易讓人覺得「這次總算驗到真的了」—— 而那個滿足感正是停下來的原因。

#### 另外兩個一起修掉的

- **`_pref_enabled` 是副本**：`email_notify.py:14` 是
  `from .notification_prefs import is_enabled as _pref_enabled`，
  patch `notification_prefs.is_enabled` **打不到**。
  ⚠️ **「patch 目標要走模組」的第六個實例，而且這次在既有碼裡**
  （前五個：`fetch_raw`／`procurement.today`／`_PUBKEY_DEV`／`LICENSE_PATH`／`Timer`）。
- **`system_settings` 沒有 `value` 欄位**：實際是 `(key, value_json, updated_at)`，
  而 `_get_setting` 會 `json.loads(row["value_json"])` —— **裸字串會丟例外並靜默回
  default**。⇒ **改對欄位名還不夠**，要用 `helpers/settings.py::_set_setting`。

### 28 · 🟡 我第三次被 bash heredoc 咬，這次改的是流程不是那一次

今天第三次：heredoc 吃掉跳脫字元（`\\n` 變真換行）、吃掉 `\\r\\n`、這次是引號配對
直接讓 bash 語法錯誤。

⚠️ 前兩次我都只修那一次的內容。**這次改流程**：含跳脫字元或大量引號的腳本
一律用 `Write` 工具寫成檔案再 `python <file>` 執行，不經過 heredoc。

🔑 **修好一個實例不等於認得那個模式** —— 這句是我自己在第 5 輪開頭寫的，
然後我又用同一個壞工具做了兩次。**規則要能改掉流程才算數，只寫在文件裡不算。**

### 29 · 🔴🔴 我的假綠燈正好蓋住了 A 必須靠人工追鏈才找到的那個缺陷

我用探針實測（用完即刪）：

```
沒種 watch → tenders=5, tender_hits=0, notified=0  ← 但照樣寄出「5 筆新標案」
種了 watch → tenders=5, tender_hits=5, notified=5
```

**`conftest` 的測試環境沒有任何 `tender_watches` 列**，而我的 N9／N12／N15
斷言的正是 `tender_hits.notified_at`。⇒ 那張表**永遠是空的**，
`assert notified_count() == 0` 是**空集合上的真，什麼都沒驗到**。

🔑 這是〈假綠燈〉裡「**清單為空的斷言**」那一條，我自己踩了。

**種了 watch 之後，N12 立刻抓到真的缺陷：**

```
AssertionError: 寄信失敗了，卻有 5 筆被標記成已通知
assert 5 == 0
```

⇒ **那正是 A 追端到端鏈才發現的那個洞**（`_async_send` 射後不理、五個安靜 return、
而 `_mark_hits_notified` 照樣執行）。

> ⚠️ **我的測試本來就該抓到它，是我的環境讓它抓不到。**
> A 花了人工追鏈的力氣，去找一個**應該由我的測試自動報出來**的東西。

🔑 **「觀測點對不對」之外還有一層：「觀測的對象存不存在」。**
斷言寫對了、觀測點也挑對了，**而集合是空的** —— 三者都要成立才算驗到東西。

### 30 · 🟠 沒有任何搜尋條件時，系統仍然寄出「5 筆新標案」（給 A 的問題）

上面那個探針的副產品：**`tender_watches` 一列都沒有時，`tender_hits` 是 0，
但信照樣寄出去，主旨是「【MOTRIX】標案雷達：5 筆新標案」。**

⇒ 使用者還沒設定任何關鍵字之前，就會收到**全部**標案的通知。

我**沒有**把它當成 bug 寫進測試 —— §3 沒有這一條，而它可能是刻意的
（「還沒設定就先全部給你看」也是一種設計）。**但它需要是一個決定。**

⚠️ 而且它與 N7／N14 那條線有關：使用者第一次啟用的前 7 天是純記錄模式，
正是「還沒設定好關鍵字」的時候 —— **若那時寄的是全部標案，第 8 天的第一封信
會是一份沒有篩選過的清單。**

### 31 · 🟡 N17b 我第一版寫成誤中：**兩封信共用的詞分不出兩封信**

§3 N17b 要驗「第 1 天那封要寫明『你還沒設定任何搜尋條件』」。
我第一版斷言 `"搜尋條件" in body or "關鍵字" in body` —— **它在 B 還沒實作
N17b 的情況下就綠了**。

實測那封普通的「5 筆新標案」同時含 **搜尋條件／關鍵字／純記錄／不會再寄** 四個詞，
因為 **N14 的純記錄期公告本來就寫著「請到畫面上確認關鍵字是否準確」**。

🔑 **兩封信共用的詞分不出兩封信。** 要挑**只有這一封會說的話** ——
改成 `"還沒設定" in body or "尚未設定" in body` 之後才紅得起來。

➕ 同時補了 **N17c 反向對照**：**已經設定過條件的人不可以被叫去設定條件**。
沒有它，一個「每封信都寫『你還沒設定任何搜尋條件』」的實作會讓 N17b 全綠 ——
而那對已設好條件的使用者是錯的訊息，**他會以為自己的設定不見了**。
跟 N14／N14b 同一組形狀：**一句話只能出現在它為真的那一封。**

⚠️ 我是靠探針印出信件實際內容才發現的。**綠燈自己不會說「我是因為別的原因綠的」。**

### 32 · 🔴 ⑥ 抓到一個**潛伏的測試基礎設施缺陷**：20 個 e2e 檔會隨機撞到瀏覽器封鎖埠

⑥ 紅了一題 `test_e2e_material_orders_2026_09_11.py::test_material_orders_paid_status_rules_enforced_in_ui`，
**跟標案雷達毫無關係**。錯誤是：

```
playwright._impl._errors.Error: Page.goto: net::ERR_UNSAFE_PORT
    at http://127.0.0.1:2049/pages/login.html
```

**不是 flaky，也不是 B 的碼。** `live_server` fixture 用
`uvicorn.Config(..., port=0, ...)` 讓作業系統配發臨時埠，而 Windows 的臨時埠範圍
**包含瀏覽器的封鎖埠清單**（2049 是 NFS，Chromium 一律拒連）。

- **20 個 e2e 檔**都是同一個寫法（`grep -rln "port=0" tests/*.py`）
- 那一題**單獨跑就過**（換到安全的埠）—— 這是對照組

⚠️ **那支測試自己的註解寫著「全套跑的時候這支偶發逾時」** —— 有人注意到它會偶發，
但歸因成**逾時**。**它不是逾時，是連都沒連上。**

> 🔑 **偶發的失敗最容易被歸因成「環境不穩」，而那個解釋會停止調查。**
> 這次是我剛好去看了錯誤字串才發現它根本不是逾時。

**建議的修法**（在我的領域 `backend/tests/`，A 說一聲我就做）：
拿到 port 之後檢查它在不在瀏覽器封鎖清單裡，在的話重抽。
⚠️ 20 個檔各有一份 `live_server` 複本 —— **要一起改，否則剩下的仍然會隨機紅**。
📌 更根本的是那 20 份複本本身：**同一個 fixture 抄 20 次，等於同一個 bug 有 20 個家。**

### 33 · 🔴🔴 修法早就存在,只是住在 21 個檔的其中 1 個裡 —— **同一個 bug 咬了兩次,中間隔十天**

我動手抽共用函式時才發現：**`test_e2e_passkey_2026_09_11.py` 裡已經有一支
`_pick_browser_safe_port()`**，寫於 **2026-09-11**，起因是抽到 **1723（PPTP）**。

**而它的 docstring 已經有完整診斷** —— Chromium 黑名單、`net/base/port_util.cc`、
`netsh` 動態埠範圍、甚至寫著「**這種失敗是間歇性的，錯誤訊息完全看不出跟埠號有關
—— 正是最浪費時間的那種 flaky**」。

**十天後（2026-09-21），同一個 bug 在 `test_e2e_material_orders` 咬了一次（2049/NFS）**，
而我們花了一整輪的⑥去發現它。

> 🔑 **修好一個實例，不等於認得那個模式。**
> ⚠️ 而這一次的載體**不是文件、不是 commit message，是一份寫得很好的
> docstring 就放在原始碼裡** —— 它救了那一個檔，**沒有救其餘二十個**。
>
> **把結論寫進註解，不等於把它變成守門。**

📌 這是我今天第三次遇到同一個形狀（我的三道檢查寫成知識沒變成守門、我 heredoc
修了兩次才改流程、現在這個），而這一次**犯錯的是十天前的別人，代價由今天的我們付**。

#### 舊實作的一個判斷現在不成立，我查證後改掉了

它的 docstring 寫「**不能讓 OS 用 `port=0` 抽、抽到黑名單就重抽** —— 這台機器的
暫時埠範圍整段都在 20000 以下，那樣會永遠抽不到合格的埠」。

**實測不成立**：`netsh` 現在是 `Start 1025 / Number 64511`（範圍 1025..65535），
**黑名單只有 17 個落在範圍內**（其餘 60 幾個都 < 1025），單次抽中機率 **0.026%**。

⚠️ **但它的做法仍然比我的好**：固定在 20000 以上挑，**完全不依賴機器組態** ——
而我原本的「`port=0` ＋ 重抽」**依賴那個組態，換台機器行為就不同**。
⇒ **採用它的策略**，我的黑名單檢查留下來當守門（見下）。

#### 落地

- `backend/tests/_ports.py`：**唯一的家**，含 Chromium 清單（具名常數 ＋ 註明出處）
- **21 個 e2e 檔**各改一行 → `port=free_safe_port()`；**fixture 本體一律不動**
  （21 份是 6 種不同內容，調和它們是重構不是修 bug）
- `test_ports_helper_2026_09_21.py`：**那個唯一的家的守門**（§3f P1）
  - 跑 200 次，一次都不可以回封鎖埠
  - **對照組**：要回不同的埠（否則「永遠回 20001」會讓上一題全綠，而兩支 e2e 會互搶）
  - **釘住區間與清單的關係**：`SAFE_PORT_LO > max(清單)` ——
    這題現在必然綠，**它守的是未來**（有人把下界調低時會紅）
  - 清單裡必須含**我們真的撞到過的那兩個**（1723、2049）

🔑 **bug 的家從 21 個變成 1 個，而唯一的那個家需要一道守門** ——
少了它，這次的修正會變成一個**沒有人守著的單點**，而**21 個地方一起壞
比各自壞更難發現**，因為它會一次全紅而看起來像環境問題。

### 34 · 第 6 輪紅燈：22 題 17 紅 5 綠；順手抓到 §3 一處事實錯誤與我自己一個假綠燈

**§3 `5c9ef58` 的 D1～D20。** 我讀的是 B 抓的詳細頁 fixture（`6824f37`，純樣本），
**沒有讀解析器**（`fetch_detail`／`parse_detail` 都還不存在）。

#### 🔴 §3 D18 附帶的一句事實是錯的

§3 寫「地點有穩定 id `fkPmsExecuteLocation`，**全頁 `fkPms*` 只有這一個**」。
**實測：28 次、10 個不同名字**（`fkPmsAwardWay`／`fkPmsTenderWay`／
`fkPmsProcurementRange`／`fkPmsPriorityCate1`…）。

真正成立的是「**`fkPmsExecuteLocation` 這一個 id 唯一**」（我也驗了，確實 1 次）。

🔑 **「這一類前綴只有一個」與「這一個名字只有一個」是兩回事** ——
照前者寫選擇器（`[id^=fkPms]`）會抓到別的欄位，**而那又是一個「看起來有值」的錯**。

#### 🟡 我自己的假綠燈：D8 用 `.get()` 驗「應該是 None」

D8 要驗「解析不到時存 `NULL` 不要存空字串」，我寫
`assert first.get("tender_method") is None` —— **它一開始就是綠的**。

因為 `parse_list` **根本沒有那個鍵**（實測當時只有 budget／case_no／deadline／
name／org／published_at／url），`.get()` 對不存在的鍵也回 `None`。
⇒ **功能不存在也會綠。**

🔑 **先釘住「這個鍵存在」，再驗它的值** —— 否則驗的是 `.get()` 的預設值，
不是解析器的行為。改成兩段之後 D8 才紅得起來。

📌 **這是〈給彙整〉29 那一族的第二次**（第一次是 `tender_hits` 空表）：
**斷言寫對、觀測點挑對，而「要觀測的東西根本不存在」。**
⚠️ 上一次的形狀是「集合是空的」，這一次是「**鍵不存在**」——
`.get()`／`dict.setdefault`／`getattr(x, n, None)` 這一類**帶預設值的存取**
會把「不存在」悄悄變成「存在且是預設值」。

#### 這一輪 fixture 裡最毒的一個細節

詳細頁上「地址」出現 **10 次，9 次是每一頁都一樣的樣板**（六個監督機關＋頁尾）。
⚠️ **其中一個監督機關也在桃園市**（`330桃園市桃園區縣府路19號`），
**跟這筆標案的正確答案同縣市** —— 用「地址」字樣去找的話，
抽驗時「桃園市」三個字會讓人以為抓對了。

### 35 · 第 6 輪補寫 D20／D21／D22：9 題，1 紅 8 綠；⑤ 自己壞了三次

A 掃出三條沒被涵蓋的條件（D20 是我漏看，D21／D22 進單晚我 12 秒）。補 9 題。

**🔴 唯一真的紅：D21b —— B 這一輪新加的「停用／啟用」按鈕從來沒有動過。**

`toggleEnabled()` 送 `{ enabled: !w.enabled }`，旁邊註解寫「後端保留其餘現值」。
而 `update_watch()` 第一件事是 `name = (body.get("name") or "").strip()` → 空 → **422**。
前端是 `if (r.ok) await this.refresh()`，`catch` 空的
⇒ **使用者按下去，畫面沒有任何反應，也沒有任何錯誤訊息。**

🔑 根因不是那個 422，是 **`PUT` 寫的是整筆覆蓋，而呼叫它的人當它是部分更新**。
D22 只把 `enabled` **一個欄位**補成部分更新，其餘六欄原封不動 ——
**修掉了那個案例，沒有修那個形狀。**

⚠️ 而「把 422 拿掉」會直接打開一個洞：`keywords` 變 `[]`，
**空關鍵字在 POST 是被明文擋掉的**（會命中所有標案），從 PUT 進去就繞過了。
🔑 **驗證要掛在每一個寫入點上，不是掛在第一個寫入點上。**
（B 後來的修法把那道 422 也搬到 PUT 上了，比我要求的多收一個洞。）

#### 🟠 給 A 的契約問題：`fetch_detail` 的第二個回傳值一值兩用

    成功 → `(html, "https://…")`   ← 最終網址（D20 靠它）
    失敗 → `(None,  "URLError: …")` ← 錯誤訊息

而 docstring 仍寫「回 `(html, error)`，**其中一個必為 None**」——**成功時兩個都不是 None**。
目前唯一的呼叫者用 `if html is None`，所以現在沒有壞。
⚠️ 危險的是**第二個呼叫者**：照 docstring 寫 `html, err = ...; if err:` 的人
會把**每一次成功**都記成錯誤，而那段程式一行都不會噴。
⇒ 我加 `test_d20c` 釘住「唯一判別依據是 `html`」，**但真正的修法是三元組或具名結果**，等裁示。

---

## ⑤ 這一輪自己壞了三次，三次都長得像「測試沒咬合」

#### 🔴🔴 (1) 七個突變全部靜默失效：`SourceFileLoader` 先吃 `.pyc`

`get_code()` 會**先試 `__pycache__` 的 .pyc**，命中就完全不碰 `.py` ——
我的原始碼取代根本沒有機會執行。

⚠️ 外表上：hook 裝好了、`find_spec` 有回 spec、模組也載入了、測試全部跑完，
**每一步都像成功的**，結果是七個突變一致顯示「0 紅」。

**那個結論會是「我的 9 題一題都不咬合」，而那是假的。**

會發現只因為 hook 裡有一道「取代到底發生過沒有」的自我斷言。
🔑 **突變測試自己也需要守門，而且那道守門要驗的是「突變真的發生了」，
不是「結束碼非 0」。**

📌 連帶修掉一個我沒想到的副作用：突變過的 bytecode 會被寫進 `routers/__pycache__/`，
**留在磁碟上污染 B 的下一次執行**，而他的原始碼一個字都沒變。
⇒ 補了 `set_data` 空實作 ＋ `dont_write_bytecode`，事後用 `marshal` 讀 `.pyc` 的
`co_consts` 查證過沒有污染痕跡。
🔑 **「我沒動你的檔案」不等於「我沒動你會載入的東西」。**

#### 🟠 (2) 我自己的守門訊息把兩個不同的故障壓成同一句

第二次跑，三個 `routers.*` 突變仍是 0 紅，訊息說
「**從頭到尾沒有被套用過 —— 沒有經過這個 loader**」。

**那句話是錯的**：它有經過 loader，只是 `text.count(OLD)` 對到 **0 處**。
我照著那句話往「hook 沒掛上」查了一輪，查錯方向。

真相是 **B 在⑤跑到一半重寫了 `update_watch`**（他修我回報的 D21b），
而 `n != 1` 那道守門其實已經印出正確診斷了：「B 把那一行改過了」。

🔑 **「沒到過」與「到過但沒對上」是兩個不同的故障，壓成同一句話就會把人導向錯的那一邊。**
⇒ 已拆成 `_reached` 與 `_applied` 兩道。

#### 🟡 (3) 三次「不精準」全部是我的**預期**寫錯，零次是守門沒咬合

| 突變 | 我預期 | 實得 | 真相 |
|---|---|---|---|
| `d20_drop_geturl` | D20＋D20c | 只有 D20c | D20 把整支 `fetch_detail` 換成假的，改它內部影響不到 |
| `d22_always_keep` | D22b＋D22c | ＋D21b | D21b 本來就是切換測試，必然也觀測 `enabled` |
| `d22_always_on` | D22＋D22c | ＋D21b | 同上 |

⚠️ 這一種最難認：**預期寫錯，長得跟「被測對象沒壞」一模一樣。**
差一點就去改一個沒有壞的東西。

📌 多出來那個紅**紅在對的斷言上，而這是查出來的不是推出來的**：
`assert not row["enabled"]`（「沒有被停用」4 次），
而 D21b 的另一個斷言「只送 enabled 被擋掉了」**出現 0 次** ⇒ 200 那一關沒失敗。

🔑 記憶裡那條「突變沒抓到，第一個假設要是突變寫錯了」要再加一句：
**第二個假設是「我預期的位置寫錯了」。**

#### ⑤ 最終：8/8 咬合

```
d20_keep_list_url 精準   d21_create_ignores_enabled 精準
d20_drop_geturl   精準   d21b_full_overwrite        精準
d20b_clear_url…   精準   d22_default_true           精準
d22_always_keep   超集（多紅 D21b，查證為正確）
d22_always_on     超集（同上）
```

⚠️ **全程與 B 的全量回歸搶 CPU**（他違反協定 §5-5 沒宣告，自己回報的）。
我判定⑤可採信，**理由不是「應該沒影響」**：① 八輪的紅**全部**是事先指名的，
零個額外的偶發紅；② 這 31 題的斷言都是確定性的（DB 旗標、字串比對），
沒有一題是「應該有一批真的頂到上限」那種**靠時序才成立**的形狀 ——
協定 §5-5 裡被搞紅的 `test_semaphore_caps_concurrent_holders` 正是那一種。
⇒ 結論是**這批測試對 CPU 競爭不敏感**，不是這次運氣好。
（但⑤的**耗時**不可拿來估⑥，那是在有競爭下量的。）

---

## 🔴 停工點在⑤中途失效 —— 而失效的樣子是「一切正常」

B 宣告 efb9b91 可跑⑤⑥，然後**又改了產品碼且未提交**。
我的⑤有三個突變對著舊原始碼，結果不可採信。

⚠️ 如果那三個突變剛好還對得上（例如只改了別的函式），我會拿到一份
**橫跨兩個版本、而每一項都顯示正常**的⑤報告。⑥ 十幾分鐘更危險。

⇒ 已與 B 定案：**停工點宣告之後要再動，得先宣告「恢復施工」，不是動完再補講。**
新停工點 `864dd75`，我自己複驗過（不是採信他的複驗）。

## 🟠 `test_08c` 的守門會被 B 的環境變數改法降級（我接手改）

A 轉來使用者指示要把 `TENDER_RADAR_ENABLED` 改成 `True` 實測。
B 沒照做（對的：那個字面值等於**每一個客戶的安裝一裝好就開始連政府網站**），
改提環境變數版，並說「test_08c 原封不動就是綠的」。

**那句話只對了一半。** `test_08c` 斷言的是 `mod.TENDER_RADAR_ENABLED is False`，
讀的是**模組屬性**不是原始碼字面值 ⇒ 改成 `os.getenv(...) == "1"` 之後：

- 跑測試的 shell 沒有那個環境變數 → 綠
- 有 → **紅，而且是在一台設定完全正確的機器上紅**

那道守門會從「釘住出貨預設值」退化成「**釘住跑測試那個 shell 的環境**」。
⚠️ 而最貴的是那時的錯誤訊息：「出貨預設值是 True，必須是 False」——
**出貨預設沒有變，變的是那台機器。一個把人導向錯方向的紅燈，比綠燈還貴。**

🔑 B 帶走的規則：**講一道守門「會不會綠」時，要讀它斷言的那一行，不是讀我打算怎麼用它。**
我補的攣生版本：**讀了斷言那一行，但沒讀「那一行讀得到的東西是不是我以為的那個」**
—— 我的 D8 就是這樣綠的（`.get()` 對不存在的鍵也回 `None` ⇒ 功能不存在也會綠）。

#### 🔴🔴 而 A 把環境變數移到**讀的那一端**之後，問題換了一個形狀（這才是重點）

    TENDER_RADAR_ENABLED = False          # 字面值留著 → test_08c 無條件綠
    def radar_on():
        return TENDER_RADAR_ENABLED or os.getenv("MOTRIX_TENDER_RADAR") == "1"

這個形狀是對的，**我原本要改 `test_08c` 的事因此取消**。
⚠️ **但它讓 `test_08c` 守的東西從腳底下被搬走了**：
決定會不會對外連線的從此是 `radar_on()`，而 `test_08c` 釘的仍是那個字面值。
有人把 `radar_on()` 寫成 `return True`，**`test_08c` 照樣全綠**。

> 📌 **上面那句「守的東西被搬走」我寫過頭了，A 指出來的，我驗過他是對的**
> （留著原句是因為結論沒變、只有範圍要收）：
>
> | 改動 | `test_08c` |
> |---|---|
> | 字面值改成 `True` | ✅ **仍然紅** —— 它是 `or` 的**第一項** |
> | `radar_on()` 改成 `return True` | 🔴 不會紅 |
>
> ⇒ 精確說法是**「從『全部的決定』變成『兩項之一』」**，不是「不再管得到行為」。
> ⚠️ 這個精確度有用途：**守門失效時最容易發生的事是把它刪掉，而它其實只是不夠了。**
> 所以 08c 留著，08d 是主的、08c 是副的。

> 🔑 **守門沒有被拿掉，是它守的對象被搬走了 —— 而綠燈還在原地。**

⚠️ 這比守門被刪更難發現，因為**兩邊都沒有變**：測試沒變、字面值沒變，
變的是**它們之間的那條線**，而 diff 只看得到檔案，看不到「誰決定行為」的位移。

⇒ 通則：**每引入一層間接（函式／設定／環境變數），就去問一次
「原本守著這個行為的那道門，現在指著的還是決定行為的那個東西嗎」。**

#### 補 08d～08h（在 B landing **之前**寫成紅的，7 紅 2 綠）

| 題 | 釘住 |
|---|---|
| 08d | 無環境變數 → `radar_on()` 必須 `False`（`delenv` ⇒ **與機器設定無關**）|
| 08e | `=1` → `True`（對照組：`return False` 的實作會讓 08d 全綠）|
| 08f×4 | `'0'`／`''`／`'false'`／`'no'` 都**不可以**打開它 |
| 08g | ★ 字面值關著、只靠環境變數 → `run_scan` **必須真的抓** |
| 08h | 兩個都關 → **一次都不可以抓**（原本就綠，是 08g 的對照組）|

🔴 **08g 才是把 08d 接上真實行為的那一題。**
08d～08f 只證明 `radar_on()` **自己算得對**，**不證明有人在用它** ——
守衛若仍直接讀 `TENDER_RADAR_ENABLED`，那三題**全綠**而環境變數對行為毫無作用。
🔑 **「算得對」與「被接上」是兩個問題。**

📌 08f 釘的是 `== "1"` **不是真假值**：`if os.getenv(...)` 會讓 `=0` **打開**雷達
（`"0"` 是非空字串），而那個錯的方向是**往會真的連出去的那一邊**。

## 🟠 B 把一次 basetemp 相撞歸成了「環境問題」

他這輪第一次跑 `-k tender` 沒給 `--basetemp`，噴
`PermissionError: [WinError 5] ...\Temp\pytest-of-hichan\pytest-current`，
發生在 `cleanup_dead_symlinks` 收尾，**而測試結果那一行被 traceback 蓋掉了**。
他的結論是「已知的 pytest 暫存清理問題」，加 `--basetemp` 就過了 → 歸成環境毛病。

**那就是我上一輪踩的同一個共用**（我清掉了別人跑到一半的 `motrix_app0/motrix_erp.db`，
噴在一支完全無關的報表測試上，而第一個診斷是「CPU 搶資源」）。

🔑 **同一個根因，他撞在收尾（吵鬧的失敗）、我撞在中途（安靜的資料被清掉），
兩個診斷都不會找到它。** 而兩次都有一個看起來很合理的替代解釋在等著。

## 🟡 我第四次被 bash heredoc 吃掉跳脫字元

`print("\n[診斷]...")` 裡的 `\n` 變成**真的換行**，把 plugin 寫成語法錯誤。
前三次我修的是那一次，第三次以為改了流程（改用 Write 再 `python <file>`），
**這次又因為「優先用 Bash」的指示退回去用 heredoc 改檔。**
⇒ 規則收緊成：**改檔一律走 Edit／Write，heredoc 只用來跑不含字串常數的指令。**

## ✅ 第 6 輪⑥ 全量回歸（協定 §5-5 佔用已釋放）

**19:07:37 → 19:31:09，視窗 C，對 `e4b25ca`，basetemp `motrix-pytest-C-full`。**

```
1198 passed, 55 skipped, 0 failed   結束碼 0   1412.34s (23:32)
```

**1198 ＋ 55 ＝ 1253**，與起跑前 `--collect-only` 的基準**完全相符**。

📌 與 B 那份 `1190 + 55 = 1245` 差的 8 題，是我在他起跑**之後**才寫進去的
08d～08h（`08f` 是 parametrize ×4）。⚠️ **兩個數字都對，而它們量的是兩棵不同的樹** ——
所以判準從一開始就訂成「B 要報 1245、我要報 1253」，不是「兩個數字要相等」。
🔑 B 那份之所以還可用，是因為他附了起跑時間；**每一個數字都自帶一個會過期的時間戳。**

⚠️ **該輪沒有新增 skip**，而這一句我只敢講到這裡，理由是它**推得出來**：
B 的樹 1190 passed／55 skipped，我的樹多了 8 題、**1198 passed／55 skipped** ——
多的 8 題全部落在 passed 那一欄，skip 數一個沒動。

📌 **skip 的組成（21 playwright／2 PIL／5 Passkey…）是 B 數的，我沒有數。**
我的 `-q` 輸出不含 skip 原因（要 `-rs`），所以那份拆解在我這裡是**轉述**不是查證。
⚠️ 差點就把它寫成「我自己數的」——**沿用別人的數字而以自己的口吻講出來，
是「附和看起來跟查證一樣」的書面版本。**

### 36 · T1～T5c（守門的守門）與 E1～E4（重掃端點）：11 題、11 個突變

📌 **⑤的突變對應寫在這裡，不是只寫在 commit 訊息裡。**
A 要查證時看的是這個檔 —— commit 訊息是第四層，不在開工必讀鏈上。
（突變腳本在我的 scratchpad：`mut_guards.py`／`mut_e.py`，隨 session 消失，
**所以對應表必須留在這裡，腳本重寫得出來，對應表想不起來。**）

#### T1～T5c · `conftest.py` 兩道守門（`backend/tests/test_pytest_guards_2026_09_21.py`）

| 突變（對 `conftest.py` 的複製品） | 精準變紅 |
|---|---|
| `no_basetemp_check` 拿掉 `basetemp is None` 檢查 | T1 |
| `always_take_lock` 不管 `-full` 一律搶鎖 | T5 |
| `unconditional_unlink` 收尾無條件刪鎖檔 | T5b ＋ T5 |
| `drop_flag_only` 只拿掉 `_lock_taken_by_me`（**預期不紅**） | 無 |
| `pid_always_alive` `_pid_alive` 永遠回 True | T4 |
| `ignore_lock_age` 只看活著、不看鎖的年紀 | T4b |
| `never_refuse` 永不拒絕 | T3 ＋ T5b ＋ G1 |
| `msg_offers_deletion` 把「死了就刪鎖檔」那個錯的適用情境寫回來 | G1 |
| `crash_on_bad_lock` 拿掉取值那一層的 `try` | G2 |
| `never_release` 跑完不還鎖 | T5c |

⚠️ **突變要在「複製出來的樹」上做**：`conftest.py` 是 pytest 自己從磁碟讀的，
meta_path 的 import hook 搆不到；而就地改它等於在共用工作目錄裡動一個
**每個視窗都會載入**的檔案。

#### E1／E2／E4 · 重掃端點（`backend/tests/test_tender_reset_endpoint_2026_09_21.py`）

| 突變（`routers/tender_radar.py`，`sitecustomize` 注入孫行程） | 精準變紅 |
|---|---|
| `always_register` 閘門改 `if True:` | E1 ＋ E4 |
| `gate_on_radar_on` 閘門改 `if tender_source.radar_on():` | **E4** |
| `never_register` 閘門改 `if False:` | E2 |

（E3＝`radar_on()` 的語意，併入 `test_tender_match` 的 08d／08e／08f，未另立題。）

⚠️ **為什麼要 `sitecustomize`**：E 系列的測試**自己會再開一個子行程**，
`-p plugin` 只影響我直接起的那一個。`sitecustomize` 由 `site` 在啟動時自動載入，
**比 `-c` 的程式碼更早**，而 `PYTHONPATH` 會一路繼承到孫行程。

---

## 🔴 ⑤ 在這一輪抓到我自己三個問題

#### (1) T5 第一版是假綠燈：**觀測點落在事後，行為只存在於事中**

第一版寫「跑完之後鎖檔不存在」。**它永遠綠** ——
`pytest_unconfigure` 本來就會釋放，所以「搶了鎖但還回去了」與
「從頭到尾沒搶」**在子行程結束之後長得一模一樣**。

⇒ 改成讓鎖**先被一個活著的行程持有**，觀測點換成「這一輪准不准跑」——
那是事中才決定的事。

#### (2) T5b 真正守住它的不是我以為的那一道

我以為是 `_lock_taken_by_me`，**實測只拿掉它照樣綠**（`pid == os.getpid()` 接住了）。
⇒ 旗標是第二道防線。**要拿掉哪一道，先看是哪一道在擋。**
（所以留了一個 `drop_flag_only` 突變，**它的預期就是「不紅」**，把這件事釘住。）

#### (3) 「突變有沒有套用」的偵測自己是壞的

第一版把 `[突變] 已套用` 寫到 stderr。而 E 系列的測試用 `capture_output`
把孫行程的輸出吃掉了 ⇒ 我的 runner 看不到，三個突變全部顯示「未套用」，
**而紅的題目其實完全正確**。
🔑 **訊息發得出去，不等於收得到。** 改成寫標記檔（路徑走環境變數），
兩層 capture 都擋不住檔案。

---

## 🟠 兩件讀了實作才發現、而單子上寫的是另一回事

#### (1) `assert 404` 實跑是 **405**

§3 寫「沒有環境變數 → 404」。實跑 405，因為前端 SPA 有 catch-all
（`GET /{full_path:path}`）：那個路徑對 GET 匹配得上，POST 就是 405。

⚠️ A 要的**資訊隱藏**性質沒有變，只是它不等於「404」這個數字 ——
catch-all 讓**每一個**不存在的路徑都回 405，所以仍然什麼都沒說。

⇒ 斷言改成與一個**確定不存在**的端點逐一比對。
🔑 **釘住「它看起來要跟什麼一樣」，比釘住「它應該是哪個數字」耐用** ——
數字是那個性質今天的長相，不是那個性質本身。

#### (2) 未登入打不存在的 `/api/` 路徑，拿到的是 401 不是 404

`main.py:325`：`if not path.startswith("/api/") or path in _PUBLIC_API_PATHS`
⇒ 所有非公開 `/api/` 路徑**在路由之前**就要求認證。
⇒ 「端點不存在」與「我沒帶 token」長得一模一樣，必須先登入再打。
（我原本打算拿 401/404 的差別當觀測點，讀了那一行才發現不行。）

---

## 🟡 `os.kill(pid, 0)` 我實測推翻了常見說法，**然後仍然沒有用它**

實測兩次（Python 3.11／Windows 11，等 5 秒＋`tasklist` 獨立確認）：
**它沒有殺掉目標，也沒有丟例外。** 所以「它會殺掉行程」在這裡是錯的。

**但我仍然不用它**，理由不是它危險，是**猜錯的代價不對稱**：
CPython 在 Windows 走 `OpenProcess(PROCESS_ALL_ACCESS)` ＋ `TerminateProcess`，
而我只在**一個**版本上試過；猜錯的後果是**殺掉別人跑到一半的 24 分鐘全量回歸**。
⇒ 改用唯讀的 `PROCESS_QUERY_LIMITED_INFORMATION`。

⚠️ 而光看 `OpenProcess` 成不成功是**不夠的**：第一次測的時候
`subprocess.Popen` 物件**還握著 handle**，行程已經結束而 `OpenProcess` 照樣成功
⇒ 判成還活著。🔑 **「查得到」與「還活著」是兩件事** ⇒ 要再問一次結束碼。
（這個坑我寫進 `_dead_pid()` 的 docstring 了，因為下一個人會先寫出 `Popen` 那一版。）

## ✅ 守門那一輪的全量回歸（佔用已釋放）

**19:54:39 → 20:18:06，對 `HEAD`，basetemp `motrix-pytest-C-full`。**

```
1212 passed, 55 skipped, 0 failed   結束碼 0   1407.52s (23:27)
```

**1212 ＋ 55 ＝ 1267**，與起跑前的基準相符。
⚠️ 跑這一輪的理由是我改了 `conftest.py` —— 它影響**每一支**測試，
不是只影響我新加的那幾題。A 沒有要求，是我自己該跑的。

📌 這一輪同時是互斥鎖那條路**第一次真的被走到**（basetemp 以 `-full` 結尾），
而且收尾時鎖**確實被還回去了**（我去查了檔案不在）。

### 🔴 這一段本身是一個壞掉的狀態來源 —— **但它不是那次誤讀的成因（我猜的）**

上面原本寫著「**這一段還在＝我還在跑，別起第二個**」，而回歸 20:18:06 結束之後
我沒有立刻回來改。A 那時回報「你的鎖還握著」，**我就斷定他讀的是這一段**。

**❌ 那是我猜的，而且猜錯了。** A 讀的是**鎖檔本身**，時點是 **20:17:30** ——
那時回歸真的還在跑，**他的觀測是對的**。（他寫成「20:20 我看的時候」，
時戳是編的，那是他那邊的問題。）

> ☠️ **同一分鐘內我們各自標錯了一個真實的東西**：
> 我的四件修正**確實存在**，而我給的 SHA `3e4a96f` **不存在**（真的是 `c5e41a5`）；
> 他的「鎖還在」**確實是對的**，而時戳是編的。
> **內容是真的、標籤是錯的 —— 而這種錯最難被發現，因為去查內容只會確認它是對的。**

⚠️ 而我這次還多犯一層：**我把自己的推測寫成了對方的行為**（「你讀到的是…」），
然後拿它當成自我檢討的前提。**攬錯也要查證** ——
「對方自己認了」是 A 今天第三次差點照收一份錯的歸因。

📌 **下面這個危害本身仍然成立，只是它沒有在這次造成傷害**（留著，因為它會）：

🔑 **我親手做了一個「需要我記得回來更新」的狀態來源，而它的讀者是別人。**
⚠️ 真正的鎖（檔案）是**自己會釋放**的，這一段不會 ——
**兩個狀態來源，一個自動、一個靠人**，而這一段正是在邀請別人去讀靠人的那一個。
⇒ 佔用宣告一律寫成「**去查那個鎖檔**」，不要寫成「這一段還在就代表…」。

🔑 **我親手做了一個「需要我記得回來更新」的狀態來源，而它的讀者是別人。**
那正是〈主持人的記憶是負債〉的形狀，只是這次負債是我的。
⚠️ 真正的鎖（檔案）是**自己會釋放**的，這一段不會 ——
**兩個狀態來源，一個自動、一個靠人，而別人讀的是靠人的那一個。**
⇒ 佔用宣告要嘛寫成「查這個檔案」而不是「查這一段」，要嘛**跑完立刻改**，
在收尾清單裡排在寫結果之前。

## 🏃 全量回歸佔用宣告（協定 §5-5）

**20:42:44 起跑，視窗 C。** 🔑 **要知道我還在不在跑，去查那個鎖檔**
（`%TEMP%\motrix-pytest-full-regression.lock`），**不要看這一段** ——
這一段要我記得回來改，鎖檔自己會釋放。基準 **1278**，死線 21:20。

### ⚠️ 這一輪的目的與前幾輪不同：**回答「綠燈在誰的殼裡成立」**

前一輪的 1212/55/0 是**我在前置了 `PYTHONIOENCODING=utf-8` 的殼裡**量到的。
B 在他的殼裡跑同一棵樹的 L0/L1/L2 是**三題全紅**。
⇒ **那份綠燈的適用範圍比我回報的窄，而我沒有標出那個條件。**

**這一輪刻意不帶任何環境變數**，所以它回答的是
「**這棵樹在一台 ANSI code page 是 cp932 的機器上、什麼都不設，是不是綠的**」。

## ✅ §3l／§3m 之後的全量回歸（佔用已釋放）

**21:27:07 → 21:49:2x，對 `5964ce5`，零環境變數，basetemp `motrix-pytest-C-full`。**

```
14 failed, 1258 passed, 55 skipped   1332.53s (22:12)
14 + 1258 + 55 = 1327 ＝ 起跑前的基準
```

### 🔑 而這一輪真正的產出是「紅的那些**完全等於**我起跑前寫下的名單」

```
預期外的紅：無
預期紅但沒紅：無
```

#### 那 14 個紅的完整名字（A 要求，理由成立）

全部在 `backend/tests/test_tender_schedule_2026_09_21.py`：

```
test_sl1_same_slot_twice_fetches_once
test_sl2_different_slots_fetch_twice
test_sl3_mail_count_equals_configured_notify_slots[18-1]
test_sl3_mail_count_equals_configured_notify_slots[9,12,15,18-4]
test_sl3_mail_count_equals_configured_notify_slots[-0]
test_sl4_data_lands_even_outside_the_notify_slots
test_sl5_each_mail_only_contains_hits_new_since_the_last_one
test_sl6_two_configured_slots_mean_two_fetches
test_sl7_clearing_the_fetch_log_does_not_resend_the_mail
test_sl9_the_time_source_is_a_module_attribute
test_sl9b_patching_the_slot_changes_the_behaviour
test_sl10_the_detail_limit_is_shared_across_slots
test_sl13_the_api_no_longer_claims_the_limit_is_unchangeable
test_sl16_no_new_hits_means_no_mail
```

📌 **A 推論「有 2 紅不在 §3j」—— 那個推論不成立，而它錯得很合理**：
`5964ce5` 版那個檔是 **12 個函式**，而 `test_sl3` 是 `parametrize` ×3
⇒ **12 − 1 + 3 = 14 個測試項**。**函式數與測試項數不是同一個東西。**
（實測：`git show 5964ce5:...| grep -c '^def test_'` → 12；
而 14 個紅**全部**以 `test_sl` 開頭。**沒有非 §3j 的紅。**）

🔑 **但 A 要求把名單寫進來是對的，而且理由跟我自己今天寫的那條一樣**：
在補上之前，「無回歸」這個結論的唯一支撐是我的證詞，
**而支撐那句證詞的那張表只存在於我的終端機裡。**
⚠️ 上一次是佔用宣告，這一次是**證據** —— 同一個形狀的第二次。
⇒ **回報一個比對結果時，要把被比對的兩邊都留下，不是只留結論。**

📌 **先預測、再量測**：A 已經事先把「紅了就 bisect 那兩個 commit」寫死成承諾，
所以我有責任先把**不該進 bisect 的那些**挑出來。
沒有那張名單的話，14 個預期中的紅會跟「真的回歸」混在同一個數字裡。

### ⚠️ 而我延長死線的那個判斷本身是個量測錯誤

21:31 看到「10%」，我線性外推成 40 分鐘、把死線從 22:05 改成 22:15。
**實際 22:12 跑完（21:49 結束）。**
🔑 **進度條不是線性的** —— 前段是大量快測試，後段才是慢的。
⇒ 拿「已完成百分比 ÷ 已用時間」推總時間，**在非均勻工作量上是無效的**。
（這個方向錯得比較安全：我講長了。但它仍然是一個沒有根據的數字。）


## ✅ 全量回歸完成（2026-09-21 23:32 → 09-22 00:00，佔用已釋放）

**對 `1c6ea91`**（B 停工 `6e4dcc9`），**零環境變數**，27:05。

```
1 failed, 1297 passed, 55 skipped      1297 + 1 + 55 = 1353 ＝ 起跑當下的基準
預期外的紅：無        預期紅但沒紅：無
```

唯一的紅是事先寫下的那一題（`test_every_declared_condition_has_a_test`，刻意紅）。

### 握手乙：**我用兩個獨立來源各驗一次**

```
① git：跑的期間沒有產品碼 commit、工作樹在產品碼範圍內乾淨
② mtime：git 追蹤中的 backend/frontend 檔（排除 tests/），
        沒有任何一個的 mtime 落在 23:32:00–00:00:10 這個視窗裡
```
📌 **刻意只看 `git ls-files` 追蹤中的檔**：A 在這段期間重啟了 666，
而那會讓 log／`__pycache__`／`.initial_admin_credentials.txt` 這些**未追蹤**檔案
的 mtime 變動 —— 那不是產品碼異動，看整個工作樹會得到一個沒有人做的異動。
🔑 **「工作樹變了」與「產品碼變了」是兩件事**（〈工作樹≠repo〉的第二個形狀）。

### 📌 死線這次估對了，而理由值得記

用「**前一次同規模實測 × 1.5**」＝ 33 分鐘，實際 27:05。
⚠️ 上一輪我用「已完成百分比 ÷ 已用時間」外推成 40 分鐘，實際 22 分 ——
**進度條不是線性的**（前段是大量快測試）。
🔑 **同一份工作的前一次實測，比這一次跑到一半的觀測更可靠。**

## 📋 這一輪的基準與預期紅燈（起跑前先寫，起跑後不改）

**基準 1353**（`--collect-only` 現場數的；上一輪是 `5964ce5` 的 1327，
之後加了 NETGUARD 自測、規格覆蓋率守門、地圖搬家 2 題、`_timefreeze` 等）。

### 預期會紅的：**只有 1 題**

```
tests/test_spec_coverage_2026_09_21.py::test_every_declared_condition_has_a_test
```
📌 它是**刻意紅**的：規格裡宣告了而還沒有人寫成測試的那 22 條，
**紅燈本身就是那張清單**（A 裁定先不分流）。

⚠️ **這份名單是「候選紅燈檔逐檔跑」量出來的，不是全量跑出來的。**
全量跑有**順序與共享狀態**這兩個變數，所以：
🔑 **名單之外的任何一題紅，第一假設是「真的回歸」，第二假設才是「跑法差異」。**

> 🔴 **而「跑法差異」這個主張要付代價**（A 加的，我同意）：
> **必須把那一題單獨再跑一次、證明它單跑是綠的，才准這樣主張。
> 沒有那個動作就是回歸。**
>
> ⚠️ 理由是這句話太好用：跑完之後「那大概是順序造成的」
> **在當下跟真話長得一模一樣**，而它不需要任何證據就能說出口。
> 🔑 **一個不需要證據的解釋，會把所有需要證據的解釋擠掉。**

📌 A 事先警告 `test_module_keys_consistency` 會因為新的 `map` 模組 key 而紅
—— **實測它現在是綠的**（B 已把 key 補進去）。寫在這裡，
因為「被警告過而沒有發生」跟「沒有被警告過」在結果上一樣，
**而只有前者需要記一筆說明為什麼沒發生。**

## 收工檢查表

**第 5 輪（2026-09-21）** —— ②完成，等 B 寫碼

- [x] 只動了 `backend/tests/**`
- [x] **我沒有讀實作**（`schedule_tender_scan`／`run_scheduled_scan`／`notify_tender_*`
      都還不存在，我用 `hasattr` 查過；既有模組的介面用 `dir`／`signature`）
- [x] 新檔 24 題：**22 紅 2 綠**（2 綠是 R3，B 第 4 輪就做對了）
- [x] 結轉 R1／R2 已套用到第 4 輪檔：**49 題，1 紅**（R1 等 B 同輪改）
- [x] S5（最外圈）實測 harness 正確：子行程 `returncode=0`，紅在「被呼叫 0 次」
- [x] 照 §3 **`7bec22d`** 寫
- [ ] ⑤反向驗證（§3 七題）—— 等 B 寫完碼
- [ ] ⑥全量回歸 —— 等 B 停手宣告，基準 **1,175**（目前 collect **1,202**）
- [x] 我釘的名字都寫進〈給彙整〉第 24 點

### 前幾輪（已結案）

- 第 1 輪：27 題、⑤三個突變、⑥ 1,111 全綠
- 第 3 輪：17＋1 題、⑤五個突變、⑥ 1,129 全綠
- 第 4 輪：46 題、⑤六個突變、⑥ 1,175 全綠（中途抓到四張新表沒登記備份）

---

### 37 · §3i 連線洩漏：觀測點搬了層，而每一道前提都得跟著重驗

**檔案**：`backend/tests/test_dashboard_connections_2026_09_22.py`（23 題，現在全綠）

#### 一、B 是對的：觀測點要包在 `db.get_db`

我第一版包 `routers.dashboard.get_db`，依據是今晚反覆用的那條
「**patch 目標要走模組**」（`dashboard.py:11` 是 `from db import get_db`，它持有副本）。
那條**當時是對的**。而 B 把 12 處全轉成 `with db_conn() as conn:` 之後，
`dashboard.py` 裡再也沒有一處裸的 `get_db()` ⇒ 我包的名字再也不會被呼叫，
九題全紅在「一個連線都沒開」。

> 🔑 **「走模組」保證你打得到，不保證你打在連線真的被開出來的那一層。**
> **重構會把「哪個模組」這件事搬走。**（B 的話，我照收）

#### 二、☠️ 而搬上游的代價落在盲側 —— 這才是這一條的重點

改包 `db.get_db` 之後，**帳本看得到的不只是被測端點**：
`make_user` 與 `/api/auth/login` 自己就會開連線。後果有兩層：

| 層 | 現象 | 誰抓到的 |
|---|---|---|
| ① | `EXEMPT` 那題紅在 `/api/now` 開了 1 個連線 | 測試自己，**很吵** |
| ② | ⚠️ **九題的前提 `open >= 1` 變成空的** —— 登入自己就滿足它 | **沒有人**，它全綠 |

第②層才貴：那道前提是我**專門寫來擋 `/api/now`** 的，
而觀測點一搬，它安靜地失效了 —— **而全綠的樣子跟它還在工作時一模一樣。**

> 🔑 一個觀測點的位置同時決定了**打不打得到**與**混不混得進來**，
> 而這兩件事往**相反方向**移動。
> ⇒ **搬了觀測點，就得回頭把每一道前提重驗一次**，不是只驗那些變紅的。

修法：`_auth()` 拿到 token 之後把帳本歸零，並在 docstring 寫死理由。

#### 三、前提自己也要有人證明它還活著

`open >= 1` 沒有辦法自我證明。證據在另一題：
`test_an_exempt_endpoint_really_does_not_touch_the_database[/api/now]` 綠
⇒ `/api/now` 歸零後開 0 個連線 ⇒ 它若進 `DASHBOARD_PATHS` 那道前提就會紅。
**兩題是一對**，已在檔內互相註明 —— 刪掉任一邊，另一邊會安靜地變空。

#### 四、清單守門（新增 2 題）

`DASHBOARD_PATHS ∪ EXEMPT` 必須等於 `dash.router.routes` 裡**真的註冊**的 GET 路由。

- 讀的是 `router.routes` 不是原始碼文字 ——
  〈診斷的層級決定覆蓋率〉：文字比對答的是「有沒有被提到」，不是「有沒有被掛上去」。
- 配反向控制：`EXEMPT` 的每一支**實際跑一次**，驗它真的開 0 個連線
  ⇒ 擋掉「把會洩漏的端點寫進排除清單就變綠」。
- 兩支 GCIS 端點換掉 `dash._gcis_get` 這個**產品自己的接縫**，
  **不放行 NETGUARD** —— 放行的話這題會變成一個對外連線的測試。

#### 五、B 的重構成效（實測，不是推論）

改完之後 21 題全綠（9 正常路徑 ＋ 1 累積 ＋ 9 例外路徑 ＋ 2 清單守門）。
改之前我量到的是：正常路徑洩漏 2 支、**例外路徑洩漏 9 支（全部）**。

#### 六、同輪順手記

- `import routers.dashboard as dash` 被我自己前一個 edit 連帶刪掉，
  **今晚第三次同一個形狀**（`run_python`／`_captured_request`／`dash`）。
  三次的共同點都是「改 A 的時候順手動到 B 共用的東西」。
  這次是靠改完**重讀自己的 diff** 抓到的，不是靠跑測試。

---

### 38 · 規格覆蓋率守門大改：它同時是一道閘門，而它的判準太寬

**檔案**：`backend/tests/test_spec_coverage_2026_09_21.py`（7 題，全綠，⑤五個突變全中）

#### 一、它原本會擋住出貨

A 查出 `build_deploy_package.ps1:303-307` 第 6 步跑 `pytest -m "not e2e"`，
**任何一題紅就中止打包** ⇒ 這一題只要有一條沒寫，整包就出不去。
而它紅的理由是「§3n 還沒開工」——**那不是一個品質問題**。

> 🔑 A 的話：**「我把一個守門當成清單用，而它同時是一道閘門。」**

⇒ 拆開兩個角色：**閘門**＝「沒有任何一條是沒人管的」；**清單**＝ `PENDING`，它是資料。
A 原本要我「把 8 條寫完才能打包」——**實際欠帳是 40 條不是 8 條**
（他用的是舊的、只認項目符號的解析器），八條寫完也打不了包。

#### 二、🔴 我的解析器漏掉 44 條，而它差一點拿去否定 A

`_DECLARED` 只認 `- **P12.**` 這種項目符號。而 §3o／§3p／§3q 的驗收條件
**寫在表格裡**（`| **P12** | … |`）⇒ A1–A10／P5–P16／Q1–Q10／G3–G9／V1–V5 一條都讀不到
⇒ 守門會指著規格說「**你沒有宣告這個編號**」，而規格明明寫了。

補上表格樣式後 declared 從 119 → 163，**零誤判**（沒有任何突變表被誤讀成宣告）。

> 🔑 〈判準的寬窄都會騙人〉的**窄**那一側，而記憶裡那條就寫著
> 「⚠ 而你會拿它去否定別人」——今天就發生了，差一步。

#### 三、🔴🔴 更嚴重：**編號不是唯一的**，163 個裡有 26 個重複

```
規格 1758-1776   T1..T8  標案→案件轉換   ← dev_case_id 在產品碼裡一處都沒有
規格 6659-6753   T1..T5  pytest 守門     ← 我寫的
測試 tiles       T1..T7  圖磚探測        ← 我寫的，完全無關
```

☠️ **標案轉案件那一整組（功能根本沒做）被判定為「已覆蓋」。**

🔴 **而最刺的是 T8**：它出現在欠帳清單上，**只因為剛好沒有別的檔用到 `t8`**
⇒ A 看到的是「T 系列只欠一條」，真相是「**八條全欠，其中一條靠運氣現身**」。

> 🔑 **一個太寬的判準不只會漏報 —— 它會給你一份看起來很具體的錯誤清單。**
> 而那份清單會被拿去排順序。

還有第三種撞名，方向完全不同：**`s3` 同時是 Amazon S3**
（`test_cloud_storage_2026_09_07.py` 有六支 `test_s3_*`）。
⇒ 前兩種是「規格重用編號」，這一種是
**「編號這個形狀本來就不是規格的專利」——`s3`／`t100` 比規格早存在。**

⇒ 撞名的一律不給「已實作」信用，改列進 `AMBIGUOUS_ACK`（凍結，多一個少一個都紅）。
**守門偵測到自己的前提被違反，而不是安靜地給出錯的答案。**

#### 四、☠️ 我在同一個動作上連犯三次：**列表用回想代替查**

| # | 我填的 | 真相 | 誰抓到 |
|---|---|---|---|
| 1 | A1–A10 填進 `PENDING`（欠帳） | **早就寫好了**（`test_geo_fallback`） | 我新加的反向控制 |
| 2 | `C_OWNED` 預寫 `LK1`／`LK2` | 那個改名我後來決定不做 | 我新加的幽靈列檢查 |
| 3 | `C_OWNED` 寫 SL20–SL28 連號 | **SL21／SL22 不存在**（跳號） | 同上 |

三次都是同一個動作：**列一張表的時候，用回想代替查。**
🔑 〈主持人的記憶是負債〉要再加一句：**恢復靠清單不靠記憶，而清單自己也要用查的。**
📌 而三次全被**我在同一輪剛寫下的那兩道反向控制**抓到 ——
那是〈修作法不要修結果〉少見的正面例子：**擋住我的，是我為了擋別人而寫的東西。**

順帶刪掉 `EXEMPT["SL15"]`：理由欄寫著「規格裡沒有這一條」
⇒ **沒有東西需要被豁免，而它佔著一個「已處理」的位置。**
🔑 **一張表的雜訊會被讀成決定。**

#### 五、⑤ 反向驗證（五個突變，全部精準變紅）

| 突變 | 精準變紅 |
|---|---|
| 從 `PENDING` 偷刪一列 | 第一題（沒人管） |
| 把已完成的編號塞回 `PENDING` | 反向控制（垃圾桶） |
| 從 `AMBIGUOUS_ACK` 偷刪 `T1` | 撞名偵測 |
| `C_OWNED` 塞一個不存在的編號 | 幽靈列 |
| 把表格解析器改成永不匹配 | 解析器量尺 |

---

### 39 · §3p 第三節（P12–P16）：16 題、14 紅 1 綠

**檔案**：`backend/tests/test_map_own_vendors_2026_09_22.py`

#### 一、🔴 抓到產品碼一個安靜的 bug，而它正好會在 P12 上爆開

`routers/map_points.py` 的點字典裡 **`"source"` 出現兩次**（172 與 176）：

```python
"source": "tenders",                                   # 172：資料集
"precision": found.precision, "source": found.source,  # 176：定位服務
```
Python 後面的鍵覆蓋前面 ⇒ `point["source"]` 實際上是 `"nominatim"`。

⚠️ 現在看不出來，因為地圖上只有一種資料集。
**而 P12 一加進廠商，每一個點都會說自己是 `nominatim`** ⇒ 前端無法分開上色。
🔑 **兩個不同的意思搶同一個名字**（資料集／定位服務），而兩個值都合法 ⇒ 不會報錯。
⇒ 釘 `dataset`（資料集）與 `source`（定位服務）兩個分開的鍵。

#### 二、🔴 規格沒寫而我加了一題：`contractors` 是**自然人**名冊

`routers/contractors.py` 每一支都是 `require_superadmin=True` ＋ `module='contractor_list'`，
欄位有 `id_number`／`bank_account_number` ⇒ **那個 `address` 是住家地址。**
畫在「已登入就看得到」的地圖上＝**從一扇新的門把保護降級**，而地圖會正常運作。
🔑 〈降級之後它還是會動〉。P12c 釘死它沿用 `contractor_list` 權限。

#### 三、P14 我**不釘 precision 的值**

§3o 實測過 Nominatim 認不得台灣門牌，沒金鑰時廠商一樣退到 `district`，而開發機沒金鑰
⇒ 斷言 `precision == "rooftop"` 會讓**正確的實作在開發機上變紅**。
今晚第三次同一個機會（CSP 萬用子網域、連線洩漏的 `with`）。
⇒ 釘不變量：**存在、在階梯上、不知道時是 `None` 不是省略**。

#### 四、⚠️ 假貨的形狀決定了題目驗得到什麼（兩件）

1. **`geo.tiles_blocked` 要關掉**：`/api/map/points` 每次都真的去打
   `tile.openstreetmap.org`。抓到的是 NETGUARD，**而它只報在第一題**
   （探測結果有快取）⇒ **後面的題看起來乾淨，只是第一題替它們打過了。**
   回 `None` 不回 `False`：回 `False` 會讓「三態被壓成兩態」的 bug 在這個檔裡看不見。
2. **`geo.locate_cached` 的假貨每個地址回不同精度**。
   一律回同一個精度的話，P14b 就只驗得到「有一個精度值」，
   而一個**全部寫死 district** 的實作會全綠 —— 那正是要防的東西。
   ⇒ 斷言變成「這一筆的精度等於這一筆地址該有的」，驗的是**逐點傳遞**。

#### 五、兩個自己抓到的空綠

- P12b 的 `None not in datasets`：**點為空時空集合裡當然沒有 `None`** ⇒ 補前提。
- P14b 原本寫「查不到對應就 `continue`」：**點沒有 `address` 鍵時整段迴圈什麼都不驗** ⇒ 改成斷言。
- `own_data` 也塞了一筆標案：不塞的話 P12b 在 B 做完之後**仍然紅**，
  而紅的理由會變成「沒有標案」—— 一個跟 P12b 無關的理由。
  🔑 **紅燈也要紅在對的地方**，否則下一輪有人會去修一個不存在的問題。

---

### 40 · 規格欠帳八條：17 題、1 紅 16 綠；而 U5c 的判準是我自己寫錯的

**檔案**：`backend/tests/test_spec_debts_2026_09_22.py`（E3／N7b／U5c／U8／U9／U10／U11）

#### 一、🔴 U5c：我第一版用文字比對，同時誤報又漏報

第一版掃 `DROP COLUMN`／`DROP TABLE`／`RENAME TO` 的字面，四紅：

| | 真相 |
|---|---|
| v62 `_m062_case_project_merge` | ⚠️ **誤報**：`DROP TABLE` 只出現在**註解**裡（「刻意不 DROP TABLE」） |
| v14／v35／v37 | SQLite 的**標準重建表慣用法**（建新表→複製→丟舊表→改名），因為 SQLite 不能 `ALTER TABLE ADD CONSTRAINT` |

🔑 〈診斷的層級決定覆蓋率〉：**文字比對答的是「有沒有被提到」，不是「有沒有發生」。**
⚠️ 而漏報那一側更重要：一支用 `CREATE TABLE new (少一欄)` ＋複製＋改名
**弄掉一個欄位**的 migration，字面上一個關鍵字都不會命中。

⇒ 判準換成**行為**：從 v0 逐支跑上來比對前後 schema，**只要有 (表, 欄位) 消失就紅**。
一次 O(n) 掃完全部 89 支（不是抽樣，也不是 O(n²)）。
✅ 結果：**89 支裡沒有任何欄位消失** ⇒ A 那條裁決（「版本比程式碼新時只記 log」）
的前提**行為上成立**，只是**文字上不成立**。

#### 二、🔴 U8 是規格與實作的真牴觸，等 A 裁

規格：`schema_version` 表不存在 → 回 0。**實測：丟 `OperationalError`。**
而 `db.py:597` 的 docstring **刻意**移除了 `try/except: return 0`，論證是
「那個情境到不了」＋「讀不到就要拒絕，不要猜一個看起來最無害的值」。

☠️ 最省事的修法 `except sqlite3.OperationalError: return 0` **會直接違反 U9**
（把鎖住／損毀也吞成 0，而 0 的意思是「從第 1 支 migration 從頭跑一遍」）。
🔑 **唯一同時滿足 U8 與 U9 的寫法是先正面查 `sqlite_master`**，不是用例外分辨。
⇒ 已寫 U8b 當護欄（表在、但欄位名不對 ⇒ 必須拋出）。

#### 三、📌 `db.py:597` 的註解寫著「守門見 `test_u5c`」—— 那支測試不存在

跟 SL17／SL18 同一個形狀。**一句「已經有人在守」的話，本身不是守門。**
同一族還有 `db.py:668`「Each function must be idempotent」—— U10 到今天才守住它。

#### 四、U10 不等於既有的 `test_u1b`

`test_u1b` 跑的是「整組再跑一次」，而第二次 `current >= CURRENT_VERSION`
⇒ **一支都不會再執行** ⇒ 它證明的是「引擎會跳過」，不是「每一支自己可重跑」。
⇒ U10 改成：建到 v(i-1) → 跑第 i 支 → **再跑一次第 i 支** → schema 快照必須相同。
⚠️ 只抽樣最後 12 支（建庫是 O(n²)），**而我把「只驗了尾巴」寫在題目裡，不留給別人猜**。

#### 五、綠著到貨的六條，每一條都寫了「什麼改動會讓它紅」

A 的判準：**說得出來就是回歸守門，說不出來就是防著不存在的問題。**

| | ↩︎ 什麼改動會讓它紅 |
|---|---|
| E3 | `== "1"` 改成 `bool(...)` 或 `!= "0"` |
| E3b | `TENDER_RADAR_ENABLED or getenv(...)` 只剩後者 |
| N7b | `0 < days` 改成 `0 <= days` |
| N7b-b | `_remember_first_scan()` 搬到 `_in_quiet_period()` 之前 |
| U9 | 補回 `try/except Exception: return 0` |
| U11 | `_get_version` 改成 `row[0]`（裸連線就能用，慣例失去強制力） |

#### 六、順手

- `from test_upgrade_path_2026_09_21 import _build_at_version`：**借用不複製**。
  那支 harness 有 `test_u0` 當量尺；複製一份的話兩份會各自演化，
  **而 U0 只覆蓋得到它自己那一份**。
- ⚠️ 單獨跑一個檔時 `sys.path` 不含 `tests/`（pytest 的插入發生在收集那一刻）。
  🔑 **「整批跑得起來」不等於「單獨跑得起來」，而單獨跑正是除錯時的跑法。**
- `b"中文"` 是 SyntaxError（bytes 只能放 ASCII），要 `"...".encode()`。

---

### 41 · §3n（G1–G9）18 題 17 紅；A16 重現 D 的三次請求

**檔案**：`test_map_user_location_2026_09_22.py`（18 題，17 紅 1 綠）
／`test_geo_request_count_2026_09_22.py`（4 題，1 紅 3 綠）

#### 一、🔴 `distanceKm` 改名會讓地圖**安靜地不顯示距離**

G1 要 `distanceFromOfficeKm`，而現在叫 `distanceKm`，
**`frontend/pages/map.html` 有 5 處在用**（230／233／236／397／398），而且是：
```js
<template x-if="p.distanceKm === null || p.distanceKm === undefined">
```
⇒ 改後端不改前端 ⇒ **每個圖釘都走進「沒有距離」那個分支**：
不會壞、不會報錯，**只是再也不顯示距離**。
📌 **這件事測試這一側看不見**，已明講給 B。

#### 二、⚠️ G7 照規格字面寫會是**空綠**，我改了它的形狀

`geo.locate()` 在 `geo_on()` 關著時直接回錯誤 ⇒ **一個點都產不出來**
⇒「每個點的距離都是 null」在空清單上永遠成立。
🔑 空集合假綠燈**這一次藏在規格條文本身裡**。
⇒ 改成**對照**：關著→沒有點且 `geoEnabled` false；開著→有點且有距離。
證明的是「**這個開關真的有差**」。

#### 三、🔴 G9（隱私）一開始是綠的，而它空綠

參數都還沒被端點接受，當然沒東西被寫下來。
⚠️ 我自己在 G9b 寫過「**一道永遠綠的隱私守門比沒有守門更糟，因為它讓人不再去看**」
—— 那句話對 G9 自己同樣適用。
⇒ 補前提：`userAccuracyM == 42`（座標真的被收下了）才往下掃。現在它紅。

📌 G9 掃的是 `system_settings`／`audit_log`（2,254 列）／`user_request_log`（1,893 列）。
⚠️ **`user_request_log` 存的是 `path`，而 query string 在 path 裡** ——
「順手記下完整網址」就足以把一個人的位置留在磁碟上。

#### 四、G5 我刻意**不驗 `accuracy=0`**

`0` 的語意不明（「完美精準」還是「沒量到」？），而**這一輪沒有人裁過它**。
🔑 不把一個沒有人決定過的值寫成斷言 —— 那等於**我替使用者做了決定**。

#### 五、A16：精確重現 D 的實測

```
['台中市西屯區台灣大道三段99號', '台中市西屯區台灣大道三段99號', '台中市西屯區']
                                  ↑ 重複的那一次
```
斷言把**問了哪些地址**一起印出來：只印次數的話，
「3 次」與「3 次而且其中兩次一模一樣」讀起來一樣，**而後者才說得出成因**。

✅ 加了 **A16d**（D 自己提出的後果）：`locate()` 與 `locate_cached()`
**各自**都要退到行政區。抽出共用函式之後，`locate()` 的退階只剩一個呼叫者
⇒ 〈兩個都對而路不存在〉：**用一次重構製造一個新的斷點。**

⚠️ 假貨把 Google／TGOS 也換掉：真實環境沒金鑰時它們回 `None`，
而**「沒有金鑰」與「查不到」在計數上長得一樣** ——
不換的話這一題會依賴開發機剛好沒設金鑰。

#### 六、覆蓋率表同步

- **V4／V5 從 `PENDING` 移到 `EXEMPT`**：§3n 🅱 節自己寫明「只有目視驗得到」。
  欠帳與豁免的差別是**時間**，混在一起的話欠帳會變成豁免，**而那是單向的**。
- **A16 暫列 `C_OWNED`**：A 口頭裁了但 **STATE.md 還沒有這三條**。
  🔑 那是 SL17／SL18 的**反向**：那次是「規格有而測試沒有」，這次是
  「測試有而規格沒有」——**兩種都是裁示與落地之間那一步沒有痕跡。**

---

### 42 · §3q（Q1–Q10）15 題 13 紅；而 Q3 第一版是我自己寫的空綠

**檔案**：`backend/tests/test_tender_search_and_org_geo_2026_09_22.py`

#### 一、🔴 Q3 第一版：`assert "Q-001" in cases`

篩選還不存在時三筆全回 ⇒ **`in` 自動成立**。
🔑 〈判準的寬窄都會騙人〉的**寬**那一側，而這一次它就在我自己寫的斷言裡。
⇒ 改成相等：異體字要命中 Q-001，**而且不相干的兩筆要被篩掉**。
📌 兩種紅法現在分得出來：三筆全回＝沒在篩；只少 Q-001＝沒套 `normalize`。

#### 二、我釘什麼、不釘什麼

**不釘「命中區排在全部清單之上」** —— `matchedWatches` 已在每一筆上（P6），
前端拿它自己分區就成立。釘後端回獨立陣列＝把一種實作寫成不變量，
**而一個在前端分區的正確實作會紅**。今晚第四次同一個機會。

**釘的是後端分辨得出來、前端分辨不出來的**：
- **Q3**：異體字表在 Python 裡 ⇒ 前端做不到「同一套」⇒ 手動搜尋必須在後端
- **Q4**：「沒有搜尋條件」vs「有條件但沒命中」⇒ 前端要再打一支 API，
  **那是兩次往返加一個競態**（中間有人刪掉條件就會講錯話）

⚠️ `_items()` 刻意**兩種回傳形狀都接受**（裸陣列或帶外殼的物件）：
Q4 要多一個欄位，而那讓回傳可能從陣列變成物件 —— **兩種都是對的實作**。

#### 三、Q8 的規格文字已過期，我照 A 的裁定寫並在檔頭記錄

規格 §3q 還寫著「名稱在資料庫裡被截斷」（**A 複驗確認是它自己 print 截斷**）
與「長度剛好等於某個上限」（**A 已裁定拿掉**）。
保留的是那個不對稱：**查不到會退階（安全），查到錯的不會。**

📌 **而資料庫裡現在一個被截斷的名稱都沒有** ⇒ 這道守門今天不會被觸發
⇒ 〈計數器要有落點〉：**必須有一題餵合成的截斷名稱證明它活著**（Q8），
**再配一題證明它不會誤殺完整名稱**（Q8b，名單含資料庫裡最長的 19 字那個）。

#### 四、⚠️ `tender_watches` 的欄位是 `name` ＋ `keywords`（JSON 陣列）

我憑印象寫成單數 `keyword`，sqlite 直接擋下來。
**今天第四次「用回想代替查」**，而這一次**是資料庫替我擋的**，不是我想到要查。

#### 五、A 的 G5b：`accuracy=0` ⇒ 422

我原本刻意不驗 0（沒有人裁過，不替使用者決定）。A 裁了，**而理由比我想的深**：
> **理由不是「0 不合理」，是「0 會讓我們說一句謊」** —— 瀏覽器定位沒有任何情境
> 能宣稱誤差為零，收下 0 的話畫面會顯示「誤差約 0 公尺」。
> ☠️ **那比不顯示誤差更糟：它是一個具體而錯誤的保證。**

#### 六、量測條件（B 與我共用工作目錄，數字會在我打字時改變）

此刻：§3p三 **18/18 綠**、A16 **4/4 綠**（三次請求已修掉）、
§3n **18/19**（只剩剛裁的 G5b）、§3q **2/15**。
⚠️ 這是**工作目錄當下的版本**，不是任何一個 SHA。
