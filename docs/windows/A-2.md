# 視窗 A-2 · 共同校驗人

> ⚙️ **這個檔由 A 維護，A-2 交內容**（比照 `D.md`）。
> 理由見下方〈為什麼要有這個檔〉。
> 🔴 **A-2 不自己寫這個檔** —— 能自己改範圍的人等於能自己決定做什麼。

## 視窗身分

```
名稱        A-2
實例        hichan-4b（2026-09-22 當日；⚠️ **名字會撞，靠對方自報的工作目錄辨認**）
設立        MULTIWIN-PROTOCOL.md §2c，使用者 2026-09-22 建立
寫入權限   🔴 **零** —— 不寫 repo 任何檔，產出一律經 A 收進權威檔
職責        當 A 判別錯誤時提出質疑；A 與 A-2 共同討論做出決策
順序        A → A-2 → B、C、D；B、C、D 同時將資料傳給 A 與 A-2
機制限制   ⚠️ **訊息驅動，沒有計時器** —— 它不會在某個時點自己醒來。
            🔑 **所以死線不可以掛在 A-2 身上，也不可以掛在 C 身上。**
```

## 🔴 為什麼要有這個檔（2026-09-22 裁定，甲案）

```
B／C／D   自己的檔自己寫   ⇒ 產出**直接**落地
A         寫 STATE／SCOPE     ⇒ 產出**直接**落地
A-2       **唯一路徑是「A 採納進 STATE.md」**
```
☠️ **被 A 採納的質疑會留下；被 A 駁回的質疑不會。**
🔑 **而一個校驗者最該留存的，正好是那些沒被採納的** ——
它們是日後回頭看「當時有沒有人提出過」的唯一證據。
📌 〈更正要留著錯的那一列〉的組織版：
**現在的結構讓「A 判斷正確」與「A 駁回了一個對的質疑」在紀錄上長得一模一樣。**

✅ 而 `A` 沒有 `A.md` 不構成同樣的問題：A 的產出本來就落在權威檔上。

---

# 2026-09-22 · 第一批內容

## 一、被 A 駁回的質疑（四條）

> 📌 四次事後看駁回都是對的。
> ☠️ **而正因為四次都對，現在沒有人能從紀錄裡分辨「第五次也會是對的」。**

```
① TD4 與 GC8 相衝，列了甲乙丙三條路要求共同決策
   駁回理由   B 實測 158 個機關名稱 ⇒ 前提不成立 ⇒ TD4 進 NEXT
   落地       ✅ 推翻理由在 §21；❌ **「A-2 提過這個風險」沒有任何紀錄**
② 主張負快取要排除機關名稱
③ RG2 的版本（A-2 版）
④ 主張現在就去量那 53 個 skipped
   ⚠️ 🔴 **而這一條事後證明駁回是錯的** —— 見下一節第三條。
```

## 🔴 二、被 A **採納而錯**的（三條）

> 🔑 A-2 指出：**這一類比「被駁回的」更重要** ——
> 它證明的不是「A 會駁回對的質疑」，是「**A 會採納錯的質疑**」。

```
① RG2 改採 A-2 的版本「非 e2e 的 skipped ＝ 0」
   實測    53 ⇒ **達不到**，而上一包也是 53
   ☠️ 那是一個**從出生就達不到的門檻**，而沒有人先去量過它
② 照收 C 的「53 skipped 全是 e2e」（這一條是 C 提、A 採）
   實測    55 deselected ⇒ 53 在非 e2e 裡 ⇒ **錯**
   ☠️ 而 A 還替它補了一層解釋（「1711 是拿錯欄位」）
        ⇒ **一個沒驗的前提看起來被獨立證實了**
③ 🔴 駁回了 A-2 「現在就去量那 53 個」的主張
   而如果當時量了，上面①②兩條**一個都不會發生**
   📌 〈量它不等於修它〉：「太貴所以先不做」算的是修它的成本，
      **被拿去否決了量它** —— 而量它只要一個 `-rs`。
```

## 三、A-2 自陳的錯（它自己提的，留著）

```
① 所有 as-of 時間戳錯八小時 —— 從 D 的截圖時間推算，從來沒跑過 `date`
② 在回覆停手握手的**同一則**訊息裡要求 A 改 §6 兩處（一邊確認停手一邊要對方動檔）
③ 承諺「17:51 我會去問你」—— **它是訊息驅動的，做不到**（已自己收回）
④ 寫「訊息無 grep」—— **一次都沒試過**，把「我沒試過」寫成了「那個載體的性質」
⑤ `ls --time-style=+%H:%M:%S | awk` 丟掉日期 ⇒ 舊檔混進來
   🔑 **救它的是一個矛盾（時間在未來），不是謹慎**
```

## ✅ 四、A-2 提出而被採納且正確的

```
· SCOPE.md 只放清單，「為什麼這樣訂」寫在 STATE.md §20
  🔑 搬一次位置是修結果；把「資料」與「關於資料的討論」分開才是修作法
· RG2 原版有漏洞（永久 skip 會投機）—— **漏洞是真的**，而修法錯了
· 量工作樹的邊界：它抳不到 CPU 競爭（那會讓時序題紅得像真 bug）
· 承諺一個未來時點的動作之前，先問「我靠什麼在那個時點醒來」
· A 的狀態清單在寫下的那一刻就已經漏了一筆（兩層都要防）
```

---

## 📌 維護規則

```
1  A-2 交內容，A 寫進來。A-2 不直接寫這個檔。
2  🔴 **被駁回的質疑一律要收**，逐條寫「提了什麼／A 為什麼駁回」。
3  🔴 **被採納而錯的也要收** —— 那一類更重要。
4  錢不要把錯的那一列改成對的當沒發生過。
```
