# `EM1` 長錯誤訊息（>25 字）的三分類 —— 施工圖

> A-2 撰寫／2026-09-23。量測座標 **`faf8df7`**，量測時工作樹 **7 個異動**（不是我的）。
> **單一版本、無修訂層。照這一份做。**
> 範圍：`HTTPException` 的 `detail` 是**字串字面值且長度 > 25** 的那 **41 條**。
> ⚠️ `call`(34)／`binop`(31)／`name`(26) 那三類 `detail` **本項不在範圍**（A 已裁）。

---

> 🔴🔴 **`§0`／`§1`／`§2` 是第一遍的結果，已被 `§2b` 取代。**
> 它們**留著**是因為結論變了三次而每一次的理由不同（〈更正要留著錯的那一列〉），
> ⚠️ **而不要照它們動工** —— 兩件已經不成立：
> ```
> ① 數字：C 類 13 -> 要改的是 **6**（`§2b`）
> ② **行號已位移**（別人在改 system.py）—— `§1` 那張表的行號**不可再引用**
> ```

## §0 🔴 分類結果與預期不同：**純口語 0 條**

逐條讀完 41 條之後：

```
A 類  已經對的，**不要動**                     **28 條**
B 類  過度口語（語域不對）                     **0 條**   ← 🔴
C 類  **讀者錯了**（寫給工程師的）              **13 條**
```

🔑 **在 `HTTPException` 這一側，使用者說的「過度口語」實際上一條都沒有。**
📌 而那與他的原話一致：他點名的是**標案雷達的信件**，不是錯誤訊息。
⇒ **`EM1` 的內容是「讀者錯了」，不是「語氣不對」。**
☠️ 若照「改語氣」去做，會把 28 條已經對的訊息改一遍，**而真正的問題一條都沒碰到**。

---

## §1 C 類：13 條，**問題不是資訊少，是讀者錯了**

```
 1 cashier.py:132           status 必須為 unreceived／received／all
 2 daily_tasks.py:345       year_month 格式錯誤，應為 YYYY-MM
 3 inventory.py:614         action 需為 void / return_to_stock / edit_note
 4 map_points.py:333        X-Map-Position 的格式是 <lat>,<lon>,<accuracy>
 5 map_points.py:359        給了座標就要給 accuracy（公尺）——我們不替瀏覽器猜一個誤差值
 6 map_points.py:429        座標不可以放在網址上：uvicorn 的 access log 會把整串
                            query string 寫進 logs/server.log（一個永久追加的檔案）…
 7 system.py:452            log_date / user_id / content 必填
 8 system.py:1371           除了 localhost 之外，Origin 必須是 https://（瀏覽器規格要求）
 9 system.py:1396           RP ID 與 Origin 必須同時設定或同時清空
10 system.py:1502           cycle_start_day 需介於 1～28 之間（29 以後的日子二月沒有）
11 system.py:1750           backend 需為 local_drive 或 s3
12 tender_radar.py:557      沒有要變更的設定（scanHours／notifyHours）
13 vendor_contractors.py:734 action 必須為 pending_acceptance 或 accepted
```

### 🔴 它們共同的形狀：**訊息裡出現的是程式的詞彙，不是畫面上的詞彙**

```
參數名／JSON 鍵   status／year_month／action／log_date／user_id／
                  cycle_start_day／backend／scanHours／notifyHours
內部列舉值        unreceived／received／void／return_to_stock／
                  local_drive／s3／pending_acceptance／accepted
協定／標頭名      X-Map-Position／Origin／RP ID
我們自己的註解    第 6 條（uvicorn access log 那一句，**132 字**）
```

### ⇒ 處置：**同樣的資訊，換使用者的詞彙**

⚠️ **不是 `EM3` 的處置。** 兩類差別很大：
```
EM3   **非預期例外** => 使用者做什麼都沒有用 => 固定文案 ＋ 追蹤碼 ＋ 全文進 log
EM1-C **使用者輸入錯誤** => 他**改得動** => 訊息必須說出「改成什麼」
☠️ 套 EM3 的處置 => 「操作失敗，代碼 A1B2C3」 => **把一個可以自己解決的問題
   變成一通電話**
```
🔑 **對齊的是工具（`helpers/errors.py` 在哪、log 怎麼寫），不是處置。**

**改寫的判準（三條）**
```
① 參數名 -> **畫面上那個欄位的標籤**
   「year_month 格式錯誤」 -> 「請選擇月份（格式 2026-09）」
② 內部列舉值 -> **使用者在畫面上看到的選項文字**
   「action 需為 void / return_to_stock / edit_note」
   -> 「請選擇「作廢」「退回庫存」或「修改備註」」
   ⚠️ 而那三個中文**必須與畫面上的按鈕文字逐字相同** —— 否則他找不到
③ 資訊量**不可以減少**（`WD1 §0`）
   「cycle_start_day 需介於 1～28 之間（**29 以後的日子二月沒有**）」
   -> 那個括號是好的，**留著**，只換掉參數名
```

### ☠️ 第 6 條（132 字那一條）是**另一件事**

```
「座標不可以放在網址上：uvicorn 的 access log 會把整串 query string
  寫進 logs/server.log（一個永久追加的檔案）。請改用 X-Map-Position…」
```
🔴 **那是我們寫給自己的設計理由，而它被放進了 `detail`。**
⇒ 使用者看到的應該是「請改用 `X-Map-Position` 標頭傳座標」（或更上層的一句話），
**而那段理由的落點是程式碼註解**。
📌 而**註解裡那一段要留著** —— 它是這個決定的唯一記錄（〈散文對工具是隱形的〉的反面：
它現在寫在一個工具讀得到、而人讀不到的地方）。

### ⚠️ 三條是**超級管理員設定頁**專用（9／10／11 那一組所在的 `system.py`）

```
RP ID／Origin／backend=local_drive|s3
⇒ 讀者確實是懂技術的人 => **它們可以留著技術詞彙**
⚠️ 而「參數名 vs 畫面標籤」那一條**仍然適用**：
   設定頁上那一格叫什麼，訊息就叫什麼
```
📌 ⇒ 這一組的判準不是「要不要技術詞彙」，是「**與畫面一致**」。

---

## §2 A 類：28 條**不要動**

⚠️ 而**「不要動」要寫成一條可驗的**，否則下一個人會順手改。

```
28 條的共同特徵（我逐條讀過）
  用**畫面上的名詞**（報價單／完工單／簽核／回簽／部門）
  說出**出路**（請先取消回簽／請重新送審／請聯絡最高管理員）
  括號裡是**理由**不是參數（如需修正請透過案件資料整體編輯）
```
📌 其中 `case_extra_expenses.py:554` 是**目標語域的樣本**：
> 「這筆額外支出已核准，附件已上鎖。要補憑證請按「編輯」提出變更申請，
> 新附件會在簽核通過後一併生效」
🔑 **發生了什麼 ／ 現在的狀態 ／ 怎麼做 ／ 做了會怎樣** —— 四件都在，而它不是散文。
⇒ **改寫 C 類時以它為範本**，不要另外發明一種語氣。

### ⚠️ 兩條讀起來卡，而我**建議不動**

```
quotations.py:477  「…此操作不支援於已結案案件…」   語句稍硬，而意思清楚
quotations.py:4266 「請**請**申請人收回並重新送審」  疑似贅字
```
📌 **本項的範圍是「讀者錯了」，不是校對。**
⇒ 標著給使用者看，他要改再改 —— ☠️ **順手改文字會讓 `EM1` 的 diff 混進 28 條沒有人要求的改動**，
而複查的人分不出哪幾條是這一項該改的。

---

## §2b 🔴 三欄定案（A-2 重掃，`19bb94c`）—— **要改的是 6 條，不是 21**

### 前置判準（它排在「讀者是誰」**之前**）

> **先問「使用者到不到得了這個錯誤」，再問「這一條的讀者是誰」。**

☠️ 我第一遍與第二遍**都沒問這一句** —— 一直在問「這些字給誰看」，
**而沒有問「這句話有沒有機會被看到」**。重掃之後：**21 條裡有 6 條到不了**。
🔑 而它**可判定**，不是主觀：去讀消費端有沒有把 `detail` 放到畫面上。

⚠️ 它與 `EM8` **方向相反、處置也相反**：
```
EM8      **有人看得到，而照做不到**（指向不存在的出路）  => **要修**
這一種   **沒有人看得到**                                => **不要動**
☠️ 兩者在「掃描命中」那一層長得一模一樣 => 只能往消費端查
```

### ① 要改：**6 條**（reachable ＋ 讀者錯了）

```
1 case_action_items.py:206  project_approve_eng
     消費端 case-management.js:1479  alert('確認失敗：' + detail)   ✅ 看得到
     畫面標籤 users.html:820 「**案件代辦－工程主管確認**」
2 case_action_items.py:211  project_approve_biz
     畫面標籤 users.html:821 「**案件代辦－業務確認**」
3 system.py:487  log_date / user_id / content
     消費端 case-management.js:3063  alert('新增工作日誌失敗：' + detail)  ✅
     畫面三格：**日期** ／ **記錄對象** ／ **工作內容**
     => 「請填寫日期、記錄對象與工作內容。」
4 system.py:1785  backend 需為 local_drive 或 s3
     消費端 _saveSetting():882  `d.detail || '儲存失敗'`            ✅
     畫面（company-profile-settings.html:694-697）標籤「**備份目標**」，選項逐字
       「**本機磁碟機（Google 雲端硬碟掛載資料夾）**」／「**S3 相容物件儲存**」
     => 「備份目標請選「本機磁碟機」或「S3 相容物件儲存」。」
5 vendor_contractors.py:734  pending_acceptance / accepted
     消費端 case-management.js:4612  alert(detail || '操作失敗')      ✅
     畫面（audit-log.html:552）「**派工待確認**」
6 helpers/startup.py:42（D 撈到）「請至『系統設定 → **Edge 執行檔路徑**』」
     而實際欄位 company-profile-settings.html:647 是「**Edge 瀏覽器路徑**」
     🔑 **地方存在而名字對不上** —— 使用者在正確的頁面上，看著正確的欄位，而不認得它
```

### ② 已查而不改：**6 條**（使用者到不了）

⚠️ **要明著列著，不要從清單裡消失** —— 否則下一次掃描會把它們撈回來，
而**下一個人會以為那是漏掉的**。

```
daily_tasks.py:345   year_month   前端自己用 yearMonthStr() 組（daily-tasks.html:2614）
                                  => 使用者**沒有輸入框可以打錯它**
system.py:2363       doc_type/doc_id
                     static/edit-presence.js **自動背景呼叫**，且
                     `.catch(function(){ /* 靜默重試下一輪 */ })` => **失敗被吞掉**
inventory.py:614     void / return_to_stock / edit_note
                     inventory.html:748 `if (r.ok) {…}` ＋ `} catch {}`
                     => **失敗時什麼都不做** => 那句話到不了
                     🔴 而它另外暴露一個缺陷 —— 見 §2c
map_points.py:333    X-Map-Position 格式
map_points.py:429    座標不可以放在網址上（**132 字**）
                     map.html:665 失敗時只顯示 `'…（HTTP ' + r.status + '）'`
                     => **不顯示 detail** => 兩條都到不了
                     📌 而 :429 那段設計理由**仍然該搬進註解**（程式碼衛生，不是 EM1）
system.py:1537       cycle_start_day
                     saveGoogleQuota() 在呼叫後端**之前**就先擋，而它的訊息是
                     「**帳單週期起算日** 需為 1～28 之間的整數」
                     🔑 **正確的措辭前端已經有了** => 後端那句是開發者的後衛
```

### ③ 分出去（`EM8`）與排除

```
EM8   dashboard.py:108   gcis_daily_limit —— 前端命中 0，**那句指示照做不到**
      inventory.py:614 的 edit_note —— 畫面上**沒有入口**
排除  WebAuthn ×8（讀者是 superadmin，而畫面上那一格就叫 RP ID／Origin）
      system.py:936 `loc_1`（**假陽性**：使用者正在輸入一個 id，例值用英文正是他要的）
```

```
21 − 8(WebAuthn) − 1(假陽性) − 2(EM8) − 6(到不了) ＋ 1(Edge 路徑) = **6**
⚠️ 每一個減項都**獨立數過**（`§5v`），不是從等式推的
```

---

## §2c 🔴 順帶查到：庫存調整失敗是**完全靜默**的（建議另開編號）

```
inventory.html:744-750
  const label = action === 'void' ? '報廢' : '退回庫存'
  if (!confirm(`確認將序號「…」設為「${label}」？`)) return
  try { const r = await fetch(…)
        if (r.ok) { …重新載入… } }        <= **沒有 else**
  catch {}                                <= **沒有訊息**
```
☠️ 使用者按下「報廢」、確認、**而畫面什麼都不做** ——
🔑 他分不出「成功了而畫面沒更新」與「失敗了」。
📌 那不是文字問題 ⇒ **不在 `EM1`**，而它是查 `EM1` 時撞到的，**要有一個編號**。

---

## §3 驗收

```
① 🔴 **`§2b①` 那 6 條，改完之後訊息裡不含程式詞彙**
   ⚙️ 判定：那 6 條的新文案**不得包含**它對應的參數名／內部列舉值
      （逐條釘，**不是**一張關鍵字黑名單 —— `WD1 §2`）
   ☠️ 而它擋不到「換了一個字而仍然是程式詞彙」 => 配一條人的規則：
      這 6 條的 diff 必須被讀過
   ⚠️ **分母是 6，不是 21 也不是 13** —— 另外 6 條在 `§2b②`「已查而不改」，
      而它們**永遠不會變綠**，不可以放進進度分母
② 🔴 **資訊量不減**：每一條改寫前後**逐句對照**，原文說了幾件事，新文案要說幾件
   ⚙️ 人工題，而要留下對照紀錄（同 `WD1 §6③`）
   ☠️ 這一題紅而其他全綠 = 我們把訊息改得更像人話而更難用
③ **A 類 28 條一個字都沒變**
   ⚙️ 判定：那 28 條的字串在改動前後**完全相同**
   🔑 它擋的是「順手改」—— 而順手改的那些**產出是對的**，所以沒有人會抓到
④ 內部列舉值換成中文之後，**與畫面上的按鈕文字逐字相同**
   ⚙️ 觀測點：訊息裡的那幾個詞要在對應的 html 裡找得到
   ☠️ 不一致的話使用者照著訊息去找按鈕**找不到**
⑤ `map_points.py:429` 那 132 字：**理由搬進註解**，而註解裡那一段**必須還在**
   ⚙️ 正對照：改完之後 `grep 'access log'` 在 `.py` 裡仍然命中
```

---

## §4 與 `EM3` 的關係

```
共用   helpers/errors.py 的 new_trace_code()（A 已裁，C 的題已定義可接受集合）
       log 的寫法
不共用 **處置**
       EM3   非預期例外 => 固定文案 ＋ 追蹤碼（使用者做什麼都沒用）
       EM1-C 使用者輸入錯誤 => **說出改成什麼**（他改得動）
```
☠️ 把 `EM1-C` 套上追蹤碼 ⇒ **把一個他自己解決得了的問題變成一通電話。**

---

## §5 我沒做的

```
✗ 沒有實作、沒有改任何一條文案
✗ `§1` 那三條改寫**是示例不是定稿**（`year_month`／`action`／`cycle_start_day`）
✗ 沒查那 13 條**各自的畫面**上對應的欄位標籤與按鈕文字叫什麼
  ⚠️ 而 `④` 的驗收需要它 ⇒ **實作前要逐條去看畫面**
✗ 沒查那 13 條裡有幾條**前端根本不會顯示 detail**（有些可能只進主控台）
  ⚠️ 若是，那幾條的優先序不同 —— **未查**
✗ 分類是我一個人讀的，**沒有第二把尺**
  📌 而 A 類 28 條「不要動」的判定最需要複核：**我判它對，而使用者才是讀者**
```
