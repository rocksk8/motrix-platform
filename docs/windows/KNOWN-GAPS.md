# 我們知道自己沒查的是什麼

> 2026-09-23 ／ 視窗 A-2 整理
> ⚙️ 從 **36 份**規格的「我沒查什麼」節裡篩出來，母體 **190 條**。

---

## ⚠️ 讀這一份之前，先讀這一行

```
⚠️ 這一節列的是**我們自己標記過的**缺口。
   **沒有被標記的那些不在這裡**，而那一類才是真正的未知。
```

> ### 🔑 所以標題是「**我們知道自己沒查的**」，不是「我們不知道什麼」——
> ### ☠️ 差一個詞，而它決定您讀完之後會不會以為「那就是全部了」。

### ⚙️ 而 190 條裡只列了 **21 條**，判準是：

```
**它若有東西，誰會先撞到？**
   您先撞到   => 列在這裡
   我們先撞到 => **不列**（它進各自規格的動工前置，B／C 動手第一天就會遇到）
```
```
❌ 而我**沒有**用「嚴重度」當判準 ——
   ☠️ 那要我逐條判，而**您看不到我判掉的那些**
   🔑 那是「用一個看不見的篩選，換一份看起來乾淨的清單」
```

> ### 📎 **有 9 個編號會在另一份（`PENDING-RULINGS.md`）裡再出現一次** ——
> ### 🔑 那不是重複：**這一份問「正式機上現在是什麼狀態」，那一份問「要怎麼處置」。**
> ### ⚙️ `IA1` `IA2` `MG3` `PK1` `QS1` `QS1-a` `UP3` `WL7` `BN17`

---

## ① 只有您查得到的（**我們不碰正式機**）

> ### 🔑 這 11 條**要您在正式機上查的**，全部是「開發機上是這樣，而正式機可能不同」。

```
⚠️ 正式機上的 `jeff`／`demo` 帳號現在是什麼狀態         `IA1`／`IA2`
⚠️ 正式機的 `logs/server.log` 裡有幾筆「密碼 60575481」  `IA2`
   🔑 那個數字 = **這台機器被重新安裝過幾次**（只在建立時印）
⚠️ 正式機的 `sales_person_id` 有幾筆是空的（開發機 3/26）`QS1`
⚠️ 正式機的 `bonus_award_lines` 有幾列、解得出幾列       `QS1-a`
⚠️ 正式機上有沒有非最高管理者實際在用獎金頁             `QS1-a`
   🔑 若有，那個「看不到任何單」的缺陷**今天正在發生**
⚠️ 正式機當初跑 `_m008` 時跳過了幾筆                      `MG3`
   ☠️ 而那個數字**永遠查不到了** —— 那是那一件最貴的部分
⚠️ 正式機有沒有 `source_type='voucher'` 的附件列          `JV24`
⚠️ 正式機的 `work_logs` 有沒有照片                        `UP1`
⚠️ 正式機的 `uploads/` 有幾種子目錄、各有多少檔           `UP3`
⚠️ 正式機的 `audit_log` 有沒有超過 730 天的獎金退回記錄   `BN17`
```

---

## ② 只有您答得出來的（**業務規則／您的環境**）

```
❓ 「**後勤單位**」指的是哪一個單位                       `BN14`
   ⇒ 而您已裁「只在獎金這邊獨立設定」，⚠️ 而**具體是哪些人**還沒說
❓ 精算表那「**十二格**」的出處                            `ACC-BN6`
   ⚙️ 我找不到它的來源，而 `ACC-BN6.md` 的 §1 整段建立在它上面
❓ 您說傳票預覽「**過小**」時，用的是什麼螢幕              `JV25`
   ⚙️ 我用 1280×800 算的版面，而那不必然是您的
❓ 憑證「**兩者都要**」那一題（帶入選單 ／ 已計算標記）    `JV18`
   ⚙️ 您裁了「只在帶入選單」，而另一半還沒問
❓ 一筆支出**被帶入兩張傳票**時該怎麼辦                    `JV21`
   ⚠️ 而系統今天**不擋** —— 那個紅字標記是唯一的防線
```

---

## ③ 您會先看到後果的（**而我們沒有量過**）

```
🔴 摘要文字**沒有長度上限** —— 帶入一筆長品名可能**爆版**      `JV21`
   ⚙️ 而我沒有查那一格有沒有限制
🔴 附件檔名很長時會**換行** ⇒ 傳票預覽的高度估算失效            `JV25`
   ⚙️ 我的 28px 是估的（由字級與內距推），而**沒有量過真的一列**
🔴 案件底下的候選憑證**實際會有幾筆** —— 沒量                    `JV18`
   ⚠️ 若一個案件有幾十筆，那個清單會變得很難用
🔴 一張獎金單對應**多個案件**的情形 —— 沒查                      `BN11-BN12`
   ⚠️ 而「一張精算表」那個版面是假設一對一做的
🔴 前端有沒有地方顯示「這張單用的是**哪個據點**」                `QL25`
```

---

## ④ 而有一條**只有您知道**，它決定一件事的嚴重度

```
🔴 那兩個已經打好的出貨包，**有沒有真的送到過客戶手上**？   `PK1`
```
> ### ⚙️ 我們這邊的紀錄是「**只上過正式機**」，而**我們沒有獨立驗證**。
> ### ☠️ 若送過，那是一個**已經發生的外洩**（包裡有我們的完整內部工程文件），
> ### 而處置完全不同 —— **要通知**。

---

## ⚠️ 而那 169 條沒列出來的，去哪了

```
它們是「**我們先撞到**」的那一類 —— 例如：
   「`bonus.js` 現在怎麼組 allocations」   => B 動手第一天就看到
   「那 27 個呼叫端我沒有逐一讀」          => B 改的時候一定會讀
   「`verify_package.py` 會不會用到 tests」 => 已經查掉了
```
> ### 🔑 它們**不是消失了** —— 每一條都在各自規格的「動工前要先查的」那一節裡，
> ### 而那一節是**那張工單的驗收前置**。

```
⚠️ 而那個做法是今天才改的（原本叫「我沒查什麼」）——
🔴 而改名的理由是一個數字：
   ⚙️ 190 條裡只有 **38 條（20%）** 被回頭查掉，
   ☠️ 而**沒有任何東西在看這些節** —— 不是測試、不是守門、不是排程
      （全 repo 掃過，唯一命中的是 `test_voucher_export_watermark_2026_09_23.py:227`
        的一行**註解**，不是斷言）
☠️ ⇒ 那 20% 不是流程產生的
```
> ### 📌 **同樣的內容，而名字決定它會不會被當成待辦。**

---

## 📎 附錄：這 21 條是怎麼挑出來的（您若要稽核）

> ### ⚠️ 這一節是**給稽核用的**，正文讀完就夠了。

```
做法：**190 條逐條讀**，套上面那個「誰會先撞到」的判準。
❌ 沒有用關鍵字篩、❌ 沒有用嚴重度排序、❌ 沒有先排除任何一整份。
```

### ⓵ 而「逐條讀」本身沒有留下痕跡 ⇒ 下面這張表是它的**可驗證等價物**

```
36 份規格 ——
   有條目入選的   16 份
   一條都沒入選   18 份   <= 全部列在下面，您可以逐份翻回去對
   該節為空       2 份
```

### ⓶ **一條都沒入選的 18 份**（您翻回去若覺得某一份該入選，那就是我判錯了）

```
BN16   BN18   EM3    EM7    EM11   EM12
EM13   JV22   JV26   MG4    SL1    UP2
WL7    EM10-NAVIGATION      EM2-ROLE-LABELS
JV16-JV17     QN1-QUOTE-RENAME      EM9-SILENT-FAILURE
```
> ### 🔑 它們落選**不是因為不重要** —— `WL7`／`PK1` 是出貨阻擋，而 `WL7` 一條都沒進。
> ### ⇒ 因為它那 7 條缺口**全部是 B 動工第一天就會撞到的**（改哪個檔、預設值在哪）。

### ⓷ ⚠️ 而有一種篩法我**試過而不能用**，記在這裡免得下次有人重提

```
「把還沒排程的編號整批排除」—— 看起來省事，而它會漏掉兩條：
   `MG3` 正式機當初跳過了幾筆   <= 那個數字**永遠查不到了**
   `UP3` 正式機的 uploads/ 結構
☠️ 它們的編號還沒排到，而**要問的人是您、要問的時機是現在**。
🔑 ⇒ 「什麼時候做」與「誰答得出來」是兩件事，用前者篩後者會漏。
```

---

## 🟡 2026-09-23 夜：兩筆欠帳（本輪確認、本輪不做）

### ① 前端靜態掃描題的 `_strip_js_comments()` 要**下沉成共用工具**
⚙️ 來源：C（2026-09-23 夜，他自己提的）。
```
第一版只有一處剝註解 => 另一處誤報在 B 那段
「解釋為什麼不可以藏 .mnav__grp 祖先」的註解上
```
☠️ **各檔自己抄一份的必然結果是有一份會忘了剝** ⇒
  那一份的紅燈會**指向別人寫對的碼**。
🔑 而這一類缺陷的成本**不落在寫掃描器的人身上，落在被誤指的那個人身上**。
⚠️ 同一晚的第二個實例：`test_subproc_helper` 用 regex 掃原始碼，
  亮在「解釋 `identity_lines()` 掃什麼」的 docstring 上（已改 AST，`dc08e64`）。
⇒ **一般形式：數「程式會做什麼」一律用 `ast`，不要用 regex 掃字串。**

### ② **行號引用會過期，而過期後它跟正確引用長得一模一樣**
⚙️ 來源：A（2026-09-23 夜，自己踩到）。
```
SCOPE.md 的 BN20 筆記引 frontend/pages/bonus.html:740 / :745 / :755
實際（B 今晚改過該檔後）是            :751 / :756 / :766
🔴 而 :755 現在是 `</template>`
```
☠️ **而那三個行號我已經報給使用者了**（回答「獎金分潤的退回鈕在哪邊」時）。
🔑 已修（`8dcc0a7`），而**修掉一次不等於解決** —— 下一次有人改那個檔就又錯了。
⇒ 引用程式位置時，**行號要搭配一個不會動的錯（函式名、字串、選擇器）**，
  否則寫「某檔的某個條件」就好，不要寫行號。
