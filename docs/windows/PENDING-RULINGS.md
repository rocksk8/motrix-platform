# 待您裁示的事項

> 2026-09-23 ／ 視窗 A-2 整理
> ⚙️ 共 **32 條**，來自 **15 份**規格。

---

## 怎麼讀這一份

```
每一條都已經**有一個決定**，而系統照那個決定在做。
=> 您只要看**不同意的那幾條**，其餘不必回。

每一條三行：
   ❓ 問題        一句話
   ✅ 目前的決定  我們裁的，而系統照它在做
   ⚠️ 若您要改    改了之後會動到什麼
```

> ### 🔑 **全部不回也可以** —— 那表示您同意目前的決定。

---


## ① 出貨阻擋

### `IA1` ① — 出貨的安裝裡有我們自己的帳號

❓ **出廠帳號叫什麼**

```
✅ 目前的決定   通用身分 —— `admin`／「系統管理員」／email 留空
   為什麼       帳號、顯示名、email 三個都是公司資料，而客戶拿到的應該是他自己的
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-IA1.md` 的待確認節（那裡寫了要改哪裡）

### `IA1` ② — 出貨的安裝裡有我們自己的帳號

❓ **demo 帳號預設關掉**

```
✅ 目前的決定   關（環境變數開啟）
   為什麼       它對客戶零用途（登入後走隔離的 demo DB，看不到他的資料），
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-IA1.md` 的待確認節（那裡寫了要改哪裡）

### `IA1` ③ — 出貨的安裝裡有我們自己的帳號

❓ **既有安裝的 demo 要停用還是留著**

```
✅ 目前的決定   停用（`active=0`）而不刪列
   為什麼       刪列會讓 `audit_log` 的歷史對不上；而停用是可逆的
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-IA1.md` 的待確認節（那裡寫了要改哪裡）

### `IA2` ① — demo 帳號不該出現在客戶的安裝裡

❓ **要不要保留展示模式**

```
✅ 目前的決定   保留功能，而預設關
   為什麼       它對客戶零用途，而對我們（業務展示）有用 ——
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-IA2.md` 的待確認節（那裡寫了要改哪裡）

### `IA2` ② — demo 帳號不該出現在客戶的安裝裡

❓ **既有安裝的 demo 要停用還是留著**

```
✅ 目前的決定   停用（`active=0`）而不刪列
   為什麼       `audit_log` 裡有指向它的登入歷史；而停用是可逆的
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-IA2.md` 的待確認節（那裡寫了要改哪裡）

### `IA2` ③ — demo 帳號不該出現在客戶的安裝裡

❓ **demo DB 檔案本身要不要清掉**

```
✅ 目前的決定   不動
   為什麼       它是空的、隔離的，而刪它要考慮正在進行的展示
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-IA2.md` 的待確認節（那裡寫了要改哪裡）

### `PK1` ① — 出貨包裡有我們的整份內部工程文件

❓ **出貨要不要附文件**

```
✅ 目前的決定   加 export-ignore，出貨包不含 `docs/windows/`
   為什麼       那一層是四個視窗的工作檔與 `STATE.md`（20,341 行的內部討論）
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-PK1.md` 的待確認節（那裡寫了要改哪裡）

### `WL7` ① — 客戶收到的文件上印著我們的公司抬頭

❓ **登入頁那句要換成什麼**

```
✅ 目前的決定   產品名（例如「MOTRIX 專案管理系統」）
   為什麼       「內部管理平台」沒有一個值可以填 —— 客戶買的是產品不是自研工具
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-WL7.md` 的待確認節（那裡寫了要改哪裡）

### `WL7` ② — 客戶收到的文件上印著我們的公司抬頭

❓ **PDF 頁尾要不要保留「由 MOTRIX 產製」這類的產品標記**

```
✅ 目前的決定   不主動加（本規格只做「拿掉我們的公司資料」）
   為什麼       那是一個商業決定（要不要在客戶的文件上留產品名）
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-WL7.md` 的待確認節（那裡寫了要改哪裡）


## ② 資料與遷移

### `MG3` ① — `_m008` 改到一半就停下

❓ **失敗時怎麼辦**

```
✅ 目前的決定   版號不推進、`break` 不再往下跑、而系統照常起來（§3① 第三條路）
   為什麼       `main.py:525` 的 `init_db()` 沒有 try ⇒ 單純 raise 會讓系統開不了機，
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-MG3.md` 的待確認節（那裡寫了要改哪裡）

### `MG3` ② — `_m008` 改到一半就停下

❓ **`_m008` 裡寫死的「黃玉龍」「jeff@miactw.com」要不要處理（§2）**

```
✅ 目前的決定   本輪不動，而要登記
   為什麼       改它等於改寫已經跑過的歷史 ——
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-MG3.md` 的待確認節（那裡寫了要改哪裡）

### `MG4` ① — migration 的回填要回報

❓ **落點要做到哪一層**

```
✅ 目前的決定   新開 `migration_log` 表 ＋ 一支 superadmin 端點 ＋ 一個入口
   為什麼       這一件的整個論點就是「log 沒有出口」——
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-MG4.md` 的待確認節（那裡寫了要改哪裡）

### `MG4` ② — migration 的回填要回報

❓ **`_m103` 要不要回頭改**

```
✅ 目前的決定   要（它是本規格的第一個使用者）
   為什麼       它是現成的、剛寫好的、而且 B 手上還熱著
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-MG4.md` 的待確認節（那裡寫了要改哪裡）

### `MG4` ③ — migration 的回填要回報

❓ **`changed == 0 and skipped == 0` 時要不要寫一列**

```
✅ 目前的決定   要寫
   為什麼       migration 一個版號只跑一次 ⇒ 「這一版沒動到任何一列」本身是資訊；
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-MG4.md` 的待確認節（那裡寫了要改哪裡）

### `QS1` ① — `quotations.sales_person` 同一欄混兩種識別

❓ **`MQ-EXPFILE-001` 那種「沒有業務」的單要怎麼算**

```
✅ 目前的決定   維持 NULL，不猜（而相容分支因此永遠拿不掉）
   為什麼       它的 `sales_person` 是空字串 —— 那不是「資料遺失」，
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-QS1.md` 的待確認節（那裡寫了要改哪裡）

### `QS1` ② — `quotations.sales_person` 同一欄混兩種識別

❓ **`test3` 那兩張（`MQ-202607-059`／`-060`）要不要回填**

```
✅ 目前的決定   要（第二層 username 比對，§3① 的新增那一半）
   為什麼       `test3` 是一個真實存在且 active 的帳號（users id=6）
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-QS1.md` 的待確認節（那裡寫了要改哪裡）

### `QS1-a` ① — `bonus_award_lines.username` 改存帳號

❓ **解不出的那幾列要怎麼處置**

```
✅ 目前的決定   原值保留（顯示名留在 username 欄裡），並在守門的排除清單裡列名
   為什麼       A 的指示「解不出就原值保留＋記一筆」；
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-QS1-a.md` 的待確認節（那裡寫了要改哪裡）

### `QS1-a` ② — `bonus_award_lines.username` 改存帳號

❓ **️ `data_json.salesPersonUsername` 與 `sales_person_id` 誰為準**

```
✅ 目前的決定   `sales_person_id`（FK、有 migration 回填、擁有者過濾已在用）
   為什麼       `quotations.py:244` 的既有寫法
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-QS1-a.md` 的待確認節（那裡寫了要改哪裡）


## ③ 獎金

### `BN16` ② — 獎金單狀態篩選

❓ **作廢單勾出來後併進頁籤（A 裁，§7①）**

```
✅ 目前的決定   列上明著標「已作廢」，不可以只靠灰色或刪除線
   為什麼       灰色在列印、截圖、色弱、只掃一眼時全部失效
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-BN16.md` 的待確認節（那裡寫了要改哪裡）

### `BN16` ③ — 獎金單狀態篩選

❓ **頁籤要不要顯示計數（A 原本問的第三格）**

```
✅ 目前的決定   顯示，而用同一個 `awards` 陣列算（§5）
   為什麼       整包已在前端 ⇒ 不一致在結構上不可能發生；
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-BN16.md` 的待確認節（那裡寫了要改哪裡）

### `BN17` ① — 獎金單退回要留記錄

❓ **退回之後要不要能改內容（§3b 的甲／乙）**

```
✅ 目前的決定   乙（維持現況＝退回後只能作廢重建）＋ 接上 `supersedes_id`
   為什麼       他這次要的是「退回要有記錄」；
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-BN17.md` 的待確認節（那裡寫了要改哪裡）

### `BN17` ② — 獎金單退回要留記錄

❓ **️ 「這次編修的內容」區塊今天不顯示**

```
✅ 目前的決定   不留空區塊（乙的直接後果）
   為什麼       空區塊會顯示「（無）」，而使用者會以為壞了
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-BN17.md` 的待確認節（那裡寫了要改哪裡）

### `BN18` ① — 產生獎金單時手動指定人員

❓ **提示的措辭（A 裁乙：「這個案件沒有指派執行人，要不要順便去補」）**

```
✅ 目前的決定   提示不可以變成阻擋
   為什麼       他的原始問題就是「帶不進來」
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-BN18.md` 的待確認節（那裡寫了要改哪裡）

### `BN18` ② — 產生獎金單時手動指定人員

❓ **草稿階段改手動指定的名單，要不要留記錄（§5②）**

```
✅ 目前的決定   要，落點 `bonus_award_edit_log`
   為什麼       手動指定最容易打錯人，而不給改的話他只能作廢重開
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-BN18.md` 的待確認節（那裡寫了要改哪裡）

### `BN18` ⓪ — 產生獎金單時手動指定人員

❓ **手動指定是「完全取代」還是「在來源之上加減」（§4c）**

```
✅ 目前的決定   完全取代，並多記一個 `manual_basis`（覆寫前解析出來的來源）
   為什麼       「加減」把這一次的例外變成一個結構，而結構會被重用 ——
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-BN18.md` 的待確認節（那裡寫了要改哪裡）


## ④ 檔案與紀錄

### `EM13` ② — 權限詞彙混用

❓ **403 訊息可以多長**

```
✅ 目前的決定   丙拆完之後約 30 字（§6b 讓它從 60 字降到 30 字）
   為什麼       `EM1` 的「讀者是誰」判準
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-EM13.md` 的待確認節（那裡寫了要改哪裡）

### `EM7` ① — 迴圈裡跳過一筆而沒有人在數

❓ **甲類的落點用 `_system_audit()`（寫 audit_log）**

```
✅ 目前的決定   是
   為什麼       `server.log` today 沒有任何消費端（實查），而 `audit-log.html` 有頁面；
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-EM7.md` 的待確認節（那裡寫了要改哪裡）

### `EM7` ② — 迴圈裡跳過一筆而沒有人在數

❓ **哪幾處要升到 email（③）**

```
✅ 目前的決定   本輪只有備份保留政策那 4 處（archive.py :1182 :1250 :1264 :1279）
   為什麼       只有那一族的「跳過」會造成資料遺失（該刪的沒刪／該留的沒留）
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-EM7.md` 的待確認節（那裡寫了要改哪裡）

### `UP2` ① — 刪檔失敗而 DB 記錄照樣移除

❓ **刪檔失敗時要不要讓整個刪除動作失敗**

```
✅ 目前的決定   要（記錄保留，回一個看得見的錯誤）
   為什麼       「記錄沒了、檔案留著」沒有任何介面碰得到它，而它會進只增不減的雲端備份
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-UP2.md` 的待確認節（那裡寫了要改哪裡）

### `UP2` ② — 刪檔失敗而 DB 記錄照樣移除

❓ **`helpers/uploads.py` 改成 raise 而不是回旗標**

```
✅ 目前的決定   raise
   為什麼       忽略一個例外要多寫一行，忽略一個旗標不用 ——
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-UP2.md` 的待確認節（那裡寫了要改哪裡）

### `UP3` ① — 已知代價的射程過期了

❓ **工作日誌照片（GPS ＋ 姓名）要不要加授權**

```
✅ 目前的決定   本輪不加，只做 §4 的三件（射程註記 ＋ 守門 ＋ 登記）
   為什麼       `UP1 §3` —— 檔名 64 bit 在實務上猜不到；今天沒有外洩事件也沒有回報；
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-UP3.md` 的待確認節（那裡寫了要改哪裡）

### `UP3` ② — 已知代價的射程過期了

❓ **浮水印要不要繼續把姓名燒在圖上**

```
✅ 目前的決定   繼續（不在本規格範圍內改）
   為什麼       浮水印的用途是證明這張照片是誰在何時拍的 —— 那是它的功能
```

⚠️ 若您要改 → 見 `docs/windows/SPEC-UP3.md` 的待確認節（那裡寫了要改哪裡）

---

## ⚠️ 而有三件**不在這份清單裡**，因為它們不是「選項」

```
① 出貨包裡有我們的整份內部工程文件（`PK1`）——
   那是**要修的**，不是要您選的。您只需要決定「出貨要不要附一份給客戶的手冊」。
② 每一個安裝都有一個 demo superadmin，密碼是我們的統一編號（`IA2`）——
   同上。您要決定的是「展示模式要不要保留」。
③ 客戶匯出的 PDF 上印著我們的公司抬頭（`WL7`）——
   同上。您要決定的是「登入頁的副標要寫什麼」。
```

> ### 🔑 ⇒ 這三件**不等您回來也會修** —— 列在這裡是讓您知道它們存在。
