# SCOPE — 這一包收哪些編號（只有視窗 A 寫）

> 🔴 **這個檔只放清單。** 「為什麼這樣訂」一律寫在 `STATE.md §20`。
> ☠️ 理由：2026-09-22 同一個根因在這裡發生兩次 ——
> ① 解析器連散文也讀 ⇒ 多出 4 個不存在的編號
> ② 真清單落在散文小節底下 ⇒ 少了 16 個（整組 `RG`）
> 🔑 **搬一次位置是修結果；把「資料」與「關於資料的討論」分開才是修作法。**
> 📌 A-2 2026-09-22 指出。第三次因此不可能發生。

**規則**（細節與理由見 `STATE.md §20`）：
1. 新編號預設進 `NEXT`。搬進 `THIS` 要 A 明著搬，並寫理由。
2. `THIS` 區塊**禁止任何省略形式**（`…`、`~`、「等」、「以下」）。
3. 每一組寫出「這一組有幾個」。

---

## ✅ THIS — 這一包要全綠

```
VR(10): VR1 VR2 VR3 VR4 VR5 VR6 VR7 VR8 VR9 VR10
BK(13): BK1 BK2 BK3 BK5 BK8 BK9 BK10 BK19 BK20 BK22 BK24 BK26 BK29
MN(7):  MN1 MN2 MN3 MN4 MN5 MN6 MN7
GB(21): GB1 GB2 GB3 GB4 GB5 GB6 GB7 GB8 GB9 GB10 GB11 GB12 GB13 GB14 GB15 GB16 GB17 GB18 GB19 GB21 GB22
GC(11): GC1 GC2 GC3 GC4 GC5 GC6 GC7 GC8 GC9 GC9b GC10
FX(2):  FX31 FX33
TD(5):  TD1 TD5 TD6 TD7 TD8
QL(22): QL5 QL6 QL7 QL8 QL9 QL10 QL11 QL12 QL13 QL14 QL15 QL16 QL17 QL18 QL19 QL20 QL21 QL22 QL23 QL24 QL25 QL26
P0(4):  P0-00 P0-0 P0-1 P0-2
RP(3):  RP1 RP2 RP3
VP(1):  VP1
UI(4):  UI6 UI8 UI9 UI10
DM(1):  DM1
MG(1):  MG1
JV(24): JV1 JV2 JV3 JV4 JV5 JV7 JV9 JV10 JV11 JV12 JV13 JV14 JV15 JV16 JV17 JV18 JV19 JV20 JV21 JV22 JV23 JV24 JV25 JV26
BN(1): BN1
AC(2): AC1 AC2
AR(1): AR1
SP(1): SP1
AI(1): AI1  <= **併進 CA1**
CA(1): CA1
AS(4): AS1 AS2 AS3 AS4
AT(1): AT1
WD(1): WD1
BR(4): BR1 BR2 BR3 BR4
BN(17): BN2 BN3 BN4 BN5 BN6 BN7 BN8 BN9 BN10 BN11 BN12 BN13 BN14 BN15 BN16 BN17 BN18
EM(9): EM1 EM3 EM5 EM6 EM8 EM9 EM10 EM11 EM12
PX(1): PX1
JV(1): JV8
AL(1): AL1
BI(1): BI1
QS(1): QS1a
JV(1): JV27
JV(1): JV28
JV(1): JV30
JV(1): JV32
JV(1): JV35
JV(1): JV29
JV(1): JV31
```

> 📌 `RP`（restore path）是 2026-09-22 21:4x 由 A 明著搬進 `THIS` 的。
> 理由：**六對 robocopy 裡，唯一有檢查結束碼的是「套用新版」那一對**，
> 復原路徑上每一個複製都是靜默的（`STATE.md §53b`，C 複驗查到）。
> ☠️ `RP1`（`:338`/`:339`）做的是**回滾快照本身** —— 靜默失敗 ⇒ 快照殘缺 ⇒
> **沒有人知道，直到有一天要用它回滾**。整套部署安全機制的地基就是那份快照。
> ⚠️ **`RP2`／`RP3` 與 `restoring` 必須成對落地**（`§53d`）：
> 少了 `RP2`/`RP3`，`restoring` 印不出來；少了 `restoring`，它們印出來的是
> `applied`（語意「完整在磁碟上」）—— 兩邊都錯，而且各自看起來都合理。
>
> 📌 `VP1` 是 2026-09-22 21:5x 由 A 從 `NEXT` 搬進來的（`STATE.md §55c`）。
> 理由不是它大，是**它什麼時候會被用到**：`verify_package.py:268` 的
> `if expect is None:` 只 print 不 fail ⇒ 呼叫端忘記帶參數，**那道版本擋關就
> 安靜關掉，而管線全綠**。
>
> 🔴 **更正（`STATE.md §59`）**：A 原本在這裡寫「**而這一包打包時就會用到那道擋關**」
> —— **那是錯的事實**。實查全 repo：`verify_package.py` **零個呼叫者**
> （`build_deploy_package.ps1` 裡 0 處），它只在**有人想起來要跑**時才跑。
> ⚠️ 錯的那一句留在這裡，因為它是**優先序理由** —— 錯了會讓人排錯順序。
>
> ✅ **優先序不變，理由換成**：**人工執行時忘記帶參數，比自動呼叫時更容易發生**
> （自動呼叫寫一次就固定了，人工每次都要記得）⇒ **嚴重度沒有降低，可能更高**。
> ☠️ 一道可以安靜關掉的守門，比沒有守門更糟，**因為有人會信它**。
> ⚠️ `VP2`–`VP4` 留在 `NEXT`：它們的接縫（參數／自測函式）**還不存在**，
> 是**新功能不是修缺陷**，不要跟著一起進來把範圍撐大。

> 📌 `P0` 是 2026-09-22 20:29 由 A 明著搬進 `THIS` 的（規則 1 要求寫理由）。
> `P0-00` 是新的，排在 `P0-0` **前面**：兩者是同一個判定邏輯的兩側，
> `P0-0` 高報失敗、`P0-00` 低報失敗，而**記成失敗會有人來看，記成成功不會**。
> 理由全文在 `STATE.md §30a`（A 自己複驗過 `apply_update.ps1:532/:546`
> 與 `deploy_dashboard.py:186`，不是照收轉述）。
> ⚠️ `P0-1` 相依 `P0-2`（按鈕時間要算出來，資料來自 history）——順序不可對調。

---

## ⏭️ NEXT — 這一包不收

```
NB(29): NB1 NB2 NB3 NB4 NB5 NB6 NB7 NB8 NB9 NB10 NB11 NB12 NB13 NB14 NB15 NB16 NB17 NB18 NB19 NB20 NB21 NB22 NB23 NB24 NB25 NB26 NB27 NB28 NB29
WL(5):  WL1 WL2 WL3 WL4 WL7
IA(2):  IA1 IA2
FX(6):  FX3 FX28 FX29 FX30 FX32 FX34
XA(2):  XA6 XA7
YB(1):  YB1
UI(5):  UI1 UI2 UI3 UI4 UI5
GC(1):  GC11
TD(5):  TD2 TD3 TD4 TD9 TD10
VC(2):  VC2 VC3
P1(1):  P1-1
P2(5):  P2-1 P2-3 P2-4 P2-5 P2-6
VP(7):  VP2 VP3 VP4 VP5 VP6 VP7 VP8
FN(7):  FN1 FN2 FN3 FN4 FN5 FN6 FN7
RP(1):  RP4
DV(1):  DV2
GT(1):  GT2
MG(1):  MG2
TC(4):  TC1 TC2 TC3 TC4
FE(1):  FE1
DB(1):  DB1
BG(1):  BG1
HG(1):  HG1
QA(3):  QA1 QA2 QA3
GT(1):  GT1
BK(3):  BK31 BK32 BK33
CL(2):  CL1 CL2
MD(1):  MD1
QN(1):  QN1
UP(3):  UP1 UP2 UP3
SL(1):  SL1
QS(1):  QS1
MG(3):  MG3 MG4 MG5
EM(1):  EM4
RT(1):  RT1
BN(1):  BN19
PK(2):  PK1 PK2
QE(1):  QE1
JV(1):  JV6
EM(3):  EM2 EM7 EM13

```

> 📌 `EM2`／`EM7`／`EM13` 從 `THIS` 移到 `NEXT`（hichan-61 2026-09-24，A 裁）。
> 使用者 2026-09-24 表單作答原文（由 hichan-0a 轉達；表單本身不在 repo）：「JV22 做，其餘下一輪」
> ——「其餘」指當時列給使用者的 L 級四項（EM2／EM7／EM13／JV22）去掉 JV22。
> 🔑 三項都沒有題可以搬（`backend/tests/` 以 `(?<![0-9A-Za-z])(EM2|EM7|EM13)(?![0-9])` 掃 0 筆；
>    同一支掃描器對 `EM11` 命中 10 筆當正對照）⇒ 只搬規格的範圍，規格本文不動。

> 📌 `JV6` 從 `THIS` 移到 `NEXT`（hichan-61 2026-09-24，A 裁「照 `ab5ba88` 移到 NEXT，出處附使用者原話」）。
> 使用者原話**只在 commit 訊息裡**，docs/ 內 0 筆（`grep -rn 剩最後跑驗收 docs/` 回 0）：
> `e8108b7` 訊息逐字：「token有限…如視窗已結束就讓他休息，剩下最後跑驗收跟輸出的」
> （引號內的「…」是原訊息就有的省略，不是我截的）；同一則訊息寫的處置是「JV6 / JV26 / UP1 / EM13 / BN20 全部不做」。
> ⚠️ 這句裁示在 2026-09-23 20:35，早於 2026-09-24 使用者「甲類全部做完」的表單作答 —— 兩者範圍重疊（JV26／EM13），**待 A 對齊**。

> 📌 `VP`（verify package）＝ `backend/tools/verify_package.py` 四項，
> 內容由 D 量測、**編號由 A 發**（`STATE.md §50c`）。
> 🔑 D 原本在訊息裡發了 `RC1`–`RC4` 並自己撤回 —— **只有能寫進權威檔的人可以發編號**，
> 因為**編號是規格的形狀**，一則帶編號的訊息比不帶編號的更容易被誤當成已下達。
> ⚠️ `VP2`／`VP3` 的紅要標明紅在「**參數／函式不存在**」，不是紅在行為錯。
>
> 🔴 `VP5`（2026-09-22 22:0x 新增，`STATE.md §59`）＝
> **把 `verify_package.py` 接進打包腳本**（`build_deploy_package.ps1` 最後一步）。
> ⚠️ **未解的設計題，不要當小事接上去**：那個 `--expect-db-version` 的數字**從哪來**？
> `deploy_manifest.json` 不帶 db 版本；最省的來源是 `CURRENT_VERSION` 本身，
> **而那會變成自己驗自己**（〈守門守的對象被搬走〉）。
>
> ✅ 而「寫進協定的打包放行那一道」**已由 A 當場落地**（`MULTIWIN-PROTOCOL.md`），
> 不列為欠帳 —— 它把「沒有人想到」變成「**有人跳過**」，而那兩者的可查性差很多。

> 📌 `P2` 原本寫在 `STATE.md` 是一串用「／」隔開的**散文**，六件事沒有編號。
> 〈散文對工具是隱形的〉——沒有編號的項目，守門與盤點工具一個都看不見。
> 2026-09-22 20:29 由 A 拆成編號：
> `P2-1` 帳密一組共用／`P2-2` 失敗判定不靠文字／`P2-3` deploy_logs 保留最近 N 筆／
> `P2-4` dirty 只看 `backend/frontend`／`P2-5` `aheadOfOrigin` 不 fetch ⇒ 數字可能過期／
> `P2-6` `:291` 半形逗號
>
> 🔴 **`P2-2` 不在上面那一列，因為它已經被 `P0-00` 吃掉了**——
> `P0-00` 要做的正是「判定不要靠文字比對，改讀結構化結果行且 fail-closed」。
> 留著它會被做第二次，而第二次做的人會以為第一次沒做完。

---

## 📌 EXEMPT — 已結案／只登記／落點不在測試

```
BK(17): BK4 BK6 BK7 BK11 BK12 BK13 BK14 BK15 BK16 BK17 BK18 BK21 BK23 BK25 BK27 BK28 BK30
GB(1):  GB20
RG(18): RG1 RG2 RG3 RG4 RG5 RG6 RG7 RG8 RG9 RG10 RG11 RG12 RG13 RG14 RG15 RG16 RG17 RG18
G(10):  G2 G3 G4 G5 G6 G7 G8 G9 G10 G11
T(6):   T2 T3 T4 T5 T6 T7
UI(1):  UI7
```

> 📌 `P1-2`（回滾演練）已移出上面的圍欄（hichan-61 2026-09-24，A 授權）：守門的 `_SCOPE_NUM` 把 `P1-x` 解析成 `P1`（`GT2`），
> 它與 `NEXT` 的 `P1-1` 撞成同一個 `P1` ⇒ 三區互斥那一題紅。它記的是使用者推翻的決定，不是一個待辦項目。
>
> 📌 `P1-2`（回滾演練）**已由使用者推翻**，不是「這一包不做」。
> 使用者原話：「回滾我這個月有測試過，升級有失敗觸發回滾也都正常」。
> ⚠️ `STATE.md §28` 標的是「已由使用者推翻」而**不是**「可選」——
> 兩者差別在於：標成可選，下一個人會以為它還等著被排進來。

🔑 **`RG` 的理由欄不是「這一包不做」，是「落點在交付說明的 `§RG`，不在測試」。**
**那一節填不出實際輸出 ⇒ 不打包。** 拒收的人是打包的那個人，不是 CI。

---

## 📌 2026-09-22 22:4x 補登記（理由全在 `STATE.md`）

☠️ **這 14 個編號今晚全部發出過，而 `SCOPE.md` 裡一個都沒有。**
成因：A 稍早那支修檔腳本 `AssertionError`（錨點不唯一），
**而同一個指令裡 `STATE.md` 的 commit 成功、`push rc=0`** ⇒ A 看到 `rc=0` 就往下走。
🔑 **assert 做了它的事，是 A 沒讀。**
⇒ `§6`：**一個指令裡有多個步驟時，`rc=0` 只代表最後一步。**

```
THIS
  UI6  財務分組加「出納」入口            §61／§65（裁甲，§46b 稀釋不是修掉）
  UI8  sidebar 四處分組條件漂移          §68（三處是真的擋人）
  UI9  出納**拆回獨立頁面**              §71／§77（驗收條件：兩頁讀同一組端點）
  MG1  migration 版本號一致性守門        §66／§73（已綠 e15edb3）

NEXT
  FN1  財務（會計支出／科目樹）獨立頁面   §72b
  FN2  獎金計算獨立頁面                  §72b
  FN3  匯出傳票獨立頁面（T100 搬出 reports） §72b
  FN4  五個模組各自的執行／操作歷史       §72a（一次定形狀，不要五種）
  MG2  init_db() 執行期版本檢查          §75b（射程窄化 ⇒ 拒絕啟動）
  TC1  backend/tools 每一支都要被分類     §62
  TC2  分類成守門的要接在會被執行的流程上 §62（本身必須是 pytest 題）
  TC3  欠帳清單每筆要指向存在的編號       §62
  TC4  **盤點**解析型守門缺「輸入非空」斷言 §74c／§76a（只做盤點，不做可執行化）
  FE1  t100BankAccounts 載入抽共用（四處） §77d（刻意製造的第四份）

EXEMPT
  UI7  act()/_deniedPages 認 query string
       🔴 **「已不需要」不是「延後」** —— 理由 §71c：
       出納拆成獨立頁面之後**不同檔名**，同檔名衝突整個消失。
```

---

## 📌 2026-09-23 00:5x 追加（理由在 `STATE.md` §107／§108）

```
NEXT
  UI10 ~~營運報表那一項的 show 條件拿掉 cCash~~  §71c
       ☠️ **這一列是錯的，留著**：UI10 在 **23:05 `761d925` 就做完了**，
          而我 00:2x 才把它派給 C、00:26 才登記成欠帳 => 已移進 THIS
       => 只有出納權限的人不該看到「營運報表」（點進去是別人的頁）
  DB1  db.py:151 的 PRAGMA foreign_keys=ON 包在 try/except: pass  §108
       => 設定失敗 => 外鍵不強制 => **系統照常運作，沒有任何東西會說話**
       ⚠️ 本輪**只做量**（對 _connect() 斷言 PRAGMA foreign_keys == 1）
          改不改那個 except 是另一個決定 => 〈量它不等於修它〉
       ⚠️ 量測要走 _connect()，自己 sqlite3.connect() 量到的是 SQLite 預設值 0
```

```
NEXT (追加 01:0x)
  DM1  demo 重置撞 v93 的 TRIGGER => demo 登入 500          §109
       => account_items 排除在清除清單外（系統資料，不是使用者資料）
       ⚠️ 驗收要**連登兩次**：第 1 次會成功（表還沒建），第 2 次才炸
       ⚙️ 守門要笛卡兒積：每張表必須落在使用者/系統其中一邊，互斥且窮盡
```

---

## ☠️ 2026-09-23 01:1x — `UI10` 是我登記錯的，成因值得留著

```
23:05  761d925  fix(UI10): 營運報表的顯示條件拿掉 cCash    <= **做完了**
00:2x  A 把 UI10 當「既有欠帳」派給 C
00:26  e2755d5  A 把 UI10 登記進 **NEXT**
00:28  C 回報：那一題現在是綠的
```
🔑 **成因：我用的是對話裡的狀態，而不是去查現況。**
⚠️ 而它的代價是雙向的：
```
往下  C 花時間寫一題「證明別人已經修好的東西」
往上  NEXT 多出一筆假欠帳 => **任何看這份清單估工時的人都被我誤導**
```
📌 〈量測比變化慢＝輸出一印出來就過期〉的派工版 ——
   我今晚才剛用「最新的包比 commit 早四小時」糾正過 A-2 的範圍，
   ☠️ **而同一則訊息裡我自己派了一個已完成的任務。**
⇒ 可操作：**派任何一個「既有欠帳」之前，先 `git log -- <那個檔>` 看最近三筆。**

```
2026-09-23 00:40
  DM1  => **THIS**（bf36922 綠 4/4，列粒度：account_items 只清 source != 'statutory'）
  CL1  UI9/FN3 搬遷留下的殘骸                              §112
       reports.js 孤兒函式 14 ／ 孤兒狀態 23（四個 modal 的狀態，markup 搬走而狀態留著）
       ⚠️ 範圍含**把那道 modal 不變量補成雙向** —— 它只問「打得開的有沒有標記」，
          掃不到「留著而沒有人打得開」；少了這一半，下一次搬遷再留一批
```

```
2026-09-23 01:05
  MD1  .md 行內標記把字拆開 => grep 假陰性                      §118
       實測切點：STATE 2,292 ／ SPEC-VOUCHER 12 ／ SCOPE 18 ／ 共 **2,377**
       ☠️ **2026-09-23 01:10 量完縮範圍**（§119）：
       解析器（test_spec_coverage:114）**只讀圍欄內的 ASCII 編號**
       => 被行內標記拆開的編號 **0 個** => 縮成**兩小題**：
          (a) 圍欄裡編號被標記拆開（`UI**10**`）=> 紅
          (b) 🔴 **自己寫的字串再用窄 pattern 去比對它** => 紅
              （C 實踩的唯一實例：斷言訊息含 ** 而突變腳本比不到）
       ⚠️ `**UI10**`（標記在外）**合法且解析得到**，不可一起擋掉
```

```
2026-09-23 01:30
  BG1  備份守門的反向控制守的不是它 docstring 說的那件事   §125
       stale = backed - tables ⇒ **完全沒碰 _NOT_IN_JSON_BACKUP**
       ⇒ 把所有表寫進排除清單，兩題都綠
       ⚠️ 進 NEXT 不擋本輪，而 B 這一次要明著選「進備份」
```

```
2026-09-23 01:35
  GC9b => **THIS**（從 GC9 拆出來的，§126）
       原條文裡有兩個可分開驗證的要求，而兩支測試各驗一個
       => 守門認成撞名，兩邊都拿不到「已實作」的信用
       GC9  不落 DB／與地理快取表分開   <= test_gc9_*  （shortcircuit）
       GC9b 分鐘級的短退避 TTL              <= **要改名成 test_gc9b_***（miss_message）
```

```
2026-09-23 01:45
  HG1  docs/*.md 裡的**逐字黑名單**：同形異碼出現 => 紅      §128
       目前兩組（實際咬過的，不推測）：
         U+5713+6B04 -> U+570D+6B04     U+5265 -> U+525D
       ⚠️ **不可用字元類別**（兩者都不在相容區，e26f995 實測）
       ⚠️ 清單只在**咬到一次**之後才新增，不先列假想的
       ⚙️ 反向控制：黑名單裡的「正確字」必須真的出現在 docs 裡
          （否則一組兩邊都不存在的清單永遠綠）
```

```
2026-09-23 03:36  更正
  RP4  A 開編號時放進 **THIS**，而它的判定是**丙**（排下一包）
       => 閘門紅在「宣告了而沒有題」，而那是我放錯區造成的
       🔑 GT1 上線之後，**放錯區會立刻紅** —— 以前不會
```

---

## 🔴 2026-09-23 清晨追加：`JV`（傳票第二段），理由在 `STATE.md` §158

```
THIS
  JV1  建立＋清單 API
  JV2  送審／簽核／退回／過帳／作廢 API
  JV3  附件（帶入既有 ＋ 補上傳）
  JV4  傳票頁面（出納底下獨立頁）
  JV5  PDF 匯出（版面照 20260330-006）
  JV6  從既有單據帶入分錄
```
🔑 **六項全進 `THIS`，因為六項都是使用者點名的**（原話逐字在 `STATE.md` §158）。
⚠️ ⇒ **閘門現在會紅六題**，那是「這一包還不能出」的機器表示，不是缺陷。
⚠️ **`VC` 不要再用**：`VC1` 已被地圖背景定位文案佔走，我派工時撞過一次。

---

## 🔴 2026-09-23 清晨追加：`BN1`／`AC1`（理由在 `STATE.md` §159）

```
THIS
  BN1  獎金頁「產生獎金單」介面 —— 後端已備，缺的是 UI
  AC1  守門：宣稱「有畫面」要有互動元件＋寫入呼叫
```
🔑 **獎金與傳票是同一個形狀**：後端做完、前端只有唯讀骨架，而我兩次都回報成「做完」。
⇒ `AC1` 是**修作法**：把「有畫面」變成可執行的守門，不是靠我下次記得。
⚠️ 使用者裁示：`JV3` **(c) 都要**、傳票**比照坊間正式傳票**、**驗證必須完整不是模棱兩可**。

---

## 🔴 2026-09-23 清晨再追加：`JV7` ＋ 「完整」的定義（`STATE.md` §159b）

```
THIS
  JV7  摘要來源分頁選單（案件／已上傳檔案）→ 帶入 → 可手動改
```
🔴 **使用者逐字給出「完整」的定義**：
```
確認前後端跟頁面都有完成才算完整
```
⇒ 少任何一項**不得回報完成、不得進打包**。已寫進 `AC1` 條文。
📌 四項衍生（合併輸出的非 PDF 附件／摘要帶哪一欄／檔案清單不要做兩套／合併順序與頁碼）全部**未裁示**，記在 `STATE.md` §159b，不表示已決定要做。

---

## 🔴 2026-09-23 清晨：`AL1`（`STATE.md` §159c）

```
THIS
  AL1  Alpine 初始化跑兩次 —— 6 頁命中，voucher.html 已修，其餘 5 頁先查後果
```
☠️ **它在多數頁面上看不見**（多打一次 API 而已）⇒ 沒有人會報修。
⚠️ 三處更正在 `STATE.md` §159c：`why_cannot_post` 那支函式**不存在**（真名 `describe_balance`，我把 docstring 語意當函式名）、審核日期沒有欄位已併 `JV2`、全量閘門那一題**不可信**（跑到一半我改了 SCOPE）。

---

## 🔴 2026-09-23 追加：`BI1`（出貨阻擋，`STATE.md` §180）

`POST /api/bonus/items` 的 `user` 未綁定 ⇒ NameError 500。
☠ 而 `bonus_items` 兩個庫都 0 列、空狀態的唯一出路就是它
⇒ **每一個新客戶的第一天，按下新增就是 500**。
🔑 1440 綠沒抓到：`grep api/bonus/items tests/` = **0**，
  連 `BN1` 也繞過 router（`_seed_item()` 直接 INSERT）。

---

## 🔴 2026-09-23 追加：`AC2`（`STATE.md` §187）

**動作要有可見的回饋** —— `AC1` 驗「有沒有接上」，`AC2` 驗「接上之後它說不說話」。
☠ 實例：按下儲存畫面什麼都不說（成功訊息被緊接著的重讀清掉），
  **而後端全綠、七題全綠** ⇒ 使用者看到的是一顆沒有回應的按鈕。
📌 它原本不在任何編號下 —— **沒有編號的工作不會被排順序，也不會被盤點到**。

---

## 🔴 2026-09-23 追加：`AR1`（`STATE.md` §191）

`_daily_backup_tables()` 回 `{中文標籤: SQL}`，而 `set(dict)` 取的是**鍵** ——
☠ **C 與 A-2 各踩一次，而 A-2 是在讀過 C 的回報之後才踩的**
⇒ 「先看既有的題怎麼做」還不夠，**那個形狀本身就在誘導人取錯一層**。
⇒ 加一支 `backed_up_table_names()`，**把坑填掉，不是要每個人繞過它**。

---

## 🔴 2026-09-23 追加：`SP1`／`AI1`（`STATE.md` §195）

```
SP1  三類憑證的 doc_no 不是案件編號 => 「選一個案件」看不到它們
     後端九類都支援帶入，**缺的只有選取路徑** => 前端介面問題
     ☠️ 症狀是「沒有那一類」不是「壞了」
AI1  account_items.is_active 有欄位（v96）而 router **只有一支 GET**
     => **沒有端點也沒有 UI** => 使用者停不掉任何科目
     🔑 與 v93「註解說得出那個能力而沒有欄位」是同一種，只是往前走了一格
```

---

## 🔴 2026-09-23 追加：`CA1`（`STATE.md` §197）

**自訂科目樹**——使用者六個裁示的第 ①，而**會計科目模組從頭到尾是唯讀的**
（新增端點 0、停用端點 0、router 只有一支 GET）。

```
🔑 AI1 不是一個獨立缺口，它是**一個不存在的功能的下游**
   —— 「停不掉」之所以沒人喊，是因為**根本還沒有東西可以停**
✅ 實查：**沒有 BEFORE INSERT TRIGGER** => 新增 custom 科目不被擋
🔴 而 547 筆**全部是 statutory** => PATCH is_active **對現有每一筆都會被 TRIGGER 擋**
⚠️ 層級深度與編碼規則「**由他定**」=> **要問使用者**，「不寫死」與「不驗證」是兩件事
```
📌 排序判準：**我們自己開的編號不該排在使用者點名的前面。**

---

## 🔴 2026-09-23 追加：`AS1`／`AS2`／`JV8`（使用者裁示，`STATE.md` §201）

```
AS1  匯款申請簽核設定**併進**簽核設定
AS2  **傳票簽核出現在簽核設定中** —— **推翻 §161 的「不接 approval_settings」**
JV8  **新增傳票後才出現傳票的頁面**（現在一打開就是編輯畫面）
```
☠️ `AS2` 的成因要留著：我從「跟坊間正式傳票一樣」推出「簽核流程也要寫死」，
**而他那句是在講版面**（那張 PDF 的三格）。〈答案沒錯，是題目問錯了〉。

---

## 🔴 2026-09-23 追加：`WD1`（使用者裁示，`STATE.md` §209）

「只要使用在該系統的文字，都必須正式語氣口吻，標案雷達的信件，信件內容過度口語」

```
範圍  所有使用者看得到的文字（信件／畫面／錯誤訊息／按鈕／欄位說明）
      ⚠️ **不含**程式碼註解與 docstring
🔴 而**要保留資訊量** —— 改得正式而把它在說什麼拿掉，**比口語更糟**
⇒ 先盤（D）、定原則（A-2）、再改（B）、出守門（C）
```

---

## 🔴 2026-09-23 追加：`EM1`／`EM2`（`STATE.md` §213）

```
🔑 **口語是語域，而語域需要空間。三個字的訊息沒有語域可言。**
993 呼叫點 -> 523 條不重複 -> **真正能口語的只有 41 條**（>25 字）
而那 41 條多數不是口語，是「**寫給工程師的**」
  —— 最極端一條 132 字，是我們自己的註解被放進 detail
  🔑 **它的問題不是資訊少，是讀者錯了** => 四格判準套不上去
EM2 的 186 條**不是語氣問題，是一致性問題**；159 條短標籤**不做**
```
🔴 而 `fstring` 137 條樣板的**插值內容沒看** —— 可能把內部變數漏給使用者，
**那與語氣無關而可能更嚴重**（`§175` token 那一格的同族）=> **先查它**。

---

## ☠️ 2026-09-23 追加：`BR1`（`STATE.md` §215）—— **今天的產出使用者一個都沒看到**

```
666 的 process 啟動於 **05:56:02**，指令**沒有 --reload**
=> 它載入的是 dd50d2e(05:54)；05:56 之後的 **25 個 commit 全部沒生效**
使用者看到的 500 是 BI1，而 BI1 在 **06:23** 就修好了
成因在一個字：start.bat 有 --reload，**restart.bat 沒有**，而使用者按的是 restart
```
🔑 **「我改好了」與「他看得到」之間有一個沒有人在看的間隔，而兩邊都不會報錯。**
⚠️ **不可以給 `restart.bat` 加 `--reload`** —— 它會被打包，正式機也用它。

---

## 🔴 2026-09-23 追加：`BN2`-`BN5`（使用者四項）＋ `EM3`（`STATE.md` §216／§217）

```
BN2  獎金項目只有最高管理者**可見**（現在只擋寫不擋讀）
BN3  人員來源新增 **manual（手動指定）** —— 「新增一種來源類型」做不出來，
     因為系統不知道要去哪裡撈人；而 manual 同時解掉 owner/engineer 欄位不存在的落差
BN4  「產生獎金單」-> 「**產生獎金分潤單**」（逐處，不是一個按鈕）
BN5  案件編號**下拉選單** —— ⚠️ **列什麼是全部內容**，要有說得出原因的空狀態
EM3  **19 處 detail 直接放例外物件** => 使用者畫面會出現
     「UNIQUE constraint failed: customers.code」（**實跑出來的，不是推的**）
     => 固定文案 ＋ **追蹤碼**；🔴 不可以只把 {e} 拿掉（診斷能力會一起消失）
```

---

## 🔴 2026-09-23 更正（`STATE.md` §220）—— **§215 的成因是錯的**

```
❌ 我寫的    「restart.bat 沒有 --reload，而使用者按的是 restart」
✅ D 實查的  行程祖先是 **Git-Bash 的 nohup.exe**（從 bash shell 背景啟動）
             => **那個入口不存在於任何檔案裡**
             => 掃檔案的盤點找不到它，補 restart.bat 也修不到它
```
```
❌ 我寫的    「25 個 commit 使用者一個都沒看到」
✅ 正確界線  新的 **.html／.js 看得見**（StaticFiles 每次讀磁碟＋no-cache）
             新的 **API 端點看不見**（路由在 import 當下註冊）
             => 那正好解釋：獎金頁面在，而「新增獎金項目」500
```
```
❌ 我寫的    EM3「實跑出來的（不是推的）」
✅ 實際      A-2 用的是**同形狀的記憶體表**，**不是產品路徑**；
             C 實查四條產品路徑**全部到不了**（code 由 next_entity_code 產生／建立前先查過）
             => EM3 嚴重度下修為「一旦發生非預期例外才會洩漏」，**但仍要修**
```
🔑 **觀測的那個世界不是受測的那個世界** —— 而「正在跑的 process」那一層最遠：
前兩層差在版本，這一層差在**時間 ＋ 有沒有人按下重啟**。

---

## 🔵 2026-09-23 追加：`BN6`／`BN7`（`STATE.md` §221）

使用者逐字：「獎金單需要詳細有張表格，像是精算頁面一樣，報價多少、成本多少、
衍生成本、比例最後總利潤多少，再用這個利潤去拆發比例，可預覽、可匯出pdf」

```
✅ 實查：要的每一項**都已經存在 settlement.summary 裡**（開發機 13 筆）
   quotedPretax／totalActualCost／extraTotal／dispatchTotal／adminCost／
   charityDonation／grossProfit／**netProfit**／各 Pct
🔴 => BN6 **一個數字都不要算** —— 10%／1% 兩個係數只寫在 settlement.html，
      再寫一份就是第三份實作（helpers/bonus.py 模組 docstring 已寫死）
☠️ dispatchTotal 只有 **6／12** 筆有 => **缺欄位標「無」不是印 0**
☠️ 13 筆有 summary 而欄位計數 12 => **有一筆 summary 是空的 {}**
```
🔑 ④利潤 到 ⑤拆發 之間那一行是這張表的重點：
使用者要「**淨利怎麼來的**」與「**它怎麼被拆掉的**」接在同一張紙上。

---

## 🔴 2026-09-23 追加：`JV9`-`JV11`（`STATE.md` §224）＋ **一件更正**（§225）

使用者逐字：「傳票匯出pdf顯示尚未設定公司抬頭，要有預覽功能，匯出PDF跟匯出PDF(含附件)，
未審核通過前不能匯出，並且需背景顯示該流程尚未簽核完成的紅字，像是報價單一樣，
可原視窗預覽並且預覽後匯出」

```
JV9   ☠️ 「尚未設定公司抬頭」**不是沒設定** —— company_profile.name 有值（12 字，實查）
      _get_setting() 已經 json.loads 過，voucher_pdf.py:82 **又 loads 一次** => TypeError
      => **被自己的 except 吞掉** => 回 "" => 印「尚未設定」
      ⚙️ ast 掃 366 檔 => **孤例 1 處**（正對照通過）
JV10  原視窗預覽（沿用報價單 modal）＋ 兩顆按鈕（含附件那顆沒附件時要停用並說話）
JV11  🔑 **預覽隨時可看（紅字浮水印）；匯出要簽核通過** —— 閘門**在後端**
```

### 🔴 更正：`§219`／`§221` 的「1 筆 netProfit<=0」是錯的
```
逐筆打開：**8 筆全是正的**；差的那筆 netProfit = **None**（summary 空 {} 而 status=finalized）
成因：CAST(NULL AS REAL) > 0 是 NULL => **「沒有值」被讀成「值是 0 或負數」**
```
🔑 **聚合查詢回來的差額，不可以直接推論成「那一筆的值是什麼」** ——
差額只說「它沒有進到這個集合」，而沒進來的理由有好幾種。
⇒ 下拉四態各一句，②「沒賺錢」與③「精算是空的」**不可合併**（後者有出路，前者沒有）。

---

## 🔑 2026-09-23 追加：`EM4`（`STATE.md` §227）—— **`EM3` 的鏡像**

```
EM3  例外的內容**說太多**  => 資料表名／欄位名／路徑上了使用者的畫面
EM4  例外**什麼都不說**    => 一個 bug **偽裝成一句通順的話**
```
`JV9` 就是 `EM4`：`TypeError` 被 `except Exception: return ""` 吞掉
⇒ 紙上印「（尚未設定公司抬頭）」⇒ **語意完全通順，而設定裡有值**。

🔑 判準**不是**「有沒有 `except Exception`」，是「**它吞掉之後，那件事還說得出來嗎**」。
⚠️ 不能用形狀掃 ⇒ 掃「`except` 之後回一個**與成功路徑無法區分的值**，且**沒有 log**」。
⚙️ 正對照：`voucher_pdf.py` 修之前那一版必須亮；負對照：`split_attachments()` 不可以亮。

---

## ☠️ 2026-09-23 追加：`EM5`／`EM6`（`STATE.md` §230）—— `EM4` 盤出 **38 個**

```
🔑 JV9 真正的形狀：「無法區分」來自**成功路徑自己的 `or ''`**
   return (...).get('name') **or ''**   => 成功與失敗在**同一個運算式裡**收斂到同一個值
   ☠️ 那種寫法在這個 repo 很常見，**比「兩個 return」難看見得多**
```
```
EM5  🔴 voucher.py:359 _chain_tiers -> **[]** ＝「這張單不需要簽核」
     => JV11 閘門讀取失敗就**判定已完成 => 放行**（未簽核的傳票匯得出去，畫面完全正常）
     ＋ build_info.py:121 _reload_flag -> None（「查不到」與「沒有」分不開）
EM6  ☠️ licensing.py `_stable_machine_id -> ''` => **機器綁定靜默降級**
```
🔑 而這份盤點最有用的一句是**它的尺三次都看起來合理**：v1=131／v2=**0**／v3=38，
**只有正對照分得出哪一個是真的** —— v2 那個 0 若沒有正對照，會被讀成「沒有同族」。

---

## ☠️ 2026-09-23 追加：`BN8`（`STATE.md` §232）—— **獎金單沒有簽核流程**

```
實查  bonus_awards **沒有任何簽核欄位**；`bonus` 不在 APPROVAL_DOC_TYPES 的八種裡
      router 七支**沒有 submit／approve／reject**
      UPDATE bonus_awards 全 repo **只有一處**，而它**只寫 voided_***
=> **建立之後 status 從來不會改**，實際資料 1 筆 = 「草稿」
```
☠️ ⇒ 使用者裁的「用印欄列數從簽核設定算」**沒有東西可以算**。
✅ **使用者裁示：現在就開簽核流程** ⇒ 獎金單成為**第九個** doc type。
⚠️ `BN8` **擋住 `BN7`**；`BN6`（明細表）可平行。📌 趁 `AS2` 剛做完第八個。

🔑 A-2 列三條路、講出每條代價、明著說「**我不自己選**」—— 他建議 (b)，而使用者選 (a)。
**當一個處置的代價是「長出一整條新流程」時，那不是技術決定。**

---

## ✅ 2026-09-23 追加：`BN9`（`STATE.md` §233）—— **使用者要的已經做了一半**

使用者逐字：「非最高管理者只能看見自己可領到錢的獎金分潤單，其餘選項都看不到，
產生獎金分潤單這些也只有最高管理員可見」

```
✅ 已實作  routers/bonus.py:307-310  `if not manager and not lines: continue`
           => **與自己無關的單完全不出現**；也看不到 base_amount 與整張單總額
🔴 差一格  _is_manager 回 role in ("superadmin", **"admin"**)
           而它的 docstring **自己標了**「A 🟡 預設沿用，**未經使用者確認**」
           => 使用者現在確認了：**只有最高管理者**
```
🔑 那個 🟡 標記做對了一件事：**它讓一個未經確認的預設值在被推翻時，
改一個地方就好，而不是去考古當初為什麼那樣寫。**
⚠️ `admin` 收緊之後**變成一般使用者**（有份就看得到自己那一列），不是看不到獎金頁。

---

## 🔴 2026-09-23 傍晚：`QS1a` 由 A 明著從 `NEXT` 搬進 `THIS`

```
母題 QS1（報價單／案件的 sales_person 存顯示名）登記在 **NEXT**
而它拆出來的 QS1-a（username 改存帳號 + migration 回填）**已經做完並 commit**（208f0d2）
=> 一個**動到 production 資料**的改動，不在任何一個「要全綠」的清單裡
```
🔑 ⇒ **拆子題的時候，子題繼承了母題的桶子，而做的人不會注意到** ——
  ☠️ `QS1` 在 `NEXT` 是對的（整段還沒做），`QS1-a` 在 `NEXT` 是錯的（它出貨了），
  **而這兩件事在清單上長得一模一樣。**
📌 抓到它的是 **D 從 commit 獨立編表**（我的清單裡它是「做完的」，SCOPE 裡它不存在）——
  🔑 **兩份清單並排，差異才看得見；只看其中一份，兩份都是自洽的。**

⚠️ 編號寫成 **`QS1a`** 不是 `QS1-a`：解析器的正則是 `[A-Z]{1,2}\d{1,2}[a-z]?`，**不吃連字號**。
  （`P0-00` 那幾個是既有的例外，本來就沒被解析器看見 —— 見 `STATE.md §339`。）

⇒ `QS1a` 的驗收＝「**K 筆都有被列出來**」，**不可以釘 `K == 0`**（`STATE.md §334`）。

---

## 🔴 2026-09-23 傍晚：`RT1` 進 `NEXT`（`retention` 欄位**沒有任何讀取端**）

```
⚙️ B 實查（BN17 收尾時，A 要求「答我沒查可以，答完去查一次」）：
   整個後端**沒有任何一處**對 *_edit_log 表下過 DELETE
   archive.py 唯一的清理函式 _prune_audit_log() **只清 audit_log**
=> voucher_edit_log／bonus_award_edit_log 的 retention 是**純寫入**
=> 🔴 標 'term' 的列**今天也是無限期留著**
```
> ### ☠️ 缺陷不是「資料沒被清掉」，是**schema 說了一件不是真的事**。
> ### 🔑 下一個人讀到 `retention='term'` 會相信那些列會被清，而**沒有任何東西會糾正他**。

📌 它是〈「寫進 log」不是一個落點，它是一個願望〉的**第二種形狀**：
```
第一種：訊息寫出去了，而**沒有人會去讀**（logger.warning 的消費端 = 0）
第二種：欄位**寫進去了**，而**沒有程式會照它做事**（retention 的讀取端 = 0）
🔑 兩種都不是 bug —— 它們是**一個被預先留下來、而沒有被寫出來的未來**
```

### ⇒ 為什麼進 `NEXT` 而不是現在做
```
清理策略（留多久、誰能刪、刪掉要不要留痕）是**使用者的決定，不是工程決定**
⚠️ 而它與 §3t 同一族：那一條是「身分證號每天上雲端」——**保存期限有法律重量**
```
✅ 而**不寫成註解就算了**（〈已知的代價 vs 要修的東西〉：當場認出缺陷卻寫成註解＝放過）——
  🔴 發編號＋標 `⏳ 待使用者回來確認`，**它才會出現在他回來要看的那張清單上**。
📌 而 B **沒有順手去建 pruner**（「建 pruner 是新的一塊工作，不在 BN17 範圍內」）是對的：
  🔑 〈我發現的≠該做的〉—— **順手做掉的那件事沒有人派，也沒有人排過順序。**

---

## 🔴 2026-09-23 傍晚：`MG5` 進 `NEXT`（一個客戶的資料寫死在產品碼裡）

```
db.py `_m008`：correct = "黃玉龍" ／ email='jeff@miactw.com'
UPDATE quotations SET sales_person='黃玉龍' WHERE sales_person IN ('jeff','Jeff',…)
⚠️ 而版號從 0 跑到 103 => **每一個新客戶的安裝都會執行這支 migration**
```

### ⚠️ 而登記理由**不是** A-2 原本寫的那個
```
❌ 他寫：「新客戶若有業務帳號叫 jeff，報價單會被改成黃玉龍」
   => 全新安裝時 quotations **是空的**（migration 先跑，之後才有資料）=> 影響 0 列
   🔑 那與他自己在同一份文件 §4 推完的那一條是同一個推理
      （**回填型 migration 在全新安裝時必然是 no-op**）—— 他在這一節沒有用它
```
✅ **兩條真的理由：**
```
① 🔴 **災難還原／降版再升版**：還原一份版號 < 8 的備份時，
   quotations 裡**有客戶自己的資料**，而 _m008 會對它執行
   📌 正是〈凍住的歷史不要呼叫活的程式碼〉記的那條路
② 🔴 **我們自己員工的姓名與 email，寫死在出貨給別家公司的原始碼裡**
   🔑 這一條**與 migration 會不會跑無關**，它現在就成立，而它比 ① 容易說明
```

### 🔴 桶子由量測決定（已派 D）
```
D 要驗：全新安裝時 _m008 每一句 UPDATE/INSERT 各影響幾列
0 列      => MG5 留 NEXT（只剩理由 ②）
不是 0 列 => **MG5 升 THIS，出貨阻擋**
```
⚠️ 而正確的修法**不是改 `_m008`**（那等於改寫已經跑過的歷史），是**新增一支 migration 改回來** ——
  ☠️ 而那需要知道「**哪些列是被 `_m008` 改的**」，而今天沒有那個紀錄。
  🔑 **那正是 `MG3` 要修的東西 —— 兩件事在這裡接上了。**

---

## 🔴🔴 2026-09-23 傍晚：`IA1` 直接進 `THIS` —— **出貨阻擋**

> D 去驗 `MG5` 的時候撿到的，而**它比 `MG5` 嚴重，理由是它不需要任何巧合。**

```python
# backend/helpers/startup.py  init_default_admin()   ← **每次啟動都跑，不是 migration**
if not conn.execute("SELECT id FROM users WHERE username='jeff'").fetchone():
    INSERT INTO users (username, password_hash, display_name, role, email, modules, active, created_at, must_change_password)
    VALUES ('jeff', ?, '黃玉龍', 'superadmin', 'jeff@miactw.com', ?, 1, ?, 1)   -- ? = _hash_pw(temp_pw) / json.dumps(_SUPERADMIN_MODULES) / datetime.now().isoformat()
```
> ### ☠️ **每一個賣出去的安裝，第一個 superadmin 帳號都是我們自己員工的帳號、姓名與信箱。**

### 🔑 為什麼它與 `MG5` 不同（這一段決定了桶子）
```
MG5（_m008）：一次性的歷史 migration，全新安裝時三句 UPDATE **都是 0 列**
IA1          ：**活碼**，每次啟動執行，**100% 發生，不依賴任何巧合**
=> 「migration 是凍結的歷史，不要回頭改」這個理由**對 IA1 不成立**
```

### ⚠️ 而 D 有一句描述我實際打開檔案後要更正
```
他寫：「else 分支**每次啟動都會重新校正**成這個值」
⚙️ 而實際的 WHERE 是**有條件的**：
   display_name 只在它是 ('Jeff 管理員','Jeff','jeff','jeff超級管理員','') 之一時才改
   email        只在它是 '' 或 NULL 時才改
```
🔑 ⇒ **客戶把帳號改成自己的名字之後，不會被改回去。**
☠️ 而我差一點把「客戶改了名字，下次重開又變回黃玉龍」寫進規格 ——
  📌 那是〈推翻的證據不會自動支持替代方案〉的那一刻：**一個更有力的說法，而它不是真的。**
⚠️ 剩下的真實面：**客戶把 email 清空，下次啟動會被填回 `jeff@miactw.com`。**（窄，但成立）

### ✅ 而安裝流程本身是想過的，不要一起否定掉
```
隨機臨時密碼 secrets.token_urlsafe(14) ／ _write_initial_credentials() 寫檔
must_change_password=1
=> 🔑 **要改的只有「身分三個值」，不是整個 init_default_admin()**
```
⇒ 驗收方向：帳號名／顯示名／email 三者**不可以是寫死的公司資料**。
⚠️ 而**不可以只把字串換掉就結案** —— 要釘「產品碼裡不存在 `miactw.com`／`黃玉龍`」這種不變量，
  🔑 否則下一次有人「順手」再加一個就沒有東西會叫。
⚠️ 而 `init_demo_account()` 就在它下面，**也是寫死的帳號** —— 已派 D 盤整個 `startup.py`。

### 🔴 `⏳ 待使用者回來確認`
```
我裁：全新安裝的第一個管理員改成**通用身分**（帳號／顯示名／email 都不是公司資料）
依據：這是要賣的產品，而現在每個客戶的第一個帳號都是我們員工
他選另一個要改哪裡：若他要保留 'jeff' 當出廠帳號（例如為了遠端支援），
  => 那 email 與顯示名仍要拿掉，而**必須在 DEPLOY.md 明寫「出貨後第一件事是改掉它」**
```

---

## 🔴 2026-09-23 傍晚：`BN19` 進 `NEXT`（草稿階段編輯已產生的手動名單）

> B 交 `BN18` 時**自己盤點出來的缺口**，而他**沒有順手做掉**。

```
今天完全沒有任何端點可以編輯**已產生**獎金單的 allocations
=> AC1 的驗收清單（①②③ⓐ-ⓕ）也全部只測「產生時」的行為
```

### ✅ 而它**不進 `THIS`**，理由要寫清楚
```
使用者的原話逐字：「**產生獎金單時**能手動指定人」
=> 「產生時」是他講的範圍，**「產生後再改」不是**
```
🔑 ⇒ 〈答案沒錯，是題目問錯了〉的預防：**把他的原話貼在結論旁逐字對照。**
⚠️ 而它**不是憑空的顧慮**：`BN17` 讓退回的獎金單**回到草稿**，
  而一張回到草稿的單，若當初就是挑錯人，今天**只能整張作廢重開**。
⇒ ⏳ 待使用者回來確認：
```
我裁：本輪不做。依據＝他講的是「產生時」
他選另一個（要能改）=> 那要一起決定「改了要不要留痕」
   —— 🔑 因為 BN17 已經把「退回」做成**長期記憶、不可刪**，
      而一個可以無痕改名單的端點會**繞過那個保證**
```

📌 而 B 的回報形狀值得記：**他把「我沒做的」與「我做了的」寫在同一則裡，並說明為什麼沒做。**
☠️ 相反的做法有兩種，而兩種都更省力：**順手做掉**（沒有人排過順序），或**不提**（清單上看不出來）。

---

## 🔴🔴 2026-09-23 傍晚：`WL7` 進 `THIS`（出貨阻擋）＋ `IA1` 的範圍比原本回報的大

> D 盤點「產品碼裡寫死本公司資料」的母體時查出來的，兩件都**我自己打開檔案複驗過**。

### 🔴 `WL7`｜兩份報表印我們的公司抬頭與**統一編號**，而它們**完全不讀 `company_profile`**
```
backend/routers/reports.py:34   _COMPANY  = "允碩整合集創"
backend/routers/reports.py:74   _COMPANY2 = "統一編號 60575481 ｜ Tel: 04-3610-6566 ｜ info@miactw.com"
   => 營運報表 Excel 標題/表頭、營運報表 PDF <h1>/sub/footer、銷項發票清單 Excel 標題
backend/network_plan_export.py:19,20  同名兩個常數
   => 網路架構規劃書 Excel A1/A2、PDF header/footer
⚙️ grep -c company_profile 兩支都是 **0**
```
> ### ☠️ **每一個客戶匯出的營運報表與網路架構規劃書，印的都是我們的公司名與統編。**
> ### 🔑 而它**不是 `JV9`**：`JV9` 是「有設定而讀不到」（雙重 `json.loads` 被 `except` 吞掉），
> ### 這一支是「**根本沒有去讀**」—— 客戶怎麼改設定都沒有用。

⚠️ 而**不可以只換掉那兩個常數就結案**（我複驗時看到的）：
```
network_plan_export.py:380  f-string 裡**直接內嵌**「允碩整合集創 ｜ info@miactw.com
                            ｜ Tel: 04-3610-6566 ｜ 統一編號: 60575481」
network_plan_export.py:481  footer 預設值裡又有一次
```
🔑 ⇒ 驗收要釘「**這兩支檔案裡不存在那些字串**」，不是「常數改掉了」。

### 🔴 `IA1` 範圍擴大：`'jeff'` 寫在**存取控制邏輯**裡，不只在 `startup.py`
```
backend/routers/auth.py:1548  delete_user()        username=='jeff' => 400「不可刪除超級管理員帳號」
backend/routers/auth.py:1582  toggle_user_active() username=='jeff' => 400「不可停用」
frontend/pages/users.html ×5  (392/396/417/1267/1276) 配合隱藏刪除/停用按鈕
```
> ### ☠️ **客戶的系統裡會有一個永遠刪不掉、停不掉、名字叫 `jeff` 的帳號。**
🔑 ⇒ 那不是資料層的巧合，是**存取控制本身認得這個字面帳號名** ——
  正確形狀是**保護「第一個 superadmin」這個角色**，不是保護一個字串。

### ✅ 而「產品碼裡不存在這些字串」這個不變量**現在還不能直接下**
D 的分類（母體 **16 個檔案**，逐項列名在 `STATE.md §351`）：
```
A 出貨阻擋（IA1）      4 檔／9 處
B 出貨阻擋（WL7）      2 檔
C 合法出廠預設（客戶改得到）  2 檔   <= db.py:711 _seed_setting／company_identity.py:43
D UI 提示/範例格式          3 處
E 維運/一次性工具            3 檔
F 純註解討論歷史脈絡         1 檔／3 處
G 良性（弱密碼黑名單）       1 處   <= helpers/auth.py:37 "miac@60575481"
=> 4+2+2+3+3+1+1 = **16**（自己加過一次）
```
🔴 ⇒ **要 = 0 的分母是 A + B = 6 個檔案**，不是全庫。
📌 〈寫「每一個／不可以有」之前先去數有幾個例外〉—— **這次先數了，所以守門寫得下去。**

---

## 🔴🔴 2026-09-23 晚間：`IA2`（demo 帳號）＋ `JV27`（重複實作擋住 fail-open）進 `THIS`

### 🔴 `IA2`｜`init_demo_account()` —— **一個客戶不知道的 superadmin，密碼是一個公開數字**
```python
# backend/helpers/startup.py:138  —— main.py:528 **無條件呼叫，沒有任何開關**
VALUES ('demo', ?, '展示帳號', 'superadmin', ?, 1, ?, 0)   # role=superadmin, must_change_password=0
_hash_pw("60575481")                                       # 密碼 = **公司統一編號**
logger.info("已建立展示帳號（demo），密碼 60575481")         # 🔴 **明文寫進 logs/server.log**
```
```
⚙️ 三件都查過，三件都空：① 有開關嗎 => 沒有  ② 登入頁看得到嗎 => 看不到  ③ 授權底座處理了嗎 => 沒有
🔑 而那個密碼**印在客戶自己匯出的 PDF 頁尾上**（reports.py:74／network_plan_export.py）
```
> ### ✅ 而**資料隔離是真的**，嚴重度要據實下修（A-2 實讀 `auth.py:394`）：
> ### `reset_demo_db()` ＋ `DEMO_TOKEN_PREFIX` ⇒ middleware 把後續請求路由到隔離的 demo DB
> ### ⇒ ❌ **不是「客戶資料會外洩」**
> ### ✅ 是「**客戶的系統裡有一個他不知道的 superadmin，而它的密碼是一個公開數字**」
🔑 ⇒ **真正的問題不是它能做什麼，是它不該在那裡。**
⚠️ 而 `4130d28` 修過一次「demo 模式背景執行緒隔離漏洞」⇒ **那道隔離已經破過一次**。
⇒ 處置與 `IA1` **不同**（所以另開編號）：`init_default_admin` 改通用身分／`init_demo_account` **預設不跑**。

### 🔴 `JV27`｜`_appr_of()` 讓 `signatures_of()` 的 fail-open 設計**不可達**
```
routers/vouchers.py:153  _appr_of()  <= **獨立於 _chain_tiers() 的第二份簽核鏈解析**
   它在 read_voucher() 裡**搶在 signatures_of() 之前** raise HTTPException(400)
=> approval_json 壞掉時，GET /api/vouchers/{id} **連分錄都讀不到**
```
🔑 而 `signatures_of()` 的 docstring 寫著「**版面壞掉不該讓整張單讀不出來**」——
  ☠️ **那個設計今天不可達，而沒有任何東西會說。**
📌 ⇒ 〈守門守的對象被搬走〉的一種：**退路還在，而有人在它前面加了一道會 raise 的門。**
⚠️ C 已寫題並用 `xfail(strict=True)` 追蹤（`test_em5_voucher_chain_unreadable_2026_09_23.py`）——
> ### 🔴 **而 `xfail(strict=True)` 在缺陷存在時是「通過」** ⇒ 打包關門會說綠。
> ### 🔑 **所以它必須有編號，否則它會以「一切正常」的樣子被出貨。**
⇒ 修法由 B 決定：`_appr_of()` 改用共用的 `_chain_tiers()`，或**直接拿掉這支重複實作**。

### ⚠️ 而 `IA1` 的不變量範圍要縮
```
'miactw' 全庫 **15 處**：身分 3 ／ 公司抬頭 7 ／ placeholder 與註解 4 ／ migration 與工具 3
☠️ 直接釘「產品碼裡不存在 miactw.com」=> 後三種全紅
   => 而最省力的反應是**把它們全部加進排除清單** => **等於沒守**
```
✅ ⇒ `IA1` 的不變量**範圍縮到 `backend/helpers/startup.py` 那一支檔**（改完應該是 0）。
📌 而公司抬頭那一族歸 `WL7`，**分母另算**（A-2 用完整字串集量到 **13 檔／34 行**）。

---

## 🔴🔴🔴 2026-09-23 晚間：`PK1` 進 `THIS` —— **出貨包裡有我們的整份內部工程文件**

> D 去查「`backend/tools/` 會不會被打包」時答出來的，而**答案比那個問題大得多**。

### ⚙️ 機制
```
build_deploy_package.ps1:670   git archive --format=tar -o $tarPath $commit
=> 匯出**整個 repo 已追蹤且已 commit 的內容**
腳本 :728 的註解逐字：「git archive 本來就只會匯出 git 追蹤的內容，這裡不需要額外過濾」
⚙️ 沒有 .gitattributes（=> 沒有 export-ignore）／archive 之後沒有任何內容過濾
```

### 🔴 而**不是推論，是打開已經產出的包看到的**
```
deploy_packages/20260922_184908_c5b1e84/docs/windows/STATE.md   ✅ 在
deploy_packages/20260922_200604_7bc1fb8/docs/windows/STATE.md   ✅ 在
兩個包都含 SCOPE.md 與 MULTIWIN-PROTOCOL.md
⚙️ 那一包的 docs 有 **17 個檔**，根目錄 **27 份 .md**
⚙️ 包裡的 STATE.md：**20,341 行／583,698 字元**
⚙️ 內含（實數）：「私鑰」**26** 次／`license.key` **15** 次／「客戶」**71** 次
```
> ### ☠️ **每一個出貨包裡，都有我們對這個系統每一個已知缺陷的完整內部討論。**

### ⚠️ 而嚴重度要據實說，不要誇大
```
✅ **目前沒有任何外部客戶收到過** —— 這兩個包只上過正式機（公司自己的機器）
🔴 而它是**出貨那一刻就會發生**的，而本期目標正是「把 ERP 變成可販售的產品」
🔴 且**今天的 STATE.md 比包裡那一版又多了**：demo 帳號的密碼、公司統編、員工姓名、
   以及 IA1／IA2／WL7／JV27 四件出貨阻擋的完整說明
=> **下一個包會把這些一起帶出去**
```
📌 而最鋒利的一句：**我今天建的 `HANDOVER-2026-09-23.md`（要交給使用者的那份），
  它自己也會被打包進出貨包。**

### 🔑 為什麼四個視窗都沒看到它
> 我們一直在問「**我們把公司資料寫到哪裡**」——
> ☠️ 而這一條的答案是「**我們沒有寫，是出貨機制把整個 repo 帶出去了**」。
📌 〈外洩的出口不一定是你寫的〉：**問「誰會看到」，不要問「我們寫到哪」。**
⚠️ 而 `rollback_snapshots/` **沒有被追蹤**（實測 `git ls-files` = 0），所以不在包裡 ✅。

### ⇒ 處置（`⏳ 待使用者回來確認`）
```
我裁：加 .gitattributes 的 export-ignore（docs/、MULTIWIN-PROTOCOL.md、
     HANDOVER-*.md、backend/tools/ 裡的一次性腳本）
     🔴 而**驗收要打開產出的包去看**，不是看腳本 —— 這一件就是這樣才被證實的
依據：客戶要的是能跑的系統，不是我們的工程紀錄
他選另一個（例如「出貨要附文件」）=> 那要分成**兩份**：給客戶的手冊 ／ 我們自己的工程紀錄
⚠️ 而 export-ignore **只影響 git archive**，若日後改用別的打包方式它會靜默失效
   => 🔑 驗收釘「**包裡不存在 STATE.md**」，不要釘「.gitattributes 裡有那一行」
```

---

## 🔴 2026-09-23 晚間：`PK2` 進 `NEXT`（出貨紀錄沒有「這一包給了哪個客戶」）

> D 查 `deploy_packages` 清理排程時答出來的，而**他把三件事分開講**，那是這一格的價值。

```
❌ 「033314 那個包被誤刪」  => 不是。`KeepPackages = 2` 是 **2026-09-15 使用者親口要求**的
                              （原話：「當第三個打包檔的時候自動刪除第一個打包檔，避免重複堆積」）
❌ 「記錄機制失效」          => 不是。build_history.jsonl 是 **033314 之後**才加的
                              => 那一筆**從一開始就沒被接住**，不是機制壞了
✅ 「哪一包給了哪個客戶」     => **從來沒有人做過這個欄位**
```
🔑 ⇒ 〈「沒有人定過」與「我沒去查」在回報裡長得一樣〉的正面版本：
> **「壞了」「還沒存在」「從沒被設計」是三句不同的話，而它們在清單上長得一樣。**

### ⇒ 為什麼進 `NEXT` 而不是 `THIS`
```
✅ 它**不擋出貨** —— 現在只有一個客戶（我們自己）
🔴 而賣出去之後它會痛：客戶回報問題時查不到他裝的是哪一版、
   授權要重發時查不到發過什麼、而 KeepPackages=2 會讓包本身也消失
```
⚠️ 而那個清理設計是**為舊情境寫的**（2026-09-15，還是內部 ERP 的時候）——
  🔑 **一個當時正確的設定，在商業模式改變之後沒有人回頭看它。**
📌 而它與 `PK1` 是一對：`PK1` 是「包裡多了不該有的」，`PK2` 是「包外少了該記的」。

### ⏳ 待使用者回來確認
```
我裁：本輪不做。依據＝不擋出貨，而它要的是一個**出貨流程**（誰、何時、哪一版、授權碼），
     不只是一個欄位 —— 而那個流程的形狀取決於他怎麼賣
他選另一個（要做）=> 最小形狀是 build_history.jsonl 多兩個欄位（customer／license_id），
     ⚠️ 而那份 jsonl **在 .gitignore 裡且不受 KeepPackages 輪替影響**（D 實查）=> 落點已經存在
```
✅ 而 `build_history.jsonl` 連**測試通過／跳過數字**都留著 ——
  🔑 交件報告的測試基準應該引它，**比 A 的記憶可靠**。

---

## 🔴 `PK1` 排除清單（D 量，A 裁）—— **寫在這裡是因為它原本只存在於訊息裡**

> ⚠️ **前提（D 逐字）**：「`.gitattributes` 目前**完全沒有** `export-ignore` 規則
> （只有換行／二進位規則）⇒ **包裡現在的一切都不是被過濾剩下的，是預設全收。**」
> 🔑 這句放在最前面，後面每一項就不必再問「這個是不是誰刻意留的」。

### ⚙️ 判準（D 訂）
```
明確排除：內容是「**我們怎麼做這個系統**」（規劃／協作紀錄／audit／測試名稱）
明確保留：客戶自架或維運**真的用得到**的操作文件／腳本／執行期依賴
需人裁決：同一份裡混雜兩種內容，或判斷不出受眾
```

### ❌ 明確排除（**43 項**，逐類列名）
```
docs/windows/ 12 檔   A-2.md／AUDIT-2026-09-22.md／B.md／BACKLOG.md／C.md／D.md／
                      DELIVERY-NOTE-DRAFT.md／HEALTH-2026-09-22.md／
                      PIP-AUDIT-2026-09-22.md／PIPELINE.md／SCOPE.md／STATE.md
docs/ 其餘 5 檔       MOTRIX_ERP_System_Plan.md／UI-BACKLOG.md／UI-REDESIGN-PLAN.md／
                      module-viz-mockup.html／system-home-mockup.html
.claude/ 1 檔         hookify.shell-quoting-python-dq.local.md
backend/tests/        195 個測試檔（算 1 類）
backend/db_migration_plan.md ／ backend/requirements-dev.txt
根目錄 13 檔          AGENT-HANDOFF-TEMPLATE／AUTOLOGON-FIX／GITFLOW／
                      MODULE-AUDIT-2026-09-13／MOTRIX-ERP-ARCHITECTURE-MAP／
                      MOTRIX-ERP-QUICK／MULTI-BRANCH-AUTO-UPDATE-DESIGN／
                      MULTIWIN-PROTOCOL／NETWORK-PLAN-MODULE-DESIGN／NEXT-SESSION／
                      SELLABLE-AND-MOBILE-SPEC／WEEKLY-AUDIT-2026-09-07_2026-09-10／
                      LETSENCRYPT-PUBLIC-CERT-PLAN
根目錄 9 檔（選型研究材料）ACCESS／AUTOMATION／ENV／GATEWAY／MONITOR／NETARCH／
                      SWITCH-GUIDE-CONTENT／SWITCH-BRAND-REFERENCE／SELECTION-DB-INDEX
                      ⚙️ **已查證不被任何 runtime 程式碼讀取**（種子資料是 *_seed.py 裡的字面值）
```
⚠️ **D 回報的總數是 34，正確是 43**（最後那 9 沒加進去）。
> ### ✅ 而**清單是完整的**：另一把尺驗過 —— 包的根目錄有 **27 份 .md**，
> ### 排除 13 + 9 = 22、保留 5 ⇒ **22 + 5 = 27** ✅
> ### 🔑 加總抓到了錯的那一邊，**而它抓得到是因為 27 是另一次量測得來的**（`§344`）。

### ✅ 明確保留
```
DEPLOY.md ／ DR-SOP.md ／ HTTPS-DEPLOY-CHECKLIST.md   客戶自架的實際操作文件
firewall_setup.bat ／ start_server.ps1                執行腳本
backend/requirements.txt                              執行期依賴（不是 -dev 那份）
backend/tools/                                        ⚠️ **不可一律排除**，正式機維運要用
```
✅ 而 `backend/rollback_snapshots/` 確認**沒進包**（`find` 零結果，與 `git ls-files` 未追蹤一致）。

### 🔴 三件裁決
```
① CHANGELOG.md ⇒ **排除**
   抬頭是我們公司、指向內部 MOTRIX-ERP-QUICK.md ⇒ 它現在是內部的
   ⏳ 待確認：要給客戶 release notes ⇒ **另生成一份**（從 SCOPE 已結案編號產），
      🔑 不是把內部那份挑一挑 —— 與 docs/ 那一族同一個處置
② PASSKEY-CA-ROLLOUT.md ⇒ **整份排除**（同一份裡混了操作步驟與我們的執行紀錄）
   🔑 正確處置是拆檔，而那是工作不是決定 ⇒ 本輪取**安全側**：
      **排除的失敗會被報修**（客戶少一份文件會來問），
      ☠️ **保留的失敗不會**（我們的執行紀錄躺在客戶機器上，沒有人會告訴我們）
   ⏳ 待確認：客戶自架 passkey 要步驟書 ⇒ 另寫一份乾淨的
③ backend/version_manifest.json ⇒ **不裁，已派 D 查**
   比對它與 /api/system/version 實際回給客戶端的內容
```

### 🔴 而 `backend/tests/` 的理由換掉了（結論相同）
```
D 的理由：測試名稱逐一描述已知缺陷   <= 成立，而它是「保密」型
🔑 更硬的理由：**同一份清單裡 requirements-dev.txt 也被排除**
   => pytest／playwright 不在包裡 => **測試在客戶機器上本來就跑不動**
=> ✅ 排除它**不損失任何東西**，這個理由不需要任何人同意「保密比較重要」
```
📌 A 原本要把它列成 ⏳（顧慮是「現場出問題時跑不了測試」）——
  **那個顧慮被 D 自己的清單推翻了**，而**推翻它的那件事要去驗**：
```
🔴 已派 D：backend/tools/verify_package.py 會不會用到 backend/tests/ 或 requirements-dev.txt
   ☠️ 若會 => 排除它們會讓「裝完驗包」在客戶端壞掉，而那是**安裝流程的一部分**
   ⚙️ 一併看 DEPLOY.md 裡有沒有叫人跑它
```

---

## 🔴 `PK1` 補充：**`version_manifest.json` 不是「排除」，是「精簡」—— 單獨列，第 44 項**

> D 查證後回報，而**他明著說「這跟其他 33 項的處置方式不一樣，請單獨列」**。

```python
# routers/auth.py:288-308  GET /api/system/version
entries = json.load(open(version_manifest.json))     # 🔴 **每次請求都重新讀磁碟**
latest = entries[0]
return {"version": latest.get("version",""), "date": latest.get("date","")}
```
```
✅ 端點只回**最新一筆的 version + date** —— 完整異動細節從來沒有回給客戶端
🔴 而整份檔案從包裡拿掉 => 端點進 except 分支回空字串
   => **登入頁的版本號會安靜消失**
   ⚠️ 那個失敗模式**docstring 自己寫著**，還引用了一次真的發生過的案例（`1c8f2e8`）
```
> ### ⇒ 處置：**打包時產生一份精簡版**（只留最新一筆的 `version`/`date`）取代整份出貨。
> ### 🔑 那是**建置流程要新增的一步**，不是 `.gitattributes` 排除清單能處理的。

### 🔑 而它給出一條所有排除項都要過的判準
> ### **排除任何一個檔案之前，先問：「執行期有沒有人讀它？」**
```
☠️ 對「執行期會讀它」的檔案，「排除」會產生一個**安靜的降級**
   —— 東西還在跑，只是少了一塊，而沒有人會報修（〈降級之後它還是會動〉）
✅ 而 D 已經對最危險的那一組做過這件事：
   9 份選型研究材料「**已查證不被任何 runtime 程式碼讀取**」
   （種子資料是 *_seed.py 裡的 Python 字面值，.md 只是當初的研究參考）
```

### ✅ 而 `backend/tests/`／`requirements-dev.txt` 的排除**已驗證安全**
```
verify_package.py:399-401  只把 backend/tests/ 檔案數印出來當**資訊性報告行**
                           （"（不在包裡）" 是被印出來的正常狀態之一，不觸發 R.fail）
verify_package.py:139      MUST_EXIST = ["autostart.bat", "DEPLOY.md"] —— 兩者都不在排除清單裡
verify_package.py 全檔     沒有 import pytest、沒有 subprocess 叫 pytest（純靜態檔案檢查）
DEPLOY.md                  全文搜 verify_package／pytest／requirements-dev **都是 0 命中**
```
🔑 ⇒ A 原本的顧慮（「現場出問題時跑不了測試」）**是對的擔心，而它與「這兩者是不是安裝流程的必要部分」是兩個問題** ——
  而答案是：**它們現在不是。**

### 🔴 ⇒ `SPEC-PK1` 的驗收要多釘一條不變量
```
**排除清單 ∩ verify_package.py 的 MUST_EXIST = 空集合**
🔑 因為 MUST_EXIST 是「打包產出物的消費端」——
☠️ 而它與「執行期會讀它」是同一類危險，只是消費者是我們自己的驗包工具
⚠️ 而 MUST_EXIST 會長 => 釘交集為空，**不要**把今天的兩個值抄下來
```

---

## ✅ `PK1`：43 項**全數通過**「執行期有沒有人讀它」—— 而清單外有一格沒有歸屬

### ⚙️ D 怎麼驗「`.md` 不會被 runtime 讀」（**不是用想的**）
```
① 正對照先跑：搜 version_manifest.json（已知會被讀）=> 4 個檔案命中 ✅ 搜尋法有效
② 正題：`.md` 不加任何限制全 backend 搜一次 => **104 筆**，**逐筆看**
   => 全部 104 筆都是 docstring／註解裡的引用（「見 XXX.md §11」這種格式）
   => **沒有一筆是 open()／read_text()／Path() 實際讀檔**
③ docs/ 下 2 個 .html mockup 同樣做法：只在 quotations.py 與 approval-queue.html
   的**註解**裡被提到，不是被 fetch 或 serve
```
🔑 〈`grep` 零筆是最常見的假答案〉的正確做法：**先讓一個明顯會命中的詞亮起來。**
📌 而「104 筆逐筆看」不是形式 —— ☠️ **只看幾筆就下結論，與看完得到同一個數字。**

### 🔑 而他撿到一件：`verify_package.py` **早就預期這些會被排除**
```python
MUST_EXIST   = ["autostart.bat", "DEPLOY.md"]                 # 缺了 R.fail
INTERNAL_DOC = ["MULTIWIN-PROTOCOL.md", "AGENT-HANDOFF-TEMPLATE.md", "GITFLOW.md",
                "DR-SOP.md", "MOTRIX-ERP-ARCHITECTURE-MAP.md"]  # **只是 print，沒有 R.fail**
```
> ### ☠️ 那五個**全部在排除清單裡** —— 寫那支工具的人**早就把「內部文件」分出來了**。
🔴 ⇒ 那改變了 `PK1` 要怎麼對使用者講：
```
「沒有人想到」  與  「**有人想到了而沒做**」是兩件事
⚠️ 而 DR-SOP.md 同時出現在 INTERNAL_DOC **與** D 的「明確保留」裡 => **兩邊判斷不一致**
```
⇒ 已派 D 查 `git log -S "INTERNAL_DOC"`：**是誰、什麼時候、為什麼**。

### 🔴 而 `E` 類三檔在 43 項清單**外**，而它們含公司資料
```
backend/tools/letsencrypt_renew.ps1        $Domain 預設值 erp.miactw.com
backend/tools/sync_pending_data_20260817.py 檔名帶日期的一次性同步腳本
backend/create_claude_account.py            註解提及 corbin
```
```
D 早先已確認：**這三個都是已追蹤檔案 => 都會進每一包**
而 backend/tools/ 被列在「明確保留」（理由：正式機維運要用）
=> 🔴 **它們既不在排除清單裡，也不在 IA1/WL7 的要改清單裡 —— 沒有歸屬**
```
🔑 ⇒ 〈用目錄劃範圍時，**範圍的邊緣就是盲點**〉：
  **`backend/tools/` 整個被判成「保留」，而它裡面有三個不該出去的檔。**
⇒ 兩條路，已派 D 判並回報依據：
```
甲 從 backend/tools/ 挑出這三個單獨排除（⚠️ 而 letsencrypt_renew.ps1 若是正式機
   憑證更新要用的，排除它會讓客戶少一支維運腳本 => 那要改成「改掉預設值」不是排除）
乙 改內容（歸 IA1/WL7 的分母）
```

### ✅ 而 43 項的副檔名組成確認過，沒有漏網的第二個 `version_manifest`
```
.md 39（12 + 3 + 1 + 1 + 13 + 9）／.html mockup 2／.py 測試檔 195（算 1 類）／.txt 1
=> 39 + 2 + 1 + 1 = **43** ✅（自己加過一次）
=> **沒有 .json／.bat／.ps1 落在「要排除」這一側**
```

---

## ✅ `PK1` 定性：**是新發現，不是一筆被漏掉的舊裁示** ＋ `backend/tools/` 分類

### ⚙️ `git log -S "INTERNAL_DOC"`（D 查）
```
只有一個 commit 觸碰過它：**ab8e700**（2026-09-22 20:32）
commit message 逐項交代了 verify_package.py 的六項修正，
**通篇沒有一句提到「哪些文件該不該出貨給客戶」**
INTERNAL_DOC 那 5 個名字只是被歸進「(3b) 內部流程文件」這個**資訊性報告區塊**
（find/print，不影響 PASS/FAIL），與同一支 commit 裡會 R.fail 的 MUST_EXIST 是刻意分開的兩張表
```
> ### 🔑 ⇒ **`PK1` 照「新發現」報給使用者**，不是「一筆沒被執行的裁示」。

### ✅ 而 `DR-SOP.md` 的「矛盾」不是矛盾（D 的定性，逐字保留）
> 「工具作者做的是『**這是內部流程類文件**』（**文件分類**），
>  而我做的是『**客戶自架時用不用得到**』（**出貨判斷**）——
>  **兩種判準本來就會對同一份文件給不同答案。**」

🔑 ⇒ 那不是「有一邊錯了」，是**兩把尺量不同的東西**。
☠️ 而 A 在派工時把它稱作「矛盾」—— **那個措辭會讓收訊的人去找誰對誰錯**，
  📌 〈診斷的層級決定覆蓋率〉的措辭面：**收訊的人會照措辭決定要做什麼。**

---

### ⚙️ `backend/tools/` 逐檔分類（28 個已追蹤檔案）
```
🔴 甲・排除（17）
   _dashboard_remote.ps1 ／ audit_account_permissions.py ／
   backfill_location_identity_snapshot.py ／ build_deploy_package.ps1 ／
   check_approval_queue_coverage.py ／ check_double_init.py ／
   check_endpoint_entrypoints.py ／ check_guide_sync.py ／ check_prod_drift.ps1 ／
   check_version_sync.py ／ code_health.py ／
   deploy_dashboard.html ／ deploy_dashboard.py ／ deploy_dashboard_ctl.pyw ／
   local_research_pipeline.py ／ parse_account_items.py ／ sync_pending_data_20260817.py

✅ 保留（6）—— 客戶自架必要
   _healthcheck_ping.py ／ apply_update.ps1 ／ https_setup.ps1 ／
   rollback_update.ps1 ／ setup_passkey_client.ps1 ／
   ⚠️ letsencrypt_renew.ps1（保留**而要改內容**：$Domain 預設值 erp.miactw.com）

🔴 需人裁決（4）—— D 明說「我判斷不出商業意圖」
   issue_license.py（docstring 自稱「產品裡放發證機」—— **客戶自己會需要簽發授權金鑰嗎**）
   fetch_root_ca.ps1（措辭是「在開發機執行」，而客戶自架 passkey 可能也需要）
   check_dependencies.py（pip-audit，對客戶維運有價值，但提到客戶機器上不存在的 requirements-dev.txt）
   list_payment_anomalies.py（純唯讀財務診斷，而引用了內部的 MOTRIX-ERP-QUICK.md §5.12）

🔴 **未歸類（1）：verify_package.py** —— 17 + 6 + 4 = **27**，而 tools/ 有 **28**
```
> ### 🔑 **做分類的那支工具，自己掉出了分類。**
☠️ 而它不是隨便一個漏網的檔：**`MUST_EXIST` 與 `INTERNAL_DOC` 就在它裡面。**
```
=> 就算那 5 份文件被排除，**它們的檔名仍然寫在 verify_package.py 裡隨包出去**
   而客戶執行它，畫面會逐行印「MULTIWIN-PROTOCOL.md（不在包裡）」
```
🔑 ⇒ **排除一份文件，不等於它的存在被藏起來。** 已派 D 判它的歸屬。

### ⚠️ 而加總這件事今天第二次，方向相反
```
上一次：明確排除 34 vs 43   => **清單對、總數錯**
這一次：28 = 18 + 6 + 4      => **總數對、清單少一項**
```
⇒ 判準：**加總自檢要真的把每個桶的長度數出來**（`len(甲)+len(保留)+len(裁決)`），
  ☠️ 不要用「我記得甲有 18 個」去對 28 —— **那是用結論去驗證結論**。
📌 而**兩次的清單本身都經得起核對** —— 錯的一直是那個數字，**而數字是別人會引用的那一個**。

---

### 🔴 `SPEC-PK1` 驗收新增一條不變量
```
**包裡不存在任何 `*_private_key*.pem`**
⚙️ 正對照：_license_private_key_dev.pem **在磁碟上**，而 git ls-files 零筆
   （check-ignore -v 命中 `.gitignore:13  **/*_private_key*.pem`，D 實測）
🔑 而它現在有了新的重量：**git archive 匯出的是已追蹤內容**
   => 那條 .gitignore 是擋住私鑰進包的**唯一一道**
```

---

## 🔴 `PK1` 更正：排除清單差一個數量級；而 `docs/reference/` 有一張真實傳票

### ⚙️ **12 vs 81**
```
SCOPE 的排除清單寫 docs/windows/ **12 檔**
而 git ls-files docs/windows/ => **81** ／ docs/windows/tools/ => **12** ／ docs/ => **91**
🔑 成因：那份清單是從 **09-22 那個包**量的，不是現在的 git ls-files
```
✅ ⇒ 改成**整個 `docs/windows/` 目錄一條規則**（B 已落地於 `e70527e` 的 `.gitattributes`）。
📌 **當「整個目錄」本身就是判準時，逐檔列舉才是那個盲點。**

### 🔴 `docs/reference/` —— **一張真實會計單據**
```
傳票-實例-20260330-006.pdf（c4e40c0，09-23 00:07，標題逐字「使用者給了一張真的傳票」）
商業會計項目表-112年度…pdf ／ .sha256 ／ README.md
✅ 已出過的兩個包裡**沒有它** —— 而那是**時間序的巧合**（PDF 是打包後 3.5~4 小時才加的）
☠️ **不是有機制擋住。下一次打包就會帶進去。**
```
⇒ 不另發編號（同一個機制、同一個修法），**而 `PK1` 的嚴重度描述要改**：
  **不只是「我們的工程文件」，是「一張真實會計單據隨產品出貨給每一個客戶」。**
⚠️ 而 `docs/ASK-ACCOUNTANT.md` 與 `docs/reference/` **原本兩張清單都沒有** ——
  📌 **我們一直在盤點 `.md` 與程式碼，沒有人想到 `docs/` 底下會有 PDF。**

---

## 🔴 `WL7` 更正：D 的「C 類：合法出廠預設」**兩檔都判錯了，翻進 `WL7`**
```
helpers/company_identity.py  DEFAULT_IDENTITY   <= 解析鏈**第四層** fallback
db.py:708                    _seed_setting      <= **全新安裝的出廠值**
=> 兩處的值**都是我們的公司**
```
> ### ☠️ ⇒ 「改讀設定」這個修法**在開發機上看起來會完全修好**（我們填了自己的資料），
> ### **而客戶沒填就落回預設 —— 還是我們的公司。**
🔑 而「客戶可在設定頁自己改」**不是一個防護**：
  ❌ 錯的不是他改不了，🔴 **錯的是他不知道要改** —— 設定頁已經填好了，PDF 印出來完全正常。

### ⇒ 處置**拆兩半，風險相反**
```
🟢 ① _seed_setting 三欄改空字串 —— **零風險、立刻可做、不必等使用者裁示**
   （既有安裝 key 已存在，種子不覆寫）⇒ 擋住往後每一個新客戶
🔴 ② DEFAULT_IDENTITY 五欄清空 —— 要先 migration 把現值寫進我們這台的 company_profile
   ⚙️ 做完之後 QL6 那個顧慮**自動消失**（第三層接住，走不到第四層）
```
⚠️ 而守門的檔案清單從 5 支加到 **6** 支（含 `letsencrypt_renew.ps1`），字串集加 `erp.miactw.com`。

---

## 🔴🔴 `PK1` 更正：**`.gitattributes` 那兩條目錄規則完全沒有生效**
```
⚙️ git check-attr export-ignore（D 量、A 複驗）
docs/windows/STATE.md ／ SCOPE.md ／ tools/px1_scan.py ／ backend/tests/conftest.py
   => 全部 **unspecified**（應該 set）
MULTIWIN-PROTOCOL.md ／ docs/UI-BACKLOG.md  => **set** ✅（單檔規則正常）
DEPLOY.md ／ backend/main.py                => unspecified ✅（負對照乾淨）
```
```
.gitattributes:87  `docs/windows/ export-ignore`    <= **尾巴加 / 不遞迴**
.gitattributes:92  `backend/tests/ export-ignore`   <= 同上
✅ 而同一份檔裡 `frontend/static/vendor/leaflet/** -text` **兩層都正確套用**
=> 🔑 差別在 `**`
```
> ### ☠️ **排除清單裡最大的兩塊（81 檔 ＋ 195 檔）現在完全沒被排除。**

### ⇒ 三件（已派 B）
```
1. 改成 `docs/windows/** export-ignore` 與 `backend/tests/** export-ignore`
   ⚠️ 而**用 check-attr 三組複驗**（應排除的兩層深／不該排除的負對照／目錄本身）
2. 🔴 verify_package.py 的 `_pattern_covers()` 改成**呼叫 git check-attr**，
   ☠️ 不要自己實作 git 的比對語意 —— 它現在用 gitignore 式語意，
   **那道守門的判斷基礎就是錯的那一套 ⇒ 它不可能抓到這個問題**
   ⚠️ 修完要證明它會紅：**把 `**` 拿掉，那道檢查必須 FAIL**
3. D 重新盤點：.gitattributes 每一條 export-ignore **宣稱涵蓋幾個檔 vs 實際 set 幾個**，
   兩個數字不相等的逐條列名
```
📌 而驗收條件**不變**（釘產出物）—— 🔑 **而這一次證明了為什麼**：
  **設定存在、守門通過、而事情沒有發生。**

---

## 🔴 2026-09-23 晚間：`VP8` 進 `THIS` —— **驗包工具量錯了對象（掃工作樹，不掃包）**

> ⚙️ 發號前先跑 `sort -u` 列出 VP 前綴已用到哪一個：`VP1 VP2 VP3 VP4 VP5 VP6 VP7` ⇒ **VP8 free**。

```
D 用真實的舊包跑整支 verify_package.py，附帶看到
   backend/rollback_snapshots/*/backend/autostart.bat  ×5
⚙️ A 查證：
   包裡有 rollback_snapshots 嗎      => **沒有**
   包裡 autostart.bat 有幾份         => **1**
   工作樹 backend/rollback_snapshots/ 底下有幾份 => **5**
=> ☠️ **那 5 筆只可能來自工作樹。**
```
> ### 🔴 後果是**假綠燈**：`MUST_EXIST` 的語意是「**包裡**一定要有這個檔」，
> ### ☠️ 而它撈得到工作樹的檔 ⇒ **包裡沒有 `DEPLOY.md`，這道檢查也會過。**
📌 〈工作樹≠repo〉：**未追蹤的檔案能讓守門閉嘴** —— 而這一次連「未追蹤」都不必，
  🔑 **只要工作樹上有，它就找得到。**
⚠️ 而 `ab8e700` 的六項修正裡**就有一項是「讀包不讀工作樹」** ——
  ⇒ 先別假設是新的回歸，**也可能是那次修正從來沒涵蓋這條路徑**。

### ⇒ 分工與驗收
```
D 先量整支：**每一項檢查量的是「包」還是「工作樹」**
   判準（機械的）：路徑是從 pkg_root／參數拼出來的（✅ 量包），
                   還是從相對路徑／os.getcwd()／寫死的 `backend/ 開頭的路徑`（🔴 量工作樹）
   ⇒ 三欄表：檢查項 ／ 實際量什麼 ／ **它宣稱量什麼**（從訊息與 docstring 讀，不要從函式名推）
B 再修，而修法**不是把路徑改對**：
   🔑 **讓「包的根目錄」成為唯一的入口** ——
   ☠️ 任何一處還能用相對路徑摸到工作樹，下一次就會再走回來
C 出題（誘餌）：在**複製出來的**包裡拿掉真正的 autostart.bat、保留 snapshots 副本，
   斷言這道檢查**仍然 FAIL** ⚠️ 不要動 deploy_packages/ 裡那兩個真的包
```

---

## ⚠️ `VP8` 修法縮小（同日更正）
```
D 逐項量完：**全檔只有 (4a) 一項不一致**，而 `_find_in_tree` 只有 **1 個呼叫點**
=> 🔴 它是一個實例，不是一類
```
```
❌ 取消「讓包的根目錄成為唯一入口」—— 只有一個呼叫點，那是**過度設計**
✅ `(4a)` 的 `_find_in_tree(WT, …)` 改成量**包**
⚠️ (4b) 的 `os.walk` **不要動**：它 walk 的是 `pkg`，名字像而對象不同
🔴 誘餌題不變（C）
```
📌 而 A 錯在**第二關用描述判而不是量** —— 詳見 `STATE.md` `§362` 的更正節。

---

## 🟢 2026-09-23 晚間：**使用者回來後的四件裁示**（出貨阻擋那一組）

> 用選單問的，四條的原文在 `PENDING-RULINGS.md` §① 出貨阻擋。

### ✅ 兩件**維持**我們的決定（不必改任何東西）
```
PK1 ① 出貨要不要附文件  => **不附**（B 已實作並用 git check-attr 驗證）
IA2 ① 要不要保留展示模式 => **保留功能而預設關**（B 的 _m107 正確）
   ⚠️ 而他是在知道「我們已經先停用了他正式機上那一個」的前提下回答的
```

### 🔴 `IA1 ①` **改了**：出廠帳號 ⇒ **安裝時由客戶自己輸入**
```
❌ 我們原本裁：通用身分（admin／系統管理員／email 留空）
✅ 使用者裁：**第一次啟動時由客戶自己輸入帳號與顯示名**
```
> ### ⚠️ **那比原本的決定大得多** —— 它不是換一組字串，是**一個新的首次啟動流程**。
```
🔴 而 init_default_admin() 現在是**無條件建立** => 整支要重新設計
⚠️ 四件必須一起想清楚，而不是邊寫邊想：
   ① **既有安裝（含我們自己這台）不可以被這個流程擋住**
      => 判準：users 表已經有 superadmin 就跳過
   ② 沒有人完成設定時，系統**不可以可用**（沒有管理員），
      ☠️ 而也**不可以磚掉**（要能再進設定流程）
   ③ `_write_initial_credentials()` 與 `must_change_password=1` 那一套要重新設計
      —— 隨機臨時密碼還要不要？還是客戶自己設？
   ④ 🔴 而 `'jeff'` 寫在存取控制裡那一格**照舊要拆**
      （delete_user／toggle_user_active／users.html ×5）
      => 改成保護「**第一個 superadmin 這個角色**」，不是保護一個字串
```

### 🔴 `WL7 ①` **改了**：登入頁副標 ⇒ **讀客戶自己的公司名**
```
❌ 我們原本裁：產品名（例如「MOTRIX 專案管理系統」）
✅ 使用者裁：**從 company_profile 讀客戶自己的公司名**
```
> ### 🔴 而它有一個 B 一定會踩的實作陷阱：
```
☠️ **登入頁還沒有 token**
=> 新增的公開端點**必須同步加進 `main.py` 的 `_PUBLIC_API_PATHS` 白名單**
   （既有規則，見記憶〈MOTRIX 版本號同步規則〉——
     登入頁的版本號當初就是踩這一格）
```
⚠️ 而兩件要在規格裡定：
```
① **客戶沒填時顯示什麼** —— 空白是刻意的（他會發現並去填），
   🔑 而**登入頁整片空白**與「空白欄位」不是同一回事 => 要有一個不難看的形狀
② ⚠️ 這會讓**未登入者看得到公司名** —— 那通常沒問題（是客戶自己的品牌），
   而要明著寫下來，不要當成沒發生
```

### ⏭ ⇒ 派工
```
A-2  改 SPEC-IA1 與 SPEC-WL7 的那兩節（**從「待確認」改成「已裁」，並寫出新的形狀**）
     ⚠️ 而 IA1 那一節要重寫，不是改一行 —— 它從「換字串」變成「新流程」
B    **等規格**。而 PK1／IA2 那兩件維持，他手上的可以收尾
```
⚠️ 而 `WL7 ②`（PDF 頁尾要不要產品標記）**還沒問** —— 留在 `PENDING-RULINGS` 裡。

---

## 🟢 2026-09-23 晚間：**使用者裁 —— 販售族 5 個從 `THIS` 搬到 `NEXT`**

> 他逐字：「**販售的先暫停，我要今天提出的功能完成就好**」／「**我提的功能都先完成，驗證後輸出升級檔**」

```
搬動：PK1 ／ IA1 ／ IA2 ／ WL7 ／ VP8      THIS -> NEXT
⚠️ 而 A 在選單裡說「7 個」是錯的 —— **實際在 THIS 的只有 5 個**
   （RT1 ／ PK2 ／ MG5 本來就在 NEXT，我查了行號才發現）
✅ 留在 THIS：QS1a（BN14/BN18 的前置，已完成）／JV27（傳票讀得出來，已完成）
   🔑 那兩個**不是販售族** —— 它們是功能線上的
```
### 🔑 為什麼是搬動而不是登記成例外
```
✅ 搬動   => 閘門變綠，而那是**誠實的**：它們確實不在這一包
❌ 例外   => ☠️ 那道守門存在的理由就是「不可以有例外」，把例外寫進去等於拿掉它
❌ 人工背書 => ☠️ 以後每一次全量都要重新分辨「這次的紅是不是同一批」
```
⚠️ 而**已經做完的不回退**：`PK1` 第一階段（出貨包不再含 `docs/windows/`）／`IA2`（demo 帳號）／
  `WL7 §5⓪`（內建預設值）／`VP8` —— 🔑 **它們是完成品，只是編號排到下一包。**
🔴 而 `IA1①`／`WL7①` 是「**已裁而未做**」（使用者剛裁了新形狀）——
  ☠️ 而 `login.html:203` 現在仍印「允碩整合集創 · 內部管理平台」。

---

## 🟢 使用者裁：**三句舊原話都不進這一包**

> 他逐字選：「**都不進**」

```
①「也找一個視窗去做部屬儀表板的升級優化」
②「你做一個真實計算器」
③「最後做一個待辦確認，找兩個視窗共同討論營運報表的數據是否有更好的顯示方式」
```
### ⚠️ 而它們**刻意不發編號**，只登記原話
```
🔑 理由：**一個沒有範圍的編號，會讓人以為它被定義過了**
☠️ 而 ② 我們看不出他指的是哪個功能（精算？獎金？報價？）——
   發一個編號就等於替他決定了那是什麼
✅ 而 ①③ 讀起來像他自己設的「之後再討論」，不是「現在要做」
```
⇒ 重啟時的第一步是**問他這三句各指什麼**，不是直接開規格。
📌 而它們登記在這裡就不會掉 —— 🔑 **而 D 之所以找到它們，是因為他掃的是原話不是編號。**

---

## 🛑 2026-09-23 晚間：**販售族改成「使用者明著提出才做」**

> 他逐字：「**販售的部分備注除非我確定要做並提出才施作**」

### 🔑 這比「暫停」強一級 —— 差別在**誰發動**
```
⏸ 暫停      => **等重啟**（而「重啟」可能是任何人判斷時機到了）
🛑 這一條    => **只有他明著提出才啟動** —— 我們不主動排、不主動問、不順手做
```
> ### ☠️ 而 `NEXT` 的語意是「**這一包不收**」，不是「不做」——
> ### 🔑 **一個排在 `NEXT` 的編號，下一輪有人看到就會以為「排到了就做」。**
⇒ 所以要在清單上看得出來，不能只寫在散文裡（〈散文對工具是隱形的〉）。

### 🛑 屬於這一條的編號（**不要主動施作**）
```
PK1  出貨包裡有整份內部工程文件
IA1  出廠管理員身分寫死（含 'jeff' 寫在存取控制裡）
IA2  demo 帳號
WL7  客戶收到的文件上印我們的公司抬頭
VP8  驗包工具量錯對象
MG5  _m008 把一個客戶的資料寫死在產品碼裡
RT1  retention 欄位沒有讀取端
PK2  出貨紀錄沒有「這一包給了哪個客戶」
```

---

## ☠️ 而系統現在處於「**修了一半**」的狀態，那本身要寫下來

```
✅ 已做完（不回退）
   PK1  出貨包**已經**不含 docs/windows/、backend/tests/、docs/reference/ 等
        （.gitattributes ＋ verify_package 已用 git check-attr 驗證）
   IA2  demo 帳號改成預設不建、隨機密碼、拿掉明文 log；
        🔴 而 **v107 migration 會在下次升級停用正式機上現有的 demo 帳號**
   WL7  **§5⓪ 內建預設值已清空**（db.py 的種子值與 company_identity.DEFAULT_IDENTITY）
        ＋ _m106 把現值寫進我們這台的 company_profile
   VP8  _find_in_tree 已改成量包
   JV27 已修（那是功能線，不是販售線）

🔴 完全沒做
   IA1  出廠仍建立 jeff／黃玉龍／jeff@miactw.com，
        而 'jeff' 仍寫在 delete_user／toggle_user_active／users.html ×5
   WL7 主體  **login.html:203 仍印「允碩整合集創 · 內部管理平台」**
        ＋ reports.py／network_plan_export.py 的 _COMPANY 仍是我們
   MG5 ／ RT1 ／ PK2  完全沒動
```
> ### 🔑 **「修了一半」有時比「沒修」更難理解** ——
> ### ☠️ 因為它讓人以為整件事已經處理過了。
📌 ⇒ 重啟時**第一步是重讀這一節**，不是從規格開始 ——
  ⚠️ 而 `IA1①`／`WL7①` 的**裁示已經改過**（使用者裁「安裝時由客戶自己輸入」／「讀客戶自己的公司名」），
  🔑 **那兩個裁示比規格新**，重啟時規格要先對齊裁示。

### ⚠️ 而有一件現在就會被使用者看見
```
🔴 IA2 的 v107：**下次升級會停用他正式機上現有的 demo 帳號**（active=0，可逆）
⇒ 要用時把那一列 active 改回 1 —— **設環境變數沒有用**（只管「以後要不要建新的」）
✅ DEPLOY.md 已寫明，而這一條是**已經做完的**，不受「明著提出才做」影響
```

---

## 🟢 2026-09-23 晚間：使用者裁 —— **`JV6` 兩件裁示**

```
① 「**購料發票**」指的是什麼   => 他裁：**指的就是叫料／材料費**
② 帶入的分錄，科目代號從哪來   => 他裁：**留空，我自己選**
```
✅ ② 照 A-2 的暫行裁示，不必改（`voucher.html` 已有「輸入代號後自動帶入」的互動）。
  🔑 而**不要自動帶** —— 那需要新表＋設定頁，是另一個編號的份量。

---

### 🔴 而 ① 造成一個碰撞，`SPEC-JV6` 要把邊界寫死
```
A-2 事先警告過：**case_extra_expenses 的「材料」是 `JV21` 的來源**
而使用者裁「購料發票＝叫料／材料費」=> 🔴 **JV6 與 JV21 會讀同一批資料**
```
> ### ✅ 而它們**不衝突** —— 判準是 A-2 訂的那一條：
> ### **產出物落在哪張表的哪幾欄，不是「從哪裡撈」。**
```
JV21  同一筆支出 -> 產出**摘要文字**（vouchers 的摘要欄）
JV6   同一筆支出 -> 產出**分錄列**（voucher_lines，金額 ＋ 科目留空）
```
🔑 ⇒ **同一個來源餵兩個產出，是設計不是重複。**
☠️ 而規格必須明著寫這一句，否則 B 看到「兩個編號都讀 case_extra_expenses」
  **最省力的反應是把其中一個做成另一個的呼叫** —— 而那會把兩種產出綁死。

### ⚠️ 而三件 A-2 已經查出來的，B 動工前要看
```
🔴 ① `source_type` **同名、不同表、無關**
   voucher_attachments.source_type  <= JV3／JV18／JV24 在用
   voucher_lines.source_type        <= **JV6 在用**
   ☠️ 而 vouchers.py 裡 8 處 source_type **全部是前者**
🔴 ② 金額：承攬商支出的金額在 snapshot_json，而**有五個候選鍵**
   ⚙️ id=2 PV-202608-001：totalAmount **0.0** ／ personnelTotal 3,500 ／ grandTotal **3,500** ✅
   ⚙️ id=3 PV-202608-002：totalAmount 12,000 ／ personnelTotal **0** ／ grandTotal **12,600** ✅
   🔑 **兩列剛好互補 => 只有 `grandTotal` 兩列都對**
   ☠️ 用 totalAmount 的話 id=2 記成 **0** —— 而**那張傳票會平衡、會過帳、金額是錯的**
   ✅ 那兩列是**天然對照組**，C 兩列都要出題（只測 id=3 的話錯的寫法也會過）
✅ ③ snapshot **不跟著動**，而理由是會計的不是一致性：
   **傳票一旦過帳，它記的就是那一刻的事實**
```

### ✅ 而 A-2 用程式碼推翻了 A 的兩個假設
```
❌ A 推「可能缺的是頁面」 => ✅ 實查：**缺寫入端，頁面有了**
   （vouchers.py 端點 **15 支**（ast 數的）／voucher.html **43KB 存在**）
   🔴 而 `STATE §158` 的「現況」段（只有 2 支／頁面不存在）**已經過期**
❌ A 問「JV1-JV3 相依滿足了嗎」 => ✅ **已滿足，JV6 有落腳處**
```

---

## 🟢 2026-09-23 晚間：`BN20` 進 `THIS` —— **已核准的獎金單也要能退回**

> 使用者逐字：「**已核准，也要有退回**」
> （起因：他問「獎金分潤的退回鈕在哪邊」，而 A 查出它只在兩種狀態下出現。）

### ⚙️ 現況（A 從 `frontend/pages/bonus.html` 讀的，不是推的）
```
:751  status === '草稿'                          => 只有「送審」
:756  status === '待審核' || status === '簽核中'  => 「簽核通過」＋「**退回**」
:766  awardDetail.status === '已核准' && !isAwardPaid(awardDetail)    => 只有「標記已發放」
```
> ### 🔴 ⇒ **已核准的單子目前退不回來。**
⚙️ 發號前跑了 `sort -u`：`BN1`–`BN19` 已用 ⇒ **`BN20` free**。
📌 而**不併進 `BN17`** —— 🔑 `BN17` 已完成且有題，擴大它的範圍會讓「`BN17` 已完成」變成不實。

### ✅ 沿用既有裁示的部分（不必再問）
```
退回要填原因（必填，空的要擋）                   <= BN17 已有
退回後寫 bonus_award_edit_log（retention 永久）  <= BN17 已有
明細頁顯示「退回記錄」（長期記憶、不可編輯刪除）  <= BN17 已有
退回之後**回草稿**（不升版）                     <= 使用者先前裁過
```

### 🔴 而有一件 **B 動工前必須查清楚**，A 只查到一半
```
⚙️ A 讀 routers/bonus.py 的 approve_award()：**它只寫 `bonus_awards`**
   （沒有 INSERT INTO vouchers、沒有 create_voucher）
🔴 而 backend/archive.py:1627 的註解逐字：
   「`bonus_awards` **會開出兩筆傳票（核定／發放）** => 它是**憑證的上游**」
```
> ### ☠️ **兩者對不起來，而 A 沒有把它對完。**
⇒ 動工前要答一個問題：**核定的那一刻，到底有沒有傳票被建立？**
```
若「有」（在別的地方建的） => 🔴 退回已核准的單**會留下一張孤兒傳票**
   ⇒ 那不是 UI 改一個條件的事，是**錢的紀錄**
若「沒有」（使用者自己從獎金單開傳票） => ✅ BN20 是一個小改動
```
🔑 ⇒ **答案決定這是十分鐘還是一天的工作** —— 而**先查再寫**。

### ⚠️ 而**不做**的部分要寫清楚
```
❌ **已撥付**的不做 —— 他說的是「已核准」
   ☠️ 而已撥付代表錢已經出去了，退回它是另一件事（要先決定錢怎麼辦）
```
⏳ 待使用者確認：**已撥付要不要也能退**（我裁：不做，依據＝他只說了已核准）。

### ⇒ 而 `BN20` 照七步的**正確順序**做（今晚已經漂移過一次）
```
① A-2 寫 SPEC-BN20（含上面那個「有沒有開傳票」的前置）
② C 先把驗收測試寫成**紅的**
③ B 才寫碼
```

---

## 🟢 2026-09-23 晚間：使用者裁 —— **`JV21` 做滿三層**

> B 讀規格時發現矛盾，**直接問使用者**並拿到答案：
> **做滿三層，承攬商派工可展開成「品項／人員」兩種子列。**

### ☠️ 而矛盾是規格**自己**的 —— 形式通過，而內容打架
```
`SPEC-JV21 §3`／`§9①`  明說「**只涵蓋前兩層，B 不可以照它做第三層**」
`SPEC-JV21 §3b`        **詳細寫了承攬商派工第三層的形狀**
A 轉達給 B 的           「三層：支出底下還有品項」（使用者原話）
=> 🔴 **同一份規格裡，一節禁止另一節描述的東西。**
```
> ### 🔑 今天第二次同一個形狀（第一次是 `SPEC-BN18 §6b` 整節是 `BN16` 的內容）：
> ### **「有沒有這一節」的檢查通過了，而「這一節與別節說的是不是同一件事」沒有人驗。**
📌 而兩次的差別值得記：
```
BN18 §6b  內容是**別份文件**的     => refcheck 抓得到（引用指不到）
JV21 §3b  內容是**這一份自己的**，而**與另一節的裁定相反**
   ☠️ => 🔑 **refcheck 抓不到它** —— 兩節都存在、都指得到、而它們互斥
```
⇒ 這一類**目前沒有任何工具抓得到**，而它只會在有人照著做的時候被發現（這次是 B）。

### ✅ 而 B 的處置對：**他沒有自己挑一邊**
```
☠️ 兩節都是規格、都有依據 => 挑任何一邊都是「替使用者決定」
✅ 而使用者人在 => 直接問他，拿到逐字裁示
```
⚠️ 而**代價是 45 分鐘沒有輸出**，而 A 在那段時間去追「他是不是卡住了」——
  🔑 ⇒ 規則不是「不要直接問使用者」（那次問得對），是：
> ### **被任何事情擋住的那一刻，先發一行說「我在等什麼」，再去等。**
📌 〈握著鎖的一方沉默成本不在自己身上〉：**「進行中」與「卡死了」長得一樣。**
⚠️ 而 A 也有一半責任：**起跑時沒給死線**（〈長時間沒有輸出的動作要有死線〉
  要求起跑就說「跑什麼、多久、幾點回報」，而 A 只說了「做 JV21」）。

---

## 🛑 2026-09-23 夜：獎金分潤模組**不進這一包**（開關預設關）

> ⚙️ 使用者逐字：「**就這樣跑到結束匯出升級檔，如果問題太多先把獎金分潤的模組拉掉，
> 之後有時間再處理，記得備註，我要離開電腦前**」
> ⚙️ A 判「問題太多」成立（`BN21` 是錢的算法，要走完整七步，而他要離開）。

### ⚙️ 拉掉為什麼是零成本

```
正式機 schema 版本 **91**（使用者貼的畫面，2026/09/22 18:55）
而 bonus_awards 建於 **_m097_bonus**（db.py:4992，ast 逐函式對位驗過：
   全檔只有那一支 CREATE 它，_m098/_m102/_m103 都是後續 ALTER）
=> 91 < 97 => **正式機上連那張表都不存在** => 這個模組從來沒上去過
```
🔑 不是「表在而沒有資料」，是**沒有那張表** —— 結構性的，不是推論。

### ⚙️ 拉掉的是什麼、沒拉掉的是什麼

```
拉掉    前端入口（開關 `BONUS_MODULE_ENABLED`，**預設關**，一行就能開回來）
沒拉掉  `routers/bonus.py` **一行沒動**，資料表照建
=> 開回來之後**資料不會少**，而 ⚠️ **算法仍然是舊的（錯的）**
```

### 🛑 而開回來**之前**必須先做的（前置條件，不是已知問題）

```
🔴 `BN21` —— 獎金池三層分攤（規格已完成，`56bc855` -> `5c1bddc`）
   現況：pool = 淨利 × total_pct，**逐項目各自算**
   而 routers/bonus.py:950 **只檢查單一項目 <= 100%，沒有檢查合計**
   => 業務 10% ＋ 執行 10% ＋ 後勤 10% = **發掉淨利的 30%**，而系統不會說話
```
> ### ☠️ **`BONUS_MODULE_ENABLED` 打開之前沒有做 `BN21`，它會開始算錯錢。**
> ### 🔑 而寫成「前置條件」不是「已知問題」是刻意的 ——
> ### ☠️ 〈已知的代價 vs 要修的東西〉：**寫成註解之後，它看起來就像被處理過了。**

### 📌 另外還欠一件（小的）

```
`BN20` 已核准的獎金單要能退回 —— 規格已完成（`84a3a3e`）
使用者逐字：「**退回按鈕我還是要**」
⚙️ 而它是小改動：approve_award() 不開傳票，voucher_no_accrual 零寫入端
   => 沒有孤兒傳票風險（SPEC-BN20 §1 有三層查證）
```

### 🔑 而這一節同時寫在 `DEPLOY.md`，因為讀者不同

```
DEPLOY.md  使用者升級時會讀 => 「這一包刻意沒有獎金分潤，你的資料沒有少」
本節       下一輪開工會讀   => 「為什麼拉掉、怎麼開回來、開回來前要先做什麼」
```
☠️ 只寫一邊的後果：
```
只寫 DEPLOY => 下一輪的人不知道有欠帳
只寫本節    => 使用者升級後找不到模組，**會以為壞了**
```

---

---

## 🔴 2026-09-23 收工：**升級檔沒有匯出** —— 卡在建包的規格覆蓋率守門

> 使用者逐字：「就這樣跑到結束匯出升級檔，如果問題太多先把獎金分潤的模組拉掉」
> → 最後一句：「**先記錄吧**」⇒ 停止推進，落檔。

### ✅ 過了的关
```
not_e2e   26 failed / 2,498 passed / 53 skipped     HEAD dc08e64
e2e        2 failed /    73 passed /  2 skipped     <= 回到基準
建包 Step 語法檢查  OK（9 支 .ps1）
建包 Step 版本紀錄  OK（376 筆，最新 2026-09-23e）
```
### 🔴 沒過的那一关
```
建包 Step 規格覆蓋率守門  FAIL（5 紅）
⚠️ 而 build_deploy_package.ps1 **沒有任何 skip 參數**（只有 -OutDir / -KeepPackages）
```
🔑 守門自己的訊息：**「先把登記補完再打包」** ⇒ 它要的是**登記**不是**放寬**。
📌 分類表在 `docs/windows/GATE-BLOCK-2026-09-23.md`（A-2，`e79b315`）：
**丁 9 撞名／丙 9 題名不帶編號／乙 4 真欠帳／甲 9 沒做**。

### ✅ 今晚進了 repo、而沒進包的
```
JV21  傳票摘要可帶入案件底下的支出項（三層）      0417f65
BN14群組備份  bonus_groups / bonus_group_members 進每日備份  34f2d2f
獎金模組關閉  BONUS_MODULE_ENABLED 預設關（只動前端兩處）  d03bf7d
```

### 🔴 而有一件使用者今晚提的，**規格寫了而碼沒寫**
```
BN21  獎金池架構：現況 pool = 淨利 x total_pct，而 total_pct 是
      **每個獎金項目各自**的；routers/bonus.py:950 只檢查單一項目 <=100%，
      **沒有任何地方檢查合計 <=100%** ⇒ 三組各 10% = 發掉淨利 30%
BN20  已核准的獎金單也要能退回（使用者：「退回按鈕我還是要」）
```
✅ 規格：`84a3a3e`（BN20）／`56bc855`+`5c1bddc`（BN21）。
🔑 而獎金模組被關掉就是因為這一件 —— **開回來的前置條件是先做完 BN21**。

### ⚠️ 下一輪開工的正確順序
```
1. 讀 docs/windows/GATE-BLOCK-2026-09-23.md 的**最後一節**（這份文件本身的限制）
   ☠️ 乙/甲 只有 BI1 / EM13 查實過，其餘 11 個是從「產品碼有沒有命中」推的
   ☠️ 而產品碼命中**可能只是註解引用** —— **EM13 就是這樣被翻過來的**
2. 補題之前先花 3 分鐘確認**產品真的做了**，否則會為一個沒有實作的東西寫題
3. 丁（9）登 AMBIGUOUS_ACK／甲（9）登 PENDING，**登完要跑那兩道反向控制**
   test_the_holding_pen_cannot_become_a_place_things_rot
   test_nothing_sits_in_pending_after_it_has_been_done
```

### ☠️ 而今晚我（A）的錯只有一個形狀，被三個視窗各推翻至少一次
> ### **有一份現成的來源，而我用「關於它的描述」代替了「讀它」。**
```
C   「6 支紅是守門缺陷」      => 逐一比對修前修後，同樣 6 支照樣紅
C   「那 5 支是既有的」        => 我只讀了 diff，他開 worktree 實跑才算證明
C   「兩支 e2e 是我們造成的」  => 實跑基準，只有一支是
B   「主題紅＋反向控制」      => 一個成因打破兩條，不是兩個獨立的紅
A-2 「BN20 仍在 THIS」       => 他用自己的尺，守門自己的解析器說 False
A   「守門誤報了」            => 我用 max(dates)，而守門量的是**第一筆**
```
🔑 而最後那一個差點讓我去改一個**寫對的守門**。
