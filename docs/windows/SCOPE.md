# SCOPE — 這一包收哪些編號（只有視窗 A 寫）

> 🔴 **這個檔只放清單。** 「為什麼這樣訂」一律寫在 `STATE.md §20`。
> ☠️ 理由：2026-09-22 同一個根因在這裡發生兩次 ——
> ① 解析器連散文也讀 ⇒ 多出 4 個不存在的編號
> ② 真清單落在散文小節底下 ⇒ 少了 16 個（整組 `RG`）
> 🔑 **搬一次位置是修結果；把「資料」與「關於資料的討論」分開才是修作法。**
> 📌 A-2 2026-09-22 指出。第三次因此不可能發生。

**規則**（細節與理由見 `STATE.md §20`）：
1. 新編號預設進 `NEXT`。搬進 `THIS` 要 A 明著搬，並寫理由。
2. `THIS` 區塊**禁止任何省略形式**（`…`、`~`、「等」、「以下」）。
3. 每一組寫出「這一組有幾個」。

---

## ✅ THIS — 這一包要全綠

```
VR(10): VR1 VR2 VR3 VR4 VR5 VR6 VR7 VR8 VR9 VR10
BK(13): BK1 BK2 BK3 BK5 BK8 BK9 BK10 BK19 BK20 BK22 BK24 BK26 BK29
MN(7):  MN1 MN2 MN3 MN4 MN5 MN6 MN7
GB(21): GB1 GB2 GB3 GB4 GB5 GB6 GB7 GB8 GB9 GB10 GB11 GB12 GB13 GB14 GB15 GB16 GB17 GB18 GB19 GB21 GB22
GC(11): GC1 GC2 GC3 GC4 GC5 GC6 GC7 GC8 GC9 GC9b GC10
FX(2):  FX31 FX33
TD(5):  TD1 TD5 TD6 TD7 TD8
QL(22): QL5 QL6 QL7 QL8 QL9 QL10 QL11 QL12 QL13 QL14 QL15 QL16 QL17 QL18 QL19 QL20 QL21 QL22 QL23 QL24 QL25 QL26
P0(4):  P0-00 P0-0 P0-1 P0-2
RP(3):  RP1 RP2 RP3
VP(1):  VP1
UI(4):  UI6 UI8 UI9 UI10
DM(1):  DM1
MG(1):  MG1
JV(25): JV1 JV2 JV3 JV4 JV5 JV6 JV7 JV9 JV10 JV11 JV12 JV13 JV14 JV15 JV16 JV17 JV18 JV19 JV20 JV21 JV22 JV23 JV24 JV25 JV26
BN(1): BN1
AC(2): AC1 AC2
AR(1): AR1
SP(1): SP1
AI(1): AI1  <= **併進 CA1**
CA(1): CA1
AS(4): AS1 AS2 AS3 AS4
AT(1): AT1
WD(1): WD1
BR(4): BR1 BR2 BR3 BR4
BN(17): BN2 BN3 BN4 BN5 BN6 BN7 BN8 BN9 BN10 BN11 BN12 BN13 BN14 BN15 BN16 BN17 BN18
EM(12): EM1 EM2 EM3 EM5 EM6 EM7 EM8 EM9 EM10 EM11 EM12 EM13
PX(1): PX1
JV(1): JV8
AL(1): AL1
BI(1): BI1
QS(1): QS1a
```

> 📌 `RP`（restore path）是 2026-09-22 21:4x 由 A 明著搬進 `THIS` 的。
> 理由：**六對 robocopy 裡，唯一有檢查結束碼的是「套用新版」那一對**，
> 復原路徑上每一個複製都是靜默的（`STATE.md §53b`，C 複驗查到）。
> ☠️ `RP1`（`:338`/`:339`）做的是**回滾快照本身** —— 靜默失敗 ⇒ 快照殘缺 ⇒
> **沒有人知道，直到有一天要用它回滾**。整套部署安全機制的地基就是那份快照。
> ⚠️ **`RP2`／`RP3` 與 `restoring` 必須成對落地**（`§53d`）：
> 少了 `RP2`/`RP3`，`restoring` 印不出來；少了 `restoring`，它們印出來的是
> `applied`（語意「完整在磁碟上」）—— 兩邊都錯，而且各自看起來都合理。
>
> 📌 `VP1` 是 2026-09-22 21:5x 由 A 從 `NEXT` 搬進來的（`STATE.md §55c`）。
> 理由不是它大，是**它什麼時候會被用到**：`verify_package.py:268` 的
> `if expect is None:` 只 print 不 fail ⇒ 呼叫端忘記帶參數，**那道版本擋關就
> 安靜關掉，而管線全綠**。
>
> 🔴 **更正（`STATE.md §59`）**：A 原本在這裡寫「**而這一包打包時就會用到那道擋關**」
> —— **那是錯的事實**。實查全 repo：`verify_package.py` **零個呼叫者**
> （`build_deploy_package.ps1` 裡 0 處），它只在**有人想起來要跑**時才跑。
> ⚠️ 錯的那一句留在這裡，因為它是**優先序理由** —— 錯了會讓人排錯順序。
>
> ✅ **優先序不變，理由換成**：**人工執行時忘記帶參數，比自動呼叫時更容易發生**
> （自動呼叫寫一次就固定了，人工每次都要記得）⇒ **嚴重度沒有降低，可能更高**。
> ☠️ 一道可以安靜關掉的守門，比沒有守門更糟，**因為有人會信它**。
> ⚠️ `VP2`–`VP4` 留在 `NEXT`：它們的接縫（參數／自測函式）**還不存在**，
> 是**新功能不是修缺陷**，不要跟著一起進來把範圍撐大。

> 📌 `P0` 是 2026-09-22 20:29 由 A 明著搬進 `THIS` 的（規則 1 要求寫理由）。
> `P0-00` 是新的，排在 `P0-0` **前面**：兩者是同一個判定邏輯的兩側，
> `P0-0` 高報失敗、`P0-00` 低報失敗，而**記成失敗會有人來看，記成成功不會**。
> 理由全文在 `STATE.md §30a`（A 自己複驗過 `apply_update.ps1:532/:546`
> 與 `deploy_dashboard.py:186`，不是照收轉述）。
> ⚠️ `P0-1` 相依 `P0-2`（按鈕時間要算出來，資料來自 history）——順序不可對調。

---

## ⏭️ NEXT — 這一包不收

```
NB(29): NB1 NB2 NB3 NB4 NB5 NB6 NB7 NB8 NB9 NB10 NB11 NB12 NB13 NB14 NB15 NB16 NB17 NB18 NB19 NB20 NB21 NB22 NB23 NB24 NB25 NB26 NB27 NB28 NB29
WL(4):  WL1 WL2 WL3 WL4
FX(6):  FX3 FX28 FX29 FX30 FX32 FX34
XA(2):  XA6 XA7
YB(1):  YB1
UI(5):  UI1 UI2 UI3 UI4 UI5
GC(1):  GC11
TD(5):  TD2 TD3 TD4 TD9 TD10
VC(2):  VC2 VC3
P1(1):  P1-1
P2(5):  P2-1 P2-3 P2-4 P2-5 P2-6
VP(6):  VP2 VP3 VP4 VP5 VP6 VP7
FN(7):  FN1 FN2 FN3 FN4 FN5 FN6 FN7
RP(1):  RP4
DV(1):  DV2
GT(1):  GT2
MG(1):  MG2
TC(4):  TC1 TC2 TC3 TC4
FE(1):  FE1
DB(1):  DB1
BG(1):  BG1
HG(1):  HG1
QA(3):  QA1 QA2 QA3
GT(1):  GT1
BK(3):  BK31 BK32 BK33
CL(2):  CL1 CL2
MD(1):  MD1
QN(1):  QN1
UP(3):  UP1 UP2 UP3
SL(1):  SL1
QS(1):  QS1
MG(2):  MG3 MG4
EM(1):  EM4
RT(1):  RT1

```

> 📌 `VP`（verify package）＝ `backend/tools/verify_package.py` 四項，
> 內容由 D 量測、**編號由 A 發**（`STATE.md §50c`）。
> 🔑 D 原本在訊息裡發了 `RC1`–`RC4` 並自己撤回 —— **只有能寫進權威檔的人可以發編號**，
> 因為**編號是規格的形狀**，一則帶編號的訊息比不帶編號的更容易被誤當成已下達。
> ⚠️ `VP2`／`VP3` 的紅要標明紅在「**參數／函式不存在**」，不是紅在行為錯。
>
> 🔴 `VP5`（2026-09-22 22:0x 新增，`STATE.md §59`）＝
> **把 `verify_package.py` 接進打包腳本**（`build_deploy_package.ps1` 最後一步）。
> ⚠️ **未解的設計題，不要當小事接上去**：那個 `--expect-db-version` 的數字**從哪來**？
> `deploy_manifest.json` 不帶 db 版本；最省的來源是 `CURRENT_VERSION` 本身，
> **而那會變成自己驗自己**（〈守門守的對象被搬走〉）。
>
> ✅ 而「寫進協定的打包放行那一道」**已由 A 當場落地**（`MULTIWIN-PROTOCOL.md`），
> 不列為欠帳 —— 它把「沒有人想到」變成「**有人跳過**」，而那兩者的可查性差很多。

> 📌 `P2` 原本寫在 `STATE.md` 是一串用「／」隔開的**散文**，六件事沒有編號。
> 〈散文對工具是隱形的〉——沒有編號的項目，守門與盤點工具一個都看不見。
> 2026-09-22 20:29 由 A 拆成編號：
> `P2-1` 帳密一組共用／`P2-2` 失敗判定不靠文字／`P2-3` deploy_logs 保留最近 N 筆／
> `P2-4` dirty 只看 `backend/frontend`／`P2-5` `aheadOfOrigin` 不 fetch ⇒ 數字可能過期／
> `P2-6` `:291` 半形逗號
>
> 🔴 **`P2-2` 不在上面那一列，因為它已經被 `P0-00` 吃掉了**——
> `P0-00` 要做的正是「判定不要靠文字比對，改讀結構化結果行且 fail-closed」。
> 留著它會被做第二次，而第二次做的人會以為第一次沒做完。

---

## 📌 EXEMPT — 已結案／只登記／落點不在測試

```
BK(17): BK4 BK6 BK7 BK11 BK12 BK13 BK14 BK15 BK16 BK17 BK18 BK21 BK23 BK25 BK27 BK28 BK30
GB(1):  GB20
RG(18): RG1 RG2 RG3 RG4 RG5 RG6 RG7 RG8 RG9 RG10 RG11 RG12 RG13 RG14 RG15 RG16 RG17 RG18
G(10):  G2 G3 G4 G5 G6 G7 G8 G9 G10 G11
T(6):   T2 T3 T4 T5 T6 T7
UI(1):  UI7
P1(1):  P1-2
```

> 📌 `P1-2`（回滾演練）**已由使用者推翻**，不是「這一包不做」。
> 使用者原話：「回滾我這個月有測試過，升級有失敗觸發回滾也都正常」。
> ⚠️ `STATE.md §28` 標的是「已由使用者推翻」而**不是**「可選」——
> 兩者差別在於：標成可選，下一個人會以為它還等著被排進來。

🔑 **`RG` 的理由欄不是「這一包不做」，是「落點在交付說明的 `§RG`，不在測試」。**
**那一節填不出實際輸出 ⇒ 不打包。** 拒收的人是打包的那個人，不是 CI。

---

## 📌 2026-09-22 22:4x 補登記（理由全在 `STATE.md`）

☠️ **這 14 個編號今晚全部發出過，而 `SCOPE.md` 裡一個都沒有。**
成因：A 稍早那支修檔腳本 `AssertionError`（錨點不唯一），
**而同一個指令裡 `STATE.md` 的 commit 成功、`push rc=0`** ⇒ A 看到 `rc=0` 就往下走。
🔑 **assert 做了它的事，是 A 沒讀。**
⇒ `§6`：**一個指令裡有多個步驟時，`rc=0` 只代表最後一步。**

```
THIS
  UI6  財務分組加「出納」入口            §61／§65（裁甲，§46b 稀釋不是修掉）
  UI8  sidebar 四處分組條件漂移          §68（三處是真的擋人）
  UI9  出納**拆回獨立頁面**              §71／§77（驗收條件：兩頁讀同一組端點）
  MG1  migration 版本號一致性守門        §66／§73（已綠 e15edb3）

NEXT
  FN1  財務（會計支出／科目樹）獨立頁面   §72b
  FN2  獎金計算獨立頁面                  §72b
  FN3  匯出傳票獨立頁面（T100 搬出 reports） §72b
  FN4  五個模組各自的執行／操作歷史       §72a（一次定形狀，不要五種）
  MG2  init_db() 執行期版本檢查          §75b（射程窄化 ⇒ 拒絕啟動）
  TC1  backend/tools 每一支都要被分類     §62
  TC2  分類成守門的要接在會被執行的流程上 §62（本身必須是 pytest 題）
  TC3  欠帳清單每筆要指向存在的編號       §62
  TC4  **盤點**解析型守門缺「輸入非空」斷言 §74c／§76a（只做盤點，不做可執行化）
  FE1  t100BankAccounts 載入抽共用（四處） §77d（刻意製造的第四份）

EXEMPT
  UI7  act()/_deniedPages 認 query string
       🔴 **「已不需要」不是「延後」** —— 理由 §71c：
       出納拆成獨立頁面之後**不同檔名**，同檔名衝突整個消失。
```

---

## 📌 2026-09-23 00:5x 追加（理由在 `STATE.md` §107／§108）

```
NEXT
  UI10 ~~營運報表那一項的 show 條件拿掉 cCash~~  §71c
       ☠️ **這一列是錯的，留著**：UI10 在 **23:05 `761d925` 就做完了**，
          而我 00:2x 才把它派給 C、00:26 才登記成欠帳 => 已移進 THIS
       => 只有出納權限的人不該看到「營運報表」（點進去是別人的頁）
  DB1  db.py:151 的 PRAGMA foreign_keys=ON 包在 try/except: pass  §108
       => 設定失敗 => 外鍵不強制 => **系統照常運作，沒有任何東西會說話**
       ⚠️ 本輪**只做量**（對 _connect() 斷言 PRAGMA foreign_keys == 1）
          改不改那個 except 是另一個決定 => 〈量它不等於修它〉
       ⚠️ 量測要走 _connect()，自己 sqlite3.connect() 量到的是 SQLite 預設值 0
```

```
NEXT (追加 01:0x)
  DM1  demo 重置撞 v93 的 TRIGGER => demo 登入 500          §109
       => account_items 排除在清除清單外（系統資料，不是使用者資料）
       ⚠️ 驗收要**連登兩次**：第 1 次會成功（表還沒建），第 2 次才炸
       ⚙️ 守門要笛卡兒積：每張表必須落在使用者/系統其中一邊，互斥且窮盡
```

---

## ☠️ 2026-09-23 01:1x — `UI10` 是我登記錯的，成因值得留著

```
23:05  761d925  fix(UI10): 營運報表的顯示條件拿掉 cCash    <= **做完了**
00:2x  A 把 UI10 當「既有欠帳」派給 C
00:26  e2755d5  A 把 UI10 登記進 **NEXT**
00:28  C 回報：那一題現在是綠的
```
🔑 **成因：我用的是對話裡的狀態，而不是去查現況。**
⚠️ 而它的代價是雙向的：
```
往下  C 花時間寫一題「證明別人已經修好的東西」
往上  NEXT 多出一筆假欠帳 => **任何看這份清單估工時的人都被我誤導**
```
📌 〈量測比變化慢＝輸出一印出來就過期〉的派工版 ——
   我今晚才剛用「最新的包比 commit 早四小時」糾正過 A-2 的範圍，
   ☠️ **而同一則訊息裡我自己派了一個已完成的任務。**
⇒ 可操作：**派任何一個「既有欠帳」之前，先 `git log -- <那個檔>` 看最近三筆。**

```
2026-09-23 00:40
  DM1  => **THIS**（bf36922 綠 4/4，列粒度：account_items 只清 source != 'statutory'）
  CL1  UI9/FN3 搬遷留下的殘骸                              §112
       reports.js 孤兒函式 14 ／ 孤兒狀態 23（四個 modal 的狀態，markup 搬走而狀態留著）
       ⚠️ 範圍含**把那道 modal 不變量補成雙向** —— 它只問「打得開的有沒有標記」，
          掃不到「留著而沒有人打得開」；少了這一半，下一次搬遷再留一批
```

```
2026-09-23 01:05
  MD1  .md 行內標記把字拆開 => grep 假陰性                      §118
       實測切點：STATE 2,292 ／ SPEC-VOUCHER 12 ／ SCOPE 18 ／ 共 **2,377**
       ☠️ **2026-09-23 01:10 量完縮範圍**（§119）：
       解析器（test_spec_coverage:114）**只讀圍欄內的 ASCII 編號**
       => 被行內標記拆開的編號 **0 個** => 縮成**兩小題**：
          (a) 圍欄裡編號被標記拆開（`UI**10**`）=> 紅
          (b) 🔴 **自己寫的字串再用窄 pattern 去比對它** => 紅
              （C 實踩的唯一實例：斷言訊息含 ** 而突變腳本比不到）
       ⚠️ `**UI10**`（標記在外）**合法且解析得到**，不可一起擋掉
```

```
2026-09-23 01:30
  BG1  備份守門的反向控制守的不是它 docstring 說的那件事   §125
       stale = backed - tables ⇒ **完全沒碰 _NOT_IN_JSON_BACKUP**
       ⇒ 把所有表寫進排除清單，兩題都綠
       ⚠️ 進 NEXT 不擋本輪，而 B 這一次要明著選「進備份」
```

```
2026-09-23 01:35
  GC9b => **THIS**（從 GC9 拆出來的，§126）
       原條文裡有兩個可分開驗證的要求，而兩支測試各驗一個
       => 守門認成撞名，兩邊都拿不到「已實作」的信用
       GC9  不落 DB／與地理快取表分開   <= test_gc9_*  （shortcircuit）
       GC9b 分鐘級的短退避 TTL              <= **要改名成 test_gc9b_***（miss_message）
```

```
2026-09-23 01:45
  HG1  docs/*.md 裡的**逐字黑名單**：同形異碼出現 => 紅      §128
       目前兩組（實際咬過的，不推測）：
         U+5713+6B04 -> U+570D+6B04     U+5265 -> U+525D
       ⚠️ **不可用字元類別**（兩者都不在相容區，e26f995 實測）
       ⚠️ 清單只在**咬到一次**之後才新增，不先列假想的
       ⚙️ 反向控制：黑名單裡的「正確字」必須真的出現在 docs 裡
          （否則一組兩邊都不存在的清單永遠綠）
```

```
2026-09-23 03:36  更正
  RP4  A 開編號時放進 **THIS**，而它的判定是**丙**（排下一包）
       => 閘門紅在「宣告了而沒有題」，而那是我放錯區造成的
       🔑 GT1 上線之後，**放錯區會立刻紅** —— 以前不會
```

---

## 🔴 2026-09-23 清晨追加：`JV`（傳票第二段），理由在 `STATE.md` §158

```
THIS
  JV1  建立＋清單 API
  JV2  送審／簽核／退回／過帳／作廢 API
  JV3  附件（帶入既有 ＋ 補上傳）
  JV4  傳票頁面（出納底下獨立頁）
  JV5  PDF 匯出（版面照 20260330-006）
  JV6  從既有單據帶入分錄
```
🔑 **六項全進 `THIS`，因為六項都是使用者點名的**（原話逐字在 `STATE.md` §158）。
⚠️ ⇒ **閘門現在會紅六題**，那是「這一包還不能出」的機器表示，不是缺陷。
⚠️ **`VC` 不要再用**：`VC1` 已被地圖背景定位文案佔走，我派工時撞過一次。

---

## 🔴 2026-09-23 清晨追加：`BN1`／`AC1`（理由在 `STATE.md` §159）

```
THIS
  BN1  獎金頁「產生獎金單」介面 —— 後端已備，缺的是 UI
  AC1  守門：宣稱「有畫面」要有互動元件＋寫入呼叫
```
🔑 **獎金與傳票是同一個形狀**：後端做完、前端只有唯讀骨架，而我兩次都回報成「做完」。
⇒ `AC1` 是**修作法**：把「有畫面」變成可執行的守門，不是靠我下次記得。
⚠️ 使用者裁示：`JV3` **(c) 都要**、傳票**比照坊間正式傳票**、**驗證必須完整不是模棱兩可**。

---

## 🔴 2026-09-23 清晨再追加：`JV7` ＋ 「完整」的定義（`STATE.md` §159b）

```
THIS
  JV7  摘要來源分頁選單（案件／已上傳檔案）→ 帶入 → 可手動改
```
🔴 **使用者逐字給出「完整」的定義**：
```
確認前後端跟頁面都有完成才算完整
```
⇒ 少任何一項**不得回報完成、不得進打包**。已寫進 `AC1` 條文。
📌 四項衍生（合併輸出的非 PDF 附件／摘要帶哪一欄／檔案清單不要做兩套／合併順序與頁碼）全部**未裁示**，記在 `STATE.md` §159b，不表示已決定要做。

---

## 🔴 2026-09-23 清晨：`AL1`（`STATE.md` §159c）

```
THIS
  AL1  Alpine 初始化跑兩次 —— 6 頁命中，voucher.html 已修，其餘 5 頁先查後果
```
☠️ **它在多數頁面上看不見**（多打一次 API 而已）⇒ 沒有人會報修。
⚠️ 三處更正在 `STATE.md` §159c：`why_cannot_post` 那支函式**不存在**（真名 `describe_balance`，我把 docstring 語意當函式名）、審核日期沒有欄位已併 `JV2`、全量閘門那一題**不可信**（跑到一半我改了 SCOPE）。

---

## 🔴 2026-09-23 追加：`BI1`（出貨阻擋，`STATE.md` §180）

`POST /api/bonus/items` 的 `user` 未綁定 ⇒ NameError 500。
☠ 而 `bonus_items` 兩個庫都 0 列、空狀態的唯一出路就是它
⇒ **每一個新客戶的第一天，按下新增就是 500**。
🔑 1440 綠沒抓到：`grep api/bonus/items tests/` = **0**，
  連 `BN1` 也繞過 router（`_seed_item()` 直接 INSERT）。

---

## 🔴 2026-09-23 追加：`AC2`（`STATE.md` §187）

**動作要有可見的回饋** —— `AC1` 驗「有沒有接上」，`AC2` 驗「接上之後它說不說話」。
☠ 實例：按下儲存畫面什麼都不說（成功訊息被緊接著的重讀清掉），
  **而後端全綠、七題全綠** ⇒ 使用者看到的是一顆沒有回應的按鈕。
📌 它原本不在任何編號下 —— **沒有編號的工作不會被排順序，也不會被盤點到**。

---

## 🔴 2026-09-23 追加：`AR1`（`STATE.md` §191）

`_daily_backup_tables()` 回 `{中文標籤: SQL}`，而 `set(dict)` 取的是**鍵** ——
☠ **C 與 A-2 各踩一次，而 A-2 是在讀過 C 的回報之後才踩的**
⇒ 「先看既有的題怎麼做」還不夠，**那個形狀本身就在誘導人取錯一層**。
⇒ 加一支 `backed_up_table_names()`，**把坑填掉，不是要每個人繞過它**。

---

## 🔴 2026-09-23 追加：`SP1`／`AI1`（`STATE.md` §195）

```
SP1  三類憑證的 doc_no 不是案件編號 => 「選一個案件」看不到它們
     後端九類都支援帶入，**缺的只有選取路徑** => 前端介面問題
     ☠️ 症狀是「沒有那一類」不是「壞了」
AI1  account_items.is_active 有欄位（v96）而 router **只有一支 GET**
     => **沒有端點也沒有 UI** => 使用者停不掉任何科目
     🔑 與 v93「註解說得出那個能力而沒有欄位」是同一種，只是往前走了一格
```

---

## 🔴 2026-09-23 追加：`CA1`（`STATE.md` §197）

**自訂科目樹**——使用者六個裁示的第 ①，而**會計科目模組從頭到尾是唯讀的**
（新增端點 0、停用端點 0、router 只有一支 GET）。

```
🔑 AI1 不是一個獨立缺口，它是**一個不存在的功能的下游**
   —— 「停不掉」之所以沒人喊，是因為**根本還沒有東西可以停**
✅ 實查：**沒有 BEFORE INSERT TRIGGER** => 新增 custom 科目不被擋
🔴 而 547 筆**全部是 statutory** => PATCH is_active **對現有每一筆都會被 TRIGGER 擋**
⚠️ 層級深度與編碼規則「**由他定**」=> **要問使用者**，「不寫死」與「不驗證」是兩件事
```
📌 排序判準：**我們自己開的編號不該排在使用者點名的前面。**

---

## 🔴 2026-09-23 追加：`AS1`／`AS2`／`JV8`（使用者裁示，`STATE.md` §201）

```
AS1  匯款申請簽核設定**併進**簽核設定
AS2  **傳票簽核出現在簽核設定中** —— **推翻 §161 的「不接 approval_settings」**
JV8  **新增傳票後才出現傳票的頁面**（現在一打開就是編輯畫面）
```
☠️ `AS2` 的成因要留著：我從「跟坊間正式傳票一樣」推出「簽核流程也要寫死」，
**而他那句是在講版面**（那張 PDF 的三格）。〈答案沒錯，是題目問錯了〉。

---

## 🔴 2026-09-23 追加：`WD1`（使用者裁示，`STATE.md` §209）

「只要使用在該系統的文字，都必須正式語氣口吻，標案雷達的信件，信件內容過度口語」

```
範圍  所有使用者看得到的文字（信件／畫面／錯誤訊息／按鈕／欄位說明）
      ⚠️ **不含**程式碼註解與 docstring
🔴 而**要保留資訊量** —— 改得正式而把它在說什麼拿掉，**比口語更糟**
⇒ 先盤（D）、定原則（A-2）、再改（B）、出守門（C）
```

---

## 🔴 2026-09-23 追加：`EM1`／`EM2`（`STATE.md` §213）

```
🔑 **口語是語域，而語域需要空間。三個字的訊息沒有語域可言。**
993 呼叫點 -> 523 條不重複 -> **真正能口語的只有 41 條**（>25 字）
而那 41 條多數不是口語，是「**寫給工程師的**」
  —— 最極端一條 132 字，是我們自己的註解被放進 detail
  🔑 **它的問題不是資訊少，是讀者錯了** => 四格判準套不上去
EM2 的 186 條**不是語氣問題，是一致性問題**；159 條短標籤**不做**
```
🔴 而 `fstring` 137 條樣板的**插值內容沒看** —— 可能把內部變數漏給使用者，
**那與語氣無關而可能更嚴重**（`§175` token 那一格的同族）=> **先查它**。

---

## ☠️ 2026-09-23 追加：`BR1`（`STATE.md` §215）—— **今天的產出使用者一個都沒看到**

```
666 的 process 啟動於 **05:56:02**，指令**沒有 --reload**
=> 它載入的是 dd50d2e(05:54)；05:56 之後的 **25 個 commit 全部沒生效**
使用者看到的 500 是 BI1，而 BI1 在 **06:23** 就修好了
成因在一個字：start.bat 有 --reload，**restart.bat 沒有**，而使用者按的是 restart
```
🔑 **「我改好了」與「他看得到」之間有一個沒有人在看的間隔，而兩邊都不會報錯。**
⚠️ **不可以給 `restart.bat` 加 `--reload`** —— 它會被打包，正式機也用它。

---

## 🔴 2026-09-23 追加：`BN2`-`BN5`（使用者四項）＋ `EM3`（`STATE.md` §216／§217）

```
BN2  獎金項目只有最高管理者**可見**（現在只擋寫不擋讀）
BN3  人員來源新增 **manual（手動指定）** —— 「新增一種來源類型」做不出來，
     因為系統不知道要去哪裡撈人；而 manual 同時解掉 owner/engineer 欄位不存在的落差
BN4  「產生獎金單」-> 「**產生獎金分潤單**」（逐處，不是一個按鈕）
BN5  案件編號**下拉選單** —— ⚠️ **列什麼是全部內容**，要有說得出原因的空狀態
EM3  **19 處 detail 直接放例外物件** => 使用者畫面會出現
     「UNIQUE constraint failed: customers.code」（**實跑出來的，不是推的**）
     => 固定文案 ＋ **追蹤碼**；🔴 不可以只把 {e} 拿掉（診斷能力會一起消失）
```

---

## 🔴 2026-09-23 更正（`STATE.md` §220）—— **§215 的成因是錯的**

```
❌ 我寫的    「restart.bat 沒有 --reload，而使用者按的是 restart」
✅ D 實查的  行程祖先是 **Git-Bash 的 nohup.exe**（從 bash shell 背景啟動）
             => **那個入口不存在於任何檔案裡**
             => 掃檔案的盤點找不到它，補 restart.bat 也修不到它
```
```
❌ 我寫的    「25 個 commit 使用者一個都沒看到」
✅ 正確界線  新的 **.html／.js 看得見**（StaticFiles 每次讀磁碟＋no-cache）
             新的 **API 端點看不見**（路由在 import 當下註冊）
             => 那正好解釋：獎金頁面在，而「新增獎金項目」500
```
```
❌ 我寫的    EM3「實跑出來的（不是推的）」
✅ 實際      A-2 用的是**同形狀的記憶體表**，**不是產品路徑**；
             C 實查四條產品路徑**全部到不了**（code 由 next_entity_code 產生／建立前先查過）
             => EM3 嚴重度下修為「一旦發生非預期例外才會洩漏」，**但仍要修**
```
🔑 **觀測的那個世界不是受測的那個世界** —— 而「正在跑的 process」那一層最遠：
前兩層差在版本，這一層差在**時間 ＋ 有沒有人按下重啟**。

---

## 🔵 2026-09-23 追加：`BN6`／`BN7`（`STATE.md` §221）

使用者逐字：「獎金單需要詳細有張表格，像是精算頁面一樣，報價多少、成本多少、
衍生成本、比例最後總利潤多少，再用這個利潤去拆發比例，可預覽、可匯出pdf」

```
✅ 實查：要的每一項**都已經存在 settlement.summary 裡**（開發機 13 筆）
   quotedPretax／totalActualCost／extraTotal／dispatchTotal／adminCost／
   charityDonation／grossProfit／**netProfit**／各 Pct
🔴 => BN6 **一個數字都不要算** —— 10%／1% 兩個係數只寫在 settlement.html，
      再寫一份就是第三份實作（helpers/bonus.py 模組 docstring 已寫死）
☠️ dispatchTotal 只有 **6／12** 筆有 => **缺欄位標「無」不是印 0**
☠️ 13 筆有 summary 而欄位計數 12 => **有一筆 summary 是空的 {}**
```
🔑 ④利潤 到 ⑤拆發 之間那一行是這張表的重點：
使用者要「**淨利怎麼來的**」與「**它怎麼被拆掉的**」接在同一張紙上。

---

## 🔴 2026-09-23 追加：`JV9`-`JV11`（`STATE.md` §224）＋ **一件更正**（§225）

使用者逐字：「傳票匯出pdf顯示尚未設定公司抬頭，要有預覽功能，匯出PDF跟匯出PDF(含附件)，
未審核通過前不能匯出，並且需背景顯示該流程尚未簽核完成的紅字，像是報價單一樣，
可原視窗預覽並且預覽後匯出」

```
JV9   ☠️ 「尚未設定公司抬頭」**不是沒設定** —— company_profile.name 有值（12 字，實查）
      _get_setting() 已經 json.loads 過，voucher_pdf.py:82 **又 loads 一次** => TypeError
      => **被自己的 except 吞掉** => 回 "" => 印「尚未設定」
      ⚙️ ast 掃 366 檔 => **孤例 1 處**（正對照通過）
JV10  原視窗預覽（沿用報價單 modal）＋ 兩顆按鈕（含附件那顆沒附件時要停用並說話）
JV11  🔑 **預覽隨時可看（紅字浮水印）；匯出要簽核通過** —— 閘門**在後端**
```

### 🔴 更正：`§219`／`§221` 的「1 筆 netProfit<=0」是錯的
```
逐筆打開：**8 筆全是正的**；差的那筆 netProfit = **None**（summary 空 {} 而 status=finalized）
成因：CAST(NULL AS REAL) > 0 是 NULL => **「沒有值」被讀成「值是 0 或負數」**
```
🔑 **聚合查詢回來的差額，不可以直接推論成「那一筆的值是什麼」** ——
差額只說「它沒有進到這個集合」，而沒進來的理由有好幾種。
⇒ 下拉四態各一句，②「沒賺錢」與③「精算是空的」**不可合併**（後者有出路，前者沒有）。

---

## 🔑 2026-09-23 追加：`EM4`（`STATE.md` §227）—— **`EM3` 的鏡像**

```
EM3  例外的內容**說太多**  => 資料表名／欄位名／路徑上了使用者的畫面
EM4  例外**什麼都不說**    => 一個 bug **偽裝成一句通順的話**
```
`JV9` 就是 `EM4`：`TypeError` 被 `except Exception: return ""` 吞掉
⇒ 紙上印「（尚未設定公司抬頭）」⇒ **語意完全通順，而設定裡有值**。

🔑 判準**不是**「有沒有 `except Exception`」，是「**它吞掉之後，那件事還說得出來嗎**」。
⚠️ 不能用形狀掃 ⇒ 掃「`except` 之後回一個**與成功路徑無法區分的值**，且**沒有 log**」。
⚙️ 正對照：`voucher_pdf.py` 修之前那一版必須亮；負對照：`split_attachments()` 不可以亮。

---

## ☠️ 2026-09-23 追加：`EM5`／`EM6`（`STATE.md` §230）—— `EM4` 盤出 **38 個**

```
🔑 JV9 真正的形狀：「無法區分」來自**成功路徑自己的 `or ''`**
   return (...).get('name') **or ''**   => 成功與失敗在**同一個運算式裡**收斂到同一個值
   ☠️ 那種寫法在這個 repo 很常見，**比「兩個 return」難看見得多**
```
```
EM5  🔴 voucher.py:359 _chain_tiers -> **[]** ＝「這張單不需要簽核」
     => JV11 閘門讀取失敗就**判定已完成 => 放行**（未簽核的傳票匯得出去，畫面完全正常）
     ＋ build_info.py:121 _reload_flag -> None（「查不到」與「沒有」分不開）
EM6  ☠️ licensing.py `_stable_machine_id -> ''` => **機器綁定靜默降級**
```
🔑 而這份盤點最有用的一句是**它的尺三次都看起來合理**：v1=131／v2=**0**／v3=38，
**只有正對照分得出哪一個是真的** —— v2 那個 0 若沒有正對照，會被讀成「沒有同族」。

---

## ☠️ 2026-09-23 追加：`BN8`（`STATE.md` §232）—— **獎金單沒有簽核流程**

```
實查  bonus_awards **沒有任何簽核欄位**；`bonus` 不在 APPROVAL_DOC_TYPES 的八種裡
      router 七支**沒有 submit／approve／reject**
      UPDATE bonus_awards 全 repo **只有一處**，而它**只寫 voided_***
=> **建立之後 status 從來不會改**，實際資料 1 筆 = 「草稿」
```
☠️ ⇒ 使用者裁的「用印欄列數從簽核設定算」**沒有東西可以算**。
✅ **使用者裁示：現在就開簽核流程** ⇒ 獎金單成為**第九個** doc type。
⚠️ `BN8` **擋住 `BN7`**；`BN6`（明細表）可平行。📌 趁 `AS2` 剛做完第八個。

🔑 A-2 列三條路、講出每條代價、明著說「**我不自己選**」—— 他建議 (b)，而使用者選 (a)。
**當一個處置的代價是「長出一整條新流程」時，那不是技術決定。**

---

## ✅ 2026-09-23 追加：`BN9`（`STATE.md` §233）—— **使用者要的已經做了一半**

使用者逐字：「非最高管理者只能看見自己可領到錢的獎金分潤單，其餘選項都看不到，
產生獎金分潤單這些也只有最高管理員可見」

```
✅ 已實作  routers/bonus.py:307-310  `if not manager and not lines: continue`
           => **與自己無關的單完全不出現**；也看不到 base_amount 與整張單總額
🔴 差一格  _is_manager 回 role in ("superadmin", **"admin"**)
           而它的 docstring **自己標了**「A 🟡 預設沿用，**未經使用者確認**」
           => 使用者現在確認了：**只有最高管理者**
```
🔑 那個 🟡 標記做對了一件事：**它讓一個未經確認的預設值在被推翻時，
改一個地方就好，而不是去考古當初為什麼那樣寫。**
⚠️ `admin` 收緊之後**變成一般使用者**（有份就看得到自己那一列），不是看不到獎金頁。

---

## 🔴 2026-09-23 傍晚：`QS1a` 由 A 明著從 `NEXT` 搬進 `THIS`

```
母題 QS1（報價單／案件的 sales_person 存顯示名）登記在 **NEXT**
而它拆出來的 QS1-a（username 改存帳號 + migration 回填）**已經做完並 commit**（208f0d2）
=> 一個**動到 production 資料**的改動，不在任何一個「要全綠」的清單裡
```
🔑 ⇒ **拆子題的時候，子題繼承了母題的桶子，而做的人不會注意到** ——
  ☠️ `QS1` 在 `NEXT` 是對的（整段還沒做），`QS1-a` 在 `NEXT` 是錯的（它出貨了），
  **而這兩件事在清單上長得一模一樣。**
📌 抓到它的是 **D 從 commit 獨立編表**（我的清單裡它是「做完的」，SCOPE 裡它不存在）——
  🔑 **兩份清單並排，差異才看得見；只看其中一份，兩份都是自洽的。**

⚠️ 編號寫成 **`QS1a`** 不是 `QS1-a`：解析器的正則是 `[A-Z]{1,2}\d{1,2}[a-z]?`，**不吃連字號**。
  （`P0-00` 那幾個是既有的例外，本來就沒被解析器看見 —— 見 `STATE.md §339`。）

⇒ `QS1a` 的驗收＝「**K 筆都有被列出來**」，**不可以釘 `K == 0`**（`STATE.md §334`）。

---

## 🔴 2026-09-23 傍晚：`RT1` 進 `NEXT`（`retention` 欄位**沒有任何讀取端**）

```
⚙️ B 實查（BN17 收尾時，A 要求「答我沒查可以，答完去查一次」）：
   整個後端**沒有任何一處**對 *_edit_log 表下過 DELETE
   archive.py 唯一的清理函式 _prune_audit_log() **只清 audit_log**
=> voucher_edit_log／bonus_award_edit_log 的 retention 是**純寫入**
=> 🔴 標 'term' 的列**今天也是無限期留著**
```
> ### ☠️ 缺陷不是「資料沒被清掉」，是**schema 說了一件不是真的事**。
> ### 🔑 下一個人讀到 `retention='term'` 會相信那些列會被清，而**沒有任何東西會糾正他**。

📌 它是〈「寫進 log」不是一個落點，它是一個願望〉的**第二種形狀**：
```
第一種：訊息寫出去了，而**沒有人會去讀**（logger.warning 的消費端 = 0）
第二種：欄位**寫進去了**，而**沒有程式會照它做事**（retention 的讀取端 = 0）
🔑 兩種都不是 bug —— 它們是**一個被預先留下來、而沒有被寫出來的未來**
```

### ⇒ 為什麼進 `NEXT` 而不是現在做
```
清理策略（留多久、誰能刪、刪掉要不要留痕）是**使用者的決定，不是工程決定**
⚠️ 而它與 §3t 同一族：那一條是「身分證號每天上雲端」——**保存期限有法律重量**
```
✅ 而**不寫成註解就算了**（〈已知的代價 vs 要修的東西〉：當場認出缺陷卻寫成註解＝放過）——
  🔴 發編號＋標 `⏳ 待使用者回來確認`，**它才會出現在他回來要看的那張清單上**。
📌 而 B **沒有順手去建 pruner**（「建 pruner 是新的一塊工作，不在 BN17 範圍內」）是對的：
  🔑 〈我發現的≠該做的〉—— **順手做掉的那件事沒有人派，也沒有人排過順序。**
