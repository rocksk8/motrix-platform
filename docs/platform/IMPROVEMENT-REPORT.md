# 改善報告（交叉稽核彙整，CORE-SPEC §9d／RUN-PLAN D6）

> 主持彙整；原始發現、證據與回覆以 `audit/` 各檔為準。必修全部關閉，D6 才算完成。
> 最後更新：2026-09-25 21:50

## 1. 稽核覆蓋

| 對象（作者） | 稽核者 | 稽核檔 | 狀態 |
|---|---|---|---|
| 部署儀表板 D1／D2／D4／D6／D7（主持） | C | `AUDIT-C-host-dashboard.md` | ✅ 結案：必修 3＋B-2 複核 2、建議 3、觀察 3 全部關閉（C-2 移到階段 S） |
| §9c②③ 模組啟停與授權（A） | 獨立代理 | `AUDIT-X-9c-module-select.md` | 進行中 |
| A11、STATES 高 5 項、A8c、升級只補空值（C） | 獨立代理 | `AUDIT-X-C-batch1.md` | 進行中 |
| §9b 升級與回滾、core.paths、個資分流（C） | 獨立代理（接手 A 的分配） | `AUDIT-X-9b-upgrade-paths-pii.md` | 進行中 |
| IP-1～IP-4、row_access（A） | 獨立代理（接手 B 的分配） | `AUDIT-X-IP1-4-row-access.md` | 進行中 |
| §9c① 選配打包、G1～G4 守門（B） | C | `AUDIT-C-B-guards.md` | 排在 C 的 P2 之後 |
| 儀表板 D3／D5（主持） | C | `AUDIT-C-host-D3D5.md` | 排在 C 的 P2 之後 |
| P6 事件匯流排、傳票修正（主持） | 待排 | — | 未排 |
| 專案 .venv、requirements 守門（B） | 待排 | — | B 合回後排 |

## 2. 已結案稽核的重點與學到的事

### 部署儀表板（C 稽核）
- **逾時上限不能比被保護的動作短**：回滾的上限原本比 convert 短。修正後，會改到正式機而且不是原子的動作，逾時不自動中止，改由人看過 log 後解除並寫原因。
- **磁碟檢查只看 C:**：改成只看安裝碟；拿不到碟代號就不放行。
- **輸出編碼取決於呼叫端的主控台**：cp932 環境會亂碼；改由腳本自己固定 UTF-8，測試刻意用 cp932 跑。
- **修正文件本身寫錯了兩處**（B-2a 判讀規則、B-2b 指令少參數）：由稽核者複核抓到，更正時保留錯的原句。

## 3. 跨稽核的系統性問題（待各稽核完成後彙整）

## 4. 改善建議（流程層級）
