# MOTRIX-PLATFORM 共用核心規格（草案 v0.1，2026-09-25）

> 基準：`c83dae6e`（V9 原版）。本 repo 是下一代產品；V9 原版只維護。
> 狀態：草案。模組分組待 `DEPENDENCY-MAP.md`（視窗 A）後由使用者裁示。

## 1. 目標與驗收

| 目標 | 驗收標準 |
|---|---|
| 模組可拆 | 移除任一 L2 模組資料夾，伺服器照常啟動、其餘模組測試全綠；只少功能不報錯 |
| 不重複開發 | 認證、DB、簽核、輸出、通知、上傳只存在 L1 一份；L2 之間 `import` 為 0（守門測試） |
| 測試縮小 | 改一個 L2 模組只跑「該模組＋L1 契約」；全量只在打包前（`tools/platform/modtest.py`） |
| 可自訂（第二階段） | 所有模組端點由 L1 端點登錄表對外公開，頁面建構器只透過登錄表綁定 |

## 2. 分層

```
L0 平台   app 啟動、模組載入器、授權、設定、log          platform/
L1 核心   認證權限、DB＋migration、簽核、輸出(PDF/Excel/Email)、  core/
          通知、上傳、稽核紀錄、端點登錄表、連接器匯流排
L2 模組   報價、案件、承攬商…（分組待裁示）                  modules/<key>/
```

相依方向只能往下：L2 → L1 → L0。L2 需要另一個 L2 的能力 ⇒ 下沉到 L1，或走連接器（§5）。

## 3. 模組目錄結構

```
modules/<key>/
  module.json        模組宣告（§4）
  api/               router（只 import core.* 與本模組）
  service/           業務邏輯（純函式優先，可單測）
  migrations/        本模組擁有的表，檔名 NNNN_<desc>.py
  pages/             前端頁面與 JS
  widgets/           第二階段：可放進自訂頁面的元件宣告
  tests/             本模組測試（modtest 以此為邊界）
```

## 4. module.json（模組宣告）

```json
{
  "key": "quotation",
  "name": "報價單",
  "version": "1.0.0",
  "core": ">=1.0,<2.0",
  "permissions": ["quotation", "financial_view"],
  "tables": ["quotations", "quotation_items"],
  "provides": {"endpoints": ["GET /api/quotations", "..."], "events": ["quotation.approved"]},
  "consumes": {"events": ["customer.updated"], "optional": true},
  "pages": [{"path": "quotations.html", "menu": "業務/報價單"}],
  "widgets": []
}
```

- `core` 用範圍宣告，不列舉版本；不相容 ⇒ 模組不載入並在系統頁顯示原因，不猜。
- `tables`：只有擁有者可寫；其他模組讀取必須透過擁有者公開的端點或 L1 查詢介面。
- 權限 key 沿用 `helpers/module_registry.py` 的 `MODULES`（已是唯一來源），授權沿用 `helpers/licensing.py` 的 `modules` 清單。

## 5. 模組間串接（連接器）

兩種形式，都經 L1：

1. **事件**：`core.events.publish("quotation.approved", payload)`；訂閱方不在 ⇒ 事件無人接收，發佈方不受影響。
2. **端點呼叫**：`core.registry.call("customer.get", id=…)`；對方未安裝 ⇒ 回 `ModuleUnavailable`，呼叫方必須處理成「少一個功能」。

每個串接節點登記六件事：形式／語法／回傳／對方不在時／契約版本／守門。登記表：`docs/platform/INTEGRATION-POINTS.md`，由守門測試驗證與程式碼一致。

## 6. 資料庫

- 單一 SQLite（WAL），`core.db` 提供連線與交易。
- migration 改為每模組獨立版本號：表 `schema_versions(module, version)`；模組未安裝 ⇒ 不建表。
- V9 既有 `db.py` 的 v1~v84 歷史凍結為 `core` 的基準 migration（不回溯改寫，凍住的歷史不呼叫活的程式碼）；新 schema 從各模組 migration 開始。
- 跨模組共用資料（客戶、使用者、組織）歸 L1 或單一擁有模組，由盤點結果決定。

## 7. 端點登錄表（第二階段的基礎）

- 載入模組時收集 `provides.endpoints`，對照 FastAPI 實際路由；宣告了卻不存在、存在卻未宣告 ⇒ 啟動記 ERROR、守門測試紅。
- `GET /api/platform/endpoints`：列出已啟用模組的端點、參數與回傳 schema、所需權限 ⇒ 頁面建構器只從這裡挑資料來源。
- 擴充：能力目錄 `GET /api/platform/catalog`（CUSTOMIZATION-SPEC P1，§3.8；2026-09-26）已包含已載入模組的端點（實際路由、參數名）、provider、事件、輸出引擎、可自訂點（`module.json` `customization`，P3 §3.9）。回傳 schema 與逐端點權限尚未列出（程式裡沒有機器可讀來源）。

## 8. 測試分層

| 層 | 位置 | 何時跑 |
|---|---|---|
| L1 契約測試 | `core/tests/` | 改 core 或任何模組時 |
| 模組測試 | `modules/<key>/tests/` | 改該模組時 |
| 邊界守門 | `tests/platform/`（L2 互 import、登錄表一致、串接總表） | 每次 |
| 整合／e2e 全量 | `tests/`（既有） | 打包發行前 |

暫存：basetemp 指到本次專屬資料夾，結束即刪。

## 9. 遷移步驟（每個模組相同）

1. 依 `DEPENDENCY-MAP.md` 定成員與擁有的表
2. 把跨模組相依改成 L1 或連接器（先改、先測，再搬）
3. 搬檔到 `modules/<key>/`，寫 `module.json`
4. 模組測試＋L1 契約全綠；行為不變（既有 API 回應比對）
5. 反向控制：刪掉該模組資料夾，伺服器啟動＋其餘模組測試全綠

## 9b. 升級轉換與回滾（V9 → 新版，2026-09-25 使用者交辦）

目的：把正式機從 V9 轉成新版時，**轉換前完整備份，出問題可以正確回到更新前的狀態**。

| 階段 | 做什麼 | 驗收 |
|---|---|---|
| 0 預檢 | 明確指定 V9 安裝路徑（不猜）；`schema_version` ≤ 116；磁碟空間 ≥ DB×3；**先查正式機健康**（最近一次備份 `.done`、告警）；服務是否已停 | 任一項不過 ⇒ 不動任何東西，列出原因 |
| 1 備份 | ① 主庫與 demo 庫用 SQLite Online Backup API（WAL 安全，不直接複製 `.db`）② V9 程式目錄快照（排除資料目錄）③ 設定與身分檔（`system_settings` 整表、heartbeat_config、license、憑證、`.env` 類）④ 資料目錄**清單**（路徑、大小、mtime、雜湊）：資料原地不動，只記錄以便驗證沒被動過 ⑤ `upgrade_manifest.json`：每個備份檔的 SHA256 | 備份完成後**立刻試還原到暫存位置並比對雜湊**；比對不過 ⇒ 中止 |
| 2 轉換 | 換上新版程式；基準 migration 補跑到 116；模組 migration；**設定轉換**（新增的旗標與設定鍵寫入預設值，不改寫既有值）；寫入轉換紀錄 | 轉換只准新增，不准改寫或刪除既有資料／設定 |
| 3 驗證 | 在正式 port 以外的 port 啟動新版；`/api/ping`；各表列數與轉換前相同；關鍵設定逐項相等；資料目錄清單與轉換前相同 | 任一不過 ⇒ 自動進入回滾（⚠ 已更正，見下方主持裁示：交給人決定） |
| 4 回滾 | **完整回滾**：還原程式快照＋DB＋設定檔，雜湊與備份一致，啟動 V9 驗證 ping（⚠ 已更正，見下方主持裁示與 O-1）。**只回程式**：保留轉換後寫入的資料，只換回 V9 程式（依 §6 只新增的原則，V9 讀得了新庫） | 兩種模式都要有自動化測試：轉換 → 回滾 → 雜湊逐一相等、V9 可以啟動 |

- 位置：L0 工具 `tools/platform/upgrade.py`，搭配 `backend/core/upgrade.py`（純函式，方便測試）；它不是業務模組，不會被選配掉。
- 回滾有時效：轉換後已有新資料寫入時，「完整回滾」會失去那些資料 ⇒ 執行前列出轉換後新增的列數，要求明確確認；預設建議「只回程式」。
- 開發機只能拿測試資料演練；**真正對正式機執行是使用者的動作**。
- **主持裁示（2026-09-25，稽核 X-9b S-4；更正上表第 3、4 列）**：
  - 原句保留在表中：第 3 列「任一不過 ⇒ 自動進入回滾」、第 4 列「啟動 V9 驗證 ping」。
  - 更正：以工具現行作法為準，**回滾交給人決定**。理由：回滾不是原子動作；部署儀表板與 UPGRADE-RUNBOOK 都設計成由人逐步確認。
  - 驗證不過 ⇒ 工具 exit 3，並明確提示「建議執行回滾」，附上兩種回滾的完整指令（先只回程式）。
  - 回滾比對通過之後，工具**自動**在非正式 port 啟動 V9 並 ping，印出結果、寫進 `rollback_<mode>.json`；**不自動做任何決定**（ping 不過 ⇒ exit 6，由人看 log 處理）。
- **驗收更正（2026-09-25，稽核 X-9b O-1）**：
  - 原句：第 4 列「雜湊與備份一致」。
  - 更正：完整回滾的 DB 以「**邏輯內容相同**」驗收：sqlite 逐表比對（schema＋各表全部欄位的內容雜湊），對象是**備份時的原檔**，不是剛從備份複製回來的那一份。
  - 原因：Online Backup API 產生的副本，SQLite 標頭的計數欄位（offset 24-27 file change counter、92-95 version-valid-for）與原檔不同，位元組只會等於備份副本，比對副本等於比對它自己。
  - 程式、設定檔仍比位元組雜湊。

## 9c. 模組選配：匯出前選擇、裝好後啟停（2026-09-25 使用者交辦）

目的：模組可以拆開販售，也可以單獨關閉或啟用。

| 層級 | 誰決定 | 效果 | 優先順序 |
|---|---|---|---|
| ① 匯出選配 | 打包時的產品設定檔 `product/<名稱>.json`（列出要包的模組） | 沒選的模組**整個資料夾不進部署包**；部署包自帶 `modules.lock.json`，記錄包了哪些模組、各自的版本 | 最高：不在包裡就不存在 |
| ② 授權 | `helpers/licensing` 的 modules 清單 | 沒授權 ⇒ 載入器不載入，並列出原因 | 次之 |
| ③ 管理者啟停 | superadmin 在系統設定開關（`modules_disabled`） | 關閉 ⇒ 路由不掛、排程不跑、選單不出現；**資料保留**，重新開啟後照舊 | 最低 |

- 關閉或未安裝的模組，其他模組只會少一項功能（對方不在時的行為，§5）。
- **授權的單位是模組**（2026-09-25 主持裁示）：`module.json` 的 `license_key`，預設等於資料夾名；授權清單含 `*` 表示全開。不用權限 key 授權（權限 key 是模組內部更細的粒度）。授權檢查沒有啟用時（開發模式）一律可載入，狀態標「授權檢查未啟用」。
- **管理者啟停在重啟後生效**：路由在啟動時掛上，不做動態卸載。系統設定頁分開顯示「目前狀態」與「重啟後狀態」；頁面不提供重啟按鈕，重啟屬於部署與運維的動作。
- 驗收：
  - 每一種產品設定檔都要能打包、啟動，並通過該組合的測試。
  - 打包守門：`modules.lock.json` 必須等於包內實際的資料夾；缺了任何一個必要的 L0／L1 檔 ⇒ 擋下。
  - 啟停守門：關閉後，端點回 404、選單不出現、資料表列數不變；開回來後恢復。
- 目前只有 M11 在 `modules/` 底下；其餘業務模組搬進 `modules/` 之後自動取得選配能力（ROADMAP 階段 B）。

## 9e. 部署儀表板強化（2026-09-25 使用者交辦：「讓它功能更完善」）

既有的 `backend/tools/deploy_dashboard.py` 已經有打包、推送套用、回滾、查看狀態。新增以下功能，所有對正式機的動作仍然只經 WinRM 腳本、只綁本機：

| # | 功能 | 內容 | 依賴 |
|---|---|---|---|
| D1 | 部署前健康檢查（唯讀） | 經 WinRM 讀正式機最近一次備份的 `.done`、`backup_alerts` 最新告警、磁碟剩餘空間、服務埠；部署與升級按鈕在健康檢查沒過時**預設擋下**（要勾「我已確認」才放行） | 無 |
| D2 | 這一包會改到哪些模組 | 正式機已部署的 commit 對比部署包的 commit：改動檔依 `modules.json` 分組，並列出各模組 CHANGELOG 在這段區間新增的條目 | 無 |
| D3 | 產品選配打包 | 打包時選擇產品設定檔（full／core-only／自訂）；部署包列表顯示各包的 `modules.lock.json` | 9c①（B） |
| D4 | V9 → 新版升級精靈 | 預檢 → 備份（顯示雜湊驗證結果）→ 轉換 → 驗證 → 回滾（完整回滾／只回程式）逐步按鈕 | 9b（C） |
| D5 | 模組狀態 | 正式機各模組：載入、停用、未授權、失敗（附原因） | 9c②③（A） |
| D6 | 測試閘門 | 打包前顯示「這個 commit 的最近一次全量結果」；沒有全綠的全量 ⇒ 預設擋下打包 | modtest 寫出 `.last_full.json`（B）〔更正（B，2026-09-26）：單一檔會被任何 worktree 的全量蓋掉（C 的 ok=False 蓋掉上一輪）⇒ 改為依 commit 分檔 `tools/platform/full_results/<完整 SHA>.json`，閘門讀這個 commit 的那一份；dirty 的全量不寫分檔；`.last_full.json` 保留為「最近一次」只供人看〕 |
| D7 | 修正 | `dev-status` 的領先數比對寫死 `origin/master`，改成目前分支的上游 | 無 |

## 9d. 交叉稽核（2026-09-25 使用者交辦）

- 完成 9b、9c 之後，由各視窗**稽核別人的成果，不稽核自己的**，產出 `docs/platform/audit/AUDIT-<稽核者>-<對象>.md`（發現、證據檔:行號、嚴重度、建議）。
- 主持人彙整成 `docs/platform/IMPROVEMENT-REPORT.md`，給使用者。
- 稽核分配（任何人都不稽核自己的成果；主持人的成果同樣交給別人稽核）：

| 稽核者 | 對象 |
|---|---|
| A | C 的升級轉換與回滾（§9b）、core.paths、個資分流 |
| B | A 的模組啟停與授權（§9c②③）、串接點 IP-1～IP-4、row_access |
| C | B 的匯出選配（§9c①）、modtest／test_map、邊界與準則守門；主持人的部署儀表板（§9e） |

- **L1 行為改變的守門分工**（稽核 G-5，2026-09-25）：G1 只看 L1 的**簽章**、G4 只看 **L2 模組**；L1 函式內容改了而簽章不變，**沒有自動守門**，靠 `backend/core/CHANGELOG.md` 自律＋全量回歸。稽核時要把「L1 行為改變有沒有寫 CHANGELOG」列為檢查項。
- 每份稽核至少包含：①照規格逐條驗收（可以重現的證據）②對「對方不在時」「失敗路徑」「回滾路徑」做反向控制 ③找「綠燈是真的，但證明的是別件事」這一類假綠燈 ④改善建議，分成必修／建議／觀察。

## 10. 環境

**開發機部署步驟（依序）**

1. **先在安裝根目錄放 `.no_email_send`**（或設 `MOTRIX_EMAIL_SEND=off`）——沒有它就會真的寄信（email 預設寄，2026-09-25 裁示）；同時放 `.no_cloud_archive`
2. **新版開發機第一次啟動或第一次打包之前，必須先建開發庫**：設 `MOTRIX_CREATE_NEW_DB=1` 建一次（啟動一次，或只跑 `python -c "import db; db.init_db()"`），建好後移除旗標。主工作樹與每一個要打包的 worktree 都要各自有一份（`backend/motrix_erp.db`，已在 .gitignore）。
   原因：`build_deploy_package.ps1` 的版本紀錄反方向守門（`check_version_sync.py`，VR7）要讀這個庫；**讀不到就判失敗、擋打包**（刻意設計：一道讀不到就放行的守門等於不存在）。新版開發庫全新建立，**不讀、不複製 V9 開發目錄**；D7 最終驗證另用 V9 開發庫的複本，放在獨立演練目錄。（2026-09-25 主持裁示；9c① 演練時發現新版 repo 沒有開發庫）

3. **建專案專用 Python 環境**：`python tools/platform/project_env.py create [--python 3.13]` ⇒ 主工作樹 `.venv`（gitignored），只照 requirements*.txt 安裝；測試與 D7 驗證一律用它（這台機器的 `python` 是別人的 venv，套件版本不等於 requirements）。正式機版本取得後（`backend/tools/prod_env.json`，儀表板健康檢查產生）用 `project_env.py check` 比對，不一致就依正式機重建。〔更正（B，2026-09-26，稽核 D B-M2）：資料夾是 `.venv<主次版號>`（3.12 ⇒ `.venv312`，3.13 ⇒ `.venv313`），不是 `.venv`；**資料夾已存在一律拒絕**（不覆寫共用環境，PLAYBOOK §C-12；原本 `--python 3.13` 會就地裝進 `.venv312`）。check 只提示、不擋（使用者裁示不綁 Python 版本），也不是「不一致就重建」〕

**正式機**：不放 `.no_email_send`、不設 `MOTRIX_EMAIL_SEND` ⇒ 照常寄信（與 V9 現況相同，不需額外步驟）。兩個標記都在 `.gitignore`，`tools/verify_package.py` 的 `dev-marker` 規則擋它們進部署包。

- 開發 port 預設 **667**（避免與 V9 開發機 666 衝突）
- 開發 DB 為測試資料，不上傳任何正式機
- 資料位置唯一來源 `backend/core/paths.py`（錨點＝安裝根目錄，值＝V9 原位置）；產品碼不可自行用 `__file__` 算資料路徑
- 主庫不存在 ⇒ 拒絕啟動（`core.paths.require_db`，只守預設位置）。全新安裝／新開發目錄：設 **`MOTRIX_CREATE_NEW_DB=1`** 啟動一次，建好後移除（旗標仍在時每次啟動記 WARNING）
- 勞報單存檔目錄設定鍵 `payslip_archive_path`（預設 `backend/export_archive`），與 6 個 `*_pdf_base_path` 同規則
- **Python 版本不綁定**（2026-09-25 使用者裁示：「不要綁 python 的版本，避免未來無法更新，以通用性為主」）：
  - 程式與相依套件以**支援範圍**宣告（下限寫在 README／requirements 註解，上限不設），不以某一版為準；不可使用只在特定小版號才有的行為，也不可用 `==` 鎖住 Python。
  - 開發機專案 venv（`.venv312` 等）是「一個受測版本」，不是規格。正式機版本（prod_env.json）只用來**提示**是否在支援範圍內、以及套件差異，**不擋、不要求一致**。
  - 新舊版行為差異（例：FastAPI 0.14x 不再攤平 `app.routes`、starlette 1.x TestClient 需要 httpx2）一律以「新舊都能跑」的寫法處理，不鎖版本繞過。

## 使用者裁示（2026-09-25）

| 項目 | 裁示 |
|---|---|
| 模組分組 | 採 `DEPENDENCY-MAP.md` §2 提案，**拿掉 M09 選型知識庫**，其餘 11 個 L2 模組照提案 |
| 選型知識庫 | 直接遺棄：從新版刪除 7 個選型導覽的 router／種子／頁面／權限 key／測試；表只留在凍結的 V9 migration，不刪既有資料 |
| 共用主檔 | 客戶（`customers`）與料號（`parts`）下沉 L1；業務開發 CRM 仍是 M02 |
| 疑似廢棄表 | `projects`／`project_stages`／`project_logs`／4 張 `*_templates` 不帶進新模組，只留在凍結 migration |
| 試拆模組 | M11 標案雷達（M09 已遺棄） |
| 測試選題 | `tables_named` 一律算入（保守）；L2 拆開後再評估縮小 |
| 分組的機器可讀來源 | `docs/platform/modules.json`（唯一來源；守門測試與 modtest 讀它） |
| 正式機既有資料 | **原地讀取**：資料位置不動，所有路徑經 L1 路徑解析層 `core.paths`（錨點＝安裝根目錄，不是呼叫者 `__file__`）；日後逐項可選搬遷，不做升級時一次搬。依據 `DATA-COMPAT.md` §4 |
| 全新安裝建表 | 基準＝V9 migration v1~v116 原封建出全部 V9 表（含未安裝模組、已遺棄選型的空表）；「模組未安裝不建表」只適用 v116 之後各模組自有的新表 |
| V9 維護期 schema | V9 新增的 migration（v117+）必須**同號同內容**追進新版；新版遇到比基準新而不認得的版本 ⇒ 拒絕升級並說明原因（不是 WARNING） |
| 模組版本表 | 名稱 `module_schema_versions`（避免與 V9 的 `schema_version` 只差一個 s） |
| 正式機判定 | 禁止以安裝路徑（`\V9.0\`）判定正式機；改明確旗標（DATA-COMPAT §0-3） |
| 通知信預設 | **預設寄信**；開發機以安裝根目錄 `.no_email_send` 或 `MOTRIX_EMAIL_SEND=off` 擋信（標記永不進部署包） |
| 案件可見範圍 | 全站搜尋、儀表板動態牆**對齊報價列表**（被指派者、cashier 可見）；規則集中於 L1 `helpers/row_access.py`，各模組登錄；未登錄的 kind 一律 fail closed |
| 測試範圍期望 | 改 L2 模組 ⇒ 只跑該模組＋契約；改 L1（main／system／db／共用 helper）⇒ 接近全量是結構性的（寫的是幾乎每支 router 都讀的表），接受全量 |
| 勞報單存檔上雲 | **進雲端，但放獨立、權限更窄的資料夾**（與一般單據 PDF 鏡像分開；含個資）。DATA-COMPAT §7b |
| 每日備份的個資 | 新版：含個資的表（contractors 影像三欄、payslips 的身分證字號／地址／電話／Email）**排除於一般每日 JSON**，完整份另存「系統存檔_個資\每日備份」（人建資料夾、程式不自動建）；還原時合回 |
| V9 個資外洩（K2） | **V9 不修，等換版**；雲端既有的歷史每日備份**保留不動**（2026-09-25 使用者裁示；依更正後的描述再次確認，結論不變） |
| 網站內自訂（核心方向） | 見 `CUSTOMIZATION-SPEC.md`：自訂模組**文件式**存資料、內建模組開放**外觀與輸出＋自訂欄位**（核心欄位與計算不動）、邏輯**積木式**（不能寫程式）、排版**公司預設＋角色覆寫**（個人只調欄位顯示）；底層現在就要留好串接點（ROADMAP 階段 P）（2026-09-25 使用者裁示） |
| 獎金分潤：通知 | 送審時通知簽核人（含代理人）、核准進入待發放時通知出納；名單上的成員不通知（金額屬敏感資訊）；可在「通知設定」個別關閉（2026-09-25 使用者裁示） |
| 獎金分潤：送交出納 | 待發放的獎金進**出納頁**：待付款清單、執行紀錄、Excel 匯出；在出納頁「標記已發放」與獎金頁的按鈕是同一個動作 |
| 獎金分潤：財務報表 | 以**發放日**記為支出，進**營運報表與月支出**；獎金傳票帶案件來源，**案件頁的相關傳票看得到**。T100 匯出這次不動（會計傳票是否進 T100 另立題目） |
| 整庫 .db 雲端副本 | 新版：每日與月備份的整庫 `.db` **改放「系統存檔_個資\每日備份／月備份」**；一般資料夾只放去掉個資的 JSON。個資資料夾未建立 ⇒ 雲端沒有整庫備份，並發出告警（不退回放一般資料夾） |
| 獎金分潤：扣繳與二代健保（U4） | **MOTRIX 自動計算**：撥付時算扣繳（5%，起扣門檻依參數表）與補充保費（超過投保金額 4 倍的部分）；新增員工投保金額與全年累計欄位；門檻與費率走法規參數版本（R1）（2026-09-25 使用者表單裁示） |
| 個資保存期限（U5） | 到期**去識別化**（不刪）；預設**離職或結案後 7 年**，各類可在設定頁調整；月備份內的歷史資料同規則處理方式另寫進規格（2026-09-25 使用者表單裁示） |
| 稽核紀錄保存（U9） | 由 730 天改為 **5 年**（2026-09-25 使用者表單裁示） |
| 電子發票（U6） | **不做**：發票在外部開立，MOTRIX 不串接也不做匯入（2026-09-25 使用者表單裁示） |
| 行動簽核通知（U7） | 先做**行動版頁面＋Email**；通知管道做成 provider，LINE 等之後再接（2026-09-25 使用者表單裁示） |
| 標案雷達授權（U8） | 自用；對外產品設定檔預設不含，販售前向工程會確認授權；README 標示（2026-09-25 使用者表單裁示） |
| 換版演練資料（U3） | **只用開發機資料**（D7）；不另做正式機複本演練（2026-09-25 使用者表單裁示） |
| V9 修補（U1、U2） | V9 部署儀表板 S-P01 **不修**；V9 備份缺陷**只修 S-CC07（時鐘跳動清光快照）與 S-CD02（損毀庫照常備份）**，其餘等換版；打包部署由使用者（2026-09-25 使用者表單裁示） |
| Python 版本（U10 延伸） | **不綁定版本**，以支援範圍＋通用寫法為主；正式機版本只作提示，不擋、不要求一致（見 §10）（2026-09-25 使用者裁示） |
| 承攬商本身的銀行帳戶（稽核 X-9b O-9） | **列為個資（F2）**：比照薪資與外包人員帳戶，只進「系統存檔_個資」，不進一般備份（2026-09-25 使用者表單裁示） |
| V9 收款帳戶查詢端點（稽核 X-IP Y-1） | V9 **不修，等換版**；新版修（2026-09-26 使用者表單裁示） |
| V9 備份修補包（S-CC07、S-CD02，a1cc2871） | **先不出包**，隨換版上線（2026-09-26 使用者表單裁示） |
| 個資蒐集告知的範圍 | 客戶聯絡人等其他蒐集個資的表單**這一輪做**，在 D7 前完成，沿用 R3 機制（2026-09-26 使用者表單裁示） |
| 階段 C 設計（STAGE-C-DESIGN §7） | 主持裁示，照推薦：D1 對外 URL 維持 `/pages/x.html`；D2 停用或未授權模組的頁面回 404（選單本來就不顯示）；〔**更正**（2026-09-26，B 發現與已合回的 P-FE-03 牴觸，原句保留）：D2 改成「**HTTP 404＋伺服器產生的提示頁**」：提示頁沿用 P-FE-03 的四種文案（停用、未授權、載入失敗、未安裝）與 `data-testid=module-unavailable`，狀態取自 registry；HTTP 層與端點一致，使用者看到的是提示，不會以為網址打錯；未授權的使用者也拿不到頁面原始碼〕D3 本階段不加頁面的伺服器端權限（權限在 API 層）；D4 模組不可以自訂選單群組（固定鍵）〔**補充**：頁面撞名時不讓整台起不來（可販售產品，比照〈不可以用安裝路徑猜正式機〉），比照 P-LD-07 路由衝突的處理：**撞名的那個模組標為 failed、不掛上**，並記 ERROR，模組管理頁與儀表板 D5 都看得到原因；其他模組照常運作〕（2026-09-26） |
| 案件子資料的權限範圍（稽核 X-IP Z-5） | **維持現狀並寫進規格**：逐案權限（row_access）只管案件本身（`case`／`dev_case`）；派工單、傳票等案件底下的子資料只受**模組權限**管（有該模組權限即可看全部案件的子資料，採購、會計、出納依職務需要）。範圍寫進 MODULE-GUIDE，之後改動要再問使用者（2026-09-26 使用者表單裁示） |
| 模組化的目的：快速驗證（使用者 2026-09-26） | 使用者原話：「我選擇模組化也是為了快速驗證，例如共用端點驗證不需要全域，可以加快驗證速度」。⇒ **驗收標準**：①共用層（L1）的改動只驗「它自己的題＋它的契約題＋直接使用它的單位的題」，不沿著 main 擴散成全域；②模組的題只載入「L1＋該模組」；③共用層與模組之間靠**契約題**守（介面、回傳形狀、缺席時的行為），不是靠把全部模組一起跑；④每次選題的比例都記錄下來並回報，常用 L1 helper 的改動目標在 30% 以下；⑤全量只保留在發版前與每批合回之後當安全網。這一項是 D1～D3 的一部分：模組搬了，驗證範圍卻沒有縮小，就不算完成 |
| 自訂模組草稿的權限（U14） | **只有建立者與超級管理員**可以修改、送出草稿；同權限的其他人只能看。送出之後照簽核流程走（2026-09-26 使用者表單裁示） |
| 勞報單的法規年度（U13） | **依給付日**套用法規版本（扣繳義務在給付時發生）；單據要有給付日，沒填時先以開單日暫算並提示；「元以下捨去」的法源另請會計確認（2026-09-26 使用者表單裁示）〔2026-09-26 12:54 使用者表單：**會計已確認照現行**（依給付日判年度、捨去到元、1000 萬上限的計算基礎）⇒ 結案〕 |
| 系統技術類信件退訂（U15） | 2026-09-26 12:54 使用者表單：超級管理員**可以退訂，但系統擋住「最後一位超管退訂」**，永遠至少一人收得到。實作歸主持（mail_settings），附反向控制 |
| 子代理推共用文件（U16） | 2026-09-26 12:54 使用者表單：**維持現狀**——子代理只推自己的 wip 分支，共用文件（RUN-PLAN 等）由主持在自己的回合寫；不調整權限設定。舊個資代理的 clone 由使用者手動刪 |
| 角色版面合併方式（U17） | 2026-09-26 12:54 使用者表單：**整份覆寫**（現行做法：角色版面以公司預設為起點複製一份，之後公司改了不影響已有的角色版面）|
| 測試負載上限（使用者離開期間） | 2026-09-26 13:00 使用者：「放寬，我人不在，可以讓電腦全速」⇒ **使用者離開期間**：全機測試名額 `MOTRIX_PYTEST_SLOTS=4`（原 2）、各線閘門 `-n 4`（原 1～2）、不必低優先權；全量維持 modtest 的 `-n 4`（上限寫在程式裡）。暫存資料夾照舊用完即刪。**使用者回來說恢復即回到 PLAYBOOK §C-13 原值**〔補充 2026-09-26 13:47 主持：全速的限制是**記憶體**不是 CPU——B 的 C4 e2e（-n 4）被 Claude Code 以系統記憶體過低停掉（每個 e2e worker 開一個 headless 瀏覽器＋測試伺服器）⇒ 非 e2e 照舊 -n 4；**e2e 一律 -n 2、全機同時最多 2 組 e2e；可用記憶體低於 4 GB 不起新的測試**〕 |
| 客戶／供應商主檔的個資告知（PN-M1） | 2026-09-26 13:47 使用者表單：**視為公司**——主檔的 email、phone、address、deliveryAddress、invoiceAddress 逐欄列 not_natural_person（客戶／供應商主檔＝往來的公司；聯絡人另有逐人告知）；日後出現個人客戶再改「主檔也要告知」|
| V9 已知問題（N-1、U11、U11 補充、U12） | **全部等換版**，V9 不再修改；N-1 的本機分支 fix/v9-clock-gap 保留不推（2026-09-26 使用者表單裁示） |
| **V9 政策（常設）** | 使用者原話：「v9 的錯誤一律等到新模組版本處理，舊版本太浪費時間；檢測轉移都依據新版本測試，讓轉移流程優先」。⇒ ①不再檢查 V9 有沒有同樣的缺陷（不 grep V9 找同類問題、不開 V9 的 U 題）；②V9 只在「轉移需要讀它」時才看：升級工具的相容性、資料位置、D7 演練；③所有驗證以新版為準；④優先序：**轉移流程（D1 模組搬遷、D7 升級演練與它需要的修正）優先於其他新功能**（2026-09-26 使用者裁示） |
| 啟動時的寫入（D 稽核 K-O2） | 主持裁示：**服務啟動時對資料庫的寫入，只能經 `helpers/startup.py`**，每一個寫入的設定鍵都要在 `core.upgrade` 分類（RUNTIME_STATE_SETTINGS 或會被 verify 比對的設定）；守門已經存在（startup.py 寫入的鍵都要有分類），範圍擴大到「startup.py 以外不可以在啟動路徑寫 DB」，由 C 補守門（2026-09-26） |
| 側欄改名（P1P3 P-O2） | 主持裁示：側欄項目**改名**只開放在「公司預設」與「角色覆寫」兩層；個人層只能調顯示與排序（隱藏、顯示、群組內換位置）。v1 不准換群組（2026-09-26） |
| 案件存取規則的歸屬（階段 B，C 提議） | 主持裁示：`guard_case_access`、`case_access_allowed`、`is_document_approver` 與 `CASE_ACCESS` 的 row_access 登記，**下沉到 L1 `helpers/case_access.py`**（符合 DEPENDENCY-MAP §3 #2「案件可見性規則 → L1 權限」、Z-5 裁示；也符合「共用能力下沉到資料層」的原則）。`helpers.quotations` 保留同名匯入，呼叫端不用改。**已知例外**：L1 讀取 M01 的 `quotations` 表，因為「案件」是權限的根，M01 不在時一律回「找不到案件」（404），與現行行為相同；這個例外寫進 DEPENDENCY-MAP 並加守門，只有 case_access 可以讀它。由 C 做，A 的 M01 搬遷以此為前提（2026-09-26） |
| 個資告知的單位（D 稽核 PN-M1） | 主持裁示：告知以**欄位與當事人**為單位決定，不以「頁」為單位；同一頁有多種當事人，就各自決定。**出貨單收件人與送貨地址可以手動輸入 ⇒ 要告知**，用途同聯絡人那一份。pii_forms.json 改為逐欄列出，守門逐欄檢查（2026-09-26） |
| 時鐘跳動判定範圍（稽核確認 N-1） | 主持裁示：S-CC07 的判定要涵蓋「往前跳但小於保留天數」。**最新快照的日期距今天超過 2 天（中間有缺口）⇒ 當輪不清理、寫 `.prune_hold` 並告警**，與 STATES 原文一致。真的停機造成的缺口，由人確認後刪掉標記就恢復（fail-safe：寧可多告警，不可以靜默刪掉真實快照）。新版由 C 修；V9 master（a1cc2871 同樣的缺陷）在 U2 已核准的範圍內一併修，不出包（2026-09-26） |
| 信件與通知的收件人、用語（使用者 2026-09-26） | 使用者原話：「這種信件語法太不正式，未來增加獨立區域，所有的信件都要能指定誰能收到，普通管理員不需要收到這類信」。①**獨立設定頁「信件與通知收件設定」**（僅超級管理員）：每一種信件類型都能指定收件人（指定帳號、角色，或「僅超級管理員」）；②**每一種信件都必須登記類型**（key、名稱、分類：業務／簽核／系統技術、預設收件人），沒登記的寄送路徑守門要擋；③**系統技術類**（標案雷達抓取或解析失敗、備份告警、健康檢查等）**預設只寄超級管理員**，一般管理員不收；④個人的通知設定只能「退訂」自己收得到的類型，不能把自己加進受限的類型；⑤**用語正式化**：主旨一律【MOTRIX 系統通知】分類－事由；內文依「事由、影響、建議處理、發送時間與來源」的固定格式，不用口語；用語規範寫進 MODULE-GUIDE，並加禁用詞守門（例如「多半」「不會自己好」「看起來」）。新版做；V9 不動 |

§6 更正：原文「V9 既有 `db.py` 的 v1~v84」→ 實際基準為 **v116**（`db.py:133`，DATA-COMPAT §3）。

## 已知問題

| # | 項目 | 現象 | 位置 | 狀態 |
|---|---|---|---|---|
| K1 | 舊資料以顯示名稱比對擁有者 | `quotations.sales_person_id` 為 NULL 的舊資料改比 `sales_person`＝使用者 `display_name`。`display_name` 是 NOT NULL 但可為空字串 ⇒ **顯示名稱為空的帳號看得到所有「業務欄也為空」的舊案件**（清單與單筆一致，不是兩份實作的差異） | L1 `helpers/row_access.py`（`legacy_name_col`）；規則宣告 `helpers/quotations.py::CASE_ACCESS` | 2026-09-25 裁示：這次不動，另開題（回填 `sales_person_id` 或禁止空顯示名稱） |
| K2 | V9 正式機一般雲端備份含個資與祕密 | ~~承攬人員身分證正反面與存摺影像（base64）、勞報單的身分證字號／地址／電話，每天寫進一般權限的雲端「每日備份」~~ **更正（C，2026-09-25）**：每日 JSON 的影像早已被 `_strip_inline_images` 換掉（`archive.py`≈1517，有守門）。真正的曝險是**整庫 `.db`** 每日與每月複製到一般雲端資料夾（`archive.py:1047-1050`、`:1953`），內含全部影像、個資與 F3 祕密（totp_secret 等），而且當時是知情的設計。仍然成立的是：payslips 的身分證字號、地址、電話、Email 以明文寫進每日 JSON | V9 `archive.py:1047-1050`、`:1953`（整庫副本）；`:1617`（payslips JSON） | **使用者接受的風險**（2026-09-25）：V9 不修、等換版；雲端歷史備份保留不動。新版依 MODULE-GUIDE §3.2 修正（C） |
| K3 | FastAPI 0.14x 的 `include_router` 不再攤平路由 | `app.routes` 只剩 `/docs` 等幾條，被 include 的路由放在 `_IncludedRouter`（沒有 `.path`）。**產品碼沒有走訪 `app.routes`**（已 grep），只有 3 支測試用它找端點 ⇒ 在照 requirements 安裝的環境判成「端點不存在」。hermes 環境的 fastapi 0.133 仍攤平，所以一直沒發現 | 測試共用 `backend/tests/_routes.py`（`effective_route_contexts()` 攤平，新舊版都適用）；`test_licensing_core`、`test_module_history` | 2026-09-25 已修（B）；不鎖版本。之後新寫「列端點」的程式一律用它 |
| K4 | playwright 瀏覽器跟著套件版本走 | 照 requirements 裝到較新的 playwright（1.63）時，舊的 chromium build 不能用 ⇒ e2e 全部啟動失敗（363 紅） | `tools/platform/project_env.py create` | 2026-09-25 已處理（B）：create 會跑 `playwright install chromium` |
| K5 | `test_e2e_hard_cap` 單程序題在全量偶發紅 | 3.12 全量（43ad469b，-n 6 滿載）紅：堆疊停在 setup，找不到「卡在第 7 行」。**.venv312 單獨跑 2 次綠**，堆疊正確指到第 7 行。成因：逐題上限從 setup 起算，滿載時 setup（共用伺服器／瀏覽器）就超過 2 秒 | `backend/conftest.py`（`_e2e_hard_cap`）；`tests/test_e2e_hard_cap_2026_09_25.py` | ✅ 2026-09-25 已修（B，9edccb0d）：setup／call／teardown 各自計時，dump 與摘要標明「X 階段逾時」；不採拉長上限。新增題：慢 setup＋卡住本體 ⇒ 兩段都報、本體那一行被抓到。重現：負載 80–88% 下 -n 4 兩輪各 16 passed（-n 6 依 §C-13 不跑，主持同意） |
| K6 | `test_login_as_writes_the_same_session_shape_as_the_login_page` 在全量偶發紅 | 同一輪 e2e（-n 4）紅：`Execution context was destroyed`——注入登入狀態後 `goto(login.html)`，頁面偵測已登入自動導向，`evaluate` 撞上導向。**.venv312 單獨連跑 3 次綠**。產品行為正確，是測試等待點不對 | `tests/test_e2e_shared_fixtures_2026_09_25.py:48` | 2026-09-25 修正中（B）：等導向結束再讀 session（〈e2e 等待的終點〉）〔更正：實際沒有採用這個做法，改成在 `/api/ping` 注入，完全不經過登入頁，所以沒有導向可以等〕 ✅ 已修（9edccb0d），重現條件同 K5，兩輪綠 |
