# 稽核：B 的 O9-2——傳票開單序號（產品競態）（wip/b-o9-2 ab182226）（D，2026-09-26 20:52）

> 完整稽核（產品碼 `frontend/js/voucher.js`）。稽核者 D 沒有寫過任何受稽核的程式碼。
> 內容：
> - `open()` 加 `_openSeq`：成功與失敗兩條路徑都丟掉過期的回應；完成時寫 `<body data-voucher-open-seq>`。
> - 按「新增」時 `_openSeq++`，作廢還在路上的開單。
> - jv7 的 `_opened` 改等本次點擊的序號。
> - 新題 `test_o92_late_deep_link_response_does_not_replace_the_clicked_voucher`。

## 0. 結論

- **必修 0、建議 2**。

## 1. 實測

| 項目 | 結果 |
|---|---|
| 基準：`test_e2e_voucher_summary` 整檔（`-n 2`）×3 | 3/3 全過 |
| 突變 Q1：拿掉 `open()` 兩處序號檢查，×3 | **3/3 紅**（新題） |
| 突變 Q2：「新增」不再 `_openSeq++` | **存活** ⇒ O92-S2 |
| 其他呼叫 `open()` 的路徑（主持重點②） | 深連結（:144）、儲存後重開（:1107）、動作後重開（:1144、:1175）、撤回後開新單（:1216）都走 `open(id, true)`，每次都取得最新序號；只有在它之後又有 `open` 或「新增」時才會被丟，而那正是使用者已經離開那一張的情況，屬正確語意。等待條件用 `>= n`，被丟的那一趟不寫屬性，不會讓等待卡住 | 沒有會被誤丟的路徑 |

## 2. 發現

### 建議

**O92-S1　新題的交錯仍然靠時間窗（主持重點①）**
- 深連結 A 的 `.json()` 固定延後 1.5 秒；題目要在這 1.5 秒內完成 `_ready`（科目、摘要來源都載完）並點 B。
- 目前負載下突變 3/3 紅。但如果 `_ready`＋點擊超過 1.5 秒，A 會在點 B 之前就套用完，這時拿掉序號也不會紅：是**假綠**的方向，不會偶發紅。
- 建議比照 O10：A 的回應改成由題目放行（`window.__releaseA()`），點 B、等 B 完成之後再放行，完全不靠時間。

**O92-S2　「按新增要作廢在途的開單」沒有題**：突變 Q2 存活。建議補一題：深連結 A 還在路上時按「新增」，A 回來之後畫面仍是空白新單。

## 3. 複核：wip/b-o9-3 17c6adfe（O92-S1／S2；D 20:58）

- 基準：`test_e2e_voucher_summary` 4 過。
- **O92-S1**：深連結那一趟的 `.json()` 改成扣住，直到題目呼叫 `window.__releaseFirst()`；先等 `__releaseFirst` 存在（確認已扣住），點 B、等 B 完成，再放行並等 `__staleRead`。
  - D 的加壓驗證：產品拿掉兩處序號檢查，**同時**題目在點 B 之前多等 3 秒 ⇒ 新題**仍紅**。順序完全由題目控制，沒有時間窗。
  - ⇒ **O92-S1 關閉**。
- **O92-S2**：新題 `test_o92_new_voucher_cancels_the_open_in_flight`。突變 Q2b「新增不 `_openSeq++`」⇒ 紅（原本存活的 Q2 現在有題守）。
  - ⇒ **O92-S2 關閉**。
