# 行尾規則（2026-09-21 新增）
#
# 為什麼需要這個檔：本 repo `core.autocrlf=true` 而先前沒有 .gitattributes，
# 於是 git 認為 checkout 該產出 CRLF，而磁碟上的 .py/.html 實際是 LF。
# 後果：`git checkout -- <某支 .py>` 會把它拉回「git 認為的樣子」＝CRLF，
# `git diff` 看不出來（會正規化），但**位元組變了、雜湊變了**。
# 最容易中的是突變測試（改檔 → 跑 → checkout 還原）與任何靠檔案雜湊的比對。
#
# ⚠️ 加這個檔是零改寫的：物件庫裡存的本來就全部是 LF
# （`autocrlf=true` 在存入時就正規化了），所以這些規則只是把
# 「checkout 該產出什麼」講清楚，產出的位元組與今天完全相同。
#
# ⚠️ 不要改成 core.autocrlf=false —— 那會讓 .bat checkout 成 LF，
# 而本專案有過 .bat 用 LF 導致排程靜默失效、外觀上仍顯示執行成功的前例。

*               text=auto

# 程式碼與文件：LF（與磁碟現況一致）
*.py            text eol=lf
*.html          text eol=lf
*.js            text eol=lf
*.css           text eol=lf
*.md            text eol=lf
*.json          text eol=lf
*.yml           text eol=lf
*.yaml          text eol=lf
*.sql           text eol=lf

# Windows 腳本：CRLF（cmd.exe 與 PowerShell 5.1 需要；.ps1 另需 UTF-8 BOM）
*.bat           text eol=crlf
*.cmd           text eol=crlf
*.ps1           text eol=crlf
*.vbs           text eol=crlf

# 🔴 測試 fixture：**原始位元組，一個都不准動**（2026-09-21，視窗 B 抓到）
#
# fixture 的用途是「保存對方網站當時真正送來的樣子」，日後改版時拿新舊兩份比對。
# 若讓上面的 `*.html text eol=lf` 把它正規化，存進去的就不是現實而是現實的正規化版
# ——而那正好打掉了它存在的理由。
# 實測：磁碟 90,207 bytes／CRLF 1,585 處，正規化後物件庫只剩 88,622 bytes／CRLF 0。
# ⚠️ 加這行之後還要 `git add --renormalize <該檔>` 重新存一次，
#    規則本身不會把已經正規化的 blob 還原。
backend/tests/fixtures/**    -text
backend/modules/*/tests/fixtures/**    -text

# 二進位：不要碰
*.png           binary
*.jpg           binary
*.jpeg          binary
*.gif           binary
*.ico           binary
*.pdf           binary
*.xlsx          binary
*.docx          binary
*.db            binary
*.7z            binary
*.zip           binary
*.pem           binary
*.key           binary
*.woff          binary
*.woff2         binary
*.ttf           binary

# ── 第三方 vendored 發布檔：**不要做任何換行轉換** ────────────────────────
#
# 這些檔案附有 `PROVENANCE.md`，裡面記著下載當下的 SHA-256。
# 🔴 若讓 git 正規化換行，**checkout 出來的位元組就不是廠商發布的那一份**，
#    而那份雜湊會驗不起來 ⇒ **憑證本身變成誤導**（它看起來還在，只是對不上）。
# 🔑 憑證的用途正是「將來有人問這個檔有沒有被動過」，
#    所以保存的必須是**原始位元組**，不是「我們這邊看起來比較整齊的版本」。
frontend/static/vendor/leaflet-1.9.4/** -text
frontend/static/vendor/leaflet.markercluster-1.5.3/** -text

# ══════════════════════════════════════════════════════════════════
# PK1（2026-09-23）：出貨包（git archive）export-ignore
#
# 決策依據見 docs/windows/SCOPE.md「PK1 排除清單」節。判準（D 訂）：
#   明確排除：內容是「我們怎麼做這個系統」（規劃／協作紀錄／audit／測試名稱）
#   明確保留：客戶自架或維運真的用得到的操作文件／腳本／執行期依賴
# ⚠️ 每一項排除前都已個別驗證「執行期沒有人讀它」——.md 只在別的檔案的
#    docstring／註解裡被引用當作規格出處，沒有一處 open()/read_text()/Path()。
# 🔑 這份清單只會變長：新增項目直接加行，**不要在別處（程式碼、註解、測試）
#    把這份清單的項目數字寫死** —— 它是今天的資料，不是機制的性質
#    （backend/tools/verify_package.py 的交集檢查是直接讀這個檔，不是抄清單）。
# ══════════════════════════════════════════════════════════════════

# 整個多視窗協作目錄：派工、狀態、稽核、交付紀錄、施工規格、輔助腳本，
# 全部是「我們怎麼做」而不是客戶自架會用到的東西（含子目錄 tools/）
docs/windows/** export-ignore
docs/windows export-ignore

# backend/tests/：195 個測試檔名逐一描述已知缺陷；requirements-dev.txt 排除後
# pytest/playwright 本來就不在包裡，測試在客戶機器上本來就跑不動（不損失任何
# 東西，已查證 verify_package.py 與 DEPLOY.md 都不依賴這兩者）
backend/tests/** export-ignore
backend/tests export-ignore
# 2026-09-25：conftest 自 backend/tests/ 上移到 backend/（讓 modules/*/tests 共用 fixture）；
# 模組測試跟著模組放在 backend/modules/<key>/tests/（CORE-SPEC §3）——同屬測試，一併排除
backend/conftest.py export-ignore
backend/modules/*/tests/** export-ignore
backend/modules/*/tests export-ignore
# 模組規格條件（自 docs/windows/STATE.md 分出，性質同 docs/windows：我們怎麼做這個系統）
backend/modules/*/SPEC.md export-ignore
backend/requirements-dev.txt export-ignore
backend/db_migration_plan.md export-ignore

# .claude/：本機開發工具設定，與客戶端無關
.claude/hookify.shell-quoting-python-dq.local.md export-ignore

# docs/ 下的內部規劃文件與 UI mockup（不是客戶操作文件；DEPLOY.md／DR-SOP.md／
# HTTPS-DEPLOY-CHECKLIST.md 是客戶自架文件，明確保留，不在這裡）
docs/MOTRIX_ERP_System_Plan.md export-ignore
docs/UI-BACKLOG.md export-ignore
docs/UI-REDESIGN-PLAN.md export-ignore
docs/module-viz-mockup.html export-ignore
docs/system-home-mockup.html export-ignore
# docs/quick/：MOTRIX-ERP-QUICK.md 於 2026-09-23 依模組拆出的章節，與 QUICK.md 同屬內部文件
docs/quick/** export-ignore
docs/quick export-ignore
docs/PLATFORM-CUSTOMIZATION-INVENTORY.md export-ignore
docs/PLATFORM-FOUNDATION.md export-ignore

# 根目錄：內部協作／規劃文件
/AGENT-HANDOFF-TEMPLATE.md export-ignore
/AUTOLOGON-FIX.md export-ignore
/GITFLOW.md export-ignore
/MODULE-AUDIT-2026-09-13.md export-ignore
/MOTRIX-ERP-ARCHITECTURE-MAP.md export-ignore
/MOTRIX-ERP-QUICK.md export-ignore
/MULTI-BRANCH-AUTO-UPDATE-DESIGN.md export-ignore
/MULTIWIN-PROTOCOL.md export-ignore
/NETWORK-PLAN-MODULE-DESIGN.md export-ignore
/NEXT-SESSION.md export-ignore
/SELLABLE-AND-MOBILE-SPEC.md export-ignore
/WEEKLY-AUDIT-2026-09-07_2026-09-10.md export-ignore
/LETSENCRYPT-PUBLIC-CERT-PLAN.md export-ignore

# 三件裁決（SCOPE.md「三件裁決」節）
/CHANGELOG.md export-ignore
/PASSKEY-CA-ROLLOUT.md export-ignore

# docs/reference/：開發期的外部原始資料，不是客戶會用到的東西 ——
# 🔴 商業會計項目表.pdf／.sha256 只是 backend/tools/parse_account_items.py
#    的一次性輸入，真正的執行期依賴是已經產生好的 backend/data/account_items_112.json
#    （不受這條規則影響，照常出貨）
# 🔴🔴 傳票-實例-20260330-006.pdf 是**一張真實的公司會計傳票**（D 已確認），
#    只在 helpers/voucher.py／voucher_pdf.py 的註解裡被當成單號格式的設計依據
#    引用，程式碼沒有讀取這個檔案 —— 這是本次 PK1 裡最嚴重的一項，優先排除
docs/reference/** export-ignore
docs/reference export-ignore

# 內部諮詢草稿，不是客戶文件
docs/ASK-ACCOUNTANT.md export-ignore

# backend/tools/ 逐檔分類（SCOPE.md「backend/tools/ 逐檔分類」節，D 判、17 個
# 排除／6 個保留／4 個需人裁決／1 個未歸類，17+6+4+1=28=git ls-files backend/tools/
# 的總數，兩兩互斥且已核對過）——這裡只落地明確的「甲・排除」17 個：
# （2026-09-25：其中 check_guide_sync.py、local_research_pipeline.py 隨選型遺棄刪除，現為 15 個）
# 開發/打包/稽核工具，客戶自架不需要
backend/tools/_dashboard_remote.ps1 export-ignore
backend/tools/audit_account_permissions.py export-ignore
backend/tools/backfill_location_identity_snapshot.py export-ignore
backend/tools/build_deploy_package.ps1 export-ignore
# 2026-09-25 新增（不在上面 17 個的原始分類裡）：build_deploy_package.ps1 的清理規則，同屬打包工具
backend/tools/_package_prune.ps1 export-ignore
backend/tools/_e2e_gate.ps1 export-ignore
backend/tools/check_approval_queue_coverage.py export-ignore
backend/tools/check_double_init.py export-ignore
backend/tools/check_endpoint_entrypoints.py export-ignore
backend/tools/check_prod_drift.ps1 export-ignore
backend/tools/check_version_sync.py export-ignore
backend/tools/code_health.py export-ignore
backend/tools/deploy_dashboard.html export-ignore
backend/tools/deploy_dashboard.py export-ignore
backend/tools/deploy_dashboard_ctl.pyw export-ignore
backend/tools/parse_account_items.py export-ignore
backend/tools/sync_pending_data_20260817.py export-ignore

# ⚠️ 尚未排除（刻意）：
# · letsencrypt_renew.ps1 —— 保留但要改內容（$Domain 預設值 erp.miactw.com），
#   歸 WL7（見 SPEC-WL7.md §8②b），本輪不動這個檔案
# · issue_license.py／fetch_root_ca.ps1／check_dependencies.py／
#   list_payment_anomalies.py —— 需人裁決（4），D 明說判斷不出商業意圖
# · verify_package.py 本身 —— 未歸類，SCOPE.md 書面紀錄仍是「未歸類」
#   （即使有訊息提過要歸類，權威紀錄沒有落地前不當作已裁決）
# · backend/create_claude_account.py（不在 backend/tools/ 下）—— 仍待歸屬
