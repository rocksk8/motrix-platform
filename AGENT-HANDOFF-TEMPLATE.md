# 交接紀錄範本（AGENT-HANDOFF）

> 用法見 `MOTRIX-ERP-QUICK.md` §14.3d。
> 收工時複製這份、填完，存成：
> - 正式機：`C:\Users\Motrix\Desktop\AGENT-HANDOFF\YYYYMMDD_HHMM_prod.md`
> - 開發機：`<專案上一層>\AGENT-HANDOFF\YYYYMMDD_HHMM_dev.md`
>
> 路徑刻意在部署覆蓋範圍之外，才不會被 `apply_update.ps1` Step 3 蓋掉。
> **七個欄位一個都不能省**——少一項，下一棒就得自己去翻，等於沒交接。

---

## 基本資訊

- **機器**：正式機（Motrix）／開發機（hichan）  ← 擇一
- **時間區間**：2026-__-__ __:__ ~ __:__
- **主題**：（一句話，例如「WebAuthn RP ID 設定與 Passkey 實測」）
- **狀態**：✅ 已完成／🔶 未完成，待接手

---

## 1. 改了哪些檔案

> 完整路徑，一行一個。沒有改任何檔案就寫「無」——**不要留白**，留白讀起來像忘了填。

```
（例）C:\Users\Motrix\Desktop\V9.0\backend\routers\auth.py
```

## 2. 每個檔案改了什麼、為什麼

> 下一棒要靠這段決定「該合併」還是「該丟棄」。寫「小修」等於沒寫。

## 3. 有沒有動資料庫

- [ ] 沒有
- [ ] 有改 schema（**這最危險**：如果 migration 沒有同步寫進 `backend/db.py`，
      兩台機器的 schema 會分岔，§0 的 v32/v33 就是這樣搞丟的）
- [ ] 有改資料列（哪張表、哪些列、為什麼）

## 4. 有沒有改設定（`system_settings`）

> **key 與實際值都要寫**。這些不在 git 裡，換機或重建環境時會整個消失，
> 只有寫在這裡才留得下來。

| key | 設成什麼 | 為什麼 |
|-----|---------|--------|
|     |         |        |

## 5. 有沒有重啟服務／改排程工作

- [ ] 沒有
- [ ] 重啟過服務（幾點、為什麼）
- [ ] 改過 Windows 工作排程器（哪個工作）

## 6. 有沒有進 git

- [ ] 有，commit：`________`（已 push／未 push）
- [ ] **沒有** ← 若勾這項，請在第 1 項清單裡明確標出哪些檔案還沒進 git。
      **沒進 git 的東西，下次 `apply_update.ps1` 一跑就會被覆蓋掉。**

## 7. 未完成、待接手的事

> 半成品最容易被下一棒當成完成品。包含：試過但沒成功的方向、
> 刻意沒做的取捨、下一步建議。

---

## 接手方的固定動作（不要只信這份交接檔）

1. 讀完這份
2. 跑 `backend\tools\check_prod_drift.ps1` —— 這份寫的是「對方以為自己改了什麼」，
   drift 檢查看的是「實際上什麼不一樣」。**兩者對不上的部分才是真正的風險**
3. 有價值、還沒進 git 的改動，依 §14.2 拉回開發機合併進 git
4. 更新 `MOTRIX-ERP-QUICK.md` §0 落差表，再開始自己的工作
