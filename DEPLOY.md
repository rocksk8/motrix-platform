# 上線前要做的決定（2026-09-22 這一版）

這一版新增了**標案雷達**、**地圖與距離**、**辦公室地址**三件事。
它們有兩個開關**預設是關的**，而關著的時候畫面看起來是正常的、只是沒有資料。

> 🔑 **整份文件只回答一件事：哪些東西不打開就不會動，而不動的時候看起來像什麼。**

---

# 🔴🔴 推上去之後，一定要多做這一個動作

> ## 工作排程器 → 「MOTRIX ERP Server Autostart」→ **結束 → 執行**
> （或直接**重新開機**，效果一樣）

**不做這一步的話，這次更新的兩個開關完全不會生效。**

為什麼：啟動服務的 `autostart.bat` 是一個**正在執行中的迴圈**。
更新包會把新檔案覆蓋上去，**而正在跑的那個迴圈仍然在用舊的內容**。
⚠️ **只重啟 uvicorn 沒有用**——更新程式停掉的是 uvicorn，
而重新把它拉起來的，正是那個**還在用舊內容**的迴圈。

☠️ **而沒做這一步的樣子是：推送成功、服務正常、畫面正常，就是雷達不掃、地圖上沒有點。**
🔑 那跟「今天沒有新標案」「地點還沒抓到」**看起來一模一樣**，
所以**不會有人發現**，只會覺得這個功能好像沒什麼用。

---

---

# ⚠️ 這一版上線之後，**卡著的簽核單會各收到一封提醒**（一次性，然後就安靜了）

這一版把「簽核逾期提醒」從**第 5 個工作日之後每天一封**
改成 **第 1／3／5／10／15／20…個工作日各一封**。

☠️ **舊的行為比想像中吵**：去重鍵裡帶了日期，所以第 5 天之後**每天都是新的一把鑰匙**
⇒ 一筆卡了 20 個工作日的單子，會寄出 **18 封**。

🔑 **而換新規則的代價是一次性的**：新舊的去重鍵格式不同
（舊：`1d`／`3d`／`5d.2026-09-22`；新：`stage.d1`／`stage.d3`／`stage.d5`／`stage.d10`…）
⇒ **系統看不到舊的紀錄，所以每一筆目前還卡在簽核柱列的單子，
會再收到一封**（只有一封，是它目前該在的那一階），**之後就照新規則安靜下來。**

| | |
|---|---|
| 會發生什麼 | 升級當天的第一次排程，每一筆待簽核的單子各寄一封 |
| 會持續多久 | **只有那一次** |
| 要做什麼 | **不用做任何事。** 知道它會發生就好 |
| 舊的設定列 | 留在資料庫裡不動（無害，也不會再被讀） |

📌 開發機實測：待簽核 **2 筆**、`approval_notif.*` 舊設定列 **51 列**。
⚠️ **正式機的筆數沒有查過** —— 若目前卡著很多單子，那一天的信會比平常多。

## 一、兩個開關（**這是最重要的一節**）

|  | 不設定會怎樣 | 設定了會怎樣 |
|---|---|---|
| **標案雷達** | 不會去抓標案。畫面上有頁面、有搜尋條件、**就是不會有新標案進來** | 每天在你設定的時段，連到政府電子採購網抓公告 |
| **地圖／距離** | 地址定位不到 ⇒ **地圖上沒有點、距離全部是空白** | 把地址送到 OpenStreetMap 換成座標 |

### 🔴 打開它們＝這台機器會主動連到外面的網站

這是刻意設計的：**預設關著，是為了讓一台裝好的機器不會在沒有人知道的情況下對外連線。**
要打開，在啟動服務時加上環境變數：

```
MOTRIX_TENDER_RADAR=1     打開標案雷達
MOTRIX_GEO=1              打開地址定位
```

### ✅ 這台正式機已經幫你設好了

那兩行已經寫進 **`backend/autostart.bat`**（更新包會帶過去）：
```
set MOTRIX_TENDER_RADAR=1
set MOTRIX_GEO=1
```
⚠️ **那是「這台機器的設定」，不是產品的預設值** ——
程式碼裡的出貨預設仍然是**關**，所以未來賣給客戶時，
**他們裝起來一樣不會自己對外連線**，除非他們自己打開。

### ⚠️ 將來要給別人安裝時，這個檔要換掉

`backend/autostart.bat` 裡面有**這台機器的路徑**與**這兩個開關**。
它跟著更新包走，所以**直接拿去給別人裝，會帶著我們的設定過去**。

🔑 「裝好之後不會自己對外連線」這個承諾，在**程式碼**那一層是守住的
（原始碼裡兩個開關都是關的）——**而這個檔在它外面。**

### 🔴 但這一次更新之後，要多做一個動作

`autostart.bat` 本身是一個**正在執行中的迴圈**。更新包會把新的檔案覆蓋上去，
**而正在跑的那個迴圈仍然在用舊的內容** ⇒ **這兩個開關不會生效。**

⇒ **更新完成後，請把排程工作重跑一次**（或直接重新開機）：
```
工作排程器 → 找到「MOTRIX ERP Server Autostart」→ 結束 → 執行
```
📌 **只重啟 uvicorn 沒有用** —— 要重啟的是外面那個 `.bat` 迴圈。
🔑 而沒做這一步的症狀是：**雷達安靜地不掃、地圖上沒有點**，
而那跟「今天沒有新標案」「還沒抓到地點」**看起來一模一樣**。
📌 打開之後，服務啟動時會印出一行寫著 `MOTRIX_TENDER_RADAR=1` 的訊息
（**在你啟動服務的那個視窗上**；如果你有把輸出導到檔案，就在那個檔裡）。
**只有打開時才會印，關著時不印**——那一行的用途是：
**哪天有人問「這台機器有沒有在對外連線」，看得出來。**

### 怎麼確認它真的開了

登入之後打開**標案雷達**頁：
- 上方沒有「⚠️ 總開關目前是關閉的」那一行 ⇒ 雷達開了
- 打開**地圖**頁：沒有「⚠️ 地理查詢未啟用」那一行 ⇒ 定位開了

---

## 二、抓取與寄信的時段（升級後第一次要看一眼）

升級之後，這兩個設定在資料庫裡**還不存在**，系統會先用內建的預設值：

```
抓取時段   每天 9、12、15、18 點          （一天四次）
寄信時段   每天 18 點                      （一天一封彙總信）
```

⚠️ **那不是「你設定過的值」，是「還沒有人設定過」。**
兩者在畫面上看起來一樣，所以建議**升級後到標案雷達頁按一次「儲存」**，
把你要的值真的存進去——之後就不會再受內建預設值變動影響。

**可以怎麼改**：
- 兩份設定是分開的：可以一天抓四次、只在傍晚寄一封
- **寄信時段留空 ＝ 完全不寄信**（標案照樣抓、照樣出現在畫面上）
- 設超過 12 個時段會先跳一個確認，**那不是上限**，按確認就會存進去

---

## 三、要指派給誰的新權限

在**使用者管理**裡，「業務」群組下多了兩個：

| 權限 | 給誰 | 不給會怎樣 |
|---|---|---|
| **標案雷達** | 要看標案、設搜尋條件的人 | 側邊欄看不到那一頁 |
| **地圖** | 任何想看地圖的人 | 側邊欄看不到地圖 |

⚠️ 這兩個權限管的是**側邊欄看不看得到**。
地圖的資料本身**只要求登入**——也就是說，會自己打網址的人仍然拿得到。
**那是刻意的**（地圖是共用能力），只是別把它當成一道安全牆。

📌 **地圖與標案雷達是分開的兩個權限**：
只給地圖、不給雷達的人**打得開地圖**，只是地圖上**不會有標案**——
而且畫面會明白寫出「沒有標案雷達模組權限，地圖上不會顯示標案」，
不會讓他以為地圖壞了。

---

## 四、辦公室地址（不填的話距離不會動）

**公司資料設定 → 基本資料 → 辦公室地址**

不填的話地圖與標案清單上的「距離」全部是空白，
而畫面會寫「尚未設定辦公室地址」——**不會安靜地什麼都不顯示**。

⚠️ 距離是**直線距離**，不是行車距離。地點目前只到**縣市**層級
（政府採購網只提供到那裡），所以距離是「縣市中心到辦公室」的概念。

---

## 五、地圖底圖：⚠️ OpenStreetMap 目前擋住瀏覽器發出的圖磚請求

地圖頁**預設只顯示清單**（機關／標案／地點／距離），**完全不連外網**。
按下「開啟地圖（需要網路）」才會去載底圖。

🔴 **實測結果：按下去會看到一張滿版寫著「Access blocked」的地圖。**
OSM 擋的方式很特別——**不是回錯誤，是回一張畫著封鎖字樣的圖**，
所以瀏覽器不會顯示任何錯誤，畫面看起來像是「地圖正常運作」。

⚠️ **那不是地圖壞了，是對方擋住我們。**
清單與距離**不受影響**，照常可用。

📌 **它擋的依據是「瀏覽器的身分」，不是這台機器、也不是你的網路** ——
所以**換網路不會好**。這是 OSM 對用量的限制，見他們的
[圖磚使用政策](https://operations.osmfoundation.org/policies/tiles/)。

<!-- 🔴 TODO（2026-09-22，B）：下面這一句目前**不成立**，要等 UA 探測修好才會成立。
     現況：探測用的是 MOTRIX 自己的 User-Agent，而 OSM 不擋那個 UA
     ⇒ 探測回「可以用」，於是那條紅字**不會出現**。
     C 正在寫紅燈（探測的 UA 必須是瀏覽器形狀）。
     ⚠️ 修好之後把下面「目前還沒有」那一段刪掉，換成「會出現」。
     這一行留在這裡而不是只記在腦袋裡——主持人的記憶是負債。 -->
⚠️ **目前畫面上還沒有事先提醒**：你按下去才會看到那張封鎖圖。
（我們正在修：讓系統在你按之前就先告訴你。）

---

## 六、資料庫會自動升級

這一版的資料庫版本是 **89**，正式機目前是 **84** ⇒ **啟動時會自動跑 5 個升級步驟**。

- ✅ 五個步驟**只新增欄位與新增資料表，不修改也不刪除任何既有資料**
- ✅ **五步全部**已用**今天的正式機備份複本**實際跑過一次：
  升級前 `version=84`／77 張表／**9,250 列**，升級後 `version=89`／83 張表／**9,250 列**
  ⇒ **一列都沒少，過程零例外**
- ✅ 既有資料的新欄位會是「空白」而不是「0」——
  那是刻意的：**「沒有這個資料」與「這個值是零」是兩件事**

📌 升級是自動的，你不需要做任何事。但**升級前請照平常的習慣留一份備份**。

---

## 七、⚠️ 一件已知的事：備份告警信以前沒有寄出去過

這一版修掉了一個既有的缺陷：**「備份太久沒跑」的告警信，過去從來沒有真的寄出去過。**

- 症狀：伺服器記錄上寫著「備份新鮮度告警」，**而信沒有出去**
- 觸發條件：**備份曾經成功過、後來停了**（從來沒備份過的情況反而會正常寄）
- 現在修好了 ⇒ **如果你的備份其實已經停了一段時間，升級後可能會收到一封告警信**

🔑 **那封信不是新問題，是一個一直存在、只是你從來沒被通知過的問題。**

---

## 八、⚠️ 地圖第一次打開只會看到十幾個點 —— **那是正常的**

**這一節講的是「正確的狀態」，不是故障。**

### 為什麼

地址要變成地圖上的點，得先問 OpenStreetMap「這個地址在哪裡」。
🔴 **對方每秒只接受一次查詢**（那是他們的規定，我們照做）。

而這台機器上要查的地址是：

```
標案的機關名稱   158 個
供應商            23 個
客戶              10 個
其餘（外包／協力廠商／出貨）10 個
──────────────────────
相異地址合計     199 個  ⇒ 一次全部查完要 3 分 19 秒
```

☠️ 如果一次請求就把它們查完，**瀏覽器會先逾時**，
而你看到的會是「地圖資料載入失敗」——**一張完全空的地圖**。

### 所以它是這樣運作的

- 每次打開地圖，**只花幾秒去查還沒查過的地址**，其餘的顯示
  「還有 N 個地址這次來不及定位」，旁邊有一顆 **「繼續定位」** 按鈕
- 🔑 **查到的結果永久存在資料庫裡** ⇒ 那個數字**只會往下掉**，
  按幾次（或平常多開幾次）之後就會全部出現，**之後永遠是瞬間的**

### 你要做的

**按「繼續定位」，或是就這樣用。** 兩種都可以，不會有任何損失。

⚠️ **不要因為點很少就以為壞了。** 判斷的依據是那句「還有 N 個…」：
- 有那句話 ⇒ **正常，只是還沒查完**
- 沒有那句話而點還是很少 ⇒ 那才要看上面第一節（開關）與第四節（辦公室地址）

### 📌 已知限制（這一版沒做）

**排程抓取標案時不會順便把地址查起來。**
⇒ 所以「暖機」完全靠有人打開地圖。
如果希望一次弄完，可以在沒人使用的時候連續按十幾次「繼續定位」。

---

## 這一版不會自動做的事

- ❌ 不會自己打開任何對外連線（要設環境變數）
- ❌ 不會自己指派新權限（要在使用者管理裡勾）
- ❌ 不會自己填辦公室地址
- ❌ 打包與部署仍然是你自己的動作
- ❌ 不會在背景把地址查起來（見第八節）
