# Secrets & local credentials
backend/.initial_admin_credentials.txt
backend/.initial_demo_credentials.txt
**/.env
**/.env.*

# License：簽發私鑰與客戶金鑰一律不進 git（2026-09-21）
# 私鑰外洩 = 所有已發出的授權全部形同虛設，而且撤不回來——離線驗證沒有撤銷清單，
# 每一把已經裝在客戶機器上的金鑰都會繼續有效到它自己的到期日為止。
# license.key 本身是 per-installation（同 heartbeat_config.json 的性質），
# 由簽發流程產生後放進客戶機器，不是程式碼的一部分。
backend/license.key
backend/tools/_license_private_key*.pem
**/*_private_key*.pem

# Database & local backups
backend/motrix_erp.db
backend/motrix_erp.db-*
backend/motrix_erp_demo.db
backend/motrix_erp_demo.db-*
backend/motrix.db
backend/db_backups/
backup_alerts/
backend/logs/
backend/server.log
server.log
motrix_erp.db

# Deployment-specific config (per-machine, not app code)
backend/heartbeat_config.json
backend/tools/.guide_sync_config.json
backend/certs/

# Python
__pycache__/
*.py[cod]
.venv/
.venv*/
venv/

# Uploads / exports (optional local data)
uploads/
報價單PDF/
出貨單PDF/
開票申請憑據PDF/
承攬商匯款申請PDF/
請款單PDF/
結案報表PDF/
exports/
backend/export_archive/

# Runtime / error logs
backend/start_err.txt

# Commit-message drafts（2026-09-10：backend/.commit_msg_webauthn.txt 曾隨
# commit 0527524 誤入 repo。訊息本身已經在 git 歷史裡，草稿檔留著只是雜訊）
**/.commit_msg*.txt

# Update-mode local artifacts (per-machine, not app code — see §15)
deploy_packages/
backend/rollback_snapshots/
backend/.deployed_commit.json
backend/tools/deploy_dashboard_history.json
backend/tools/upgrade_session.json
backend/tools/upgrade_session.json.tmp
backend/tools/upgrade_session.corrupt-*.json
backend/tools/prod_env.json
backend/tools/deploy_logs/
backend/tools/deploy_dashboard_run.log

# Root-level debug/temp files (generated during development)
/*.txt
/*.py
/_*.js

# OS / editor
Thumbs.db
Desktop.ini
.DS_Store
*.bak
*.bak2
*.recovered

# 這台機器不上傳雲端存檔的標記（2026-09-15）。**必須是 per-machine 的本機檔案**：
# 開發機放、正式機不放。打包走 git archive（只含已追蹤內容），所以它永遠不會被
# 帶到正式機——這正是選用「gitignore 過的檔案」而不是設定值的原因。
# 見 archive.py::cloud_archive_enabled()
.no_cloud_archive
.no_email_send

# ── demo 帳號的隔離存檔目錄（2026-09-22，RG4）───────────────────────────────
# `db.py` 的 `DEMO_*_ARCHIVE_DIR` 那一組常數，跑測試與登入 demo 帳號時會建。
#
# ⚠️ 理由要寫對，寫錯的話下一個人會以為不必管：
#   ❌「跑測試不會寫工作樹」          —— **它會寫**，十個路徑。
#   ✅「它寫的東西被 ignore 或剛好是空目錄，所以 `git status` 看不到」
#
# ☠️ 而「剛好是空目錄」不是防線：git 不追蹤空目錄而已。任何一次測試在裡面
#    留下一個檔（中斷、例外、清理沒跑到）⇒ `git status` 立刻變髒 ⇒ **打包被擋**，
#    而下一個人會去追「為什麼突然冒出七個未追蹤目錄」。
#
# 用萬用字元不逐條列七行：那組常數還會長（2026-09-22 當天就新增過），
# 而逐條的版本只擋得住今天這七個。
backend/_demo_*

# ── 自簽憑證（2026-09-22，RG6 的 repo 這一側）──────────────────────────────
# `_CERT_PATH` 指向 `certs/`。現在那個目錄不存在、git 也沒追蹤任何 certs 檔
# ⇒ **這不是外洩，是一個開著的門**：哪天有人在這台機器上產生自簽憑證，
#    它會直接出現在 `git status` 裡，而下一步就是被 commit 進去。
# ⚠️ 擋整個目錄不是只擋 `cert.pem` —— **私鑰會叫別的名字**。
certs/

# 2026-09-23 (BR1): written by build_deploy_package.ps1 into the shipped
# package so the running process can report which commit it loaded.
# On a dev machine git is the source of truth, so this file must not be
# committed -- a stale copy would answer for git when git is unavailable.
backend/.build_commit

# modtest --full 的結果（本機狀態，部署儀表板讀它；CORE-SPEC §9e D6）
tools/platform/.last_full.json
tools/platform/.last_full.json.*.tmp
tools/platform/full_results/
