# 選型資料庫 — 總索引

> 允碩整合集創是系統整合商，「場域選型導覽」模組會逐步擴充成涵蓋多產品線的選型知識庫。
> 這份文件是**跨類別的總目錄＋需求提交格式**；個別類別的欄位細節、品牌清單、待處理清單，
> 各自有獨立的內容手冊（見下方「類別清單」）。

---

## §1 · 分類邏輯

選型資料庫用兩個維度描述一筆情境／建議：

1. **產品類別**（大分類，決定用哪一套資料表／欄位邏輯）
   例：無人載具部署場域、商用／工業網路交換器、監控系統、門禁系統…

2. **行業別／應用範疇**（同一產品類別底下，依客戶所屬行業細分情境）
   例：科技廠、營造、流通業…

不是每個產品類別都要同時套用兩個維度——無人載具目前純用「部署場域」分（溫度/IP/認證門檻），
網路交換器未來可能會需要「行業別」情境（科技廠機房 vs 營造工地 vs 零售店面的安裝條件不同）。
每新增一個產品類別時，先確認這個類別實際需要哪些維度，不要預先套死格式。

**已驗證的教訓（網路架構類別上線後）**：類別之間的「內容形狀」可能差很多——無人載具是
「情境×分層×三級」，網路架構是「技術族系×世代演進×產品」，兩者硬塞進同一張表反而彆扭。
做法是**每個類別可以有自己專屬的資料表**（但欄位設計盡量涵蓋：核心規格說明、相對前一項的
進步/差異、典型情境或標籤、建議售價區間、依賴/相關備註、注意事項——這幾類欄位目前看來是
跨類別共通的），**共用的是外層殼**（MOTRIX topbar/sidebar、後台 CRUD 慣例、Excel 匯出入權限、
淺色系統配色）。不要為了「共用一張表」而犧牲每個類別本來該有的瀏覽邏輯。

---

## §2 · 目前類別清單

| 產品類別 | 狀態 | 內容手冊 | 資料表 |
|---------|------|---------|--------|
| 無人自動化載具部署場域 | ✅ 已上線 | [ENV-GUIDE-CONTENT.md](./ENV-GUIDE-CONTENT.md) | `env_guide_environments` / `env_guide_recommendations` / `env_guide_links` |
| 網路架構（Wi-Fi／行動網路 4G-5G） | ✅ 已上線 | [NETARCH-GUIDE-CONTENT.md](./NETARCH-GUIDE-CONTENT.md) | `netarch_families` / `netarch_generations` / `netarch_products` |
| 商用／工業網路交換器 | ✅ 已上線 | [SWITCH-GUIDE-CONTENT.md](./SWITCH-GUIDE-CONTENT.md) | `switch_scenarios` / `switch_categories` / `switch_fit` / `switch_products` |
| 監控系統 | ✅ 已上線 | [MONITOR-GUIDE-CONTENT.md](./MONITOR-GUIDE-CONTENT.md) | `monitor_scenarios` / `monitor_categories` / `monitor_fit` / `monitor_products` |
| 門禁系統 | ✅ 已上線 | [ACCESS-GUIDE-CONTENT.md](./ACCESS-GUIDE-CONTENT.md) | `access_scenarios` / `access_categories` / `access_fit` / `access_products` |
| 路由器／閘道器與硬體控制器 | ✅ 已上線 | [GATEWAY-GUIDE-CONTENT.md](./GATEWAY-GUIDE-CONTENT.md) | `gateway_scenarios` / `gateway_categories` / `gateway_fit` / `gateway_products` |
| 自動化系統（倉儲/產線：AGV／AMR／機械手臂） | ✅ 已上線 | [AUTOMATION-GUIDE-CONTENT.md](./AUTOMATION-GUIDE-CONTENT.md) | `automation_scenarios` / `automation_categories` / `automation_fit` / `automation_products` |

> 新類別上線後，記得回來這裡加一行，並在下面「§5 變更記錄」留一筆。

---

## §3 · 提需求的格式

你要新增類別、或幫既有類別找新內容時，用這個格式（貼在這份文件的「§4 待處理需求」，或直接聊天視窗貼給我都可以）：

```
類別：[產品類別名稱，若資料庫裡還沒有這個類別，我會先設計資料結構再開始填內容]
行業別／情境（選填）：[例如：科技廠機房／營造工地／零售店面]
需求說明：[你想要的東西——可以是一句話方向，也可以是具體型錄/網站連結]
優先度／備註（選填）：
```

**範例**（照你剛剛的例子）：

```
類別：商用／工業網路交換器
行業別／情境：（先不分，之後再細分）
需求說明：幫我搜尋商務或工業級的網路交換系統，建立對應的規格、說明、使用場域
優先度／備註：新類別，第一批內容
```

### 我收到需求後的處理方式

- **類別已存在** → 直接研究、判斷該放哪個情境／哪一級，寫進資料庫（透過管理後台或 API，不改 seed 檔），回報做了什麼異動。
- **類別不存在** → 先跟你確認這個類別需要哪些欄位／維度（不會直接套用無人載具那套「場域×分層×三級」模板，除非你說要沿用），設計好資料表結構、跑 migration、建立對應的內容手冊，再開始填內容。**新類別的資料表設計，等真的有第一批具體需求時才動工**——用真實案例來設計欄位，比憑空預想欄位更準，避免做到一半要翻表。

### §3.1 · 品牌深度研究委派流程（2026-08-09 新增，節省主線程 token）

當需求是「幫某幾個品牌把型號補齊／查證」這種**單純資料蒐集**（不需要跟你來回確認欄位設計）時，
改用委派子代理（subagent）研究、只把最終結構化結果帶回主線程寫入資料庫，避免大量 WebSearch
原始結果塞滿對話：

1. **先查現況**：直接查資料庫該品牌目前有哪些筆資料（含哪些是「XX 等系列」這類模糊佔位、
   `price_note` 空白等需要修正的既有缺口），整理成「現有清單」附進委派內容，避免子代理重複
   建議已存在的型號
1.5. **（選用）本機 Ollama 腦力激盪候選品牌／方向**：這台機器有 Ollama（`localhost:11434`，
   有 qwen3.6/deepseek-r1 等模型可用，RTX 5070 Ti 跑推論），可以先用它免費/免 token 發想「這個
   分類還可以考慮哪些品牌」這類開放式問題。**但僅止於發想方向，不可直接採信它給的具體型號／
   規格／價格**——Ollama 沒有連網能力，這類細節極容易編造（已有前例：`SWITCH-GUIDE-CONTENT.md`
   2026-07-30h 條目記錄過「本輪 Ollama 建議的 4 個品牌全數查無實據，捨棄未採用」）。它給的品牌
   候選名單一樣要走下一步的 WebSearch subagent 查證，查無實據就捨棄，不會因為是 Ollama 建議的
   就放寬查證標準。
2. **研究方式擇一（依使用者指示，2026-08-09 起預設用本機管道，省雲端 token）**：
   - **(a) 本機管道腳本（`backend/tools/local_research_pipeline.py`，優先採用）**：
     `python local_research_pipeline.py --brand "品牌" --urls URL1 URL2 --out result.json`。
     原理是用 `requests`+`BeautifulSoup` 直接抓官網真實網頁純文字，再交給本機 Ollama
     （預設 `qwen3.6`，`localhost:11434`）從**這段真實文字**抽取結構化資料，不是叫模型憑訓練
     資料記憶回答，避免下方 1.5 提過的編造風險。實測品質良好（見
     `MONITOR-GUIDE-CONTENT.md` 2026-08-09 條目附近的 Spark 測試案例，抽出的規格比雲端
     subagent 還完整）。**已知踩過的坑**：`qwen3.6` 是推理模型，Ollama API 預設會把內容塞進
     獨立的 `thinking` 欄位而非 `response`，一定要在請求 body 加 `"think": false`，且程式
     兩個欄位都要檢查（`response` 優先，空的話 fallback `thinking`），否則會誤判成「抽不到
     任何資料」。**限制**：只能抓純 HTML 就能看到內容的頁面，抓不到需要 JavaScript 才會渲染
     出產品列表的網站（工具本身在文字量過少時會印警告，遇到警告改用 (b)）；抽出的都是「候選
     資料」，主線程仍要看過再寫入，不能無腦直接寫資料庫。**只能在開發機（hichan，有獨立顯卡）
     跑，正式機是 H255 低功耗 CPU、無獨立顯卡，不具備跑本地 LLM 的硬體條件，不要在正式機嘗試
     執行這支腳本或安裝 Ollama**
   - **(b) 委派 subagent（Agent tool，`subagent_type: general-purpose`，可多個並行、背景
     執行）**：適合 (a) 抓不到內容（JS 渲染頁面）、或需要同時查證多個網址交叉比對的情境。
     prompt 內容固定包含：
     - 目標品牌、所屬選型資料庫類別／資料表結構（世代/分類代碼有哪些）
     - 附上「現有清單」，明確要求不要重複、且要修正已知的模糊/缺值項目
     - **強制要求**：每一筆都必須是查證存在的真實型號（用 WebSearch），不可用「XX 等系列」這種
       籠統寫法帶過；查無實據寧可少列，不要編造；**也不可因為「規格看起來跟已列的重疊」就自行
       篩選跳過**——官網真實列出的都要收錄，這是 2026-08-09 Omada 官網完整品項掃描那次的教訓
       （前一輪因為篩選漏了 17 款，事後被要求重做）
     - **回傳格式固定**：一個表格，每列 `[分類代碼/世代id, 品牌, 型號, url, price_note（含查價
       日期）, specs（[[規格名,值],...] 陣列）]`，字數壓到最精簡，不要附原始搜尋過程/多餘敘述
3. 主線程收到資料後，直接呼叫對應的 CRUD API 寫入（不需要重新做研究），修正/新增動作一次到位；
   單一品牌型號數量多時（20+ 筆），改寫一支批次 Python 腳本用 `requests` 迴圈呼叫 API，比一筆
   一筆下 curl 有效率也更不容易出錯
4. 寫完後更新對應內容手冊（如 `NETARCH-GUIDE-CONTENT.md`）§5 變更記錄；若目標類別在其他機器
   （如正式機）已有資料，記得依 §5 下方「內容跟程式碼是兩條軌道」的教訓，另外準備一次性同步
   腳本（資料量大時用程式從資料庫直接匯出產生同步腳本內容，不要手動謄寫，避免抄錯/漏筆）

---

## §4 · 待處理需求

> 目前空白，之後照 §3 格式累積在這裡。

---

## §5 · 變更記錄

> 新類別上線、資料表改版等大事記，新的寫在上面。

### 2026-08-10 — 新增「路由器／閘道器與硬體控制器」類別（第六個類別，DB v41）
- 詳見 [GATEWAY-GUIDE-CONTENT.md](./GATEWAY-GUIDE-CONTENT.md) §5，同日條目
- 同一輪也把交換器選型導覽的 `switch_categories` 從 4 個跨品牌通用分類擴充為 11 個（新增 7 個
  `OMADA_` 前綴的 Omada 官方系列專屬分類，75 款產品），詳見 SWITCH-GUIDE-CONTENT.md §5
  「2026-08-10」條目——這兩次異動都建立了「品牌專屬分類」的先例，之後新增品牌前請先讀過
  這兩份文件的 §1 說明再決定要塞進通用分類還是新建專屬分類

### 2026-08-09f — 監控系統新增 4 品牌（84 款）＋建立本機研究管道
- 使用者指名新增 VIGI／Spark（附官網）／AXIS／i-PRO 四個品牌，並要求比照 Omada 教訓（不篩選、
  官網真實列出的都收錄），監控系統選型導覽從 2 品牌 10 筆擴充到 **6 品牌 94 筆**，詳見
  `MONITOR-GUIDE-CONTENT.md` §5「2026-08-09c」
- 使用者反映「派發雲端 subagent 研究太浪費使用度」，要求之後優先用本機 AI（Ollama，
  RTX 5070 Ti）處理這類研究——但本機 Ollama 沒有連網能力，設計成「腳本抓真實網頁＋本機模型只
  負責從真實文字抽結構化資料」的管道，新增 `backend/tools/local_research_pipeline.py`，已用
  Spark 官網實測驗證品質良好；§3.1 流程已更新為優先用這支腳本，抓不到內容（JS 渲染頁面）才
  退回委派 subagent
- 準備了 `backend/sync_2026-08-09e_monitor_brands.py`（84 筆，用程式從資料庫匯出產生，非手動
  謄寫）供正式機套用更新後執行

### 2026-08-09e — Omada 官網完整品項總表掃描＋新增涵蓋度總覽頁面
- 使用者反映「即使做完深度擴充，Omada 官網還是有很多品項沒收錄」——原因是上一輪 agent 主動篩選
  跳過了「規格看起來重疊」的型號，這次改為「不篩選，逐一造訪官網分類頁面，力求 1:1 對應」，
  補齊 17 款（Wi-Fi 6 補 8、Wi-Fi 7 補 9），Omada 品牌現有 34 筆，詳見
  `NETARCH-GUIDE-CONTENT.md` §5「2026-08-09e」
- 同時新增 **`frontend/pages/selection-db-overview.html`**（涵蓋度總覽頁面）：彙總 4 個
  選型資料庫類別（網路架構／交換器／監控系統／門禁系統）在各世代/分類底下的品牌數與產品數，
  紅/黃/綠三色標示完全空白／偏薄弱／足夠，一眼看出哪裡還需要補內容，不用再靠人工口頭反映才
  發現缺口；**不新增後端 API 或 DB**，純前端複用既有 5 個類別各自的 GET 端點彙總而成；
  sidebar 新增「涵蓋度總覽」入口，admin+ 可見（無獨立模組旗標）
- 準備了 `backend/sync_2026-08-09c_omada_full_sweep.py` 供正式機套用更新後執行

### 2026-08-09d — 全品牌深度擴充（§3.1 流程首次大規模驗證）
- 使用者反映「只補 1-2 款代表款」不夠完整（例如 Omada Wi-Fi 6 實際產品線遠超過當時的數量），
  要求每個項目/產品線都要盡量完整；依 §3.1 流程對剩餘薄弱品牌逐一委派 subagent 深度查證：
  - **網路架構選型導覽**：`netarch_products` 從約 30 筆擴充到 60 筆，涵蓋 Omada（Wi-Fi 6 世代
    深度重研究，達 12 款，含吸頂/牆插/戶外三型態）、Ruckus、Teltonika、Askey、Inseego、ZTE、
    Cisco／Cisco Meraki（注意是兩個不同 brand 欄位值）
  - **監控系統選型導覽**：新增 Hikvision 品牌（5 個相機分類各 1 款），從單一品牌變成可跨品牌比較
  - **門禁系統選型導覽**：新增 Akuvox 品牌（3 個元件分類各 1 款，`GATE_CONTROLLER` 分類查證後
    確認 Akuvox 無對應產品，據實記錄不硬湊）
- 部分查證結果為「查無實據，不新增」（Peplink／Teltonika／Cisco 皆無 mmWave 機種），符合查證
  標準不硬湊數量
- 這批內容比照既有做法，準備了 `backend/sync_2026-08-09b_brand_depth.py` 供正式機套用更新後
  執行，同步新增/修正的資料（已用全新空白 db 驗證正向寫入與冪等重跑）

### 2026-08-09 — 新增 UniFi：兩個新類別上線＋既有類別補品牌
- 使用者提出「新增 UniFi」需求，範圍涵蓋四個類別：
  - **監控系統**（第四個類別，DB v39）與**門禁系統**（第五個類別，DB v40）：兩個全新類別皆
    沿用交換器選型導覽的「情境 × 分類矩陣」四表結構，第一批資料皆為 UniFi（Protect／Access），
    詳見各自的 CONTENT.md
  - **交換器選型導覽**：`MANAGED_L3` 分類補上 UniFi Pro 24 PoE／Enterprise 48 PoE，補齊
    `SWITCH-GUIDE-CONTENT.md` §4 記錄已久的品牌缺口
  - **網路架構選型導覽**：校正文件記載的「Wi-Fi 6E 尚無產品」過時缺口記錄（資料庫其實已有
    U6-Enterprise 掛在 6E 世代下，只是文件沒同步更新）
- 順便補上本索引本來缺漏的「商用／工業網路交換器」類別列（原本 §2 表格沒有這一行，屬於既有
  落差，這次一併補齊）
- 排程自動化機制（每週檢查產品異動＋新廠商觸發同步）本次**未建置**，本次仍是手動走 §3 流程；
  自動化留待後續另外討論設計

### 2026-07-30b — 網路架構類別上線（第二個類別）
- DB v31：新增 `netarch_families` / `netarch_generations` / `netarch_products`（族系→世代→產品，非場域×分層×三級）
- 第一批資料：Wi-Fi（Wi-Fi 6/6E/7）、行動網路（4G LTE/5G Sub-6/5G mmWave），驗證品牌 UniFi／Omada／Peplink／Netgear
- 新增欄位：建議售價區間、依賴/相關備註（文字+標籤，不做正式關聯表）
- 前端 `frontend/pages/netarch-guide.html`：改用「先選族系方塊→世代橫向對照卡片」的簡化瀏覽邏輯（不是無人載具那套矩陣/篩選），從一開始就用系統淺色配色
- 側邊欄／users.html 新增 `netarch_guide`／`netarch_guide_edit` 模組旗標

### 2026-07-30 — 建立總索引
- 確認未來分類邏輯：產品類別 × 行業別（兩維度，非強制套用）
- 已知規劃中類別：商用／工業網路交換器、監控系統、門禁系統
- 現有「無人自動化載具部署場域」類別維持獨立內容手冊 `ENV-GUIDE-CONTENT.md`
