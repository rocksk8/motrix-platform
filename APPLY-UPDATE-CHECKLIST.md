# 正式機套用更新步驟清單（apply_update.ps1）

> 對應這次部署包：`deploy_packages\20260828_224413_932ccb8`（commit `932ccb8`，
> 含財務顧問/視覺化管理/資訊安全/企業管理四面向優化（`87da4d0`）+ 金額同步稽核
> 三項修正/營運報表邏輯修正（`20b9387`）+ 簽核佇列代理人可見度/勞報單編輯鎖
> （`932ccb8`），pytest 248/248）。
> 通用流程，之後每次套用新的部署包都可以照這份清單走，套用前對照一下部署包
> 資料夾名稱裡的 commit 值跟目前這份是否一致，不一致代表又有新的一批要套用。

## 前置確認

- [ ] 確認目前在**正式機**（`Motrix` 帳號，路徑必須是 `C:\Users\Motrix\Desktop\V9.0`——腳本會自動檢查這個路徑，不對會直接中止，不需要自己額外確認）
- [ ] 確認正式機能執行 `python`（腳本內部的 db 快照／migration 乾跑驗證會呼叫 `python`）
- [ ] 部署包資料夾（`20260828_212438_87da4d0`）已整個複製到正式機（隨身碟／網路芳鄰／雲端硬碟皆可，路徑不限，等一下當參數傳入即可）

## 步驟

1. **執行套用指令**
   ```powershell
   powershell -ExecutionPolicy Bypass -File "C:\Users\Motrix\Desktop\V9.0\backend\tools\apply_update.ps1" -PackagePath "<複製過去的部署包資料夾路徑>"
   ```

2. **腳本會自動做完以下事情，過程中留意有沒有紅字 [FAIL]**（若中止，正式庫保證完全未被觸碰，可放心重跑）：
   - [1/5] 版本比對（如果偵測到跟目前已套用的版本相同，會直接中止，避免重複套用——這種情況不需要加 `-Force`，代表這次真的不用套）
   - db 快照（SQLite Online Backup API，存到 `backend\db_backups\pre_update_<時間戳>\`）
   - **Migration 乾跑驗證**：新版 `db.py` 只在 db 快照的副本上先跑一次確認沒問題，正式庫這階段完全不會被碰——這裡失敗一樣會直接中止
   - 程式碼回滾快照（存到 `backend\rollback_snapshots\<時間戳>\`，自動只保留最新 5 份）

3. **互動確認**——腳本會停下來問：
   ```
   確認要套用這個更新嗎？(y/N)
   ```
   確認上面資訊（目前版本 vs 部署包版本）無誤後輸入 `y`

4. **腳本會自動做完以下事情**：
   - [2/5] 安全停服（殺掉監聽 666 埠的行程，等待最長 15 秒確認埠真的釋放；不用擔心服務就此關掉——`autostart` 排程工作的 crash-restart 迴圈會在幾秒內自動接手重啟）
   - [3/5] 複製新程式碼（只加不刪，**不會**動到 `db`／`uploads`／`logs`／`報價單PDF`／既有設定檔）
   - [4/5] 套用後健康檢查（輪詢 `/api/ping` 最長 30 秒＋掃 `server.log` 有沒有新的 Traceback/ERROR）

5. **檢查最終結果**：
   - ✅ 綠字「更新完成：`<commit>`」→ 成功，跳到下面「套用後」
   - 🔴 紅字「更新失敗，已自動回滾到套用前版本」→ **不需要你手動處理**，腳本已經自動把程式碼＋資料庫都還原成套用前的狀態；記下輸出裡的「回滾後健康狀態」是否顯示「正常」，若顯示「仍異常，需要人工介入」才需要進一步排查（回滾快照路徑會印在畫面上）

## 套用後

- [ ] 瀏覽器開 `http://172.16.10.177:666`（或已設定 HTTPS 的話用 `https://`），確認能正常登入、頁面功能正常
- [ ] 確認登入頁版本號有更新（讀 `version_manifest.json` 最新一筆，這次應該顯示 `2026-08-28d`）
- [ ] 這次部署**沒有**需要 superadmin 手動重新設定的系統設定值（新功能都是純新增，預設值即可正常運作，不像過去某些版本需要重新設定簽核流程或公司資訊）

## 這次部署包含的新功能重點（供套用後快速對照測試）

- 營運報表新增「資金水位」分頁（含銀行對帳單 CSV 比對、稅務匯出）
- 案件執行看板／月支出報表新增部門篩選
- 庫存管理新增安全庫存水位燈號（需先到料號主檔設定安全庫存才會顯示紅/黃燈）
- 側邊欄新增「簽核代理人」頁面
- superadmin/admin 帳號閒置 2 小時會自動登出（其餘角色維持原本 8 小時）
- 簽核佇列／topbar 角標現在會正確顯示代理人身分待簽核項目（`myDelegatedFor`）
- 勞報單已匯出後不可再修改（比照既有「已匯出不可刪除」保護）
- 金額同步稽核修正：精算快照過期彙總統計、稅額沖銷金額換算、案件動態時間排序、承攬商派發編輯鎖等 7 項，詳見 `CHANGELOG.md` `2026-08-28d`

## 如果想同時處理 HTTPS

這次的部署包本身**不含** HTTPS 憑證產生這一步（那是獨立的一次性操作，見同目錄下 `HTTPS-DEPLOY-CHECKLIST.md`）。若要一併處理，建議順序：先完成上面這份 §15 更新流程 → 確認健康 → 再執行 HTTPS 清單，兩者互不影響、也可以分開不同時間做。
